# MUPC 南向通信扩展 设计文档

**版本**: v1.0
**日期**: 2026-05-29
**状态**: DRAFT

---

## 1. 概述

### 1.1 背景

Phase 2+ 需要完善南向通信能力，包括：
1. **RS485 设备扩展**：TTU、光伏逆变器、充电桩等设备驱动
2. **HPLC 驱动**：高速电力线载波通信支持

### 1.2 目标

- 实现统一的 `SouthDevice` trait 抽象南向设备
- RS485 插件通过协议处理器注入支持多种设备
- HPLC 插件预留芯片 SDK 接口，后续集成

### 1.3 架构决策

| 决策项 | 选择 |
|--------|------|
| HPLC 实现方式 | 通用抽象 + 芯片 SDK 后续集成 |
| RS485 设备扩展 | 统一设备抽象 + 协议处理器注入 |
| 架构风格 | 插件独立，仅共享 device_trait |

---

## 2. 架构设计

### 2.1 模块结构

```
mupc/crates/
├── rs485-plugin/           # 增强：协议处理器注入
│   └── src/
│       ├── lib.rs
│       ├── device.rs       # Rs485Device (注入 ProtocolHandler)
│       ├── config.rs
│       ├── errors.rs
│       ├── protocol.rs      # Frame + DataUnitParser
│       └── handlers/        # 新增：协议处理器
│           ├── mod.rs
│           ├── modbus_handler.rs
│           ├── ttu_handler.rs
│           ├── inverter_handler.rs
│           └── charger_handler.rs
│
├── hplc-plugin/            # 新建：HPLC 驱动
│   └── src/
│       ├── lib.rs
│       ├── driver.rs       # HplcDriver trait
│       ├── device.rs       # HplcDevice
│       ├── mock.rs         # Mock 实现用于开发
│       ├── errors.rs
│       └── config.rs
│
└── device-trait/            # 共享基础接口
    └── src/
        ├── lib.rs
        └── south_device.rs  # SouthDevice trait
```

### 2.2 接口设计

#### SouthDevice trait (统一设备抽象)

```rust
use device_trait::{DataFrame, DeviceError, DeviceStatus};

/// 南向设备统一接口
///
/// 所有南向设备（RS485/HPLC/其他）都实现此 trait
pub trait SouthDevice: Send + Sync {
    /// 获取设备ID
    fn device_id(&self) -> &str;

    /// 获取设备类型
    fn device_type(&self) -> &str;

    /// 获取设备状态
    fn status(&self) -> Result<DeviceStatus, DeviceError>;

    /// 读取数据
    fn read(&self) -> Result<DataFrame, DeviceError>;

    /// 写入数据
    fn write(&self, data: &[u8]) -> Result<(), DeviceError>;
}
```

#### ProtocolHandler trait (RS485 协议注入)

```rust
use device_trait::{DataFrame, DeviceError};

/// RS485 协议处理器
///
/// 通过依赖注入支持多种协议
pub trait ProtocolHandler: Send + Sync {
    /// 编码请求数据
    fn encode_request(&self, device_id: &str, data: &[u8]) -> Vec<u8>;

    /// 解码响应数据
    fn decode_response(&self, frame: &[u8]) -> Result<DataFrame, DeviceError>;

    /// 获取协议名称
    fn name(&self) -> &'static str;
}
```

#### HplcDriver trait (HPLC 驱动抽象)

```rust
use crate::errors::HplcError;

/// HPLC 驱动接口
///
/// 抽象不同芯片厂商的 PLC SDK
pub trait HplcDriver: Send + Sync {
    /// 初始化驱动
    fn init(&self, config: HplcConfig) -> Result<(), HplcError>;

    /// 发送数据
    fn send(&self, data: &[u8]) -> Result<(), HplcError>;

    /// 接收数据
    fn recv(&self, timeout_ms: u64) -> Result<Vec<u8>, HplcError>;

    /// 检查连接状态
    fn is_connected(&self) -> bool;

    /// 获取驱动名称
    fn driver_name(&self) -> &'static str;
}
```

---

## 3. RS485 设备实现

### 3.1 Rs485Device 增强

```rust
/// RS485 设备（支持协议注入）
pub struct Rs485Device {
    device_id: String,
    device_type: String,
    config: Config,
    port_fd: Mutex<Option<RawFd>>,
    status: Mutex<DeviceStatus>,
    handler: Arc<dyn ProtocolHandler>,  // 注入的协议处理器
}
```

### 3.2 协议处理器实现

| 处理器 | 实现协议 | 支持设备 |
|--------|----------|----------|
| `ModbusHandler` | Modbus RTU | 通用 Modbus 设备 |
| `TtuHandler` | TTU 专用协议 | 配变终端 |
| `InverterHandler` | 厂商私有协议 | 光伏逆变器 |
| `ChargerHandler` | GB/T 27930 | 充电桩 |

### 3.3 ModbusHandler 示例

```rust
pub struct ModbusHandler {
    device_addr: u8,
    crc_mode: CrcMode,
}

impl ProtocolHandler for ModbusHandler {
    fn encode_request(&self, _device_id: &str, data: &[u8]) -> Vec<u8> {
        // data: [func_code, addr_hi, addr_lo, ...]
        let mut frame = vec![self.device_addr];
        frame.extend_from_slice(data);
        let crc = Frame::calculate_crc(self.device_addr, data[0], &data[1..], self.crc_mode);
        frame.push((crc >> 8) as u8);
        frame.push(crc as u8);
        frame
    }

    fn decode_response(&self, frame: &[u8]) -> Result<DataFrame, DeviceError> {
        if frame.len() < 5 {
            return Err(DeviceError::protocol_error("响应太短"));
        }
        // 解析响应并返回 DataFrame
        Ok(DataFrame::new(format!("modbus_{}", self.device_addr), frame.to_vec()))
    }

    fn name(&self) -> &'static str {
        "ModbusRTU"
    }
}
```

---

## 4. HPLC 驱动实现

### 4.1 HplcDevice

```rust
/// HPLC 设备
pub struct HplcDevice {
    device_id: String,
    device_type: String,
    config: HplcConfig,
    driver: Arc<dyn HplcDriver>,
    status: Mutex<DeviceStatus>,
}
```

### 4.2 MockHplcDriver (开发/测试用)

```rust
/// Mock HPLC 驱动（用于开发测试）
pub struct MockHplcDriver {
    connected: AtomicBool,
}

impl HplcDriver for MockHplcDriver {
    fn init(&self, _config: HplcConfig) -> Result<(), HplcError> {
        self.connected.store(true, Ordering::SeqCst);
        Ok(())
    }

    fn send(&self, data: &[u8]) -> Result<(), HplcError> {
        tracing::debug!("MockHplcDriver 发送 {} 字节", data.len());
        Ok(())
    }

    fn recv(&self, timeout_ms: u64) -> Result<Vec<u8>, HplcError> {
        // 模拟返回空数据
        Ok(Vec::new())
    }

    fn is_connected(&self) -> bool {
        self.connected.load(Ordering::SeqCst)
    }

    fn driver_name(&self) -> &'static str {
        "MockHplcDriver"
    }
}
```

### 4.3 芯片 SDK 绑定（预留）

```rust
/// 芯片 SDK FFI 绑定（预留接口）
pub struct SdkHplcDriver {
    // FFI 句柄
}

impl HplcDriver for SdkHplcDriver {
    fn init(&self, config: HplcConfig) -> Result<(), HplcError> {
        // 调用 libhplc.so 中的 hplc_init()
        Ok(())
    }

    fn send(&self, data: &[u8]) -> Result<(), HplcError> {
        // 调用 libhplc.so 中的 hplc_send()
        Ok(())
    }

    fn recv(&self, timeout_ms: u64) -> Result<Vec<u8>, HplcError> {
        // 调用 libhplc.so 中的 hplc_recv()
        Ok(Vec::new())
    }

    fn is_connected(&self) -> bool {
        true
    }

    fn driver_name(&self) -> &'static str {
        "SdkHplcDriver"
    }
}
```

---

## 5. 配置文件

### 5.1 RS485 设备配置

```json
{
  "rs485_devices": [
    {
      "device_id": "ttu_001",
      "device_type": "ttu",
      "port": "/dev/ttyUSB0",
      "baud_rate": 9600,
      "data_bits": 8,
      "stop_bits": 1,
      "parity": "none",
      "timeout_ms": 1000,
      "device_addr": 0x01,
      "handler": "ttu"
    },
    {
      "device_id": "inverter_001",
      "device_type": "inverter",
      "port": "/dev/ttyUSB1",
      "baud_rate": 19200,
      "handler": "inverter"
    }
  ]
}
```

### 5.2 HPLC 设备配置

```json
{
  "hplc_devices": [
    {
      "device_id": "hplc_001",
      "device_type": "hplc",
      "driver": "mock",
      "config": {
        "serial_port": "/dev/ttyUSB2",
        "baud_rate": 115200
      }
    }
  ]
}
```

---

## 6. 错误处理

### 6.1 HPLC 错误类型

```rust
#[derive(Debug, Error)]
pub enum HplcError {
    #[error("驱动初始化失败: {0}")]
    InitFailed(String),

    #[error("发送失败: {0}")]
    SendFailed(String),

    #[error("接收失败: {0}")]
    RecvFailed(String),

    #[error("连接断开: {0}")]
    Disconnected(String),

    #[error("SDK 错误: {0}")]
    SdkError(String),
}
```

---

## 7. 依赖关系

```
device-trait (SouthDevice trait)
    ↓
rs485-plugin (实现 SouthDevice)
    ↓
hplc-plugin (实现 SouthDevice)
```

---

## 8. 实现任务

| 任务 | 模块 | 优先级 |
|------|------|--------|
| 新建 `SouthDevice` trait | device-trait | 必须 |
| 新建 `ProtocolHandler` trait | device-trait | 必须 |
| 新建 `HplcDriver` trait | device-trait | 必须 |
| 增强 `Rs485Device` 注入 handler | rs485-plugin | 必须 |
| 实现 `ModbusHandler` | rs485-plugin | 高 |
| 实现 `TtuHandler` | rs485-plugin | 高 |
| 实现 `InverterHandler` | rs485-plugin | 中 |
| 实现 `ChargerHandler` | rs485-plugin | 中 |
| 新建 hplc-plugin 模块 | hplc-plugin | 高 |
| 实现 `MockHplcDriver` | hplc-plugin | 高 |
| 预留 `SdkHplcDriver` FFI | hplc-plugin | 低 |

---

## 9. 安全考虑

- RS485 半双工通信需要正确的 DE/RE 信号控制
- HPLC 通信需要考虑电力线噪声和干扰
- 配置文件中的设备地址和串口路径需要访问控制

---

**文档状态**: 草稿，待评审
**下一步**: 提交设计评审，评审通过后进入开发阶段