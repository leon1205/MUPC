# 12-MUPC 本地显示终端（触摸式本地 HMI）产品需求文档（PRD）

> `[REVIEWED: PASS]` — 2026-09-10 / 评审员 / 摘要：v2.0 上轮 4 项 REQUEST_CHANGES 全部落实（R1 §4.1 删「可能的软键盘」残留、预算口径收敛为「选项式触摸 + 只读文本渲染」；R2 固化 6 页 IA 并补齐 P4 安全·联锁 / P5 审计落点，消除「F6 或独立联锁区」二选一，IL-01~03 / AU-01 与 PL-03~06 双向索引成立；R3 §3.7 #15 订正为真实 `/api/v1/ai/stream`、#16 如实改述静态资源未挂载，与代码核对一致；R4 PL-1 写操作集合与「模式切换=暂停无入口」口径统一，A.5 旧表述已有紧邻订正说明）。交叉引用改号同步无死引用，F16–F19 验收均可量化。通过。
> （v1.0 的 `[REVIEWED: PASS]` 仅对其「只读状态屏」范围有效，**已被本 v2.0 取代**，见文末「附录 A：v1.0 → v2.0 变更说明」。）
> - **`[REVIEWED: PASS: 2026-09-23]`** — 2026-09-23 / 评审员 / 摘要：**v2.1「外设数值上屏」增量（§3.9 F20–F26 + §3.9.0 数据可用性要求 / §3.9.1 页面归属建议 / §3.9.2 验证分档；§4.5 刷新频率行、§5 EDGE-18~24、§8 T-8~T-12、附录 B EX-01~EX-34、附录 C #8~#14、附录 D）评审通过**。① 范围与边界齐备（明示"本节只定展示什么 + 验收标准"，页位 / 帧结构 / 取数形态均声明属设计合同，且声明既有 F 项正文与两条 PM 降级补注一律未改）；② 需求条目编号可追溯（F20–F26 / RQ-9.0-1~5 / EDGE-18~24 / EX-01~EX-34 / T-8~T-12）；③ 验收可量化且按本机 / 真机 / 现场三档分列，各功能项均给出可机械断言的反例（「屏上不存在柜外温湿度数值字段」「无 STS 误用」「无混标」「不补 0」「空态与不可用文案互异」）；④ 边界与异常写全（站离线 / 值无效 / 配置非法 / 通道不可达 / 帧版本不匹配 / 段缺失 / 预留未配置 七类）；⑤ **未见越界替设计做决定**：F26 只要求「版本号递增 + 不得放宽拒帧校验」，F22/F23/F24 的点名明确「以登记表为最终依据、设计阶段逐条复核」，帧结构不预设；⑥ 事实取证逐条复核通过（`PROTO_VERSION` 确为 **2** / `MAX_FRAME_BYTES` 确为 64 KiB / HVAC 段确仅有 3 个标量、**确无柜外温湿度测量值**、31 个 DI 位构成确为 20 告警 + 10 状态 + 1 预留 / BMS·消防·储能表的点名与量纲与 `point_table.rs` 登记逐条吻合 / 「6 页」口径与 F11·TT-01 一致、回写清单正确 / cmap 324 码位与「41 字缺口」独立复算一致）。**建议改进（不阻塞）3 条**，见审查意见。**本标记仅对 v2.1 增量有效**；v2.0 的 `[REVIEWED: PASS]`（2026-09-10）原范围不变。
>
> - 版本：**v2.2**（v2.0 合并版 + PM 裁决固化 + v2.1「外设数值上屏」增量 + **本版 T-8 页面归属裁定登记**）
> - 日期：2026-09-10（v2.0）／ 2026-09-23（v2.1 增量、v2.2 裁定登记）
> - 状态：v2.0 已评审通过（`[REVIEWED: PASS]`，2026-09-10，原范围不变）；**v2.1 增量已于 2026-09-23 评审通过**（`[REVIEWED: PASS: 2026-09-23]`，仅对 §3.9 增量有效）；**v2.2（2026-09-23）为 T-8 页面归属裁定的登记增量——不含新功能需求，不改变 v2.0 / v2.1 的门禁标记**（`[REVIEWED: PASS: 2026-09-23]` 原样保留）
>
> - **v2.1 增量（2026-09-23）——「外设数值上屏」**：新增 **§3.9 F20–F26**（HVAC / 消防 / BMS / 储能表 / PCS 数值上屏 + 刷新与新鲜度 + 帧契约版本化）；追加 §4.5 刷新频率行、§5 **EDGE-18~EDGE-24**、§8 **T-8~T-12**、附录 B 验收条目（**EX-01~EX-34**）、附录 C 待办、**附录 D（版本演进）**。
>   **范围与纪律**：本节为**新需求**（技术债 **U-73**，来源审查报告 §三 缺陷 #10），**非缺陷**；**既有 F 项正文、门禁标记与 PM 降级补注（2026-09-16 / 2026-09-19）一律未改**；本增量**只写需求与验收**，不写实现、不改代码、不改 `display-proto`。
> - **v2.2 增量（2026-09-23）——T-8 页面归属裁定登记**：用户于 **2026-09-23** 就 **T-8（新增外设数值的页位归属）** 作出裁定，**采用「两处拆」（合并进既有页，不新增页）**：① **消防类**（系统状态、火警等级、钢瓶气压「未配置」口径、探测器，即 **F21**）→ 并入 **P4 安全 · 联锁页**（与联锁同源，现场处置时同页可见）；② **HVAC 数值 + 储能表 + BMS 数值扩展**（**F20 / F22 / F23**）**及 PCS 扩展数值（F24）** → 并入 **P6**，并将该页**改名为「装置与外设」**（F8 装置信息 + 外设只读数值，同属"只读、非处置"类信息）。**页数仍为 6 页** ⇒ 既有 **F11 / TT-01 的「6 页」口径与导航栏不变**（这正是选择"合并"而非"新增 P7"的理由：不破该口径）。**本版只做登记**：不新增功能需求、不改代码 / 设计文档 / 其他 PRD，**既有 F 项正文、门禁标记与 PM 降级补注一律未改**。
> - **本版已固化 PM 对 v2.0 草案的裁决**：T-1（日志检索仅选项式，不做自由文本搜索）、T-2（日志导出本期不支持）、T-3（无登录 + 审计 + 二次确认）、T-6（主读数区不横滚、允许整页纵向滚动）；**T-5 保留「待设计阶段确认」**（见 §6.2）。裁决明细见附录 A.5。
> - 取代：本文件 v1.0（只读状态屏）；并**并入** `08-MUPC-Web管理与AI可视化-PRD.md` 的全部本地展示与操作类内容
> - **补注（2026-09-16，PM 裁定）——CF-04 降级**：**CF-04「保存成功后自动生效、无需重启」降级为"部分生效"** —— 实测 **7 个可写参数中仅 `system.log_level` 真热生效**，其余 **6 个连接类参数（`intercore.*` 4 + `gateway.*` 2）需重启 `mupcd` 进程生效**。PM **接受该降级**（不投入"把 6 个参数做成真热生效"的改造），并要求**屏上文案如实分级**。**本文档正文的验收条目与全部数值一律未改**（降级口径落点见 §3.2 F9 的补注块）；实现依据见 `12-MUPC-本地显示终端-设计文档.md` §4.3.5（该条已标注"已裁定"）。
> - **补注（2026-09-19，PM 裁定）——F9 字段级降级**：**§3.2 F9 的 7 个配置项中 3 项在现网无配置承载**（IEC 104 心跳间隔 / 核间本地端口 / 遥测上报周期）⇒ **CF-01 在字段级为 4/7**、**CF-02 不可达**。PM **接受该降级**，本期不为这 3 项新增配置承载；**正文的字段表与数值一律未改**（降级口径见 §3.2 F9 的第二处补注块）。
> - 关联：MUPC 主控（mupcd）+ 核间 modbus（PCS 通讯）+ BECG-3568 平台（RK 芯片，8 路隔离 RS485）
> - 权威点表：`e:\MUPC2\60kW 双级式PCS产品资料包\5.通讯协议\协议V1.3：两级式PCS设备通讯说明版本（EMS)(2).pdf`（下称「协议 V1.3」）
> - ⚠️ 本 PRD 取代的既有产物（其「无浏览器 / 只读渲染 / 无交互层」前提**已被推翻**，须按本文件重新设计）：
>   - `docs/superpowers/plans/modules/12-MUPC-本地显示终端-设计文档.md`
>   - `docs/superpowers/plans/modules/12-MUPC-本地显示终端-UI设计文档.md`

---

## 0. PM 已裁定边界（不得违背，本 PRD 的硬边界）

| # | 裁定 | 对本 PRD 的约束 |
|---|------|------------------|
| B1 | 设备屏**有触摸屏** | 本地屏为可交互 HMI，不再是只读屏 |
| B2 | 原 08 的**操作类功能也由本地屏承载**（配置管理、日志等），用户以触摸操作 | 配置/日志/联锁等写操作入口全部迁入本地屏 |
| B3 | 08 的 AI 相关页面（§6 AI 可视化 / §7 专家干预 / §8 A/B 测试）**保留需求设计但标暂停**（AI 引擎已停用，无数据源），本期不实现 | 见 §3.8 暂停范围 |
| B4 | **移除 `web-api`**，取消通过 Web 页面访问的机制 | 系统需展示/操作的内容统一由本模块在本地屏承载；原 web-api 出口功能须做需求级迁移声明（§3.7） |
| B5（T-3 固化） | **本机权限模型 = 无登录 + 审计 + 二次确认** | 物理在场即授权，**无登录 / 会话 / PIN / RBAC**；写操作（**配置保存、联锁释放、M1 授权、任何下发**）须二次确认 + 记审计（§2.1 / §3.7 PL-1）。**模式切换为暂停项、本期无界面入口**（§3.8），不产生该类写操作 |
| B6（T-1 固化） | **日志检索 = 仅选项式筛选** | 仅「级别多选 + 模块筛选 + 预设时间区间」；**不做自由文本搜索**，本期**不引入虚拟键盘 / 文本输入**（§3.3 F10 / §3.6 F12） |
| B7（T-2 固化） | **日志导出本期不支持** | 本地屏无文件 / 下载通道，不提供任何导出入口；现场取日志诉求另立需求（§3.3 F10） |
| B8（T-6 固化） | **主状态页：主读数区不横向滚动，允许整页纵向滚动** | 主读数（SOC / PCS 状态 / 三相 P、I）不得横向滚动；信息量超单屏时允许整页纵向滚动或分区切换（§4.2） |

> **「移除 web-api」的准确含义（本 PRD 口径）**：取消**对外 HTTP 访问机制**与**浏览器访问途径**（监听地址 / Web UI / REST / SSE / WebSocket / Session 登录面）。本模块自身不因此被禁止使用本机回环（127.0.0.1）等内部通道——**通道形态属设计合同阶段裁决**，本 PRD 只约束能力与非功能边界（同 v1.0 §1.3 精神，见 §4.4）。

---

## 1. 产品概述

### 1.1 背景（Why）

MUPC 设备为台区储能治理装置的非实时处理核心，长期 **headless 部署**（无本地面板）。原有两个并行的人机面：

1. **12 号本地显示终端（v1.0）**：HDMI 外接 8 寸 1024×768 屏，**只读**呈现 SOC / PCS 充放 / 三相功率电流。
2. **08 号 Web 管理与 AI 可视化**：`web-api` crate 提供的浏览器访问面（8080），承载系统状态监控、配置管理、日志查看，以及 AI 可视化/专家干预/A/B 测试。

平台现状已变化：

- **AI 引擎已停用**，本地策略引擎为唯一默认下发源；08 号中占比最大的 AI 类页面（§6/§7/§8）已无数据源，实际处于空转状态。
- `web-api` 的浏览器访问面在**现场无网 / 远端不可达**时完全不可用；而现场运维与调试恰恰发生在柜前。
- 同一份信息被两套机制重复承载（12 号屏 + 08 号 Web），维护面与一致性成本双份。

**结论（B4）**：取消 Web 访问机制，**把本地屏从「只读状态屏」升级为「触摸式本地人机界面（HMI）」**，统一承载原 08 的本地展示与操作。

### 1.2 产品定位（v2.0 升级）

> 12-MUPC 本地显示终端 = **台区储能装置的柜面触摸式本地人机界面**。

- **唯一本地人机面**：设备需向现场人员呈现与操作的内容，统一经本模块在本地屏呈现，不再有第二套 Web 面。
- **就地可视 + 就地可操作**：既读得到（SOC / PCS 充放 / 三相量 / 装置与连接状态 / 告警 / 版本），也操作得了（运行参数配置、日志查询、联锁人工释放与授权）。
- **可靠性兜底展示**：数据不可得时**明示不可得（不造假值）**，避免把「离线 / 未取数」误读为「真 0 功率」（沿用 v1.0 铁律，见 §5）。

### 1.3 产品价值

| 价值 | 说明 |
|------|------|
| 就地可视 | 柜前直接读屏，无需笔记本 / 远端即可确认储能 SOC、PCS 充放状态、三相功率电流 |
| 就地可操作 | 现场即可改运行参数、查日志、释放联锁，消除「必须开电脑」的运维瓶颈 |
| 降低误判 | 以 PCS 运行状态寄存器（REG 1013）为主判据，不靠「看功率符号猜充放」 |
| 单一维护面 | 取消 Web 面，消除两套人机面的实现与一致性成本 |
| 可靠性兜底 | 数据不可得时明示「不可得」，不造假值 |

### 1.4 目标平台与形态边界

| 项目 | 要求 |
|------|------|
| 操作系统 | Linux（openEuler 22.03+ / Ubuntu 20.04+，aarch64） |
| 硬件 | BECG-3568（RK3568/RK3588），HDMI 外接 8 寸 **1024×768** 触摸屏 |
| 技术形态 | **独立原生渲染进程**（本机进程，不依赖浏览器内核、不依赖 Web UI） |
| 数据获取 | 显示数据由 mupcd（主进程运行时）经数据通道供给；显示进程**不直连核间 modbus 总线** |
| 写操作通道 | 运维写操作（配置 / 联锁释放 / 授权）经 mupcd 提供的**受控接口**完成（见 §3.7）；**禁止**显示进程直连 PCS / 核间总线下发 |
| 交互能力 | 单点触摸（是否需多点触控：**待设计确认**，本期按单点触摸设计） |
| 通道形态 | 跨进程通道 / 写操作接口的具体形态属**设计合同阶段**裁决，本 PRD 只约束能力与非功能边界 |

---

## 2. 用户角色

现场触屏场景下，用户面收窄（无远程角色）：

| 角色 | 说明 | 关注 | 典型操作 |
|------|------|------|----------|
| **现场运维工程师** | 例行巡检、状态确认、故障初步处置 | SOC、充放状态、功率量级、告警、装置整体状态 | 读主状态页；查告警；查日志；人工释放联锁 |
| **调试 / 试验工程师** | 台区联调、策略验证、参数下发核对 | 三相功率/电流与下发目标是否一致、SOC 裁决值是否可信、参数配置 | 读三相量；改运行参数；按模块/级别筛日志 |
| **（间接）远程支持人员** | 电话指导现场人员操作 | 屏上字段与编号稳定可复述，便于远程对拍 | 不直接操作设备 |

### 2.1 身份与权限模型（本机触摸，PM 已裁定 T-3）

**现场语义判定**：本地屏物理安装在设备柜面，**能触到屏幕即已在物理安全边界内**。原 08 的 Session 登录 / 密码 / 角色（RBAC）体系是为**网络可达面**设计的；Web 面取消后，其登录面失去存在理由。

**PM 裁定结论（T-3，本 PRD 基线，不再有备选项）**：

1. **无登录、无会话、无 PIN、无 RBAC**——**物理在场即授权**。本模块**不提供**任何账号 / 密码 / 登录 / 登出 / 角色分级 / 会话管理界面与需求；原 08 §2.3 的认证授权机制**不再适用**，随 Web 面一并废止。
2. **写操作必须记审计**：所有写操作（**配置保存、联锁释放、M1 授权、任何下发**）写入审计记录，操作者标识为本地控制台（固定标识，如 `local-console`），记录时间、操作类型、操作前后值、结果与失败原因（见 §3.7 PL-1）。**模式切换属暂停项、本期无界面入口（§3.8），不在本期写操作集合内**——避免与「已暂停」自相矛盾。
3. **写操作必须二次确认**：所有生效性 / 破坏性写操作须经二次确认对话框（展示「将执行的操作 + 影响范围 + 前后值摘要」），默认焦点在「取消」（见 §3.6 F14）。
4. **防误触补偿**：因无身份把关，须以「二次确认 + 防抖 / 幂等 + 控件尺寸与间距」共同兜底（见 §3.6 F14），写接口须具备防重放能力（见 §4.4 第 5 条）。

---

## 3. 核心功能列表

> 每个功能给出「用户故事 + 可量化验收标准」。验收阈值按平台现状给定；评审调整须回写本文件（禁止另建独立文件）。
> 分类：F-状态展示类（§3.1）、F-配置管理类（§3.2）、F-日志管理类（§3.3）、F-安全与联锁类（§3.4）、F-审计类（§3.5）、F-触摸交互通用需求（§3.6）、F-平台整合（§3.7）、暂停范围（§3.8）、**F-外设数值展示类（§3.9，v2.1 增量追加）**。

### 页面结构（信息架构，需求级）

**本期信息架构 = 6 个页面**：主状态页 P1 / 配置页 P2 / 日志页 P3 / 安全 · 联锁页 P4 / 审计页 P5 / **装置与外设页 P6**（原「系统 · 关于页」，v2.2 更名，理由见下 T-8 裁定）。
其中 **P2、P4 含写操作**（必须二次确认 + 记审计）；**P1、P3、P5、P6 为读取 / 展示页**（P3、P5 含选项式筛选，无文本输入；**P6 现含外设只读数值，仍为纯读取页**）。逐页给出功能落点，**不留「二选一」未决归属**：

| # | 页面 | 定位 | 承载功能（落点） | 状态 |
|---|------|------|------------------|------|
| P1 | **主状态页** | 默认页（开机 / 无操作超时回归页） | F1 SOC、F2 PCS 运行状态、F3 三相有功、F4 三相电流、F5 刷新与新鲜度、F6 装置整体状态、F7 告警列表 | 本期实现 |
| P2 | **配置页** | 运行参数触摸读写 | F9 运行参数触摸读写与保存生效 | 本期实现 |
| P3 | **日志页** | 日志查看 / 选项式筛选 / 实时推送 | F10 日志查看与选项式筛选 | 本期实现 |
| P4 | **安全 / 联锁页** | 联锁状态展示与人工处置（原 web-api 联锁出口的需求级落点，§3.7 映射 #1–#3）；**并承载消防数值展示**（v2.2 T-8 裁定：与联锁同源，现场处置时同页可见） | F16 联锁状态展示、F17 人工释放联锁、F18 M1 人工授权重启（PL-04 / PL-05 / PL-06）、**F21 消防数值展示** | 本期实现 |
| P5 | **审计页** | 写操作审计记录查询（只读） | F19 审计记录查询（PL-2 / PL-03） | 本期实现 |
| P6 | **装置与外设页**（原「系统 / 关于页」，v2.2 更名） | 版本信息、装置信息 + **外设只读数值**（只读，无写操作；二者同属"只读、非处置"类信息） | F8 版本与装置信息、**F20 HVAC 数值展示、F22 BMS 数值扩展、F23 储能表数值展示、F24 PCS 数值扩展** | 本期实现 |
| — | **AI 页（可视化 / 干预 / A/B）** | 原 08 §6–§8 | 无（**不提供导航入口**，不做置灰占位） | **暂停**（§3.8） |

**信息架构裁决（消除原「二选一」未决）**：

1. **联锁相关功能统一归入独立「安全 / 联锁」页（P4）**——**不并入**主状态页的 F6 装置状态区。理由：联锁状态展示 + 人工释放 + M1 授权构成一组「安全处置」闭环，且释放 / 授权为**破坏性写操作**，与只读的主状态页混排会拉高误触风险（§3.6 F14 要求破坏性操作与返回控件间距 ≥ 48 px）。
2. **审计查询归入独立「审计」页（P5）**——**不并入**日志页。理由：审计筛选维度（时间范围 / 操作类型）与日志筛选维度（级别 / 模块 / 时间）不同；且审计为**不可删改的合规凭据**，与诊断日志混排易造成语义混淆（违反 §5「语义不同不得混淆」原则）。
3. 导航栏常驻上述 6 页入口（含 P4 / P5），任意页到任意页触摸 ≤ 2 次（F11 / TT-01，**TT-01 须覆盖新增的 P4 / P5**）。
4. **（v2.2，T-8 裁定）外设数值采用「两处拆」，不新增页**：**消防类（F21）并入 P4 安全 · 联锁页**（与联锁同源）；**HVAC / BMS / 储能表 / PCS 扩展数值（F20 / F22 / F23 / F24）并入 P6，并将该页由「系统 / 关于页」改名为「装置与外设页」**。**页数仍为 6 页**，故 **F11 验收 1/2 与 TT-01 的「6 页」口径及导航栏不变**（不新增 P7、无需回写页数）。理由：外设数值与既有页承载的信息同属"只读、非处置"语义（消防属安全处置闭环 ⇒ 归 P4；装置信息 + 外设只读数值同属"核对 / 排障"类信息 ⇒ 归 P6），**合并优于新增页**——既不破坏 6 页口径，也不使 100+ 数值打散 P1 主读数区（方案 A 已否决，见 §3.9.1）。


---

### 3.1 F-状态展示类

> 合并来源：原 12 号 F1–F5（保留并沿用验收阈值）+ 原 08 §3 系统状态监控。

#### F1 储能 SOC 展示

**用户故事**：作为现场运维，我在柜前扫一眼屏就能看到当前电池 SOC 与其健康区间，判断是否接近上下限，无需连远端。

**数据来源**：mupcd 运行时 SOC 双源裁决后的域值（BMS 站 SOC 与核间读 PCS REG 1010 经 `resolve_soc_source` / `apply_soc_source` 裁决后的输出），非单一原始源。本 PRD 不定裁决算法，只要求「展示裁决值 + 展示所用源」。

**验收标准（可量化）**：
1. 屏上常显 SOC 数值，单位 %，量程 0–100%，显示精度整数位。
2. SOC 旁以可视手段标注**数据源**：`BMS` / `PCS(REG1010)` / `双源一致`；源信息与展示值同帧更新。
3. 区间着色：`≤15%`（下限邻近）与 `≥85%`（上限邻近）使用警示色块/边框（阈值对齐平台安全配置 soc_min/soc_max，默认 0.10/0.90）。
4. SOC 双源皆失效时按 §5 降级，**禁止补 0 或沿用陈旧值冒充实时值**。

#### F2 PCS 运行状态展示（充 / 放 / 停机 / 待机）

**用户故事**：作为现场运维，我要在屏上直接读出 PCS 此刻是充还是放，且该结论来自设备状态机而非我肉眼猜功率正负。

**判定依据**：PCS **模块运行状态**寄存器 REG 1013（UInt16）：`0=停机、1=待机、2=充电、3=放电`。该状态机为主判据；SOC 裁决值与三相有功**仅作一致性佐证**（异常时提示，不覆盖主判据）。

**验收标准（可量化）**：
1. 显式给出四态之一：**停机 / 待机 / 充电 / 放电**；文字 + 语义色 + 图标三重冗余（不依赖颜色单一信道）。
2. 判据为 REG 1013；1013 读失败（PCS 离线）时进入降级（§5），**不得**由功率正负反推充放。
3. 一致性佐证：1013=2（充电）而三相有功和（Σ1029–1031）显著为负、或 1013=3（放电）而 Σ 显著为正（反向显著 = 超量程 5% 阈值）时，显示「状态与功率方向不一致」角标；**主状态仍以 1013 为准**。

#### F3 三相有功功率展示（A/B/C）

**用户故事**：作为调试工程师，我要在屏上看到 PCS 交流输出 A/B/C 三相各有功功率（kW），确认下发分相 P 目标与实读一致。

**数据来源**：REG 1029/1030/1031（Int16，量纲 `*0.1 kW`）；可加显设备总有功 REG 1032（Int16，`*0.1 kW`）。

**验收标准（可量化）**：
1. 三相互相 + 总计常显，单位 kW，保留 1 位小数；显示值 = 寄存器读值 × 0.1（由 mupcd 完成量纲化，渲染端不重算）。
2. 刷新跟随 F5 帧节奏（帧到即更新，≤ 500 ms 生效）。
3. 每相数值按当前充放语义（来自 F2 主判据 REG 1013）着色区分方向性；**方向标签一律取自 F2 状态机，不以功率符号自判**（避免方向反置造假）。
4. 读失败 / 未取数：该相显示 `--` + 「源离线 / 未取数」标，**禁止显 0.0**。

> **依赖点表追认声明**：F3 数值（绝对值）展示不阻塞；唯「用三相功率正负号直接表达充/放」这一呈现方式依赖「读回测量值极性与设定侧一致（正 = 放 / 负 = 充）」的厂方追认。追认前一律以 F2 状态机标注方向。

#### F4 三相电流展示（A/B/C）

**用户故事**：作为现场运维，我要在屏上看到 PCS 交流输出 A/B/C 三相电流（A），配合功率确认设备出力。

**数据来源**：REG 1022/1023/1024（Int16，量纲 `*0.1 A`）。

**验收标准（可量化）**：
1. 三相电流常显，单位 A，保留 1 位小数；显示值 = 寄存器读值 × 0.1。
2. 刷新跟随 F5 帧节奏（帧到即更新，≤ 500 ms 生效）。
3. 读失败 / 未取数：该相显示 `--` + 「源离线 / 未取数」标，**禁止显 0.0**。
4. 方向性标注策略同 F3。

#### F5 数据刷新与新鲜度策略

**用户故事**：作为运维，我希望屏上数字是「够新」的，并且当数据不新鲜 / 掉线时屏上会**明确告诉我**，而不是默默停更让我误以为现场稳定。

**验收标准（可量化）**：
1. mupcd 数据推送周期（帧率）≥ **1 Hz**（帧间隔标称 1 s，容抖 ±30%）。
2. 渲染端收到新帧到屏上更新生效 ≤ **500 ms**（不含帧率本身）；数值变动即时反映，无闪屏 / 残影。
3. 新鲜度判定：`当前时间 − 帧时间戳 > 2 s` 即判「过期」；过期后对该字段显示「数据过期」角标，**保留最近有效帧数值但不冒充实时**。
4. 每帧携带**时间戳**与**每字段的有效标志（valid / invalid）**，由 mupcd 在域值化后打标；渲染端仅按标志展示或降级。
5. 各字段独立降级：单点读失败不影响其余字段刷新（点级 valid 标志）。

#### F6 装置整体状态展示（原 08 §3 并入）

**用户故事**：作为现场运维，我要在一块「装置状态」区看清设备整体是否正常——固件版本、运行时长、CPU 温度、内存占用、各连接状态、当前控制源。

**展示内容与更新频率**：

| 数据项 | 说明 | 更新频率 |
|--------|------|----------|
| 固件版本号 | 当前运行固件版本 | 页面加载时 |
| 编译时间 | 固件编译时间戳 | 页面加载时 |
| 运行时间（uptime） | 系统持续运行时长 | 5 秒 |
| CPU 温度 | RK 处理器温度 | 5 秒 |
| 内存使用率 | 系统内存占用百分比 | 5 秒 |
| IEC 104 连接状态 | 已连接 / 连接中 / 断开 / 未配置 | 5 秒 |
| 核间（intercore）连接状态 | 实时控制模块连接状态 | 5 秒 |
| 跨进程数据通道状态 | 本地屏与 mupcd 的数据通道连接状态 | 5 秒 |
| 当前控制源 | 本地策略引擎（AI 停用期唯一默认下发源） | 5 秒 |

> **AI 引擎状态项处置**：原 08 §3 的「AI 引擎状态」在 AI 停用期为固定值，**并入 §3.8 暂停范围**（本期不设该状态项；若需保留，仅作只读固定文案「AI 引擎已停用，本地策略引擎为默认下发源」）。

**状态指示灯规范**（沿用 08 §3.3）：

| 状态 | 颜色 | 样式 |
|------|------|------|
| 已连接 / 正常 | `#28A745` | 实心圆 |
| 连接中 / 等待 | `#FFC107` | 闪烁动画（1 s 间隔） |
| 断开 / 错误 | `#DC3545` | 实心圆 |
| 未配置 | `#5F6368` | 空心圆 |

**验收标准（可量化）**：
1. 装置整体状态以状态卡网格展示，每卡含图标、标签、动态数值。
2. 各连接状态以状态指示灯展示，且**同时给出文字**（不依赖颜色单一信道）。
3. uptime / CPU 温度 / 内存使用率刷新周期 ≤ 5 s。
4. 版本号与编译时间在页面加载时一次性读取并展示，不随刷新跳动。
5. 连接状态不可得时显示「未配置 / 未知」，**不显示为「正常」**。

#### F7 告警列表展示（原 08 §3 并入）

**用户故事**：作为现场运维，我要在屏上看到最近的告警，按时间倒序，知晓设备最近出过什么事。

**验收标准（可量化）**：
1. 告警列表最多展示 **10 条**最新记录，按时间**倒序**排列。
2. 每条含时间戳、级别、消息文本；级别以颜色 + 文字双通道区分（ERROR = `#DC3545` / WARN = `#FFC107` / INFO = `#17A2B8`）。
3. 新增告警 ≤ **2 s** 反映到屏上（无需人工刷新）。
4. 告警为空时显示明确空态文案（如「近 24 小时无告警」），**不显示空白区**（见 §5）。
5. 告警数据来源须明确（见 §6）；来源不可用时该区显示「告警源不可用」，**不显示为「无告警」**（二者语义不同，禁止混淆）。

#### F8 版本与装置信息展示（原 08 §3 并入）

> **页面归属（v2.2，T-8 已裁定 2026-09-23）**：原落点页 P6「系统 / 关于页」**已扩展并改名为「装置与外设页」**——除本项装置 / 版本信息外，**并入 F20 HVAC / F22 BMS 扩展 / F23 储能表 / F24 PCS 扩展**四项外设只读数值（同属"只读、非处置"类信息）。**页数不变（6 页）**，本项验收标准（1–4）**不因扩页而更改**。

**用户故事**：作为远程支持人员指导现场读屏时，我需要现场人员能一步报出设备版本信息。

**验收标准（可量化）**：
1. **装置与外设页（P6，原「系统 / 关于页」，v2.2 更名）**展示：固件版本号、编译时间、装置型号 / 序列号（若可得）。
2. 版本信息以「字段名 + 值」列表形式呈现，字号满足 0.5–1.5 m 读距可读（见 §4.2）。
3. 版本信息不随数据刷新跳动（一次性读取）。
4. 不可得的字段显示「未提供」，**不臆造**。

---

### 3.2 F-配置管理类

> 合并来源：原 08 §4 配置管理。**降级约束**：本地屏**无浏览器下载通道**，原 Web 形态的输入与反馈方式须按触摸场景重新定义。

#### F9 运行参数触摸读写与保存生效

**用户故事**：作为调试工程师，我要在现场屏上直接查看并修改装置运行参数，保存后立即生效，不必回办公室开电脑。

**可配置项与取值域**（沿用 08 §4.2）：

| 配置分组 | 参数 | 默认值 | 范围 / 选项 | 触摸输入方式（需求级） |
|----------|------|--------|-------------|------------------------|
| IEC 104 连接参数 | 对端 IP 地址 | — | 合法 IPv4 地址 | **四段数字步进**（无键盘，见 §3.6 F12） |
| IEC 104 连接参数 | 端口 | 2404 | 1–65535 | 数字步进（受约束） |
| IEC 104 连接参数 | 心跳间隔 | 10 秒 | 1–60 秒 | 数字步进（受约束） |
| 核间通信参数 | 本地端口 | 2500 | 1–65535 | 数字步进（受约束） |
| 核间通信参数 | 对端端口 | 2501 | 1–65535 | 数字步进（受约束） |
| 遥测与日志 | 遥测上报周期 | 1 秒 | ≥ 1 秒 | 数字步进（受约束） |
| 遥测与日志 | 日志级别 | INFO | ERROR / WARN / INFO / DEBUG | **枚举选择**（无需键盘） |

**交互规范（触摸化重写，替代表单规范）**：
1. 配置按分组分区展示（IEC 104 / 核间 / 遥测与日志），组内字段纵向排列，一屏可见组内全部字段（超出时允许整页滚动，滚动规范见 §3.6）。
2. 仅提供**受约束输入控件**（枚举选择 / 数字步进）；控件本身限制非法值，使绝大多数越界在输入阶段即不可达。**全程不出现虚拟键盘 / 文本输入**（见 §3.6 F12）。
3. 值变更即时本地校验；非法值时该字段错误态（红色边框 + 文字原因），且保存按钮置灰。
4. **保存需二次确认**（确认弹层展示「将修改的字段：旧值 → 新值」摘要）。
5. 保存成功：屏上 Toast 提示（3 秒自动消失）；保存失败：错误提示 + **保留用户已输入值**，并给出失败原因。
6. 保存过程中按钮显示「保存中…」且禁止重复触发。
7. 配置**自动生效，无需重启装置**（生效语义要求见验收 4）。

**验收标准（可量化）**：
1. 所有配置项通过本地屏**可读、可写**；只读查看不触发任何确认流程。
2. 心跳间隔范围 1–60 秒，越界时显示具体错误提示（非泛化提示）。
3. 配置保存需二次确认；确认弹层展示修改前后值摘要。
4. 保存成功后**自动生效，无需重启**；生效判定：保存返回成功后 ≤ 5 s 内，新参数在对应模块的运行行为中可见（验证方式由设计阶段给出，见 §6 「配置生效链路」）。
5. 保存失败（校验失败 / 写入失败 / 目标模块拒绝）时显示失败原因，且**装置保持原配置运行**（不得半生效）。
6. 修改值在**保存成功后**持久化，装置重启后恢复为最后一次保存值。
7. 「恢复默认值」为独立控件，与「保存」控件的触摸目标间距 ≥ 48 px（防误触，见 §3.6）。

> **补注（2026-09-16，PM 裁定）——CF-04 降级，上文交互 7 / 验收 4 的实际达成度**：
>
> | 口径 | 结论 |
> |------|------|
> | **实际达成** | **7 个可写参数中仅 1 个真热生效**（`system.log_level`，`tracing_subscriber::reload`） |
> | **其余 6 个** | `intercore.host` / `intercore.port` / `intercore.heartbeat_interval_sec` / `intercore.reconnect_interval_sec` / `gateway.listen_addr` / `gateway.listen_port` ⇒ **已落盘 + 内存副本已更新，但需重启 `mupcd` 进程才改变运行行为** |
> | **PM 裁定** | **接受该降级**（不投入"把 6 个参数做成真热生效"的改造）⇒ 上文交互 7「无需重启装置」与验收 4「自动生效，无需重启」**对上述 6 个参数不成立**；**参数条目与数值一律未改** |
> | **对屏上的要求（已落地）** | 页面说明行与保存确认弹层「影响范围」改为分级口径「**日志级别立即生效 · 连接类参数需重启进程生效**」；保存成功后的 Toast **并入后端回执 `message`**（逐字点名需重启的键），**不得**再统一写「已生效」 |
> | **降级可见性** | 后端在**回执 `message` / 审计 `reason` / 日志 `tracing::warn!`** 三处如实声明"需重启"（实现见 `mupc/crates/mupc-core-bin/src/hot_apply.rs` 的逐字段结论表、`config_service.rs`） |
> | **计数口径** | 字段表共 **9** 键 ⇒ `editable=true` 可写 **7** ⇒ 真热生效 **1** ⇒ 需重启 **6** |
>
> **⚠️ 已知残余（如实登记，非本批范围）**：`Toast` 文本区宽 400 px ⇒ 长回执在真机上按 `DOTS` 截断（「需重启」可见、**具体键名可能被截掉**）；且回执 `message` 的用字不在字体码表控制面内（`项` / 全角括号等缺字形）。详见 UI 设计文档 §3.6 与 `p2_config.rs` 偏差登记 **PD24**。

> **补注（2026-09-19，PM 裁定）——F9 字段级降级：上表 7 个配置项中 3 项在现网无配置承载**：
>
> | 口径 | 结论 |
> |------|------|
> | **无承载的 3 项** | ① **IEC 104 心跳间隔**（`GatewayConfig` 只有 `listen_addr` / `listen_port`，**无心跳字段**）② **核间「本地端口」**（`InterCoreConfig` 只有 `host` / `port`——是**对端**——无本地绑定端口）③ **遥测上报周期**（上送节拍在 `startup.rs` 是**硬编码常量**，`CoreConfig` 无对应项） |
> | **影响** | 验收 **CF-01「所有配置项可读可写」在字段级为 4/7**；相应地 **CF-02「心跳间隔 1–60 秒越界提示」不可达**；上表 7 行中这 3 行**本期无法在屏上读写** |
> | **PM 裁定** | **接受该降级**：本期**不**为这 3 项新增配置承载（新增键 + `watch` 接线 + 校验，属功能新增而非缺陷修复）。**上表字段条目与数值一律未改**（保留为需求），本补注是其**达成度声明** |
> | **诚实性** | 前端**不臆造键**：这 3 项**不出现**在配置视图里（不是"显示了但改不动"），实现侧已逐条登记理由（`mupc/crates/mupc-core-bin/src/console_host.rs` 的 `PENDING_NO_CARRIER` 与紧邻注释） |
> | **⚠️ 另一处口径差异（非降级，属落地口径）** | 代码实际**可写集合**与上表是 **−3 / +3**：**多 3 项**（`intercore.host`「对端地址」/ `intercore.heartbeat_interval_sec` / `intercore.reconnect_interval_sec`，本 PRD 未列）；**「对端 IP 地址」按设计 §4.3.4 落为 `gateway.listen_addr`（本机监听地址）**——该改动属设计阶段裁决（R-08），屏上标签已写作「本机监听地址 · IEC 104」，**不得**再表述为"对端 IP / 远程主站地址"。逐字段对账表见设计 §4.3.3 末的注 |

---

### 3.3 F-日志管理类

> 合并来源：原 08 §5 日志管理。**降级约束**：本地屏无文件导出通道。

#### F10 日志查看 / 选项式筛选 / 实时推送（PM 已裁定 T-1、T-2）

**用户故事**：作为现场运维，设备出问题时我要在柜前直接翻日志，按级别、模块和预设时间区间筛选，定位最近发生的错误，而不是回去拷日志文件。

**功能要求（触摸场景，PM 裁定 T-1：仅选项式筛选）**：

| 功能 | 说明 | 本地屏口径 |
|------|------|------------|
| 实时日志查看 | 新日志实时追加到列表顶部 | 保留（推送延迟 ≤ 2 s） |
| 级别筛选 | ERROR / WARN / INFO / DEBUG **多选**过滤 | 保留（**选项式多选**控件） |
| 模块（target）筛选 | 按日志来源模块过滤 | 保留（**选项列表**，可多选） |
| 时间范围筛选 | 按时间区间筛选 | **预设区间选择**：最近 1 h / 最近 24 h / 自定义起止；自定义起止用**日期 + 时间选项式步进**（非文本输入） |
| 分页 / 滚动展示 | 浏览历史日志 | 保留（滚动加载） |
| ~~关键字搜索（自由文本）~~ | 按任意关键字匹配消息内容 | **明确不做**（T-1 裁定）：不提供自由文本搜索，不引入虚拟键盘 / 文本输入 |
| ~~日志导出~~ | 下载为 `.log` 文件 | **明确不支持**（T-2 裁定）：本地屏无文件 / 下载通道，不提供任何导出入口 |

**日志级别标签颜色**（沿用 08 §5.3）：

| 级别 | 颜色 |
|------|------|
| ERROR | `#DC3545`（红色背景） |
| WARN | `#FFC107`（橙色背景） |
| INFO | `#17A2B8`（蓝色背景） |
| DEBUG | `#9AA0A6`（灰色背景） |

**实时推送规范**（由 WebSocket 改为本机通道，语义保留）：
1. 日志页顶部显示实时通道状态：连接成功「实时日志已连接」（绿）/ 断开「实时日志已断开，正在重连…」（红）。
2. 新日志自动滚动到最新；用户手动向上滚动查看历史时**停止自动滚动**，并显示「回到最新」按钮。
3. 通道断开后自动重连；重连期间不丢已展示内容。

**⚠️ PM 裁定声明（T-1 / T-2，不得再作待定）**：

- **T-1 检索仅选项式，明确不做自由文本搜索**：`web-api` 原「逐行读取全部日志文件 + 内存关键字过滤」的检索实现，既无索引、又需自由文本输入（虚拟键盘，含中文则为重大工程），在嵌入式触屏路径上**不可行**。PM 裁定：本期日志检索**仅提供**「日志级别多选 + 模块筛选 + 预设时间区间（最近 1 h / 24 h / 自定义起止）」三类**选项式**筛选，**不提供**关键字 / 文本搜索，**不引入**虚拟键盘。检索范围由预设区间天然限定，**不存在全库无界检索**。
- **T-2 导出明确不支持**：本地屏无浏览器下载通道，`web-api` 的「拼接日志文件字节流返回下载」在本地屏无对应出口。PM 裁定：本期**不提供任何导出入口**（无文件、无下载、无 USB / 局域网导出）。
  - **边界说明**：若现场确有「取走日志」诉求，属**本 PRD 范围外**，须由 PM **另立需求**（候选：USB 介质导出、局域网导出），届时在设计阶段评估可行性；本 PRD 不预设实现、不承诺时间点。

**验收标准（可量化）**：
1. 实时日志推送延迟 ≤ **2 s**。
2. 级别筛选支持**多选**（选项式），过滤结果更新 ≤ 1 s。
3. 模块筛选为**选项列表**（不出现文本输入框），过滤结果更新 ≤ 1 s。
4. 时间范围筛选提供**预设区间**（最近 1 h / 24 h / 自定义起止）**选项**；自定义起止通过日期 + 时间选项式步进完成，**全程无文本输入**（见 §3.6 F12）。
5. **日志筛选经 ≤ 3 次触摸完成**（级别 / 模块 / 时间三个维度各 ≤ 1 次选择；组合筛选不引入额外确认步骤）。
6. **无任何文本 / 键盘入口**：日志页及其筛选流程中**不出现**文本输入控件或虚拟键盘（验收项为「无键盘入口」）。
7. 列表使用斑马纹（奇偶行不同背景色），行高 ≥ 40 px（触摸可点选）。
8. 实时通道断开后自动重连，重连成功 ≤ 5 s；重连期间界面不冻结、不丢已展示内容。
9. **日志为空时**显示明确空态（如「当前筛选条件下无日志」），不显示空白区。
10. 明确验收：**本版不提供日志文件导出**（验收项为「无导出入口」，见本节上方 T-2 声明）。

---

### 3.4 F-安全与联锁类

> 本节为 v2.0 本次修订新增，承载原 `web-api` 联锁出口的需求级迁移（§3.7 映射 #1 / #2 / #3）。
> **页面落点已裁决：统一归入「安全 / 联锁」页（P4）**，不并入主状态页 F6 装置状态区（理由见 §3 信息架构裁决）。三个功能构成「看状态 → 释放 → 授权」的安全处置闭环。

#### F16 联锁状态展示

**用户故事**：作为现场运维，我要在柜前看清装置当前是否处于联锁态、由哪个触发源引起、故障灯 / 运行灯状态如何，判断能否恢复运行。

**展示内容**：联锁触发源明细（DI 触发源）、latch（自锁 / 保持）态、停机失败标志、指示灯 DO1 / DO2（故障灯 / 运行灯）状态。

**验收标准（可量化）**：
1. 「安全 / 联锁」页常显联锁总态：**已联锁 / 未联锁**，以「文字 + 语义色 + 图标」三重冗余表达，不依赖颜色单一信道。
2. 逐条列出触发源明细（含数量）；无触发源时显示明确空态（「当前无联锁触发源」），**不显示空白区**（§5 / EDGE-08 同口径）。
3. latch 态独立可见（已保持 / 未保持）；停机失败标志独立可见。
4. DO1 / DO2 指示灯以「指示灯 + 文字」双通道展示（故障灯亮 / 运行灯亮 / 灭），不可得时显示「未知」，**不得默认显示为「正常」**。
5. 联锁状态变化后 ≤ **2 s** 反映到屏上（无需人工刷新，通道能力见 PL-3）。
6. 状态源不可用时整区显示「联锁状态不可用」，**不得显示为「未联锁」**（二者语义不同，禁止混淆，同 EDGE-09 / EDGE-12 口径）。

#### F17 人工释放联锁

**用户故事**：作为现场运维，触发源已复位后，我要在柜前直接人工释放联锁让装置恢复运行，不必回办公室。

**验收标准（可量化）**：
1. 释放入口位于「安全 / 联锁」页；触摸目标 ≥ **64×64 px**（关键操作，§3.6 F14）。
2. 释放须**二次确认**：弹层展示「将执行的操作 + 影响范围 + 当前触发源与 latch 状态」，默认焦点在「取消」（§3.6 F14）。
3. 释放须**记审计**（PL-1）：含时间戳、操作者固定标识（`local-console`）、操作类型、操作前后状态、结果；成功与失败均留痕。
4. 释放条件不满足时（触发源未复位 / latch 保持时间不足 `release_hold_secs` 等）**必须明示具体拒绝原因**，**不得静默失败**（EDGE-12）。
5. 释放请求不得重复生效：≥ 500 ms 内重复点击不重复提交（防抖 / 幂等，§3.6 F14 / EDGE-14）。
6. 释放成功 ≤ **2 s** 内页面联锁态更新为「未联锁」（若条件仍不满足，保持「已联锁」并显示原因）。

#### F18 M1 保护跳闸 / 停机人工授权重启

**用户故事**：作为现场运维，M1 保护动作导致停机后，我要在柜前完成人工授权重启。

**验收标准（可量化）**：
1. 授权入口位于「安全 / 联锁」页；触摸目标 ≥ **64×64 px**（关键操作）。
2. 授权须**二次确认**（弹层展示操作 + 影响范围），默认焦点在「取消」。
3. 授权须**记审计**（PL-1），成功与失败均留痕。
4. **仅非 latch 态可授权**：处于 latch 态时入口不可用并**明示原因**（「处于 latch 态，须先释放联锁」），**不得静默失败**（EDGE-12）。
5. 失败（含条件不满足）须明示具体拒绝原因；≥ 500 ms 内重复点击不重复生效。

---

### 3.5 F-审计类

> 本节为 v2.0 本次修订新增，承载 PL-2「审计查询」能力的**页面落点裁决**：**独立「审计」页（P5）**，不并入日志页（理由见 §3 信息架构裁决）。
> 审计**写入**能力（PL-1）为跨功能通用要求，约束所有写操作，定义见 §3.7。

#### F19 审计记录查询（PL-2）

**用户故事**：作为现场运维 / 技术支持，本机无登录，我要在柜前查到「谁在什么时候改了什么、成功还是失败」，审计就是唯一凭据。

**验收标准（可量化）**：
1. 「审计」页按时间**倒序**展示审计记录，每页 **20 条**，支持滚动加载。
2. 每条含：时间戳、操作者标识（`local-console`）、操作类型、操作前后值摘要、结果（成功 / 失败）、失败原因。
3. 提供**选项式**筛选：时间范围（最近 1 h / 最近 24 h / 自定义起止，日期 + 时间**选项式步进**）、操作类型（**选项列表多选**）；**全程无文本输入**（同 §3.6 F12 口径）。
4. 筛选结果更新 ≤ **1 s**；查询响应 ≤ **2 s**。
5. 审计页为**只读**：不提供任何编辑 / 删除 / 清空入口（PL-1「仅可追加」在界面上的体现）。
6. **无导出入口**（同 T-2 口径，PL-2）。
7. 审计为空（或被筛选排除）时显示明确空态（「当前筛选条件下无审计记录」），**不显示空白区**，**不得显示为「未加载」**；审计**数据源不可用**时显示「审计记录不可用」，**不得显示为「无审计记录」**（EDGE-17）。
8. 首页展示最近一条审计的时间戳（便于判断审计链路是否在持续写入）。

---

### 3.6 F-触摸交互通用需求

> 本节为 v2.0 新增（v1.0 主体为只读屏，明确排除交互）。以下为**交互需求**，不含渲染栈 / 输入栈选型（属设计）。

#### F11 页面导航与切页

**验收标准（可量化）**：
1. 提供常驻导航入口（导航栏 / 标签），可到达全部 **6 个页面**：主状态页、配置页、日志页、**安全 / 联锁页**、**审计页**、**装置与外设页（原「系统 / 关于页」，v2.2 更名）**（与 §3 信息架构表 P1–P6 一一对应）。
   > **（v2.2，T-8 已裁定 2026-09-23）页数口径不变**：裁定为"合并、不新增页" ⇒ 本条「**6 个页面**」与第 2 条**均无需回写**（原"若裁决新增页则改 7 页"的前提**不再成立**）；仅 **P6 页名**按裁定同步为「装置与外设」（**功能承载增加外设只读数值，导航入口数量与可达性判据不变**）。
2. 从任意页面到达任一目标页面（**含新增的安全 / 联锁页与审计页**）**点击次数 ≤ 2**。
3. 子页面提供**返回**控件（固定位置，同一应用内位置一致）。
4. 当前所在页面在导航中有明确选中态（视觉 + 文字）。
5. 导航**不提供**任何 AI 类页面入口（见 §3.8），亦不做置灰占位。

#### F12 表单输入范式（PM 已裁定 T-1）

**需求口径（裁定结论，全程无文本输入）**：

1. **基线范式：全程无需虚拟键盘 / 文本输入——仅「选项列表 + 数字步进 + 确认 / 取消」**：
   - 枚举类（日志级别、时间预设区间、模块筛选、**审计操作类型**）→ **选项列表**（单选 / 多选）；
   - 数值类（端口、心跳、周期、IPv4 每段）→ **数字步进**（受最小值 / 最大值硬约束）；
   - IPv4 地址 → **四段数字步进**（每段 0–255）；
   - 确认 / 取消 → 弹层按钮（含联锁释放 / M1 授权的二次确认弹层）。
2. **日志检索仅选项式**（T-1 裁定）：检索维度为「级别多选 + 模块筛选 + 预设时间区间」，**不提供自由文本搜索**，因此**不存在**需要虚拟键盘的场景（见 §3.3 F10）。**审计检索同口径**（时间范围 + 操作类型，见 §3.5 F19）。
3. **本期不引入虚拟键盘**（含中文 / 字母 / 数字软键盘均不引入）；交互全程仅由选项列表、数字步进、确认 / 取消构成。

**验收标准（可量化）**：
1. **全部 6 个页面**（含配置页、日志页、**审计页**、**安全 / 联锁页**）的**全部交互**可在**全程不出现任何键盘 / 文本输入控件**的前提下完成（验收项为「零键盘 / 零文本输入」）。
2. 配置页全部字段可在**无键盘**前提下完成编辑与保存；审计页筛选（时间范围 / 操作类型）同样零文本输入。
3. 数值输入控件具备「最小值 / 最大值」硬约束，越界值无法被输入（而非仅事后报错）。
4. 日志筛选经 **≤ 3 次触摸**完成（同 §3.3 F10 验收 5）；审计筛选同口径（§3.5 F19）。

#### F13 触摸响应时延与反馈

**验收标准（可量化）**：
1. 触摸按下到**视觉反馈**（按下态/高亮）≤ **100 ms**（P95 ≤ 200 ms）。
2. 页面切换（主状态页 ↔ 配置页 ↔ 日志页）≤ **300 ms**。
3. 列表滚动帧率 ≥ **30 fps**（单帧 ≤ 33 ms），滚动中无卡顿白屏。
4. 触摸响应不得阻塞主状态页的数据刷新（F5 指标在任何页面下均须满足）。

#### F14 误触防护

**验收标准（可量化）**：
1. 全部可点控件的触摸目标 ≥ **48×48 px**（8 寸 1024×768 下约 7.6 mm，满足人机工程学最小触摸尺寸）；关键操作（保存、联锁释放、M1 授权）触摸目标 ≥ **64×64 px**（约 10 mm）。
2. 相邻可点控件触摸目标间距 ≥ **16 px**；「破坏性/生效性操作」与「取消/返回」控件间距 ≥ **48 px**。
3. 生效性 / 破坏性操作需**二次确认**：确认弹层明确写出「将执行的操作 + 影响范围」，默认焦点在「取消」。
4. 连续快速重复点击同一生效性控件不得重复生效（生效操作具备幂等 / 防抖，≥ 500 ms 内不重复触发）。
5. 滑动与点击不得互相误触发（列表滑动过程中不触发列表项的点击动作）。

#### F15 屏幕无操作超时回归主状态页

**用户故事**：作为现场运维，我希望屏幕长期停在我离开时的配置页时不会误导后来的人，而是自动回到状态总览。

**验收标准（可量化）**：
1. 无触摸操作达到超时时间（默认 **60 s**，可配置）后，自动返回**主状态页**。
2. 超时返回**不得**丢失正在编辑但未保存的输入：若有未保存修改，超时前须给出提示或保留草稿状态（语义由设计确认，但**不得静默丢弃用户输入**）。
3. 超时计时在触摸事件时重置；超时返回动作本身不产生审计记录。
4. 存在未关闭的确认弹层时不触发超时返回（避免与进行中的操作冲突）。

---

### 3.7 F-平台整合（移除 web-api 后的需求级迁移声明）

> 本节**只写「应具备什么能力」**，不设计实现。`web-api` crate 当前承载的出入口，逐一给出处置结论。

**原 web-api 出口 → 本地屏归属映射**：

| # | 原 web-api 出口 | 能力说明 | 处置 |
|---|------------------|----------|------|
| 1 | `GET /api/v1/interlock/status` | 联锁状态（DI 触发源 / latch / 停机失败 / 指示灯 DO1/DO2） | **迁移**：本地屏须具备**联锁状态展示**能力（含触发源明细、latch 态、故障灯/运行灯状态）。**落点已裁决：归入「安全 / 联锁」页（P4）**，见 §3.4 **F16**（不再「F6 或独立联锁区」二选一） |
| 2 | `POST /api/v1/interlock/release` | 人工释放联锁（触发源复位且保持 ≥ release_hold_secs 才清 latch） | **迁移**：本地屏须具备**人工释放联锁**操作入口；须二次确认 + 审计留痕；**释放条件不满足时须明示拒绝原因**（不得静默失败）。**落点已裁决：归入「安全 / 联锁」页（P4）**，见 §3.4 **F17** |
| 3 | `POST /api/v1/interlock/ack_m1` | M1 保护跳闸/停机人工授权重启（仅非 latch 态生效） | **迁移**：同上，须具备授权入口 + 二次确认 + 审计留痕 + 失败原因明示。**落点已裁决：归入「安全 / 联锁」页（P4）**，见 §3.4 **F18** |
| 4 | `GET /api/v1/status` | 装置整体状态 | **迁移** → F6 / F7（主状态页 P1）/ F8（装置与外设页 P6，原系统页，v2.2 更名） |
| 5 | `GET/PUT /api/v1/config` | 运行参数读写 | **迁移** → F9（配置页 P2） |
| 6 | `GET /api/v1/logs` | 日志查询 | **迁移** → F10（日志页 P3） |
| 7 | `GET /ws/logs` | 实时日志推送 | **迁移** → F10 实时推送（日志页 P3；通道形态属设计） |
| 8 | `GET /api/v1/strategy-mode` | 当前控制源（local_priority）查询 | **迁移（只读）**：本地屏须展示「当前控制源」。AI 停用期该值为固定语义（本地策略引擎为唯一默认下发源），可并入 F6（主状态页 P1） |
| 9 | `PUT /api/v1/strategy-mode` | 切换 local_priority | **暂停**：AI 停用期该写操作在后端已被拒绝（503）；随 AI 重启一并恢复（§3.8） |
| 10 | `GET/PUT /api/v1/mode`、`GET /api/v1/mode/list` | 5 种预设运行场景查询与切换 | **暂停**：依赖 AI 引擎 `mode_selector`，无数据源（§3.8） |
| 11 | `GET /api/v1/ai/*`（predictions / decision / rewards / status / finetuning） | AI 可视化 | **暂停**（§3.8） |
| 12 | `PUT /api/ai/weights`、`GET /api/ai/audit` | 专家干预 / 干预审计 | **暂停**（§3.8）。⚠️ 但「**写操作审计留痕与查询**」作为**通用能力**须保留并要求本地屏具备——审计写入见 PL-1，审计查询见 PL-2 与 §3.5 **F19**（「审计」页 P5） |
| 13 | `/api/ai/models`、`/api/ai/abtest/*`、`/api/ai/rollback` | 模型版本管理 / A/B 测试 / 回滚 | **暂停**（§3.8） |
| 14 | `/api/auth/*`（login / logout / password） | Session 登录、密码修改、角色 RBAC | **取消**：Web 访问面移除后登录面失去载体；PM 裁定本机权限模型为「无登录 + 审计 + 二次确认」（T-3，见 §2.1），原 08 §2.3 认证机制（含 `RequireRole` 等持权校验）**不再适用**。本地屏**不设**登录 / 会话 / 角色界面 |
| 15 | `GET /api/v1/ai/stream`（SSE 事件推送；**代码中并无 `GET /sse` 路由**，原表述有误，本次订正） | 服务端事件推送（场景切换 / AI 决策 / 预测 / 告警 / 遥测 / 联锁事件，见 `web-api/src/sse/mod.rs`） | **迁移（能力级）**：本地屏的实时刷新通道须具备等价的推送 / 拉取能力（形态属设计，见 PL-3）。⚠️ 该端点自身承载的多为 **AI / 场景切换类事件**，随 §3.8 一并暂停；本地屏对状态 / 日志 / 告警 / 联锁的实时能力要求由 PL-3 与各功能验收独立约束，**不依赖复用该端点** |
| 16 | 静态 Web 资源（`static/*.html`） | 浏览器 UI | **取消**。⚠️ **如实说明现状**：源码中仅存 `web-api/src/static/ai-monitor.html` 等文件，但**运行路由从未挂载任何静态资源服务**（`startup.rs` 组装的 Router 无 `ServeDir` / `fallback`），即该资源对外**本不可达**；随 web-api 移除一并清理（含孤立静态文件） |

**平台整合的通用能力要求（验收级）**：

| ID | 要求 | 验收标准（可量化） |
|----|------|--------------------|
| PL-1 | **写操作审计（T-3 裁定）**：所有来自本地屏的写操作（**配置保存、联锁释放、M1 授权、任何下发**）必须留痕；因无登录，操作性以「审计 + 二次确认」补偿。**模式切换为暂停项，本期无界面入口（§3.8 映射 #9 / #10），故本期不产生该类写操作；AI 恢复后须按同一口径纳入审计** | 每条审计含：时间戳、操作者标识（本机固定标识，如 `local-console`）、操作类型、操作前后值、结果（成功/失败）、失败原因；写入成功率 ≥ 99.99%；审计记录**仅追加、不可删改** |
| PL-2 | **审计查询**：本地屏须提供审计记录查询能力（**页面落点已裁决：独立「审计」页 P5，见 §3.5 F19**） | 支持按时间范围 / 操作类型筛选；查询响应 ≤ 2 s；分页展示（每页 20 条）；**导出降级为不支持**（同 F10 / T-2）；**全程无文本输入**（T-1 同口径） |
| PL-3 | **实时通道**：本地屏须具备与 mupcd 的实时数据通道（状态/日志/告警） | 通道断开 ≤ 3 s 被感知并显示；恢复 ≤ 1 s 回到实时（沿用 v1.0 §4.4 阈值）；通道**不得**被用作绕过受控接口的旁路写通道 |
| PL-4 | **远程访问取消**：移除对外 HTTP 访问机制 | 验收项：装置**不再监听对外 HTTP 端口**，不再提供 Web UI；远程运维能力本期取消（若日后需要，另立需求） |
| PL-5 | **遗留物清理的可见性**：与 web-api 相关的对外行为须消失 | 验收项：原 8080 端口不再可达；`web_api` 配置项处置结论须在设计阶段明确（删除或改作本地通道用途，不得保留无效配置误导运维） |

> ⚠️ **能力缺口提示（须 PM 知悉）**：移除 web-api 后，**远程（网络可达）运维手段归零**——所有展示与操作只能在设备柜面完成。原 08 面向远程用户的登录/权限体系随之取消。若现场存在「不便到柜前」的运维场景，须由 PM 评估是否另立远程通道需求。

---

### 3.8 暂停范围（保留需求设计，本期不实现）

> **暂停统一说明**：以下章节的**需求设计保留**（原 08 §6 / §7 / §8 内容不删），但**本期不实现**。暂停原因：**AI 引擎已停用，无数据源**；恢复条件：**观测空间重构 + AI 引擎重启后启用**。
> 相应地在本地屏上：**不提供 AI 类页面入口**（不做置灰占位，避免现场误以为"坏了"）。

| 暂停项 | 来源 | 原需求要点（保留） | 恢复条件 |
|--------|------|--------------------|----------|
| **AI 决策可视化** | 08 §6 | 预测曲线（光伏/负荷，含置信区间）、AI 决策逻辑与依据、实时奖励值及历史趋势、运行模式状态、在线微调监控 | AI 引擎重启 + 观测空间重构 |
| **专家干预** | 08 §7 | 优化目标权重滑块调整、强制切换运行模式、干预审计 | 同上 |
| **A/B 测试** | 08 §8 | 流量分配配置、效果对比指标、模型安全回滚 | 同上 |
| **模型版本管理** | 08 §8.1 | 模型多版本并存、状态管理（active / standby / archived / failed） | 同上 |
| **在线微调监控** | 08 §6.5 | 微调进度、数据缓冲区、效果变化 | 同上 |
| **控制源切换（local_priority 写）** | 08 §7.2 / web-api `/strategy-mode` PUT | 本地优先模式热切换 | AI 引擎重启（停用期后端已拒绝写） |
| **预设运行场景切换** | 08 §7 / web-api `/mode` GET/PUT、`/mode/list` | 5 种预设运行场景查询与切换 | AI 引擎重启（依赖 `mode_selector`，无数据源） |
| **AI 引擎状态项** | 08 §3 | AI 引擎就绪/异常状态展示 | 同上（停用期该值为固定语义，无展示价值） |

> **注**：暂停 ≠ 删除。恢复时以上需求须重新按**本地触摸**形态评估（原 08 的滑块/图表/表格交互在 1024×768 触摸屏上的可用性需重新设计）。
> **与 PL-1 的口径一致**：上表「控制源切换」「预设运行场景切换」本期**无界面入口**，故不产生写操作、亦无审计记录；恢复后按 PL-1 同口径登记审计（含二次确认）。

---

### 3.9 F-外设数值展示类（v2.1 增量）

> **本节为 v2.1 本次增量新增**，承载技术债 **U-73**（来源：`docs/superpowers/reports/端到端数据流审查-外设采集到显示存储上云-2026-09-23.md` §三 缺陷 #10）。**性质：新需求，非缺陷**——用户已于 **2026-09-23 裁定立项**：外设（空调 HVAC / 消防 / BMS / 储能表 / PCS）的数值**须能在本地显示终端上看到并实时更新**。
> **本节只定「展示什么 + 验收标准」**；页位归属见 §3.9.1（**T-8 已于 2026-09-23 裁定：采用「两处拆」，并入 P4 / P6，不新增页、6 页口径不变**），帧结构 / 取数形态属**设计合同**，本节不预设。

**现状实证（写作依据，逐条可核）**：

| # | 事实 | 证据（文件:行） |
|---|------|------------------|
| N-1 | 既有功能项**无一项**涉及外设数值：P1 F1–F7 / P2 F9 / P3 F10 / P4 F16–F18 / P5 F19 / P6 F8 | 本文件 §3 信息架构表；审查报告 §三 缺陷 #10 |
| N-2 | 屏帧契约只有 F1–F4 段 + `device`/`alarms`/`info`/`interlock` 四段，**无外设段**——不是"采了没显示"，是**契约里没有** | `mupc/crates/display-proto/src/frame.rs:379-419`（`DisplayFrame`）；`PROTO_VERSION: u8 = 2`（`frame.rs:21`） |
| N-3 | 屏上**唯一**外设数值 = F1 SOC（BMS 站）；消防 / HVAC / BMS 只能以**告警文本**间接出现 | `mupc/crates/mupc-core-bin/src/startup.rs:445-450`（SOC 注入）；`mupc/crates/mupc-core-bin/src/display_host.rs:409-427`（`storage.events` → `AlarmItem`） |
| N-4 | `meter_batt` / `hvac` / `fire` / `pcs` 四角色的 `DataPackage` **恒为空包**，真值只走 `telemetry_points`（逐点） | `mupc/crates/mupc-southd/src/mapper.rs:461`（`empty_package()`） |
| N-5 | 外设真值当前**只进 `telemetry` 表（写侧）**，中途无可读内存快照 ⇒ 屏侧无入口可取 | 审查报告 §一① / §二③；`startup.rs:451`（`SouthSink::on_station_data`）→ `buffer_telemetry` |
| N-6 | 站级**在线 / 离线 / 最后成功时刻**无结构化出口；离线仅产生 `events` 文本 | `mupc/crates/mupc-southd/src/scheduler.rs:701-743`（`handle_failure`）、`:728-735`（`mark_success`）；`stale_timeout_s: 5`（`mupc/deploy/config/mupc_core_config.production.yaml:144`） |
| N-7 | 新增中文标签存在**真机豆腐块**风险：生成字体 cmap 仅 **324 码位** | `mupc/crates/local-display/fonts/lv_font_cmap.txt`（清单头「本清单码位数：324」）；既有门禁批见 `mupc/crates/mupc-core-bin/src/console_host.rs:752`（23 码位） |

#### 3.9.0 共同前置：外设实时值的数据可用性要求（跨模块，需求级）

**RQ-9.0-1（可消费）** 五类外设（HVAC / 消防 / BMS / 储能表 / PCS）的**实时值**须能被本地屏消费；**不得**要求屏侧轮询数据库（`telemetry` 表）取数——「轮询数据库挑最后一条」不满足本要求。

**RQ-9.0-2（最新值快照 + 变更通知）** 须存在「**最新值快照 + 变更通知**」语义的入口：屏在**帧节拍内**（≤1 s）获得「当前最新值 + 距上一拍发生变化的量」。入口形态（内存快照 / 通知 / 通道）属**设计合同**裁决，本 PRD 不预设。

**RQ-9.0-3（单一真源）** 该入口与 **01 号（上云）**、**03 号（存储 / 查询）** 消费**同一份**最新值快照；不得各自另建第二份真源——同一量在不同出口必须一致。

**RQ-9.0-4（故障隔离）** 单站离线 / 单点读失败**不得**导致其余站、其余点的最新值不可得（站级与点级可用性标志，沿用 F5.5 口径）。

**RQ-9.0-5（跨文档口径对齐 —— 本 PRD 只提要求，不代改他文）**

| 对端 | 现状缺口 | 本 PRD 的要求 |
|------|----------|----------------|
| **03 号（数据处理与存储）** | `03 PRD` 把遥测历史的消费方写作 `web-api`，而该 crate 已整 crate 删除 ⇒ 消费方**当前不存在**（技术债 **U-70**） | 改判时须把「**12 号本地屏外设数值展示（消防 → P4，HVAC / BMS / 储能表 / PCS → P6「装置与外设」；v2.2 T-8 裁定，不新增页）**」列为该入口的消费方之一（与「上云点表 / 导出」并列或择一）。**回写动作落在 03 号文档** |
| **01 号（通信网关 / 上云）** | 01 号只定义"周期上送遥信 / 遥测"的**机制**，**未定义外设点表**（技术债 **U-74**） | 本 PRD **不定义**上云点表；只要求上云与上屏**共用同一最新值入口**（RQ-9.0-3） |

> **边界声明**：本节为**数据可用性要求**；若实现需新增缓存 / 通知机制，其形态、落点、容量属**设计合同**，本 PRD 不规定。

---

#### F20 HVAC（风冷空调机组）数值展示

> **页面归属（v2.2，T-8 已裁定 2026-09-23；原「待定」）**：并入 **P6「装置与外设页」**（与 F8 装置信息同页——同属"只读、非处置"类信息）。**页数不变（6 页）。**

**用户故事**：作为现场运维，我要在柜前直接读到空调的**柜内温度 / 湿度 / 内盘管温度、当前是否在运行、有哪些告警位**，而不是只能从告警文本里猜设备状态。

**展示内容（量纲已由采集侧按 `point_table` 登记消解，屏侧不重算）**：

| 显示项 | 点名（位置式口径） | 厂方地址 | 单位 | 精度 | 备注 |
|--------|--------------------|----------|------|------|------|
| 柜内测量温度 | `hvac_in_1` | 30001 | ℃ | 1 位小数 | 有符号（×0.1） |
| 内盘管测量温度 | `hvac_in_3` | 30003 | ℃ | 1 位小数 | **厂方文档自相矛盾（Q-10）**，投运须比对；屏上不得据此作判决 |
| 柜内测量湿度 | `hvac_in_4` | 30004 | %RH | 1 位小数 | 无符号（×0.1） |
| 系统运行状态 | `hvac_di` 位 7 | — | 枚举 | 停止 / 运行 | 文字 + 语义色 + 图标三重冗余 |
| 告警位（20 个） | `hvac_di` 位 9–24、26–29 | — | 布尔 | 活跃 / 非活跃 | **逐位**展示，位名可复述 |
| 辅助状态位（可选） | `hvac_di` 位 0–6、8、30 | — | 布尔 | — | 含内风机 / 应急风机 / 制冷 / 加热 / 各除湿态 / 自检 / 报警继电器输出 / 循环模式 |

**告警位清单（必须逐位可见，位名取自点表登记）**：柜内温感故障、柜内高温告警、柜内低温告警、柜外温感故障、柜外高温告警、柜外低温告警、柜内湿感故障、柜内高湿告警、柜内低湿告警、柜外湿感故障、柜外高湿告警、柜外低湿告警、压缩机高压告警、压缩机低压告警、制冷失效告警、制热失效告警、内盘管温感故障、内盘管低温告警、三相电报警、接管 / 回风温差报警。
> 点表全量登记见 `mupc/crates/mupc-southd/src/point_table.rs:833-863`（`BitClass::Alarm` 20 条 + `State` 10 条 + `Reserved` 1 条，位 25 为保留位）。

**⚠️ 不得编造的量（如实声明）**：**柜外温 / 湿度的「测量值」不在当前点表中**——`point_table.rs:829-831` 仅登记 3 个 HVAC 标量（柜内温度 / 内盘管温度 / 柜内湿度）。因此屏上**只允许**出现"柜外高温 / 柜外低温 / 柜外湿感故障 / 柜外高湿 / 柜外低湿"等**告警位**，**不得**出现"柜外温度数值 / 柜外湿度数值"字段。若产品要求显示柜外温湿度**数值**，须由厂方补点（**待产品确认 P-3**，见 §8 T-10）。

**验收标准（可量化）**：
1. **本机可验证**：柜内温度 / 柜内湿度 / 内盘管温度三项常显，单位与小数位与上表一致；量纲换算不在屏侧发生（屏侧显示值 == 采集侧工程值，逐值比对）。
2. **本机可验证**：系统运行状态以「停止 / 运行」文字 + 语义色 + 图标三重表达，不依赖颜色单一信道。
3. **本机可验证**：20 个告警位**逐位**可见（不得只有汇总「有告警」）；每位可读出其名称与活跃 / 非活跃态。
4. **本机可验证**：屏上**不存在**"柜外温度 / 柜外湿度"数值字段（验收项为「无该字段」，防臆造）。
5. **本机可验证**：三项测量值中任一点无效时，该点显示 `--` + 「未取数 / 数据异常」，**禁止显 0.0**（同 F3.4 / F4.3 口径）。

---

#### F21 消防（工商储火灾报警控制器）数值展示

> **页面归属（v2.2，T-8 已裁定 2026-09-23；原「待定」）**：并入 **P4 安全 · 联锁页**（与联锁同源——现场处置时"看状态 → 释放 → 授权"与消防状态须同页可见）。**页数不变（6 页）。**

**用户故事**：作为现场运维，我要在柜前读到消防主机**系统状态、火警等级、探测器登记数与逐只状态**，并看懂「钢瓶气压未配置」。

**展示内容**：

| 显示项 | 点名 | 厂方地址 | 类型 | 展示要求 |
|--------|------|----------|------|----------|
| 系统状态 | `fire_sys_1` | 4 | 位图 | 逐位语义展示：bit14 主电故障 / bit13 备电故障 / bit11 驱动电路 / bit10 压力传感器 / bit9 电磁阀 / bit8 喷洒标记；**未定义位显「未定义位 n」** |
| 钢瓶气压 | `fire_sys_2` | 5 | kPa | **"未配置"口径**：见下 |
| 烟感 / 温感 / 可燃状态 | `fire_sys_3` / `_4` / `_5` | 6 / 7 / 8 | 位图 | 逐位（bit0 干接点 / bit1 复合；bit2 预留显「预留」） |
| 火警等级 | `fire_sys_6` | 9 | 枚举 | 0 正常 / 1 一级报警 / 2 二级火警 / **3 预留 ⇒ 显「未定义」** / 4 紧急启动 / 5 紧急停止；**枚举外值显「未知」** |
| 探测器登记数量 | `fire_det_count`（= `fire_sys_7`） | 10 | 个 | 常显 |
| 逐只探测器 | `fire_det_*` / `fire_sys_8`–`_13` | 11 起 / 17 起 | 混合 | 地址、状态（bit12 报警总状态 / bit14 故障总状态）、数据 1（**展示层拆解**为烟雾 0.1 dB/M 与温度 raw−55 ℃）、数据 2 CO ppm、数据 3 VOC ppm、数据 4 H₂ ppm |

> 点表登记见 `point_table.rs:805-825`；探测器按 `(addr−17) % 6` 取组内模板（`FIRE_DET_TEMPLATE_START = 17`，`:867` / `FIRE_DET_STRIDE = 6`，`:869`；展开与查表见 `:884-887`）。

**「钢瓶气压未配置」口径（强制）**：
1. 站生命周期内该点**从未出现过非 0 值** ⇒ 屏上显「**未配置**」，**不得**显 `0 kPa`；**不得**据此判「气压异常 / 泄漏」。
2. 该点**不产任何事件**（点表登记口径），屏上**不得**为它生成告警。
3. 取数接缝**已存在**（`mupc/crates/mupc-southd/src/scheduler.rs:748-760`，`cylinder_pressure_configured(station_index)`），但**当前 core-bin 零调用点**（审查报告 §二②）⇒ 屏侧接线属**设计/实现**动作。

**验收标准（可量化）**：
1. **本机可验证**：系统状态位图的已定义位**逐位**可见；未定义位显「未定义位 n」，**不猜测语义**。
2. **本机可验证**：火警等级 6 个枚举值文案齐全（含 3 显「未定义」）；馈入枚举外值时显示「未知」，**不落到「正常」**。
3. **本机可验证**：钢瓶气压未配置场景下显「**未配置**」（断言不含字符串 `0 kPa`）；且该场景**不产生**告警条目。
4. **本机可验证**：探测器登记数量与逐只明细一致（明细条数 == 登记数量；不一致时须显式提示不一致，**不得静默裁剪**）。**展示口径（逐只 / 汇总 + 下钻 / 上限 N）待产品确认 P-4**（现网登记 n=20 ⇒ `fire_det` 块 **114 寄存器** = 19 只探测器；PRD 上限 n=100 ⇒ 该块 6×(n−1) = **594 寄存器**，**一屏不可全显**）。
5. **本机可验证**：数据 1 的两项拆解（烟雾 dB/M、温度 ℃）显示单位正确，且**拆解发生在展示层**（与采集侧整字口径一致，不改变 telemetry 中的整字值）。

---

#### F22 BMS 数值扩展展示（SOC 已有）

> **页面归属（v2.2，T-8 已裁定 2026-09-23；原「待定」）**：并入 **P6「装置与外设页」**。**页数不变（6 页）。** 第 2 条所述"下钻形态"属 UI 设计，仍见 §3.9.1。

**用户故事**：作为调试工程师，我要在柜前核对电池簇的**电压 / 电流 / 温度 / 单体极值 / 绝缘 / 累计电量 / 告警位**，而不只是看一个 SOC。

**范围声明**：F1 SOC（裁决值）**已实现**，本项**不重复**；本项覆盖 BMS 站其余**已采集但未上屏**的数值。

**展示内容**（分组；量纲精度取自 `mupc/crates/mupc-southd/src/point_table.rs:323-675` 的登记值——该段登记 **BMS 345 点**，其中 `bms_alarm` 为 FC02 离散位块 200–487 共 **288 位**（`:387-675`））：

| 分组 | 显示项（点名） | 单位 | 精度 |
|------|----------------|------|------|
| 簇组核心量 | 簇组电压（`bms_io_16`）/ 簇组电流（`bms_io_17`）/ 簇组模块温度（`bms_io_18`） | V / A / ℃ | 1 位 / 1 位 / 整数 |
| 健康度 | SOH（`bms_io_20`）/ SOE（`bms_meta_3`）/ 绝缘电阻（`bms_io_21`） | % / % / kΩ | 整数 |
| 单体极值 | 簇平均 / 最高 / 最低单体电压（`bms_io_22/24/26`）+ 最高 / 最低**对应点**（`_25`/`_27`，整字）；簇平均 / 最高 / 最低单体温度（`_23/28/30`）+ 对应点（`_29`/`_31`） | V / ℃ | 电压 **3 位小数**，温度整数 |
| 极差 | 单体温度极差（`bms_meta_4`）/ 单体电压极差（`bms_meta_5`）/ 最高单体温升（`bms_energy_13`，寄存器 151） | ℃ / V / ℃ | 整数 / 3 位小数 / 整数 |
| 功率 | 簇实时充放电功率（`bms_meta_6`）/ 允许充 / 放最大功率（`bms_io_2`/`_3`） | kW | 1 位小数 |
| 端子温度 | 端子温度 001–004（`bms_term_1`–`_4`） | ℃ | 整数（采集侧已消解 −40 偏移） |
| 累计量 | 簇累计充 / 放电电量（`bms_energy_1`/`_3`）、单次累计充 / 放（`_5`/`_7`）、可充 / 可放电量（`_9`/`_11`）；累计充 / 放容量 Ah（`bms_cap_1`/`_3`/`_5`/`_6`） | kWh / Ah | 1 位小数 |
| 极柱温度 | 最高 / 最低单体极柱温度（`bms_energy_17` / `_19`，寄存器 155 / 157） | ℃ | 整数 |
| 装置信息 | 主控程序版本号（`bms_meta_1`）/ 从控数量（`bms_meta_2`）/ PACK 组压最高 / 最低（`bms_meta_7`/`_9`） | — / 个 / V | 整数 / 1 位小数 |
| 告警位 | `bms_alarm`（FC02，288 位） | 布尔 | 见下 |

> 上表点名为按**位置式口径**（`<块名>_<块内偏移+1>`，`mupc/crates/mupc-southd/src/points.rs:129-131`）推得，**设计阶段须以 `point_table.rs` 登记表逐条复核**（本节只定"要展示这个量"，点名以登记表为准）。
> **分组展示口径**：一屏不可能容纳上述全部量 ⇒ **允许分组 / 分页 / 纵向滚动**（遵循 B8「主读数区不横向滚动」）。

**BMS 告警位（288 位）口径**：
1. **不要求逐位全屏罗列**（288 位在 1024×768 触摸屏上不可用）。
2. **要求**：显示「**当前活跃告警位的数量 + 活跃位点名清单**」，并可下钻查看（下钻形态属 UI 设计，见 §3.9.1）。
3. **空态与不可用须区分**（EDGE-09 同口径）：无活跃位 ⇒ 「无活跃告警位」；**源不可用** ⇒ 「BMS 告警源不可用」，**不得**显为「无活跃告警位」。
4. 下钻 / 汇总的具体口径**待产品确认 P-5**（见 §8 T-11）。

**验收标准（可量化）**：
1. **本机可验证**：上表各分组可读，且每量单位 / 小数位与登记表一致（逐量比对，量纲换算不在屏侧发生）。
2. **本机可验证**：「最高 / 最低单体电压 / 温度」与其**对应点**成对展示（极值与其位置不可分离，否则极值无从定位）。
3. **本机可验证**：288 位告警以「活跃数量 + 活跃位清单」呈现；无活跃位时显空态；源不可用时显「不可用」且两者文案**互不相同**。
4. **本机可验证**：温度类字段（模块温度 / 单体温度 / 端子温度 / 极柱温度）显示值与采集侧工程值一致（偏移量已由采集侧消解），屏侧**不重算偏移**。

---

#### F23 储能表（`meter_batt` / ADL400）数值展示

> **页面归属（v2.2，T-8 已裁定 2026-09-23；原「待定」）**：并入 **P6「装置与外设页」**。**页数不变（6 页）。**

**用户故事**：作为调试工程师，我要在柜前读到**储能侧电表**的三相电压电流、功率、功率因数与电能，用于核对储能回路的实际计量。

**展示内容**：

| 分组 | 显示项（点名） | 单位 | 精度 |
|------|----------------|------|------|
| 电压 / 电流 | 三相电压 A/B/C（`mb_ui_1`/`_2`/`_3`）；三相电流 A/B/C（`mb_ui_4`/`_5`/`_6`）；线电压 A-B / C-B / A-C（`mb_freq_line_2`/`_3`/`_4`） | V / A / V | 1 位 / **2 位** / 1 位 |
| 频率 | 频率（`mb_freq_line_1`） | Hz | **2 位** |
| 功率 | 三相有功 / 总有功（`mb_power_1`/`_3`/`_5`/`_7`）；三相无功 / 总无功（`_9`/`_11`/`_13`/`_15`）；三相视在 / 总视在（`_17`/`_19`/`_21`/`_23`） | kW / kvar / kVA | **3 位** |
| 功率因数 | A/B/C/总功率因数（`mb_power_25`–`_28`） | — | 3 位 |
| 电能（累计量） | 组合 / 正向 / 反向有功总电能（`mb_e_act_comb_1`/`mb_e_act_fwd_1`/`mb_e_act_rev_1`）；组合 / 正向 / 反向无功总电能（`mb_e_rea_comb_1`/`mb_e_rea_fwd_1`/`mb_e_rea_rev_1`）；分相正向有功电能 A/B/C（`mb_phase_1`/`_3`/`_5`） | kWh / kvarh | **2 位** |
| 质量指标 | 零序电流（`mb_phase_12`）；电压不平衡度（`mb_phase_13`）；电流不平衡度（`mb_phase_14`） | A / % / % | 2 位 / 1 位 / 1 位 |

> 点表与量纲见 `mupc/crates/mupc-southd/src/point_table.rs:753-800`（该段登记 **ADL400 40 点**）；站配置见 `mupc/deploy/config/mupc_core_config.production.yaml:286-354`。
> ⚠️ 上表点名按**位置式口径**推得（同 F22 注）；**以 `point_table.rs` 登记表为最终依据**（设计阶段逐条复核）。

**误用防护（硬约束，沿用 §6.1「误用提醒」同口径）**：
1. 储能表是**储能回路**计量（ADL400），**不是**台区关口总表（`grid_meter`，`/dev/ttyS4`，slave 3）。两表在屏上**不得混排、不得互相替代**。
2. 储能表相关字段的**标签须含"储能表"字样**（使远程支持人员电话对拍时不产生歧义）。
3. **台区关口总表的数值不在本增量内**（其落库与上云分别见技术债 U-69 / U-74）；若产品要求总表数值上屏，**另立需求**（见 §7）。

**验收标准（可量化）**：
1. **本机可验证**：上表各量常显，单位 / 小数位与登记表一致；电能类为**累计量原值**直接展示（不得自行差分 / 换算成日增量）。
2. **本机可验证**：储能表字段的标签文本含"储能表"；屏上**不存在**任何以"台区 / 关口 / 总表"命名且取自 `meter_batt` 的字段（验收项为「无混标」）。
3. **本机可验证**：电能类为累计量，**须带采集时刻**（见 F25.2），使"数值不变"可被判定为"未变化"而非"掉线"。

---

#### F24 PCS 数值扩展展示（在 F2 / F3 / F4 之外）

> **页面归属（v2.2，T-8 已裁定 2026-09-23；原「待定」）**：并入 **P6「装置与外设页」**。**页数不变（6 页）。** 既有 F2 / F3 / F4 仍在 P1 主状态页，本项为其扩展读数、**不改变 P1 落点**。

**用户故事**：作为调试工程师，我要在柜前读到 PCS 的**无功 / 功率因数 / 频率 / 机内温度 / 直流侧与累计电量**，用于核对下发目标与实读。

**展示内容（同一只读 3 区点表，`protocol V1.3`）**：

| 分组 | 显示项 | 寄存器 / 点名 | 单位 | 精度 |
|------|--------|----------------|------|------|
| 交流电压 / 频率 | 电网 A/B/C 相电压（1018–1020）、STS 电网电压幅值（1049）、交流母线频率（1021） | `pcs_3zone_19`–`_21` / `_50` / `_22` | V / Hz | 1 位 / **2 位** |
| 交流功率 | 视在功率 A/B/C/总（1025–1028）、无功功率 A/B/C/总（1033–1036）、功率因数 A/B/C/总（1037–1040） | `_26`–`_29` / `_34`–`_37` / `_38`–`_41` | kVA / kvar / — | 1 位 / 1 位 / **3 位** |
| 直流侧 | BMS 系统总电压（1008）、BMS 系统总电流（1009）、直流母线 / 中点电压（1011–1012） | `_9` / `_10` / `_12`–`_13` | V / A | 1 位 |
| 温度 | PCS 温度（1041）、DCDC 温度（1071） | `_42` / `_72` | ℃ | 整数 |
| 累计电量 | 交流累计充 / 放电电量（1042–1043 / 1044–1045）、直流累计充 / 放电电量（1072–1073 / 1074–1075） | `_43` / `_45` / `_73` / `_75` | kWh | 1 位 |
| 工作模式 | 工作模式判断（1066） | `_67` | 枚举 | 枚举文案；**枚举外值显「未知」** |

> 点表见 `mupc/deploy/config/mupc_core_config.production.yaml:246-284`（`pcs_3zone`，当前**整段注释**，待现场 RC-8 裁定后启用；点名按**位置式口径** `<块名>_<at>` 推得，**以配置 / 登记表为最终依据**）。
> ⚠️ **前置**：该站当前**未启用**；若本项上屏，PCS 增量读数的真源为 `intercore` 的 3 区读通道（有效），而非 `south_stations.pcs` 站——**两者不得同时启用**（禁双 master 共总线，`production.yaml:130-141`）。实现形态属设计。

**明确排除（硬约束）**：**1046–1065 的 STS 网侧 / 负载区**（负载电流 1051–1053、负载视在 1054–1056、负载有功 1057–1059、负载无功 1060–1062、负载功率因数 1063–1065）**不得**作为「PCS 输出」或「台区负荷」展示（沿用本文件 §6.1 误用提醒：属 STS / 负载侧数据，非 PCS 充放输出）。产品若要求展示 STS / 负载区，须**另立需求**（见 §7）。

**依赖声明（真源可得性）**：`intercore` 当前**仅**读 1010 / 1013 / 1022–1024 / 1029–1032（`mupc/crates/intercore/src/transport/modbus.rs:170-172`；`mupc/crates/intercore/src/pcs.rs:20/22`）⇒ 上表新增量属「**点表存在、真源可得、需扩展读取**」（同一 FC04 通道、同一 3 区只读点表，**无新增协议 / 无新增设备**）。

**验收标准（可量化）**：
1. **本机可验证**：上表各量常显，单位 / 小数位与点表一致；量纲（×0.1 / ×0.01 / ×0.001）换算由 mupcd 完成，**屏侧不重算**。
2. **本机可验证**：屏上**不存在**任何取自 1046–1065（STS / 负载区）的字段被标注为「PCS 输出 / 充放功率 / 台区负荷」（验收项为「无误用」）。
3. **本机可验证**：点级读失败时该量显示 `--` + 「源离线 / 未取数」，**禁止显 0.0**（同 F3.4 / F4.3）。
4. **本机可验证**：既有 F2 / F3 / F4 的判据（REG 1013 主判据、方向标注取自 F2）**不因本项扩展而改变**。

---

#### F25 外设数值的刷新与新鲜度

> **适用页位（v2.2，T-8 已裁定 2026-09-23）**：本项为**跨页通用要求**，适用 **P4（F21 消防）** 与 **P6（F20 HVAC / F22 BMS / F23 储能表 / F24 PCS）** 两处落点；本项自身**不引入新页面**。

**用户故事**：作为现场运维，我要能分辨"这个数是 5 秒前采的"与"这个站掉线了"——不能因为 HVAC 采得慢就以为设备坏了。

**验收标准（可量化）**：
1. **沿用 F5 基线，不新造第二套新鲜度口径**：帧到即更新；`当前时间 − 帧时间戳 > 2 s` 判「过期」并打标；过期后保留最近有效值但**不冒充实时**；各字段独立降级。（既有 F5.1–F5.5 全部继续有效。）
2. **外设自身采集节奏须可见**：现网站内采集周期 —— HVAC **5000 ms**（`production.yaml:399`）、BMS **1000 ms**（`:154`）、储能表 **1000 ms**（`:293`）、消防 **1000 ms**（`:363`；⚠️ **探测器块 n>20 时须 ≥5000 ms**，见 §9.5.4）⇒ 屏上每个外设量**须能显示其自身的采集 / 更新时刻（或"最近更新"指示）**，使 ≤5 s 的节奏**不被误读为掉线**。
3. **端到端上屏时延（要求值）**：外设量从"采集完成"到"屏上生效" ≤ **站周期 + 1 帧节拍（≤1 s）**；离散**告警位**在**变化后 ≤ 2 s** 上屏（与 F7.3 / F16.5 同口径），稳态期间不要求逐拍重传。
4. **「站离线」口径（强要求）**：
   - 离线判定阈值沿用采集侧（`south_stations.stale_timeout_s`，现网 **5 s**，`production.yaml:144`）——本 PRD **不重定义**该阈值。
   - 屏上须以**结构化字段（非告警文本）**呈现每站**在线 / 离线 / 降级**与**最后成功时刻**。
   - 站离线时该站**全部**数值显示 `--` + 「站离线」；**不得**保留旧值冒充实时，**不得**显 0。
   - **三种语义必须互不相同**（禁止混淆，EDGE-09 同口径）：①「**站离线**」②「**点未取数 / 未覆盖**」③「**值无效 / 数据异常**」。
5. **故障隔离**：某外设站离线**不得**影响其余外设站与 PCS 主链路的屏上刷新（站级 / 点级隔离）。
6. **预算不破**：新增字段不得使稳态 CPU 越过 §4.1 的 **40% 单核**上限；F13.4「触摸响应不阻塞主状态页刷新」继续有效。

---

#### F26 外设帧契约版本化与降级显示（硬要求）

> **适用页位（v2.2，T-8 已裁定 2026-09-23）**：本项为**帧契约级要求**（无自身页位）；其降级 / 缺省显示落点同上——**P4（消防）与 P6（HVAC / BMS / 储能表 / PCS）**。

**背景（Why）**：`mupc/crates/display-proto` 的 `DisplayFrame` 是 mupcd（发布方）↔ 本地屏（订阅方）的**跨进程冻结契约**（`frame.rs:1-14` 模块头；`PROTO_VERSION: u8 = 2`，`frame.rs:21`），消费方**已多处依赖**（HMI 侧 `local-display` 的通道层、测试桩）。新增外设字段属**线格式扩展**，须按版本化处理（技术债 U-73 落点③）。

**验收标准（可量化 / 可审查）**：
1. **版本号递增**：新增外设字段须伴随**契约版本号递增**（现 `PROTO_VERSION = 2` ⇒ **递增为 3**）；版本号是**单一真源**，**不得**让各段各带版本。
2. **版本校验不得放宽（硬约束）**：消费方**必须**继续执行"版本不一致即拒帧"（`DisplayFrame::check_version` / `from_json_slice`，`frame.rs:426` / `:441`）——**不得**为"兼容旧屏"而改为"容忍任意版本、静默按新语义展示"（既有设计 §3.5 条 1 / PRD §4.4.1 口径不变）。
3. **旧屏 / 旧后端共存表现**：
   - **旧 HMI + 新 mupcd**、**新 HMI + 旧 mupcd**：两端一律**拒帧**，屏上进入既有降级画面（≤ **3 s** 感知，§4.3）；**不得**黑屏、**不得**显示半帧 / 混版帧。
   - 降级文案须能**区分**「**版本不匹配**」与「**通道断开**」（现场排障需要，二者处置不同）。
   - **两端须同版本发布**：须在部署文档登记「mupcd 与 `local-display` 必须同版本发布」（属设计 / 部署动作）。
4. **字段缺失即"不可得"（新增段 / 新增字段一律）**：缺省不得落在"正常"语义上——屏上显式降级（`--` / 「未提供」/「未取数」），**严禁补 0 或臆造**（与 `AlarmsSection.available=false` / `InterlockSection.available=false` 的既有缺省语义同源）。
5. **帧预算不破**：新增字段后整帧仍须 ≤ `MAX_FRAME_BYTES`（**64 KiB**，`frame.rs:30`）；编码侧与解码侧**同源同值**（`to_json_slice` / `from_json_slice` 共用同一常量，口径不变）。
6. **未知字段容忍不破**：解码侧继续容忍**同版本下的未知键**（既有回归锚点），使未来再加字段时旧屏仍可解析；该容忍与第 2 条**不冲突**——容忍的是"同版本未知键"，拒绝的是"版本号不同"。
7. **既有字段语义不得改**：F1–F5 / F6–F8 / F16 既有字段的**含义、量纲、缺省语义**在本增量中**不得更改**（含 `Field.flag` 四态 `Valid / NotRead / Offline / RangeError` 的语义）。新增一律**加段 / 加字段**。
8. **帧时间戳与点级有效标志（既有 F5.4）对新字段同样适用**：外设字段不得绕过"每帧时间戳 + 每字段 valid 标志"的既有机制另立一套。

---

#### 3.9.1 本增量的页面归属（**已裁定：2026-09-23，采用「两处拆」**）

**裁定结论（用户 2026-09-23）：采用「两处拆」——合并进既有页，不新增页。**

| 外设类别 | 裁定页位 | 理由 |
|----------|----------|------|
| **消防类**（系统状态、火警等级、钢瓶气压「未配置」口径、探测器，即 **F21**） | **P4 安全 · 联锁页** | 与联锁**同源**；现场处置时"看状态 → 释放 → 授权"与消防状态须**同页可见** |
| **HVAC 数值 + 储能表 + BMS 数值扩展**（**F20 / F23 / F22**）**+ PCS 扩展数值（F24）** | **P6**，并将该页由「系统 / 关于页」**改名为「装置与外设」** | F8 装置信息 + 外设只读数值**同属"只读、非处置"类信息**（核对 / 排障用途） |

**后果（明确）**：**页数仍为 6 页** ⇒ 既有 **F11 验收 1/2 与 TT-01 的「6 页」口径及导航栏入口数量均不变**（这正是选择"合并"而非"新增 P7" 的理由：**不破该口径**）；`§3` 信息架构表已同步 P4 / P6 的承载功能与 P6 页名（**属本次登记的正文同步，非"因新增页而回写"**）。

**方案取舍留痕（原三方案的评价，裁定后结果）**：

| 方案 | 说明 | 约束 / 风险 | 裁定结果 |
|------|------|-------------|----------|
| **A** | 新增独立「**外设**」页（**P7**） | 需同步 §3 信息架构表、F11 / TT-01 的「**6 页**」口径 ⇒ 须回写页数 | **已否决**——**为保 6 页口径不被破坏**，且合并后无信息丢失 |
| **B** | **P1 主状态页追加「外设摘要区」** + 独立页展开 | P1 已承载 F1–F7；B8 / T-6 已裁定"主读数区不横滚、允许整页纵向滚动"，追加会**加剧单屏信息密度**，与 §4.2 字号要求（主读数 ≥64 px / 过程量 ≥32 px）冲突 | **已否决**——P1 保持 F1–F7 主读数定位不变 |
| **C** | **全部并入既有页** | 原评"违反语义不同不得混排、维护与导航成本高" | **采纳，但按"两处拆"精确落点**（消防 → P4；HVAC / BMS / 储能表 / PCS → P6）——**未混入 P1 主读数区**，故不触原评风险 |

**跨页一致性要求（两处拆分页均适用）**：
1. **无需回写页数口径**：裁定为"合并、不新增页" ⇒ **F11 验收 1/2（6 页）与 TT-01（6 页）口径不变，无待回写清单**（原"若新增页则须回写 7 页"的前提**不再成立**）。**已按裁定同步**：§3 信息架构表 P4 / P6 承载功能行、P6 页名，以及 §3.9 各 F 项的页面归属标注。
2. 外设数值展示区**为只读**：不产生任何写操作，因此**不触发** PL-1 审计与 §3.6 F14 的二次确认要求（与 P3 / P5 / P6 同为读取页）；**P4 既有的写操作（联锁释放 / M1 授权）不受本项影响**，其二次确认与审计要求照旧。
3. 导航须遵守 F11.5：**不提供** AI 类页面入口，亦不做置灰占位。
4. **中文标签字体门禁（硬要求，见 F26 / 附录 B EX-24）**：新增的全部中文标签 / 单位 / 文案，**逐字**须落在生成字体码表 `mupc/crates/local-display/fonts/lv_font_cmap.txt`（当前 **324 码位**）内。**本项目实测**：本节候选标签用字中 **41 个码位不在该码表内**（`柜` `火` `防` `压` `湿` `热` `盘` `管` `烟` `感` `探` `钢` `瓶` `高` `低` `冷` `风` `表` `量` `视` `频` `零` `极` `累` `绝` `缘` `阻` `项` `循` `环` `子` `容` `差` `平` `衡` `等` `器` `体` `空` `气` `燃`）⇒ **必须先扩字库**（与既有 23 码位门禁批合并处置），否则真机上必现豆腐块。该门禁**未完成前不得通过 P4 真机验收**（既有裁定，U-42 关联批）；**最终缺口数以 UI 文案定稿后重算为准**，本节仅给出**下界**。

---

#### 3.9.2 本增量验收的验证分档（本机 / 真机）

| 档位 | 含义 | 覆盖条目 |
|------|------|----------|
| **本机可验证** | 在开发机（x86_64，**默认 feature**：无 `noto-font`）以单元 / 集成测试与代码审查可判定 | 数据接线、量纲精度、降级语义、空态与不可用区分、帧版本拒绝、帧尺寸、字段缺失不补 0、站离线三语义互异、"无柜外温湿度字段"、"无 STS 误用"、"无混标" |
| **需真机验证** | 依赖屏硬件 / 字库（`--features noto-font` + `fonts/gen_fonts.sh`）或真实串口设备 | **新增中文标签的豆腐块（cmap 覆盖）**、**槽宽是否装得下**（带符号数值 / 长标签，参照 U-42 同类风险）、字号与 0.5–1.5 m 可读性、列数与分组在 1024×768 的实际可读性、真机触摸页切换 ≤300 ms |
| **需现场验证** | 依赖现场 RS485 接线与设备 | 各站 `port` / `slave` / `baud_rate` / `parity` 校准（RC-1/3/5/8/11）、HVAC `interval_ms=5000` 下屏端节奏是否被误读、消防探测器登记数与地址序（RC-5/RC-10） |

> **门禁继承声明**：U-42 的「真字库下带符号 P 值装不下槽宽 ⇒ 数据被截断展示」与「字体豆腐块」两项为**既有 P4 真机验收硬门禁**；**本增量新增字段同样受其约束**（未处置前不得通过真机验收）。

---

## 4. 非功能性需求

### 4.1 资源预算

> ⚠️ **形态变更导致预算重定档**：v1.0 为「只读渲染」，本 v2.0 为「交互式 HMI」（含触摸控件、确认弹层、滚动列表、**6 个页面**），资源占用显著高于只读渲染。以下为**重新定档**后的边界上限，**不是放水**：超出即须评审。
>
> **预算口径（本次修订订正）**：本预算基于**「选项式触摸 + 只读文本渲染」**，**不含虚拟键盘 / 输入法（IME）栈、不含可编辑文本框**——T-1 已裁定全程无文本输入（§3.6 F12），交互仅由选项列表 / 数字步进 / 确认取消构成（§3.3 F10、§3.4 F16–F18、§3.5 F19 均遵循）。**原文「可能的软键盘」为裁决残留，已删除**；字体预算仅为**只读文本渲染**所需（中文字形集 + 数值字形），不含键盘 / 编辑态字形与候选词资源。

| 指标 | v1.0（只读，历史参照） | **v2.0 交互式 HMI（本期要求）** | 说明 |
|------|------------------------|--------------------------------|------|
| 常驻内存 | ≤ 64 MB | **≤ 256 MB** | 含触摸控件树、选项式控件状态、6 页页面状态、日志 / 审计列表缓存、字体资源（**只读文本渲染用；不含键盘 / IME 栈**） |
| CPU（稳态均值，1 s 窗口） | ≤ 15% 单核 | **≤ 40% 单核** | 稳态 = 停在主状态页或静态页面 |
| CPU（交互瞬时，页切换/列表刷新） | — | **≤ 60% 单核（瞬时）** | 瞬时峰值，不得持续 |
| CPU（数据通道断开满载） | ≤ 5% 单核 | **≤ 10% 单核** | 断连不得引起忙等/自旋 |
| 磁盘占用（可执行 + 资源） | 未约束 | **≤ 100 MB** | 含字体资源（**无 IME / 键盘资源**）；日志/审计属既有数据，不计入 |

**硬性要求**：
1. 渲染/交互进程内**不得有忙等 / 自旋等待**数据通道；空闲时应让出 CPU（阻塞读或事件驱动）。
2. 上述为**边界上限**；若设计所选轻量栈实测更优，须**下调**并写明实测值。
3. 若某栈超限，**必须**在设计阶段另行说明并获评审同意（不得静默放水）。
4. 内存不得随运行时间单调增长（长期运行无泄漏；日志列表须有上限与淘汰策略）。

### 4.2 布局与可读性（1024×768）

1. **主状态页：主读数区不横向滚动，允许整页纵向滚动（PM 裁定 T-6）**：SOC、PCS 运行状态、A/B/C 三相功率、A/B/C 三相电流、装置整体状态摘要、告警摘要（10 条中的可见部分）在 **1024×768** 下**不得横向滚动**。当信息量超出单屏时，**允许整页纵向滚动**或**分区切换**；纵向滚动**不得**导致主读数区横向滚动或数值跳位（v1.0「无滚动」约束仅在纵向方向放松，横向仍为硬约束）。
2. 目标阅读距离 **0.5–1.5 m**；主读数（SOC、PCS 状态词）字号 **≥ 64 px**，过程量（三相 P/I）字号 **≥ 32 px**，标注/辅助信息字号 **≥ 24 px**。
3. 状态语义用**「文字 + 语义色 + 图标」三重冗余**，不依赖颜色单一信道（满足色弱 / 单色可读）。
4. 布局固定分区，字段位置稳定不跳变（值变化不改布局，仅数值变化），便于现场记忆。
5. 屏幕长期静态显示时防烧屏：允许对静态装饰做低影响处理（周期微移 / 反色节拍），但**核心数值区不得因防烧屏措施而抖动可读性**。
6. 触摸目标尺寸与间距要求见 §3.6 F14（人机工程学：≥ 48×48 px ≈ 7.6 mm；关键操作 ≥ 64×64 px ≈ 10 mm）。

### 4.3 可靠性与自恢复

| 指标 | 要求 |
|------|------|
| 渲染进程异常退出后自恢复 | ≤ **3 s** 自动重启并恢复显示（由拉起机制保证，归属在系统集成侧） |
| 进程隔离 | 显示进程崩溃 / 重启**不得影响** mupcd 主进程、策略引擎、核间 / 南向通信 |
| 数据通道断开感知 | ≤ **3 s** 感知并进入断连降级画面 |
| 数据通道恢复 | mupcd 恢复并重建通道后，≤ **1 s** 回到实时显示 |
| 上电启动顺序 | 显示进程先于 / 并行于通道就绪时可显示「初始化中」占位；通道就绪后 ≤ 1 s 切到实时画面；显示进程**不得阻塞** mupcd 启动关键路径 |
| 触摸失效 | 触摸无响应时**不得**影响数据刷新与其他既有画面（见 §5） |
| 界面可用性 | 主状态页在装置运行期间可用性 ≥ **99.9%** |
| 配置写入送达率 | ≥ 99.99%（本地总线异常时保持原配置并明示失败，见 §5） |

### 4.4 跨进程数据通道可靠性边界（通道形态留给设计）

本 PRD 不裁定通道具体技术形态，但要求其对上层表现满足：

1. **数据投递语义**：允许丢帧，但**不允许静默传输损坏数值**（须有校验 / 序列号 / 版本一致性手段，或丢帧重传最新帧）。
2. **断连检测**：渲染端须在通道中断 ≤ 3 s 内感知并进入降级画面；恢复 ≤ 1 s 回实时。
3. **畸形帧防护**：渲染端不得因通道对端行为异常而崩溃或内存失控（畸形帧丢弃并计数告警）。
4. **单向展示通道**：展示数据通道只承载「读展示 + 心跳/健康」信息，**禁止**承载下行写指令。
5. **写操作独立通道（v2.0 新增）**：运维写操作（配置 / 联锁释放 / M1 授权）走**独立的受控接口**，与展示通道解耦；该接口须具备：参数校验、鉴权（若 §2.1 定为无身份则至少须有防重放/防误触）、审计留痕、失败原因回传。
6. **禁止直连**：显示进程**任何情况下**不得直连核间 modbus / PCS 总线。

### 4.5 数据刷新频率

| 数据项 | 刷新频率 | 方式 |
|--------|----------|------|
| SOC / PCS 状态 / 三相 P / 三相 I | ≥ 1 Hz（帧到即更新） | 数据通道推送 / 拉取 |
| 装置整体状态（uptime / 温度 / 内存） | 5 s | 数据通道 |
| 连接状态指示灯 | 5 s | 数据通道 |
| 告警列表 | 新增 ≤ 2 s | 数据通道推送 |
| 实时日志 | ≤ 2 s | 实时通道推送 |
| 联锁状态 / latch / DO 灯（F16） | 变化后 ≤ 2 s | 数据通道 |
| 版本信息 | 页面加载时一次性读取 | 数据通道 |
| 配置项当前值 | 进入配置页时读取 | 受控接口 |
| 审计记录（F19） | 进入审计页时查询（筛选变更即时重查） | 受控接口 |
| **（v2.1 增量）** 外设 HVAC 量（温 / 湿度 / 运行态 / 告警位，F20） | 站周期 **5 s** + 帧节拍 ≤1 s；告警位变化 ≤2 s | 数据通道 |
| **（v2.1 增量）** 外设 消防量（系统状态 / 火警等级 / 探测器，F21） | 站周期 **1 s** + 帧节拍 ≤1 s；告警位变化 ≤2 s | 数据通道 |
| **（v2.1 增量）** 外设 BMS 扩展量（F22） | 站周期 **1 s** + 帧节拍 ≤1 s；288 位告警按活跃位变化 ≤2 s | 数据通道 |
| **（v2.1 增量）** 外设 储能表量（电气量 / 电能，F23） | 站周期 **1 s** + 帧节拍 ≤1 s | 数据通道 |
| **（v2.1 增量）** 外设 PCS 扩展量（F24） | 帧节拍 ≥1 Hz（心跳读 1010/1013 + 3 区扩展读） | 数据通道 |
| **（v2.1 增量）** 外设站在线 / 离线 / 最后成功时刻（F25.4） | 变化后 ≤2 s 上屏（**判定阈值**沿用 `stale_timeout_s` = 5 s，本 PRD 不重定义） | 数据通道 |

---

## 5. 边界与异常（不造假值原则）

> **总原则**：**源不可得 → 显式「不可得」**；可展示冻结帧但必须打标。**任何情况下不得用 0 / 陈旧值 / 臆测值冒充实时真值。**
> 「不可得」与「值为 0 / 无告警」是**语义不同**的两件事，界面不得混淆（见 EDGE-05 / EDGE-08）。

| ID | 异常场景 | 展示行为（渲染端） | 数据侧职责（mupcd） |
|----|----------|---------------------|----------------------|
| EDGE-01 | PCS 离线（核间 modbus 读失败，REG 1013 / SOC 源失效） | F2 显示「PCS 离线」；F3/F4 各相显示 `--` + 「源离线」；F1 若有另一源则显示该源并标注 | 帧中对应字段 valid=false；整体标「PCS 离线」 |
| EDGE-02 | SOC 双源皆失（BMS 站 + PCS REG1010 均无效） | SOC 区显 `--` + 「SOC 源失效」警示色；**不补 0、不沿用旧值** | valid=false，不下发疑似值 |
| EDGE-03 | 跨进程数据通道断（mupcd 侧 / 通道侧异常） | ≤ 3 s 切换「与主进程数据通道断开」画面；可保留最近有效帧并打「冻结 / 过期」标 | 通道健康状态随连接生命周期维护 |
| EDGE-04 | 点表未覆盖 / mupcd 尚未实现某点读取 | 该字段显 `--` + 「未取数」；不显示 0 | 该字段 valid=false，注明原因 |
| EDGE-05 | 字段数值域异常（读值越界 / 非有限 / 超物理量程） | 显 `--` + 「数据异常」；不做插值 | 域值化时做量程 / 有限性校验，异常置 invalid |
| EDGE-06 | REG 1013 与功率方向显著相反 | 主状态以 1013 展示，另加「方向不一致」角标 | 聚合比对后随帧给一致性标志 |
| EDGE-07 | 渲染进程自身异常 / 启动失败 | 黑屏或「显示进程异常」占位；由拉起机制 ≤ 3 s 重启 | 不阻塞主进程 |
| EDGE-08 | **日志为空 / 筛选无结果** | 显示明确空态（「当前筛选条件下无日志」），**不得显示为"未加载"或空白** | 返回 `total=0` 与筛选条件回显 |
| EDGE-09 | **告警源不可用**（区别于「无告警」） | 告警区显示「告警源不可用」，**不得显示为「无告警」** | 提供告警源可用性标志 |
| EDGE-10 | **配置写入失败**（校验失败 / 落盘失败 / 目标模块拒绝） | 显示失败原因；**保留用户已输入值**；装置保持原配置运行 | 返回结构化失败原因；保证不半生效 |
| EDGE-11 | **配置保存过程中被超时回归 / 页面切换打断** | 不得静默丢弃；须提示「有未保存修改」或保留草稿 | — |
| EDGE-12 | **联锁释放 / M1 授权被拒绝** | 明示拒绝原因（如「触发源未复位」「保持时间不足」「处于 latch 态」），**不得静默失败** | 返回具体拒绝原因 |
| EDGE-13 | **触摸无效 / 触摸设备异常** | 数据刷新与其他显示**不受影响**（触摸是独立输入路径）；可选：显示「触摸不可用」提示 | — |
| EDGE-14 | **触摸抖动 / 重复点击** | 生效性操作具备防抖，500 ms 内不重复触发 | 写接口幂等 |
| EDGE-15 | **日志检索范围超限**（自定义起止区间跨度过大，超出设计阶段评估的上限） | 提示「检索范围超限，请缩小时间范围」，**不执行全库检索** | 拒绝越界查询请求 |
| EDGE-16 | **版本 / 序列号字段缺失** | 显示「未提供」，**不臆造** | 字段为空即如实返回空 |
| EDGE-17 | **审计数据源不可用 / 审计查询失败** | 显示「审计记录不可用」，**不得显示为「无审计记录」**（同 EDGE-09 口径，二者语义不同） | 提供审计存储可用性标志；查询失败返回结构化错误 |

**以下 EDGE-18 ~ EDGE-24 为 v2.1「外设数值上屏」增量（§3.9）新增**：

| ID | 异常场景 | 展示行为（渲染端） | 数据侧职责（mupcd） |
|----|----------|---------------------|----------------------|
| EDGE-18 | **外设站离线**（HVAC / 消防 / BMS / 储能表任一站读失败，超过 `stale_timeout_s`） | 该站**全部**数值显 `--` + 「**站离线**」；**不补 0、不保留旧值冒充实时**；其余站与 PCS 主链路**刷新不受影响**；同时显该站**最后成功时刻** | 提供**站级**可用性标志与最后成功时刻（**结构化字段**，非仅 `events` 文本）；离线判定沿用 `stale_timeout_s`（现网 5 s） |
| EDGE-19 | **外设单点未覆盖 / 未取数**（点表无该点，或该点本轮未采） | 该点显 `--` + 「**未取数**」 | 该字段 valid=false（NotRead），注明原因 |
| EDGE-20 | **外设值无效 / 越界 / 非有限** | 该点显 `--` + 「**数据异常**」；**不做插值、不沿用旧值** | 域值化时做量程 / 有限性校验，异常置 invalid（RangeError） |
| EDGE-21 | **帧版本不匹配**（新 HMI + 旧 mupcd，或旧 HMI + 新 mupcd） | ≤3 s 进入降级画面，文案须能**区分「版本不匹配」与「通道断开」**；**不得**黑屏 / 半帧 / 混版帧（F26.3） | 帧内版本号为单一真源；两端同版本发布（部署口径） |
| EDGE-22 | **外设段缺失**（同版本帧内无该段，或旧帧落入降级路径） | 该段整段显「**不可用**」（`--` / 「未提供」），**不得**显为 0 / 「正常」/「无告警」 | 新增段缺省即"不可得"（与 `alarms.available=false` / `interlock.available=false` 同源语义） |
| EDGE-23 | **消防钢瓶气压未配置**（站生命周期内该点从未出现非 0 值） | 显「**未配置**」，**不得**显 `0 kPa`；**不得**据此判「气压异常 / 泄漏」；**不产生**告警条目 | 提供"是否曾出现非 0 值"的接缝语义（现成接缝见 `mupc-southd/src/scheduler.rs:748-760`；core-bin 当前零调用） |
| EDGE-24 | **BMS 告警位全 0（无活跃位）vs 告警源不可用** | 前者显「**无活跃告警位**」；后者显「**BMS 告警源不可用**」——**两者文案互不相同**（EDGE-09 同口径） | 提供告警源可用性标志，与"活跃位为空"分列 |

**量程 / 语义越界阈值口径**（供 EDGE-05 与 EDGE-06）：以「读值 × 量纲」后偏离物理合理区（如单相有功 / 电流绝对值超出该机型额定区显著比例，或 1013 状态枚举外值）判定；具体阈值在数据源字段表符号缺口追认时一并由厂方 / 设计确认。评审前采用「读值非有限或越 Int16 工程界」即判异常。

---

## 6. 数据源真实可得性

> 原则：**只列已核对到的来源；无法确定者标「待设计阶段确认来源」，不得臆造。**

### 6.1 PCS 侧（点表来源，沿用 v1.0 精读结论）

| 需求项 | 寄存器 | 类型 | 量纲 | 可用性 / 状态 |
|--------|--------|------|------|---------------|
| F1 SOC | **1010** | UInt16 | `*1 %` | ✅ 现有代码已读（`REG_SOC=1010`，FC04）；展示用裁决值 |
| F2 运行状态 | **1013** | UInt16 | 枚举 | ✅ 现有代码已读（`REG_RUN_STATE=1013`，FC04） |
| F4 电流 A/B/C | **1022/1023/1024** | Int16 | `*0.1 A` | ⚠️ 点表存在；mupcd 需扩展读取（真源可得） |
| F3 有功 A/B/C | **1029/1030/1031** | Int16 | `*0.1 kW` | ⚠️ 点表存在；mupcd 需扩展读取（真源可得） |
| F3 总有功 | **1032** | Int16 | `*0.1 kW` | ⚠️ 同上 |
| （佐证）故障 / 降额 / 并离网 | 1014/1015/1016 | UInt16 | 0正常1故障；0正常1降额；0并网1离网2异常 | 可选辅助 |
| ⚠️ 误用提醒 | 1046–1065（STS 区） | — | — | **负载(LOAD)/STS 侧数据**，非 PCS 充放输出；**不得**用作 F3/F4 |

**可行性结论**：F1–F4 的真实数值展示**均可落地**（同一只读点表 / 同一 FC04 读通道扩展读取，无新增协议 / 新设备）。**唯一缺口**为「用功率 / 电流正负号直接表达充放方向」的符号语义，处置见 §5 EDGE-06 与 F2/F3/F4 追认声明。

### 6.2 装置 / 系统侧（v2.0 新增，环平台现状核对）

| 需求项 | 核对到的来源（现状） | 可得性 |
|--------|----------------------|--------|
| F8 固件版本号 | `env!("CARGO_PKG_VERSION")`（mupcd 编译期版本） | ✅ 可得 |
| F8 编译时间 | 现状 `web-api` 的 `build_time` **与版本号取同一常量**（占位，非真实编译时间戳） | ⚠️ **待设计确认**：需补真实编译时间戳 |
| F6 uptime | 进程启动时刻起算 | ✅ 可得 |
| F6 CPU 温度 | `system-monitor` 的温度采集（`read_cpu_temp` / `TemperatureMetrics.cpu_temp_c`） | ✅ 可得（需经 mupcd 汇聚） |
| F6 内存使用率 | `system-monitor` 的 `MemoryMetrics`（含 `FullCollector`） | ✅ 可得（需经 mupcd 汇聚） |
| F6 IEC 104 连接状态 | 现状 `web-api` 的 `status` 返回硬编码 `"unknown"`（占位）；gateway 侧真实状态来源 | ⚠️ **待设计阶段确认来源** |
| F6 核间（intercore）连接状态 | 同上，现状为硬编码 `"unknown"` | ⚠️ **待设计阶段确认来源** |
| F6 跨进程数据通道状态 | 本地屏与 mupcd 通道自身的健康状态 | ✅ 可得（通道自有） |
| F6 当前控制源 | `AiIntegrator::engine_status()` / `is_local_priority()` | ✅ 可得（AI 停用期为固定语义） |
| F7 告警数据 | `security` 的 `AlertManager`（`AlertEvent` / `AlertType` / `AlertSeverity`，支持 active / history / acknowledge） | ⚠️ **待设计阶段确认**：现有 `AlertManager` 与运维告警的汇聚点及实际数据填充情况须核实 |
| F9 配置项当前值 | 现状 `web-api` 的 `AppConfig` 为**进程内内存值**，未落盘、未接入真实模块 | ⚠️ **待设计阶段确认**：真实配置落点与生效链路 |
| F10 日志 | `common::logging`：`RollingFileAppender`（DAILY）写入 `/var/log/mupc/mupc.log`（JSON 行格式，含 `timestamp` / `level` / `target` / `fields.message`）；`web-api` 的 `LogsHandler` 已实现按级别 / 关键字 / 时间过滤与分页（逐行读取，无索引）。⚠️ **本 PRD 不采用其「关键字过滤」能力**（T-1 裁定仅选项式，见 §3.3 F10） | ✅ 可得（检索性能须按 §3.3 评估；实现须按选项式筛选重构筛选路径） |
| PL-1/PL-2 审计 | 现状 `web-api::AuditLogger`，落点 `{log_dir}/audit`（含 `WebAuditEntry`：id / 时间 / 用户 / 角色 / 动作 / 资源 / 方法 / 状态码 / IP / UA） | ✅ 可得（须随 web-api 移除迁移落点与写入点）。⚠️ 现有条目含 `用户 / 角色 / IP / UA` 字段，本机无登录（T-3）后这些字段按 §2.1 口径落为固定标识 / 空值，**字段口径须在设计阶段定稿** |
| F16–F18 联锁状态与操作（PL-04~06） | `web-api` 的 `InterlockApi` trait（status / request_release / ack_m1），由 mupcd 在 `io.enabled` 时注入真实实现 | ✅ 可得（须迁移为本地屏受控接口；页面落点见 §3.4） |

> **说明**：上表「⚠️ 待设计阶段确认」项**不阻塞本 PRD 定稿**，但**阻塞对应功能的实现**。设计阶段须逐项给出确认结论；若某来源最终不可得，对应展示/操作项须按 §5 降级（显式「不可得」），**不得以占位值充当真实值**。

**PM 裁决 T-5：以下 5 项数据源来源留待设计合同阶段逐项确认（本 PRD 不臆造来源）**：

| # | 待确认数据源 | 对应需求项 | 现状 | 影响 |
|---|--------------|------------|------|------|
| 1 | **IEC 104 连接状态**真源 | F6（§3.1） | 现状 `web-api` 硬编码 `"unknown"` 占位 | 连接指示灯能否真值展示 |
| 2 | **核间（intercore）连接状态**真源 | F6（§3.1） | 现状硬编码 `"unknown"` 占位 | 同上 |
| 3 | **告警汇聚点**（`AlertManager` 与运维告警的汇聚及实际填充） | F7（§3.1） | 现有 `AlertManager` 与运维告警的汇聚关系须核实 | 告警列表能否真值展示 |
| 4 | **配置真实落点与生效链路** | F9（§3.2） | 现状 `AppConfig` 为进程内内存值，未落盘、未接入真实模块 | 配置读写能否真值 / 真生效 |
| 5 | **编译时间戳** | F8（§3.1） | 现状 `build_time` 与版本号取同一常量（占位） | 编译时间能否真值展示 |

> **处置**：以上 5 项**不阻塞本 PRD 定稿**；由**设计合同**逐项给出确认结论（来源定位 / 数据填充路径 / 生效验证方式）。若某项最终不可得，对应功能按 §5 显式降级（「不可得 / 未取数」），**禁用占位值冒充真实值**。

---

## 7. 明确范围外（本期不做）

1. **控制类指令下发**：不经本地屏向 PCS 下发充放/功率设定等**控制指令**；显示进程与展示通道均不承载下行控制写。本地屏的写操作仅限「配置 + 联锁人工处置」两类运维操作（§3.7）。
2. **AI 类全部页面**：见 §3.8 暂停范围（保留设计，本期不实现）。
3. **历史趋势 / 曲线 / 录波**：功率/SOC 历史曲线、统计报表不在本期（仅实时值与列表）。
4. **独立告警屏 / 声光告警**：只做告警列表展示（F7），不做独立告警页与声光输出。
5. **日志文件导出**：见 §3.3 F10 的 T-2 裁定声明（本期不支持，无任何导出入口）；现场取日志诉求须另立需求。
6. **远程访问 / 网络运维面**：投屏、截图上传、Web 化、局域网/远程访问一律取消（B4）。若日后需要，另立需求。
7. **DSTS / DCDC / 光伏 / 负载（STS 侧）专屏数据**：不在本期（避免误用 STS 负载区当 PCS 输出，见 §6.1 误用提醒）。
8. **虚拟键盘 / 自由文本输入**：**本期不做**（PM 裁定 T-1）。日志检索仅选项式（§3.3 F10），交互全程无键盘 / 文本输入（§3.6 F12）。
9. **多点触控手势**（缩放 / 双指）：不在本期（按单点触摸设计）。
10. **（v2.1 增量追加）台区关口 / 总表（`meter_grid`）数值上屏**：不在本期（其历史落库见技术债 U-69、上云点表见 U-74）。**储能表（`meter_batt`）≠ 总表**，不得互相替代（§3.9 F23 误用防护）。
11. **（v2.1 增量追加）PCS 点表的 STS / 负载区（1046–1065）上屏**：不在本期（沿用 §6.1 误用提醒：属 STS / 负载侧数据，非 PCS 充放输出）。若产品要求展示，另立需求（§3.9 F24 明确排除）。
12. **（v2.1 增量追加）外设数值的历史趋势 / 曲线 / 报表**：不在本期（沿用本清单第 3 条）；**待产品确认（T-9）**。本增量只做**实时值**。

---

## 8. 需求风险与待确认项

> **状态：T-1 / T-2 / T-3 / T-6 已由 PM 裁定并固化（见 §0 B5–B8、各功能章节）；T-4 属设计阶段裁决；T-5 保留「待设计阶段确认」（见 §6.2 五项清单）；T-7 为厂方追认项。**
> **（v2.1 增量追加）T-8 ~ T-12**：外设数值上屏（§3.9）引入的 5 项待确认 / 联动项——**均不阻塞** §3.9 需求定稿，但**阻塞**对应功能的实现或页面落地。**T-12 为跨文档联动项，其回写动作在 01 / 03 号文档，本 PRD 不代改。**
> **（v2.2 更新，2026-09-23）T-8 已裁定**：采用「两处拆」（消防 → P4；HVAC / 储能表 / BMS / PCS → P6 更名「装置与外设」），**不新增页、6 页口径不变**（裁定原文与后果见下表 T-8 行、§3.9.1）。**T-9 / T-10 / T-11 / T-12 仍为待确认 / 联动项，状态不变。**

| ID | 事项 | 影响 | 结论 / 处置 |
|----|------|------|-------------|
| **T-1** ✅已裁定 | 日志关键字搜索是否必需自由文本输入 | 高（§3.6 F12 与 §4.1 预算） | **裁定：日志检索仅选项式**（级别多选 + 模块筛选 + 预设时间区间），**不做自由文本搜索**，**不引入虚拟键盘**。已固化于 §3.3 F10、§3.6 F12、§7.8 |
| **T-2** ✅已裁定 | 日志导出降级方式 | 中 | **裁定：本期不支持导出**（无文件 / 下载通道，无任何导出入口）；现场取日志诉求另立需求。已固化于 §3.3 F10、§7.5 |
| **T-3** ✅已裁定 | 本机触摸权限模型 | 中（§2.1 与 PL-1） | **裁定：无登录 + 审计 + 二次确认**（物理在场即授权；无登录 / 会话 / PIN / RBAC）；写操作须二次确认 + 记审计。已固化于 §0 B5、§2.1、§3.7 PL-1 |
| **T-4** ⏳设计阶段 | `web_api` 配置项与遗留代码的处置结论（删除 / 改作本地通道用途） | 中（PL-5） | 本 PRD 只要求「对外行为消失」；具体处置由设计阶段裁决 |
| **T-5** ⏳待设计确认 | **5 项数据源来源待设计阶段确认**：① IEC 104 连接状态 ② intercore 连接状态 ③ 告警汇聚点 ④ 配置真实落点与生效链路 ⑤ 编译时间戳 | 中（决定 F6/F7/F8/F9 能否真值展示） | **保留「待设计阶段确认」**（不臆造）；清单见 §6.2；设计合同逐项确认，不可得则按 §5 显式降级 |
| **T-6** ✅已裁定 | 主状态页信息量在 1024×768 单屏的取舍 | 中（§4.2） | **裁定：主读数区不横向滚动，允许整页纵向滚动 / 分区切换**。已固化于 §0 B8、§4.2 |
| **T-7** ⏳厂方追认 | PCS 功率 / 电流符号极性追认（点表） | 低（数值展示不阻塞） | 追认前方向一律取 F2 状态机；符号仅作佐证 |
| **T-8** ✅**已裁定（2026-09-23）** | **外设数值的页面归属**：新增独立「外设」页（P7）/ P1 主状态页追加摘要区 / 并入既有页 | 中（决定导航与 §3 信息架构口径） | **原提案（保留）**：本 PRD 只给建议（推荐方案 A：独立 P7 只读页，见 §3.9.1），由产品 / UI 设计裁决；**裁决前 6 页口径不变**；裁决为新增页时须回写 §3 信息架构表、F11（页数）、TT-01（7 页），属**需求增量**而非推翻既有验收。**裁定（2026-09-23，用户）：采用「两处拆」——合并进既有页，不新增页**：**消防类（F21：系统状态 / 火警等级 / 钢瓶气压「未配置」口径 / 探测器）→ P4 安全 · 联锁页**（与联锁同源，现场处置时同页可见）；**HVAC 数值 + 储能表 + BMS 数值扩展（F20 / F23 / F22）及 PCS 扩展数值（F24）→ P6**，并将该页**改名为「装置与外设」**（F8 装置信息 + 外设只读数值，同属"只读、非处置"类信息）。**后果：页数仍为 6 页 ⇒ F11 / TT-01 的「6 页」口径与导航栏不变（无需回写、7 页前提作废）**；这正是选择"合并"而非"新增 P7" 的理由：**不破该口径**。方案 A / B 已否决（理由见 §3.9.1）。已落点：§3 信息架构表 P4 / P6 行与 P6 页名、§3.9.1、§3.9 各 F 项（F20–F26）的页面归属标注、附录 C #9 |
| **T-9** ⏳待产品确认（P-2） | **外设数值是否要历史趋势 / 曲线** | 中（若"要"，则超出本增量范围） | **本期默认不做**（沿用 §7.3「历史趋势 / 曲线 / 录波不在本期」）；若产品要求，**另立需求** |
| **T-10** ⏳待产品确认（P-3）＋厂方补点 | **HVAC 柜外温 / 湿度「测量值」是否上屏** | 中 | **当前点表无此量**（`point_table.rs:829-831` 仅 3 个 HVAC 标量）⇒ 屏上**只允许**呈现柜外相关**告警位**。若要求数值 ⇒ 须**厂方补点**（新寄存器 + 新登记行），属**功能新增**，须重走需求 / 设计流程 |
| **T-11** ⏳待产品确认（P-4 / P-5）＋UI 设计 | **外设大批量条目的展示口径**：① 消防探测器（现网登记 n=20 ⇒ 114 点；PRD 上限 n=100 ⇒ 595 点）② BMS 告警位（288 位） | 中（决定页内布局与滚动 / 下钻形态） | 本 PRD 已定**下界**：探测器须"登记数与明细一致且不静默裁剪"、告警位须"活跃数量 + 活跃位清单且空态与不可用互异"（F21.4 / F22.3）。**逐只罗列 / 汇总 + 下钻 / 分页上限**由产品 + UI 设计裁决 |
| **T-12** ⏳跨文档联动（U-70 / U-74） | **「最新值入口」的消费方改判（03 号）与外设上云点表（01 号）** | 高（决定 RQ-9.0-1~3 能否落地） | 本 PRD 只提**数据可用性要求**（§3.9.0）并要求"上云 / 上屏共用同一最新值快照"；**回写落在 01 / 03 号文档**（消费方清单须纳入「12 号本地屏外设数值展示（P4 / P6）」）。**不改他文** |

---

## 9. 术语与参考

| 术语 | 说明 |
|------|------|
| MUPC | 微电网特种调控装置 |
| HMI | 人机界面（Human-Machine Interface）；本 PRD 指柜面触摸式本地人机界面 |
| SOC 裁决值 | strategy-engine `ai_integration.rs` 中 `resolve_soc_source` / `apply_soc_source` 输出的双源（BMS 站 / 核间 PCS REG 1010）裁决域值 |
| 核间 | intercore（TCP/RJ45）与实时控制模块的通信；本屏不直连，数据由 mupcd 经通道供给 |
| 控制源 | 当前实际向 PCS 下发决策的来源；AI 停用期为本地策略引擎（TaiStorage） |
| 受控接口 | 本地屏执行写操作（配置 / 联锁）所经的、由 mupcd 提供并具备校验与审计的接口（形态属设计） |
| web-api | 原 08 模块承载的浏览器访问面 crate（REST / SSE / WebSocket / 静态资源 / Session 登录）；本期移除 |
| 就地表 | 设备柜面就地安装的显示 / 操作终端（本模块） |

**参考文档**：

| 文档 | 路径 / 说明 |
|------|-------------|
| 原 08 模块 PRD（已并入本文件） | `docs/superpowers/specs/modules/08-MUPC-Web管理与AI可视化-PRD.md`（顶部已加废弃/并入声明，保留追溯） |
| 本模块 v1.0 PRD（已被本文件取代） | 见文末附录 A |
| 权威点表 | 协议 V1.3（EMS）3 区只读点表（FC04）；见文首路径 |
| 功能清单 | `docs/微电网特种调控装置（MUPC）通信管理模块功能清单.md` |
| 既有一致性核对 | `mupc/crates/intercore/src/pcs.rs` + `transport/modbus.rs`（`REG_SOC=1010`、`REG_RUN_STATE=1013`、`REG_PHASE_P_A=1006` / `REG_PHASE_Q_A=1009`；读用 `read_input_registers` FC04） |

---

## 附录 A：v1.0 → v2.0 变更说明（可追溯）

### A.1 变更动因

见 §1.1。核心：**AI 引擎停用 + 取消 Web 访问机制**，本地屏由「只读状态屏」升级为「触摸式本地 HMI」，统一承载原 08 的本地展示与操作。

### A.2 逐条映射（v1.0 → v2.0）

| v1.0 内容 | v2.0 处置 |
|-----------|-----------|
| §1 产品概述（只读、headless 背景） | 重写为「触摸式 HMI」定位（§1），保留背景与价值中的可靠性/不造假原则 |
| §1.3 技术形态「不依赖浏览器内核」「显示进程只读」「不直连核间 modbus」 | **保留**「不依赖浏览器内核」「不直连核间 modbus」；**推翻**「只读」——改为「读 + 受控写（配置/联锁），写经 mupcd 受控接口，禁止直连总线」（§1.4 / §3.7 / §7.1） |
| §2 用户角色（现场运维 / 调试 / 远程支持，无交互） | **保留角色**；新增身份模型（本机无登录 + 审计 + 二次确认，§2.1），删除原 08 的 Session/角色/权限表 |
| F1 SOC 展示 | **保留**（§3.1 F1），验收阈值不变 |
| F2 PCS 运行状态 | **保留**（§3.1 F2），验收阈值不变 |
| F3 三相有功 | **保留**（§3.1 F3），验收阈值不变 |
| F4 三相电流 | **保留**（§3.1 F4），验收阈值不变 |
| F5 数据刷新与新鲜度 | **保留**（§3.1 F5），验收阈值不变 |
| §4 非功能性（只读预算：CPU ≤15% / 内存 ≤64MB） | **重定档**（§4.1：CPU ≤40%、内存 ≤256MB，交互式 HMI），并保留「不得忙等」「超限须评审」约束 |
| §4.2 布局（无滚动、无分页） | **放松**（§4.2：主读数区无横向滚动；信息量超单屏允许整页纵向滚动 / 分区切换）+ 新增触摸目标尺寸要求 |
| §4.4 数据通道可靠性 | **保留**（§4.4），新增第 5 条「写操作独立通道」、第 6 条「禁止直连」 |
| §5 边界与异常（EDGE 不造假值） | **保留全部原条目**（§5 EDGE-01~07），**新增** EDGE-08~17（日志空态、告警源不可用、配置写入失败、联锁拒绝、触摸无效、检索越界、审计源不可用等） |
| §6 范围外（交互操作 / 历史趋势 / 告警屏 / 远程访问 / 控制下发 / STS 专屏） | **修改**：「交互操作」由范围外 → **本期核心**（§3.6）；其余保留（§7） |
| §7 数据源字段表 | **保留**（§6.1），并新增 §6.2 装置/系统侧来源核对 |
| §8 可行性结论段 | **保留**，并扩展为 §6 全章（含多个「待设计确认」项） |
| — | **新增**：§3.1 F6–F8（并入原 08 §3）、§3.2 F9（并入原 08 §4）、§3.3 F10（并入原 08 §5）、**§3.4 F16–F18（安全与联锁，本次修订新增）**、**§3.5 F19（审计查询，本次修订新增）**、§3.6 F11–F15（触摸交互通用需求）、§3.7 平台整合（web-api 移除）、§3.8 暂停范围（原 08 §6–§8）、§8 风险与待确认项 |

### A.3 并入自 08 模块的内容清单

| 08 来源章节 | 并入位置 | 处置 |
|-------------|----------|------|
| §3 系统状态监控 | §3.1 F6（装置整体状态）/ F7（告警列表）/ F8（版本信息） | 并入实现（AI 引擎状态项除外 → 暂停） |
| §4 配置管理 | §3.2 F9 | 并入实现（表单交互改为触摸范式） |
| §5 日志管理 | §3.3 F10 | 并入实现（**T-1：仅选项式筛选，不做自由文本搜索；T-2：导出不支持**） |
| §6 AI 决策可视化 | §3.8 | **暂停**（保留设计） |
| §7 专家干预 | §3.8 | **暂停**（保留设计；web-api `/strategy-mode` PUT、`/mode` PUT 同暂停） |
| §8 A/B 测试管理 / §8.1 模型版本管理 | §3.8 | **暂停**（保留设计） |
| §9 边界条件（AI 类为主） | §3.8 / §5 | AI 类随暂停；通用边界并入 §5 |
| §10 非功能性（页面加载/AI 面板可用性等） | §4 | **重写**为本地屏口径（页面加载→触摸响应/页切换；Web 兼容性→触摸目标尺寸与可读性） |
| §11 数据持久化（审计/奖励/权重/模式/模型清单/A-B） | §3.7 PL-1/PL-2 + §6.2 | 审计保留（迁移）；AI 类随暂停 |
| §12 验收标准汇总 | §3 / §9 | 按新分类重构（AI 类验收项随暂停保留于 08 原文） |
| §2 用户角色与认证授权 | §2 / §2.1 | 角色收窄为现场人员；Web 登录体系取消（含 RBAC / `RequireRole` 持权校验），改为「无登录 + 审计 + 二次确认」（T-3） |

### A.4 v1.0 关键历史事实（保留供追溯）

- v1.0 已交付 `[REVIEWED: PASS]`（2026-09-09），依据协议 V1.3 点表与 `intercore/pcs.rs` 核对 1010 / 1013 / 1022-1024 / 1029-1032 字段一致——**该点表核对结论在本 v2.0 §6.1 中原样保留**。
- v1.0 既定技术形态（独立原生渲染进程、不依赖浏览器内核、不直连核间 modbus）在本 v2.0 中**继续有效**；仅「只读」一条被推翻。
- v1.0 衍生的设计文档与 UI 设计文档（`[DESIGN_APPROVED]`）因其「只读 / 无交互层」前提被推翻，须按本 v2.0 重新设计（见文首警示）。

### A.5 v2.0 草案 → v2.0（PM 裁决固化）变更说明

本次不改变产品范围，仅将 PM 对 v2.0 草案 7 项待确认中的 4 项**裁决结论固化**，消除未决表述：

| 裁决 | 结论 | 固化位置 |
|------|------|----------|
| **T-1** 日志检索 | **仅选项式筛选**（级别多选 + 模块筛选 + 预设时间区间 1 h/24 h/自定义起止）；**不做自由文本搜索**，**不引入虚拟键盘**；输入范式收敛为「选项列表 + 数字步进 + 确认/取消」；新增可量化验收「筛选经 ≤ 3 次触摸完成」「零键盘/零文本输入」 | §0 B6、§3.3 F10、§3.6 F12、§7.8、§8 |
| **T-2** 日志导出 | **本期不支持**，无任何导出入口；现场取日志诉求另立需求 | §0 B7、§3.3 F10、§7.5、§8 |
| **T-3** 权限模型 | **无登录 + 审计 + 二次确认**（物理在场即授权；无登录/会话/PIN/RBAC）；写操作（配置保存、模式切换、任何下发）须二次确认 + 记审计；原 08 §2.3 认证机制（含 `RequireRole`）废止 | §0 B5、§2.1、§3.7 映射 #14 与 PL-1、§8 |
| **T-6** 主状态页 | **主读数区不横向滚动，允许整页纵向滚动** | §0 B8、§4.2、§8 |
| **T-5** 数据源 | **保留「待设计阶段确认」**，并在 §6.2 列出 5 项清单交设计合同（不臆造） | §6.2、§8 |
| 其余 | T-4 属设计阶段裁决；T-7 为厂方追认项，均不阻塞定稿 | §8 |

> **T-3 口径补充（本次修订）**：上表 T-3 行引用的「模式切换」属 §3.8 **暂停项**（本期无界面入口、不产生写操作），因此**本期实际须审计的写操作集合 = 配置保存 / 联锁释放 / M1 授权 / 任何下发**；已同步订正 §0 B5、§2.1 第 2 条、§3.7 PL-1、附录 B PL-01。

### A.6 v2.0 需求评审修订说明（本次，2026-09-10）

本次为**评审 REQUEST_CHANGES 后的修订**，不改变产品范围与 PM 裁决（T-1/T-2/T-3/T-6 维持），仅消除裁决残留、补足页面落点、订正与代码不符的事实描述：

| # | 修订项 | 处置 | 落点 |
|---|--------|------|------|
| R1 | §4.1 预算说明残留「可能的软键盘」，与 T-1（本期不引入虚拟键盘）矛盾 | **删除软键盘表述**；预算口径明确为「**选项式触摸 + 只读文本渲染**」，**不含键盘 / IME 栈**；字体与内存预算理由同步改写 | §4.1 |
| R2 | 信息架构缺页落点：联锁展示 / 释放 / M1 授权、审计查询无页面；§3.7 #1 遗留「F6 或独立联锁区」二选一 | **裁决为 6 页信息架构**：新增「**安全 / 联锁**」页（联锁展示 + 释放 + M1 授权）与「**审计**」页（审计查询）；**不并入** F6 / 日志页（理由见 §3）；同步更新 §3 信息架构表、F11 导航、TT-01、PL-01~PL-06 落点、附录 B（新增 IL-01~IL-03、AU-01） | §3 / §3.4 / §3.5 / §3.6 F11 / §3.7 |
| R3 | §3.7 #15 记「`GET /sse`」——代码中无该路由（SSE 实为 `/api/v1/ai/stream`）；#16 静态资源实为未挂载 | #15 **订正为 `/api/v1/ai/stream`**；#16 **如实改述**：运行路由从未挂载静态资源服务（源码残留孤立 HTML），随 web-api 清理 | §3.7 映射 #15 / #16 |
| R4 | PL-1 将「模式切换」列为须审计写操作，与 #9 / #10 暂停（本期无入口）不一致 | **注明暂停期不适用**：本期须审计写操作集合移除模式切换；同步订正 §0 B5、§2.1、PL-1、附录 B PL-01、§3.8 补「预设运行场景切换」暂停行 | §0 B5 / §2.1 / §3.7 / §3.8 / 附录 B |

> **改号影响提示**：因本次新增 §3.4 / §3.5 两节，原 §3.4 → §3.6、§3.5 → §3.7、§3.6 → §3.8（**本文件内所有交叉引用已同步更新**）。
> `08-MUPC-Web管理与AI可视化-PRD.md` 文首「并入映射」表所引章节号为**改号前口径**（该文件已标 `SUPERSEDED`、仅作历史追溯，本次按约束未改动）；**以本文件章节号为准**。

---

## 附录 B：验收标准汇总（ID 索引）

> 各条验收标准的完整描述见对应章节；本表用于验收追踪。

| ID | 验收标准（摘要） | 验证方法 |
|----|------------------|----------|
| ST-01 | SOC 常显 + 数据源标注（BMS / PCS(REG1010) / 双源一致），同帧更新 | 集成测试 |
| ST-02 | SOC ≤15% / ≥85% 区间警示色，阈值对齐 soc_min/soc_max | 集成测试 |
| ST-03 | PCS 四态（停机/待机/充电/放电）文字+语义色+图标三重冗余 | 集成测试 |
| ST-04 | 充放主判据为 REG 1013；1013 失败不进降级反推 | 集成测试 |
| ST-05 | 1013 与 ΣP 方向显著相反时显示「方向不一致」角标，主状态不变 | 集成测试 |
| ST-06 | 三相有功/电流常显，保留 1 位小数，量纲由 mupcd 完成 | 集成测试 |
| ST-07 | 三相 P/I 刷新跟随帧节奏，帧到即更新 ≤500 ms | 性能测试 |
| ST-08 | 读失败 / 未取数显示 `--` + 标注，**禁止显 0.0** | 集成测试 |
| ST-09 | mupcd 推送帧率 ≥1 Hz；帧时间戳超 2 s 判过期并打标 | 集成测试 |
| ST-10 | 单帧更新上屏 ≤500 ms，无闪屏/残影 | 性能测试 |
| ST-11 | 每帧含时间戳 + 每字段 valid 标志；字段级独立降级 | 单元测试 |
| ST-12 | 装置整体状态卡网格（版本/编译时间/uptime/CPU 温度/内存/连接状态/控制源） | 集成测试 |
| ST-13 | 连接状态指示灯 + 文字双通道；不可得显示「未配置/未知」非「正常」 | 集成测试 |
| ST-14 | uptime / 温度 / 内存刷新 ≤5 s | 性能测试 |
| ST-15 | 告警列表最多 10 条，时间倒序，级别颜色+文字双通道 | 集成测试 |
| ST-16 | 新告警 ≤2 s 反映到屏 | 集成测试 |
| ST-17 | 告警源不可用 ≠ 无告警（分别显示） | 集成测试 |
| ST-18 | 版本信息页字段完整呈现，不随刷新跳动，缺失显「未提供」 | 集成测试 |
| CF-01 | 全部配置项触摸可读可写；只读不触发确认 | 集成测试 |
| CF-02 | 心跳 1–60 s 越界显示具体错误 | 单元测试 |
| CF-03 | 保存需二次确认，弹层展示前值→新值摘要 | 集成测试 |
| CF-04 | 保存成功后自动生效无需重启；生效判定 ≤5 s 内可见（方式由设计给出） | 集成测试 |
| CF-05 | 保存失败保留用户输入 + 明示原因 + 装置保持原配置 | 集成测试 |
| CF-06 | 保存成功后持久化，重启恢复最后保存值 | 集成测试 |
| CF-07 | 「恢复默认值」与「保存」控件间距 ≥48 px | UI 检查 |
| LG-01 | 实时日志推送延迟 ≤2 s | 集成测试 |
| LG-02 | 级别**多选**（选项式）筛选，结果更新 ≤1 s | 集成测试 |
| LG-03 | **模块筛选为选项列表（无文本输入框）**，结果更新 ≤1 s | 集成测试 |
| LG-04 | 时间范围为预设区间选项（最近 1 h / 24 h / 自定义起止，选项式步进） | UI 检查 |
| LG-05 | **日志筛选经 ≤3 次触摸完成**（不作自由文本检索） | 集成测试 |
| LG-06 | 列表斑马纹，行高 ≥40 px | UI 检查 |
| LG-07 | 实时通道断开自动重连，重连 ≤5 s，界面不冻结 | 集成测试 |
| LG-08 | 日志空态显示明确文案（EDGE-08） | 集成测试 |
| LG-09 | **本版无日志导出入口**（T-2） | 集成测试 |
| LG-10 | **日志页无任何文本 / 键盘入口**（T-1，零键盘 / 零文本输入） | UI 检查 |
| TT-01 | 常驻导航可达**全部 6 页（主状态 / 配置 / 日志 / 安全联锁 / 审计 / 装置与外设〔原系统页，v2.2 更名〕）**，**任意页到任意页（含新增的安全联锁页与审计页）点击 ≤2 次**；子页有返回；当前页有选中态；**导航无 AI 类入口** | UI 检查 |
| TT-02 | **全部 6 页（配置 / 日志 / 审计 / 安全联锁等）全程零键盘 / 零文本输入**即可完成全部交互（T-1） | 集成测试 |
| TT-03 | 数值控件具备最小/最大硬约束，越界不可输入 | 单元测试 |
| TT-04 | 触摸按下→视觉反馈 ≤100 ms（P95 ≤200 ms） | 性能测试 |
| TT-05 | 页面切换 ≤300 ms | 性能测试 |
| TT-06 | 列表滚动 ≥30 fps | 性能测试 |
| TT-07 | 触摸响应不阻塞主状态页数据刷新 | 集成测试 |
| TT-08 | 控件触摸目标 ≥48×48 px；关键操作 ≥64×64 px；间距 ≥16 px；危险控件间距 ≥48 px | UI 检查 |
| TT-09 | 生效性操作二次确认，默认焦点在「取消」 | 集成测试 |
| TT-10 | 生效性操作 500 ms 防重复触发 | 集成测试 |
| TT-11 | 滑动不误触发列表项点击 | 集成测试 |
| TT-12 | 无操作 60 s（可配置）回归主状态页，不静默丢弃未保存输入 | 集成测试 |
| TT-13 | 确认弹层打开时不触发超时回归 | 集成测试 |
| PL-01 | 本地屏写操作（**配置保存 / 联锁释放 / M1 授权 / 任何下发**）全部留痕（时间/操作者固定标识/类型/前后值/结果/失败原因）（T-3）。**模式切换为暂停项、本期无入口，不在本期验收范围**（§3.8） | 集成测试 |
| PL-02 | 审计记录仅追加、不可删改；写入成功率 ≥99.99% | 集成测试 |
| PL-03 | 审计查询支持时间范围/操作类型筛选，响应 ≤2 s，分页 20 条，无导出（页面落点 = **审计页 P5**，见 AU-01） | 集成测试 |
| PL-04 | 联锁状态展示（触发源/latch/停机失败/DO 灯）（页面落点 = **安全 / 联锁页 P4**，见 IL-01） | 集成测试 |
| PL-05 | 联锁人工释放：二次确认 + 审计；条件不满足须明示拒绝原因（见 IL-02） | 集成测试 |
| PL-06 | M1 人工授权重启：二次确认 + 审计；失败原因明示（见 IL-03） | 集成测试 |
| PL-07 | 数据通道断开 ≤3 s 感知、恢复 ≤1 s 回实时 | 集成测试 |
| PL-08 | 展示通道不承载下行写；写操作走独立受控接口 | 架构检查 |
| PL-09 | 装置不再监听对外 HTTP 端口、不再提供 Web UI | 集成测试 |
| PL-10 | **本地屏无登录 / 会话 / PIN / RBAC 界面入口**（T-3：物理在场即授权） | UI 检查 |
| IL-01 | 「安全 / 联锁」页（P4）常显联锁总态（已联锁/未联锁，文字+色+图标）；触发源明细 + latch 态 + 停机失败可见；DO1/DO2 指示灯双通道；变化 ≤2 s 上屏；**「联锁状态不可用」≠「未联锁」**（F16） | 集成测试 |
| IL-02 | 人工释放须二次确认 + 审计留痕；条件不满足（未复位 / 保持时间不足）**明示拒绝原因**；成功 ≤2 s 页面联锁态更新；500 ms 内重复点击不重复生效（F17） | 集成测试 |
| IL-03 | M1 人工授权须二次确认 + 审计留痕；**latch 态入口不可用并明示原因**；失败原因明示（F18） | 集成测试 |
| AU-01 | 「审计」页（P5）时间倒序、20 条/页、选项式筛选（时间范围 / 操作类型多选，**零文本输入**）、**只读无编辑/删除**、**无导出入口**、空态明确（F19） | 集成测试 |
| NF-01 | 常驻内存 ≤256 MB；稳态 CPU ≤40% 单核；断连态 ≤10% 单核 | 性能测试 |
| NF-02 | 无忙等/自旋；空闲让出 CPU | 代码审查 |
| NF-03 | 长期运行内存不单调增长 | 长稳测试 |
| NF-04 | 主读数字号 ≥64 px、过程量 ≥32 px、标注 ≥24 px；0.5–1.5 m 可读 | UI 检查 |
| NF-05 | 状态语义文字+色+图标三重冗余 | UI 检查 |
| NF-06 | 渲染进程崩溃后 ≤3 s 自恢复，不影响 mupcd | 集成测试 |
| NF-07 | 显示进程不阻塞 mupcd 启动关键路径 | 集成测试 |
| NF-08 | 界面可用性 ≥99.9%（装置运行期间） | 长稳测试 |
| NF-09 | **主状态页主读数区不横向滚动**；超单屏时允许整页纵向滚动（T-6） | UI 检查 |

**（v2.1 增量）外设数值上屏（§3.9）验收条目 —— `EX-xx`**（完整描述见 §3.9；验证分档见 §3.9.2）：

| ID | 验收标准（摘要） | 验证方法 |
|----|------------------|----------|
| EX-01 | 五类外设实时值**可被屏消费**，且**不依赖轮询数据库**（RQ-9.0-1） | 架构检查 |
| EX-02 | 存在「最新值快照 + 变更通知」入口；屏在 ≤1 s 帧节拍内取得最新值 + 变化量（RQ-9.0-2） | 集成测试 |
| EX-03 | 上云（01 号）/ 上屏（12 号）/ 存储查询（03 号）**共用同一最新值快照**，无第二真源（RQ-9.0-3） | 架构检查 |
| EX-04 | 单站离线 / 单点失败不影响其余站与其余点的最新值（RQ-9.0-4 / F25.5） | 集成测试 |
| EX-05 | HVAC 柜内温度 / 柜内湿度 / 内盘管温度常显；单位与小数位符合（℃，%RH，1 位小数）；量纲换算不在屏侧发生（F20.1） | 集成测试（本机） |
| EX-06 | HVAC 系统运行状态以「停止 / 运行」文字 + 语义色 + 图标三重冗余表达（F20.2） | 集成测试（本机） |
| EX-07 | HVAC 20 个告警位**逐位**可见（名称 + 活跃态），非仅汇总（F20.3） | 集成测试（本机） |
| EX-08 | 屏上**不存在**「柜外温度 / 柜外湿度」**数值**字段（点表无此量，防臆造）（F20.4 / T-10） | 集成测试（本机） |
| EX-09 | 消防系统状态位图已定义位逐位可见；未定义位显「未定义位 n」，**不猜语义**（F21.1） | 集成测试（本机） |
| EX-10 | 火警等级 6 枚举文案齐全（含 3 = 「未定义」）；枚举外值显「未知」，**不落「正常」**（F21.2） | 集成测试（本机） |
| EX-11 | 钢瓶气压未配置时显「**未配置**」（断言不含 `0 kPa`），且不产生告警条目（F21.3 / EDGE-23） | 集成测试（本机） |
| EX-12 | 消防探测器登记数与明细一致；不一致须显式提示，**不静默裁剪**（F21.4） | 集成测试（本机） |
| EX-13 | 探测器「数据 1」拆解为烟雾（0.1 dB/M）与温度（raw−55 ℃）且单位正确；telemetry 侧整字值不变（F21.5） | 单元测试（本机） |
| EX-14 | BMS 扩展量按分组可读；单位 / 小数位与登记表一致（含单体电压 3 位小数）（F22.1） | 集成测试（本机） |
| EX-15 | 最高 / 最低单体电压与温度**成对**展示其「对应点」（F22.2） | 集成测试（本机） |
| EX-16 | BMS 288 位告警以「活跃数量 + 活跃位清单」呈现；空态与源不可用**文案互异**（F22.3 / EDGE-24） | 集成测试（本机） |
| EX-17 | BMS 温度类字段（含 −40 ℃ 偏移类）显示值与采集侧工程值一致，屏侧**不重算偏移**（F22.4） | 单元测试（本机） |
| EX-18 | 储能表三相 U/I、频率、线电压常显，单位 / 小数位符合（F23.1） | 集成测试（本机） |
| EX-19 | 储能表三相 / 总有功、无功、视在、功率因数常显；PF 3 位小数（F23.1） | 集成测试（本机） |
| EX-20 | 储能表 6 个总电能 + 3 个分相电能 + 不平衡度 / 零序常显，**累计量原值直接展示**（不做日增量差分）（F23.1 / F23.3） | 集成测试（本机） |
| EX-21 | 储能表字段标签含「储能表」；屏上无任何以「台区 / 关口 / 总表」命名却取自 `meter_batt` 的字段（F23.2） | UI 检查（本机） |
| EX-22 | PCS 扩展量（电压 / 频率 / 视在 / 无功 / PF / 温度 / 直流侧 / 累计电量 / 工作模式）常显，量纲不在屏侧重算（F24.1） | 集成测试（本机） |
| EX-23 | 屏上**不存在**取自 1046–1065（STS / 负载区）的字段被标注为「PCS 输出 / 充放 / 台区负荷」（F24.2） | 集成测试（本机） |
| EX-24 | **新增全部中文标签 / 单位 / 文案逐字落在 `lv_font_cmap.txt` 内**；缺字形须先扩字库，否则真机必现豆腐块（§3.9.1 第 4 条 / F26 关联） | **真机（`--features noto-font`）** |
| EX-25 | 新增字段不得使**带符号数值 / 长标签**越出槽宽（参照 U-42 同类风险）；字号满足 §4.2（主读数 ≥64 px / 过程量 ≥32 px / 标注 ≥24 px）（§3.9.2） | **真机** |
| EX-26 | 外设量显示其**自身采集 / 更新时刻**或"最近更新"指示（HVAC 5 s 节奏不被误读为掉线）（F25.2） | 集成测试（本机） |
| EX-27 | 外设端到端上屏 ≤ 站周期 + 1 帧节拍（≤1 s）；离散告警位变化 ≤2 s 上屏（F25.3） | 性能测试（本机） |
| EX-28 | 每站在线 / 离线 / 最后成功时刻以**结构化字段**呈现；站离线时全量 `--` + 「站离线」，三语义（离线 / 未取数 / 数据异常）互异（F25.4 / EDGE-18~20） | 集成测试（本机） |
| EX-29 | 新增外设字段后契约版本号递增；消费方**继续拒收版本不匹配帧**（不得放宽校验）（F26.1 / F26.2 / EDGE-21） | 单元测试（本机） |
| EX-30 | 新字段缺失即"不可得"显式降级（`--` / 「未提供」），**不补 0**（F26.4 / EDGE-22） | 单元测试（本机） |
| EX-31 | 新增字段后整帧仍 ≤ 64 KiB；编 / 解码侧尺寸判据同源同值（F26.5） | 单元测试（本机） |
| EX-32 | 既有 F1–F5 / F6–F8 / F16 字段与 `Field.flag` 四态语义**未被本增量更改**（F26.7） | 代码审查 + 回归 |
| EX-33 | 新增字段不破 §4.1 预算（稳态 CPU ≤40% 单核）与 F13.4（触摸不阻塞主状态页刷新）（F25.6） | 性能测试（本机） |
| EX-34 | 各站 `port` / `slave` / `baud_rate` / `parity` 现场校准后，屏上数值与设备实际一致（RC-1/3/5/8/11） | **现场** |

---

## 附录 C：待办（供流程追踪）

| # | 事项 | 归属 |
|---|------|------|
| 1 | 本 PRD v2.0 评审（评审通过后于文首改标 `[REVIEWED: PASS]`） | PRD Reviewer |
| 2 | 08-MUPC-Web管理与AI可视化-PRD.md 顶部废弃/并入声明已加（见该文件文首） | 已完成 |
| 3 | §8 风险 T-1 / T-2 / T-3 / T-6 已由 PM 裁定并固化（T-4 属设计阶段，T-5 / T-7 保留待确认） | 已完成 |
| 4 | §6.2 T-5 五项「待设计阶段确认」来源项须在设计阶段逐项确认 | 设计阶段 |
| 5 | v1.0 的设计文档与 UI 设计文档须按本 v2.0 重新设计（原前提已推翻）；**新增页面（P4 安全 / 联锁、P5 审计）须一并纳入 UI 设计** | 设计/UI |
| 6 | 移除 `web-api` 的实施评估（含 §3.7 映射表的迁移落点） | 设计阶段 |
| 7 | **v2.0 需求评审 REQUEST_CHANGES 修订已完成**（R1–R4，见附录 A.6），**待评审员复审**（复审通过前不加 `[REVIEWED: PASS]`） | 已完成 |
| 8 | **v2.1 增量（§3.9 外设数值上屏）已完成评审**（`[REVIEWED: PASS: 2026-09-23]`，2026-09-23）——文首以**独立标记行**加注，**未覆盖** v2.0 的 `[REVIEWED: PASS]` 原范围 | PRD Reviewer（已完成） |
| 9 | **T-8（页面归属）已裁定（2026-09-23，用户）**：采用「**两处拆**」——消防（F21）→ **P4 安全 · 联锁页**；HVAC / BMS / 储能表 / PCS 扩展（F20 / F22 / F23 / F24）→ **P6（更名「装置与外设」）**。**不新增页 ⇒ 页数口径不变、无需回写**（原"6 页 → 7 页"前提作废）；**已同步** §3 信息架构表、§3.9.1、§3.9 各 F 项归属标注、F8（P6 更名）、F11.1 / TT-01（页名）、§8 T-8 | 用户（已完成） |
| 10 | **T-9 ~ T-11 须产品确认**（历史趋势 / 柜外温湿度补点 / 消防探测器与 BMS 告警位展示口径） | 产品 |
| 11 | **T-12 跨文档回写**：03 号「遥测消费方改判」（U-70）清单须纳入「12 号本地屏外设数值展示（P4 / P6）」；01 号外设上云点表（U-74）须与上屏共用同一最新值入口 | 01 / 03 号文档负责人 |
| 12 | **字体门禁（P4 真机验收硬门禁）**：`lv_font_cmap.txt` 现 324 码位，本增量候选标签用字**实测 41 个码位不在表内** ⇒ 须与既有 23 码位批**合并扩字库**并重跑 `gen_fonts.sh` 提交派生清单；未完成前真机验收不通过 | 设计 / UI + 实现 |
| 13 | **帧契约 v3 扩展**（`display-proto`）：新增外设段 + 版本号递增 + 两端同版本发布口径（部署文档登记）；**属设计/实现动作，本 PRD 不预设结构** | 设计 / 实现 |
| 14 | **U-73 / U-70 台账状态更新**：随本文档定稿与后续设计落地同步 `docs/technical-debt.md` | 技术债维护 |

---

## 附录 D：版本演进（本文档）

| 版本 | 日期 | 变更 | 门禁状态 |
|------|------|------|----------|
| v1.0 | 2026-09-09 | 只读状态屏（SOC / PCS 充放 / 三相量） | `[REVIEWED: PASS]`（**已被 v2.0 取代**，见附录 A.4） |
| v2.0 | 2026-09-10 | 合并版 + PM 裁决固化（T-1/T-2/T-3/T-6）；升级为触摸式 HMI；并入原 08 模块；移除 web-api；新增 §3.4 F16–F18 / §3.5 F19 / §3.6 F11–F15 / §3.7 平台整合 / §3.8 暂停范围 | `[REVIEWED: PASS]`（2026-09-10，**对其原范围有效**） |
| v2.0 补注（降级） | 2026-09-16 | CF-04 降级：7 个可写参数中仅 `system.log_level` 真热生效，其余 6 个连接类参数需重启 `mupcd`（PM 接受，正文数值未改） | PM 裁定（不改变门禁标记） |
| v2.0 补注（降级） | 2026-09-19 | F9 字段级降级：7 个配置项中 3 项现网无配置承载 ⇒ CF-01 字段级 4/7、CF-02 不可达（PM 接受，正文数值未改） | PM 裁定（不改变门禁标记） |
| **v2.1** | **2026-09-23** | **「外设数值上屏」增量（新需求，技术债 U-73）**：新增 **§3.9 F20–F26**（HVAC / 消防 / BMS / 储能表 / PCS 数值上屏；刷新与新鲜度；帧契约版本化）+ §3.9.0 数据可用性要求（跨模块）+ §3.9.1 页面归属建议 + §3.9.2 验证分档；追加 §4.5 刷新频率行、§5 EDGE-18~EDGE-24、§8 T-8~T-12、附录 B **EX-01~EX-34**、附录 C #8~#14；更新版本号与本节。**既有 F 项正文、门禁标记与上两条 PM 补注一律未改** | **`[REVIEWED: PASS: 2026-09-23]`**（2026-09-23，**仅对本 v2.1 增量有效**；v2.0 的 `[REVIEWED: PASS]` 原范围不变） |
| **v2.2** | **2026-09-23** | **T-8 页面归属裁定登记（不含新功能需求）**：用户裁定采用「**两处拆**」——**消防（F21）→ P4 安全 · 联锁页**；**HVAC / BMS / 储能表 / PCS 扩展（F20 / F22 / F23 / F24）→ P6，并将该页由「系统 / 关于页」改名为「装置与外设」**；**不新增页 ⇒ 6 页口径不变**。同步：文首版本 / 状态与 v2.2 增量行、§3 信息架构表 P4 / P6 行与裁决第 4 条、F8 落点说明、§3.9 引言、F20–F26 各自页面归属标注、§3.9.1（结论化 + 方案 A / B 标记已否决 + 第 1 条改「无需回写」）、F11.1 与 TT-01（P6 页名）、§8 状态行与 **T-8 行（保留原提案 + 追加裁定结论）**、§3.9.0 / T-12 / 附录 C #11 的「外设页」措辞、附录 C #9。**既有 F 项正文（F1–F19）、门禁标记与上两条 PM 降级补注一律未改** | **T-8 裁定登记**（**不改变** `[REVIEWED: PASS]`（v2.0）与 `[REVIEWED: PASS: 2026-09-23]`（v2.1）的既有门禁标记与原范围） |

**本版（v2.1）增量写作依据**：

| 依据 | 出处 |
|------|------|
| 用户 2026-09-23 立项裁定 | 外设（HVAC / 消防 / BMS / 储能表 / PCS）数值须能在本地显示终端上看到并实时更新 |
| 技术债 **U-73**（新需求） | `docs/technical-debt.md` §6.12（端到端数据流审查登记，2026-09-23） |
| 技术债 **U-70**（遥测消费方改判） | `docs/technical-debt.md` §6.12 |
| 只读审查报告 | `docs/superpowers/reports/端到端数据流审查-外设采集到显示存储上云-2026-09-23.md`（§一 ②、§二 ①②、§三 #10）；另：审核报告末尾「P0 修复批次核验」对 §一③ 的两项已修复，与本增量**无关**（本增量只涉"上屏"，不涉落库 / 上云） |
| 点表与站配置 | `mupc/crates/mupc-southd/src/point_table.rs`（618 点登记）、`mupc/deploy/config/mupc_core_config.production.yaml`（`south_stations` 段） |
| 帧契约现状 | `mupc/crates/display-proto/src/frame.rs`（`DisplayFrame` v2 / `PROTO_VERSION = 2` / `MAX_FRAME_BYTES = 64 KiB`） |
| 字体门禁 | `mupc/crates/local-display/fonts/lv_font_cmap.txt`（324 码位）；U-42 关联批（`mupc/crates/mupc-core-bin/src/console_host.rs:752`） |

> **本增量未做的事（如实声明，v2.1 范围）**：① 未改任何代码、未改 `display-proto`；② 未改既有 F 项正文与 PM 补注；③ 未回写 01 / 03 号文档（T-12 属他文动作）；④ 未定义上云点表（U-74 属 01 号）；⑤ 未裁决页面归属（T-8 属产品 / UI）。

> **（v2.2 补充说明）** 上条 ⑤ 已于 **2026-09-23** 由用户裁定闭环（「两处拆」：消防 → P4；HVAC / BMS / 储能表 / PCS → P6「装置与外设」；**不新增页**）。**v2.2 仍不涉及** ①–④，亦**未改**任何代码 / 设计文档 / 其他 PRD。
