# MUPC 通信网关模块产品需求文档（PRD）

> 本文档为通信网关模块的权威需求文档。
>
> - 版本：**v1.2**（2026-09-23 新增 §8「外设数据上云（U-74，含 U-71）」；v1.1 及以前为合并版正文）
> - **`[REVIEWED: PASS: 2026-09-23]`** — 2026-09-23 / 评审员 / 摘要：本轮增量 **§8「外设数据上云（U-74，含 U-71「MQTT 真做」）」评审通过**。① 范围与边界声明齐备（§8.2 两条通道职责分工 + 「本章不覆盖上屏」边界 + PCS 前置条件如实标注）；② 需求条目编号可追溯（LV-1~6 / GI-1~6 / CFG-1~5 / TLS-1~4 / BF-1~6 / EX-1~10 / AC-U74-01~14 / RC-U74-01~06）；③ 验收分本机 / 真机网络 / 现场三档，AC-U74-02/05/06/07/08/10/11/12 均可机械化断言；④ 边界与异常 10 条（外设站离线 / 值无效 / 配置非法 / 通道不可达 / 未配置即不连接 / 预留点 / PCS 未启用）逐条给出「发什么、不发什么」，无含糊；⑤ **未见越界替设计做决定**：IOA 分段、主题串、JSON 字段属**对外接口契约**（需求级，可接受），逐点 IOA 映射表明确「由设计按分段规则机械生成」、最新值入口形态明确「留给设计」；⑥ 事实取证逐条复核通过（上送点表点数、IOA 分段依据、带宽估算假设均可复算；无点数 / 现状错误）；⑦ TLS fail-closed（无有效证书不得连生产 broker）与「未配置即不连接」（消除 `mqtt.example.com` 假域名 + dummy 证书默认值缺陷）已落为硬要求。**建议改进（不阻塞）4 条**，见审查意见。**本标记仅对 v1.2 的 §8 增量有效**。

---

## 1. 产品概述

### 1.1 产品定位

通信网关是 MUPC 微电网特种调控装置"异构双核心模块主控架构"中**非实时处理核心**（大脑）的北向通信子系统。它负责与调度主站、配电自动化主站、物联平台等外部系统建立和维护通信连接，完成数据的上报和指令的接收转发。

**核心职责：**
- 北向 IEC 104 协议通信：与调度主站互通（遥测/遥信/遥控）
- 北向 IEC 61850 MMS 协议通信：与配电自动化 IED 设备互通
- 北向 MQTT 协议通信：与物联平台 / 虚拟电厂（VPP）互通
- 消息总线：进程间通信（本地）与北向消息路由（云端）
- 协议转换与数据格式统一
- 连接管理、心跳保活、自动重连
- 通过核间通信监控实时控制模块运行状态（系统级看门狗），详见模块10-PRD 第4章
- OTA 远程升级（模型 + 固件），详见模块07-PRD

### 1.2 目标平台

| 项目 | 要求 |
|------|------|
| 操作系统 | Linux (openEuler 22.03+) |
| 硬件 | RK3588 |
| 编程语言 | Rust (>= 1.75) |
| 异步运行时 | Tokio |
| 网络框架 | Tower + tokio-net |

### 1.3 涵盖模块

| Crate | 说明 | 状态 |
|-------|------|------|
| `gateway` | 北向 IEC 104 协议网关 | Phase 1 ✅ |
| `iec61850-plugin` | IEC 61850 MMS 客户端插件 | Phase 2+ ✅ |
| `mqtt-plugin` | MQTT 协议客户端插件（北向扩展） | Phase 2+ ✅ |
| `mqtt-bridge` | MQTT 网桥（本地+北向分层架构） | Phase 3B ✅ |
| `device-trait` | 核心 trait 定义（Plugin、MessageBus 等） | Phase 2+ ✅ |

### 1.4 用户角色

| 角色 | 描述 | 通信协议 | 权限范围 | 优先级 |
|------|------|----------|----------|--------|
| **调度主站运维人员** | 通过 IEC 104 协议远程监控和控制 MUPC 装置 | IEC 104 | 只读监控，可下发调度指令但需经本地策略引擎校验 | 低（次优先） |
| **配电自动化操作员** | 通过 IEC 61850 协议管理配电自动化 IED 设备 | IEC 61850 MMS | 设备状态监控、遥控操作 | 低 |
| **物联平台管理员** | 通过 MQTT 协议接入虚拟电厂或物联平台 | MQTT | 数据上报、指令接收 | 低 |
| **本地运维人员** | 通过 Web UI、星闪/Wi-Fi/蓝牙进行本地有线及无线运维 | HTTP / 星闪 / Wi-Fi / 蓝牙 | 最高权限，可覆盖调度指令 | 高（最高优先） |
| **系统管理员** | 系统整体运维和 OTA 升级 | 所有协议 | 所有配置权限 | 高 |

**权限优先级规则：**
- 权限冲突时，本地运维人员指令优先于调度主站 / 配电自动化 / 物联平台指令
- 调度主站 / 配电自动化 / 物联平台下发的指令需经本地策略引擎校验后才转发

---

## 2. IEC 104 功能需求

### 2.1 IEC 104 协议通信

**User Story：**
> 作为调度主站运维人员，我需要通过 IEC 104 协议与 MUPC 装置通信，以便实现远程监控和控制。

**验收标准：**
- 全面兼容 IEC 60870-5-104 协议
- 支持 TCP 连接建立、保持（心跳）
- 支持 U 帧：STARTDT、STOPDT、TESTFR
- 支持 S 帧：确认收到的 I 帧
- 支持 I 帧：发送/接收应用数据
- 默认端口：2404

### 2.2 连接管理

**User Story：**
> 作为系统，我需要管理 IEC 104 连接状态，以便在连接异常时自动重连。

**验收标准：**
- 连接状态机：断开、连接中、已连接、等待激活
- 心跳间隔：**默认 10 秒，支持运行时配置**，配置范围：1 秒 ~ 60 秒
- 连接超时：30 秒无响应则判定超时
- 自动重连机制：断连后 5 秒开始重连，最多重试 10 次
- 重试失败后，每 1 分钟尝试一次
- 支持最多 5 个并发连接
- 心跳间隔配置方式：通过 Web UI 配置

### 2.3 数据收发

**User Story：**
> 作为调度主站运维人员，我需要通过 IEC 104 协议收发遥测、遥信、遥控数据。

**验收标准：**

**类型标识（TypeID）支持：**
- M_SP_TA_1（单点遥信，带时标）
- M_DP_TA_1（双点遥信，带时标）
- M_ME_TA_1（测量值，归一化值，带时标）
- M_ME_TD_1（测量值，归一化值，带时标）
- C_SC_TA_1（单点遥控，带时标）
- C_DC_TA_1（双点遥控，带时标）
- C_SE_TA_1（调节命令，带时标）

**数据上行（MUPC → 调度主站）：**
- 周期性发送遥测数据，周期可配置（默认 1 秒）
- 告警时立即上送
- 时标采用 UTC 时间

**数据下行（调度主站 → MUPC）：**
- 接收并确认收到的数据
- 遥控命令需经过本地策略引擎校验后才转发

### 2.4 调度指令接收与转发

**User Story：**
> 作为调度主站，我需要下发有功/无功设定值、一次调频参数、远程启停命令给 MUPC。

**验收标准：**
- 接收调度主站下发的控制指令
- 指令类型：
  - 有功设定值（P_set）：范围 -1000kW ~ 1000kW
  - 无功设定值（Q_set）：范围 -1000kVar ~ 1000kVar
  - 电池启停命令：启动/停止
  - 一次调频参数：K 值、Deadband
- 指令校验：检查数据范围、格式有效性
- **权限分级：**
  - 调度主站下发的指令需经过本地策略引擎校验后才转发
  - 如本地运维人员下发冲突指令，本地运维人员指令优先
- 校验通过后，通过 intercore 转发给实时控制模块
- 响应时间：从接收到转发 ≤ 100ms

### 2.5 IEC 104 连接异常流程

```
连接断开
    ↓
等待 5 秒后自动重连
    ↓
最多重试 10 次
    ↓
重试失败后，每 1 分钟尝试一次
    ↓
恢复后自动建立连接
```

---

## 3. IEC 61850 功能需求

### 3.1 概述

IEC 61850 插件提供 MMS（制造报文规范）协议客户端能力，支持连接符合 IEC 61850 标准的 IED 设备，实现数据对象读写、GOOSE 消息订阅等功能。

> **IEC 61850-7-420 DER 逻辑节点说明：** 完整的 IEC 61850-7-420 DER 逻辑节点模型（如 Photovoltaic、Storage、ElectricVehicle 等）延后至 Phase 2+ 实现。Phase 2 实现 MMS 传输层基础读写能力（7-2/7-3/8-1），支持标准 7-4 逻辑节点。

目标对接设备：
- 配电自动化 IED
- 支持 IEC 61850-7-420 的光伏逆变器
- 支持 IEC 61850 的消防控制系统

### 3.2 技术选型

| 特性 | 说明 |
|------|------|
| **协议栈实现** | libIEC61850 C 库 + Rust FFI 绑定 |
| **标准** | IEC 61850-7-2/7-3/8-1（MMS） |
| **平台** | Linux（RK3588） |
| **许可** | GPL-2.0 / 商业许可 |
| **依赖** | `iec61850-sys` (0.3, features: ["tls"])、`rustls` (0.23)、`webpki-roots` (0.26) |

### 3.3 MMS 客户端功能

**User Story：**
> 作为配电自动化操作员，我需要通过 IEC 61850 MMS 协议读写 IED 设备的数据对象。

**验收标准：**

**连接管理：**
- 支持 TCP 连接建立与断开
- 短连接模式：每次请求建立新连接
- 默认端口：102
- 连接超时可配置（默认 5000ms）
- 读取超时可配置（默认 3000ms）

**MMS 服务支持：**
- Read 服务：读取数据对象（DataObject）
- Write 服务：写入数据对象
- DefineVariableAccess（预留）
- GetDataAccessAttributes（预留）

**数据对象：**
- 逻辑节点（LN）：如 LLN0、MMXU1、XCBR1 等
- 数据对象名（DO）：如 ST$Pos、MX$Meas 等
- 支持标准 IEC 61850-7-4 数据对象模型

**TLS 支持（可选）：**
- MMS over TLS 1.2+
- 双向证书认证
- 支持 CA 证书、客户端证书、客户端私钥配置
- 可配置是否验证对端证书

**验收标准（MMS 专用）：**

| ID | 标准 | 验证方法 |
|----|------|----------|
| MMS-01 | MMS Read 服务正常工作 | 读取 IED 数据对象 |
| MMS-02 | MMS Write 服务正常工作 | 写入 IED 数据对象 |
| MMS-03 | TLS 连接成功 | MMS over TLS 连接测试 |
| MMS-04 | 超时处理正确 | 模拟超时场景 |
| MMS-05 | 错误处理正确 | 模拟错误响应 |
| MMS-06 | 短连接模式正确 | 验证每次请求建立新连接 |

### 3.4 GOOSE 消息订阅

**User Story：**
> 作为系统，我需要订阅 IED 设备发出的 GOOSE 消息，以便获取实时事件。

**验收标准：**
- 支持订阅 GOOSE 消息
- 通过 GOOSE ID（`go_id`）识别消息源
- 消息通过 MessageHandler 回调处理
- 支持配置本地 IP、本地端口、远端 IP、远端端口

### 3.5 错误类型

| 错误 | 说明 |
|------|------|
| MmsConnectFailed | MMS 连接失败（含 TLS 连接失败） |
| MmsTimeout | MMS 请求超时（连接/读取） |
| MmsProtocolError | MMS 协议错误 |
| DataObjectNotFound | 数据对象不存在 |
| WriteFailed | 写操作失败 |
| MmsInvalidResponse | 无效响应 |
| TlsConnectFailed | TLS 连接失败 |
| CertVerifyFailed | 证书验证失败 |
| Asn1EncodeFailed | ASN.1 编码失败 |
| Asn1DecodeFailed | ASN.1 解码失败 |

### 3.6 配置定义

```rust
/// MMS 配置
pub struct MmsConfig {
    pub local_ip: String,            // 本地 IP
    pub local_port: u16,             // 本地端口
    pub remote_ip: String,           // 远端 IP（IED 设备）
    pub remote_port: u16,            // 远端端口（MMS 标准端口 102）
    pub tls: Option<MmsTlsConfig>,   // TLS 配置
    pub connect_timeout_ms: u64,     // 连接超时（毫秒），默认 5000
    pub read_timeout_ms: u64,        // 读取超时（毫秒），默认 3000
}

/// MMS TLS 配置
pub struct MmsTlsConfig {
    pub enabled: bool,               // 是否启用 TLS
    pub ca_cert_path: String,        // CA 证书路径
    pub client_cert_path: String,    // 客户端证书路径
    pub client_key_path: String,     // 客户端私钥路径
    pub verify_peer: bool,           // 是否验证对端证书，默认 true
}
```

### 3.7 文件结构

```
mupc/crates/iec61850-plugin/src/
├── lib.rs              # 模块导出
├── mms_client.rs       # MMS 客户端封装（libIEC61850）
├── mms_types.rs        # MMS 数据类型定义
├── asn1_utils.rs       # ASN.1 编码/解码工具
├── config.rs           # MmsConfig, MmsTlsConfig
├── device.rs           # Iec61850Device trait
├── goose.rs            # GOOSE 消息订阅
└── errors.rs           # 错误类型定义
```

### 3.8 性能要求

| 指标 | 要求 |
|------|------|
| MMS 连接建立时间 | ≤ 5s |
| Read 请求响应时间 | ≤ 3s |
| Write 请求响应时间 | ≤ 3s |
| 操作成功率 | ≥ 99% |
| 并发连接 | ≥ 1 个 IED（可扩展） |

---

## 4. MQTT 功能需求

### 4.1 概述

MQTT 功能分为两个层次：

1. **MQTT 北向客户端（mqtt-plugin）**：与物联平台 / 虚拟电厂（VPP）通信，基于 emqx 企业级 MQTT Broker，支持 TLS 双向证书认证
2. **MQTT 本地网桥（mqtt-bridge）**：进程间通信，基于本地 mosquitto Broker，用于 data-processing 与 strategy-engine 等模块的消息传递

### 4.2 分层 MQTT 架构

```
┌─────────────────────────────────────────────────────────────┐
│                   emqx（北向/云端）                             │
│         物联平台 + 配电自动化主站                              │
└─────────────────────────┬───────────────────────────────────┘
                          │ MQTT + TLS + 证书认证
                          │ Port: 8883
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    本地 mosquitto                             │
│                   （进程间通信）                                │
│                   Port: 1883                                 │
└───────┬─────────────────┬─────────────────┬─────────────────┘
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│data-processing│  │strategy-engine│  │   其他模块   │
│   （发布）     │  │   （订阅）     │  │             │
└──────────────┘  └──────────────┘  └──────────────┘
```

### 4.3 MQTT 北向客户端功能

**User Story：**
> 作为物联平台管理员，我需要通过 MQTT 协议与 MUPC 装置安全通信，以便接收遥测数据和下发指令。

**验收标准：**

**连接管理：**
- 连接可配置的 MQTT Broker（emqx）
- 支持客户端 ID 配置
- 支持用户名/密码认证（可选）
- 支持 Keep Alive 保活机制
- 默认 QoS 级别可配置
- 断线重连：初始间隔 1 秒，指数退避（2 倍），最大间隔 60 秒，无重连次数上限

**TLS 安全：**
- 支持 MQTT over TLS 1.2+（默认端口 8883）
- 支持双向证书认证（CA 证书 + 客户端证书 + 客户端私钥）
- 证书格式：X.509 v3（PEM）

**QoS 支持：**
- QoS 0：至多一次（AtMostOnce）
- QoS 1：至少一次（AtLeastOnce）
- QoS 2：恰好一次（ExactlyOnce）

**订阅/发布：**
- 支持按主题（Topic）订阅
- 支持按主题发布消息
- 消息带 QoS 级别
- 订阅的消息通过回调处理

**错误类型：**
- ConnectFailed：连接失败
- AuthFailed：认证失败
- SubscribeFailed：订阅失败
- PublishFailed：发布失败
- Disconnected：连接已断开

### 4.4 MQTT 本地网桥功能

**User Story：**
> 作为系统，我需要通过本地 MQTT 网桥实现模块间松耦合通信，并路由消息至北向 MQTT。

**验收标准：**

**MqttBridge trait：**

```rust
/// MQTT 网桥 trait
pub trait MqttBridge: Send + Sync {
    async fn publish(&self, topic: &str, payload: &[u8], qos: u8) -> Result<(), MqttBridgeError>;
    async fn subscribe(&self, topic: &str, qos: u8) -> Result<(), MqttBridgeError>;
    fn is_connected(&self) -> bool;
}
```

**本地客户端（LocalMqttClient）：**
- 连接本地 mosquitto（`127.0.0.1:1883`）
- 用于进程间通信
- QoS 0/1
- 无 TLS

**北向客户端（NorthMqttClient）：**
- 连接 emqx（可配置地址 `:8883`）
- 用于北向通信
- QoS 1/2
- TLS + 双向证书认证

### 4.5 Topic 设计

#### 北向 Topic（emqx）

| Topic | 方向 | QoS | 说明 |
|-------|------|-----|------|
| `mupc/north/telemetry` | → 物联平台 | 1 | 高频遥测数据 |
| `mupc/north/fault` | → 物联平台 | 2 | 故障事件 |
| `mupc/north/strategy/command` | ← 物联平台 | 2 | 下行指令 |
| `mupc/north/status` | ↔ 双方 | 0 | 设备状态 |

#### 进程间 Topic（mosquitto）

| Topic | 方向 | QoS | 说明 |
|-------|------|-----|------|
| `mupc/local/telemetry` | → | 0 | 遥测数据 |
| `mupc/local/strategy/command` | → | 1 | 策略指令 |
| `mupc/local/ai/ready` | → | 0 | AI 就绪状态 |

### 4.6 消息持久化策略

| 消息类型 | QoS | 持久化 | 说明 |
|---------|-----|--------|------|
| 遥测数据 | 1 | 否 | 仅实时展示 |
| 故障事件 | 2 | 是 | 需要事后分析 |
| 策略指令 | 2 | 是 | 断线重连恢复 |
| 设备状态 | 0 | 否 | 周期性刷新 |

### 4.7 数据流设计

**遥测数据流：**

```
intercore → DataCollector → LocalMqttClient → mosquitto → NorthMqttClient → emqx → 物联平台
```

**策略指令流：**

```
物联平台 → emqx → NorthMqttClient → mosquitto → AiCommandValidator → intercore
```

### 4.8 配置定义

```rust
/// MQTT 总配置
pub struct MqttConfig {
    pub local: LocalMqttConfig,   // 本地 mosquitto 配置
    pub north: NorthMqttConfig,   // 北向 emqx 配置
}

/// 本地 MQTT 配置
pub struct LocalMqttConfig {
    pub broker_addr: String,       // "127.0.0.1:1883"
    pub client_id: String,
    pub clean_session: bool,
    pub keepalive_secs: u64,
}

/// 北向 MQTT 配置
pub struct NorthMqttConfig {
    pub broker_addr: String,       // "mqtt.example.com:8883"
    pub client_id: String,
    pub keepalive_secs: u64,
    pub tls: TlsConfig,
    pub credentials: Credentials,
}

/// TLS 配置
pub struct TlsConfig {
    pub ca_cert: PathBuf,
    pub client_cert: PathBuf,
    pub client_key: PathBuf,
}
```

---

## 5. 消息总线功能需求

### 5.1 概述

消息总线是模块间松耦合通信的基础设施，支持 publish/subscribe 模式。Phase 1 使用 `tokio::sync::mpsc` 实现进程内通信，Phase 3B 扩展为基于 MQTT 的分层消息架构，同时支持进程内和进程间通信。

### 5.2 MessageBus Trait

**验收标准：**

```rust
/// 消息总线接口
pub trait MessageBus: Send + Sync {
    /// 发布消息
    fn publish(&self, msg: Message) -> Result<(), BusError>;
    /// 订阅主题
    fn subscribe(&self, topic: Topic, handler: Arc<dyn MessageHandler>) -> Result<(), BusError>;
    /// 取消订阅
    fn unsubscribe(&self, topic: &Topic) -> Result<(), BusError>;
}
```

**数据类型：**

```rust
/// 消息主题
pub struct Topic(String);

/// 消息封装
pub struct Message {
    pub topic: Topic,
    pub payload: Vec<u8>,
    pub timestamp: u64,
}

/// 消息订阅者
pub trait MessageHandler: Send + Sync {
    fn handle(&self, msg: Message);
}
```

### 5.3 错误类型

```rust
pub enum BusError {
    TopicNotFound(String),
    PublishFailed(String),
    SubscribeFailed(String),
}
```

### 5.4 实现策略

| Phase | 实现方式 | 说明 |
|-------|----------|------|
| Phase 1 | `tokio::sync::mpsc` | 进程内通信，最简实现 |
| Phase 2+ | plugin-loader + MessageBus trait | 可替换为 AMQP/MQTT |
| Phase 3B | mosquitto（进程间）+ emqx（北向） | 分层 MQTT 总线架构 |

### 5.5 性能要求

| 指标 | Phase 1 | Phase 3B |
|------|---------|----------|
| 消息总线吞吐量 | ≥ 1000 msg/s | ≥ 10000 msg/s |
| 进程间消息延迟 | N/A | < 100ms |

---

## 6. 非功能性需求

### 6.1 性能需求

| 指标 | 要求 |
|------|------|
| IEC 104 数据上报周期 | ≥ 1Hz（可配置） |
| 调度指令处理延迟（接收→转发） | ≤ 100ms |
| IEC 104 并发连接数 | ≤ 5 个 |
| MMS 连接建立时间 | ≤ 5s |
| MMS Read/Write 响应时间 | ≤ 3s |
| MMS 操作成功率 | ≥ 99% |
| MQTT 消息延迟（QoS 0） | < 100ms |
| MQTT 消息延迟（QoS 1/2） | < 500ms |
| 进程间消息传递延迟 | < 100ms |
| Web UI 页面响应时间 | ≤ 2 秒 |
| 消息总线吞吐量 | ≥ 10000 msg/s |
| 消息总线吞吐量（Phase 1 mpsc） | ≥ 1000 msg/s（接口预留） |
| MQTT 断线重连最大间隔 | 60 秒（指数退避） |

### 6.2 可靠性需求

| 指标 | 要求 | 来源 |
|------|------|------|
| 系统 MTBF | ≥ 50,000 小时 | |
| IEC 104 心跳间隔 | 默认 10 秒，可配置（1秒~60秒） | |
| IEC 104 连接超时 | 30 秒 | |
| IEC 104 自动重连 | 5 秒后开始，最多 10 次，之后每 1 分钟尝试 | |
| MQTT 断线重连 | 初始 1 秒，指数退避（2 倍），最大 60 秒，不限次数 | |
| 消息总线 QoS | 支持 0/1/2 | |
| 系统级看门狗 | 通过核间通信监控实时控制模块运行状态，异常时告警并尝试恢复 | 详见模块10-PRD 第4章 |

### 6.3 安全需求

| 需求 | 说明 |
|------|------|
| **MQTT over TLS** | MQTT 北向连接强制使用 TLS 1.2+，双向证书认证 |
| **MMS over TLS** | MMS 连接可选使用 TLS 1.2+，双向证书认证 |
| **国密算法（SM2/SM4）** | 数字签名、密钥交换、对称加密（Phase 2+） |
| **证书管理** | X.509 v3 格式，支持 SM2 签名算法，PEM 文件存储 |
| **密钥管理** | SM2 私钥仅本地存储禁止网络传输，SM4 密钥定期轮换（默认 30 天），文件权限 600 |
| **Web UI 安全** | Session 登录认证（`POST /api/auth/login`），默认用户 admin |
| **配置安全** | 敏感配置不得明文存储 |
| **日志安全** | 日志中不得记录明文密码、密钥 |
| **Topic 权限** | 进程间 Topic 仅允许本地服务订阅，北向 Topic 按角色分配读写权限 |
| **纵向加密认证合规** | 纵向加密认证需符合电力监控系统安全防护规定（发改委14号令），详见模块06-PRD 第5章 |

### 6.4 兼容性需求

| 项目 | 要求 |
|------|------|
| 操作系统 | openEuler 22.03+ |
| 硬件平台 | RK3588 |
| 协议 | IEC 60870-5-104、IEC 61850-7-2/7-3/8-1、MQTT 3.1.1 |
| Web 浏览器 | Chrome 90+、Firefox 88+、Edge 90+ |
| 本地无线运维通道 | 星闪 / Wi-Fi / 蓝牙本地运维通信，详见模块09-PRD |

### 6.5 依赖关系

| Crate | 版本 | 用途 | 来源 |
|-------|------|------|------|
| tokio | 1.x | 异步运行时 | 通用 |
| serde | 1.x | 序列化 | 通用 |
| serde_json | 1.x | JSON 解析 | 通用 |
| thiserror | 1.x | 错误类型 | 通用 |
| libloading | 0.8 | 动态库加载 | Phase 2 |
| rumqttc | 0.20+ | MQTT 客户端 | Phase 2 / Phase 3B |
| iec61850-sys | 0.3 | MMS 协议栈 FFI 绑定 | Phase 2 |
| rustls | 0.23 | TLS 实现 | Phase 3B |
| webpki-roots | 0.26 | TLS 根证书 | Phase 3B |
| serial | 0.4 | 串口通信（RS485 依赖） | Phase 2 |
| GmSSL | (绑定) | 国密算法 | Phase 2+ |

---

## 7. 验收标准汇总

### 7.1 IEC 104 验收标准

| ID | 功能点 | 验收条件 |
|----|--------|----------|
| IEC104-01 | 协议兼容 | 全面兼容 IEC 60870-5-104，支持 U/S/I 帧 |
| IEC104-02 | 连接管理 | 支持状态机、心跳（10s~60s 可配置）、超时判定（30s） |
| IEC104-03 | 自动重连 | 断连 5s 重试，最多 10 次，之后每 1 分钟尝试 |
| IEC104-04 | TypeID 支持 | 支持 M_SP_TA_1、M_DP_TA_1、M_ME_TA_1、M_ME_TD_1、C_SC_TA_1、C_DC_TA_1、C_SE_TA_1 |
| IEC104-05 | 数据上行 | 周期性遥测（默认 1s），告警立即上送，UTC 时标 |
| IEC104-06 | 数据下行 | 接收确认，遥控命令需经策略引擎校验 |
| IEC104-07 | 调度指令 | 支持 P_set/Q_set、电池启停、一次调频参数，响应 ≤ 100ms |
| IEC104-08 | 并发连接 | 支持最多 5 个并发连接 |

### 7.2 IEC 61850 / MMS 验收标准

| ID | 功能点 | 验收条件 |
|----|--------|----------|
| MMS-01 | MMS Read | MMS Read 服务正常工作，读取 IED 数据对象 |
| MMS-02 | MMS Write | MMS Write 服务正常工作，写入 IED 数据对象 |
| MMS-03 | MMS TLS | MMS over TLS 连接成功（可选） |
| MMS-04 | 超时处理 | 超时场景下正确处理 |
| MMS-05 | 错误处理 | 错误响应场景下正确处理 |
| MMS-06 | 短连接模式 | 每次请求建立新连接 |
| MMS-07 | GOOSE 订阅 | 支持订阅 GOOSE 消息，通过 MessageHandler 回调处理 |
| MMS-08 | IED 连接 | 能连接 IEC 61850 IED 设备，读写数据对象；超时 < 5s，成功率 ≥ 99% |

### 7.3 MQTT 验收标准

| ID | 功能点 | 验收条件 |
|----|--------|----------|
| MQTT-01 | Broker 连接 | 能连接 MQTT Broker，支持订阅/发布 |
| MQTT-02 | QoS 0 消息 | 至多一次送达，延迟 < 100ms |
| MQTT-03 | QoS 1/2 消息 | 至少一次 / 恰好一次送达，延迟 < 500ms |
| MQTT-04 | MQTT over TLS | MQTT over TLS 1.2+ 连接成功，双向证书认证 |
| MQTT-05 | 断线重连 | 初始 1s，指数退避（2 倍），最大 60s，不限次数 |
| MQTT-06 | 消息持久化 | QoS 2 消息持久化正确 |

### 7.4 消息总线验收标准

| ID | 功能点 | 验收条件 |
|----|--------|----------|
| MB-01 | 本地 mosquitto 连接 | 本地 MQTT 连接成功 |
| MB-02 | 北向 emqx 连接（TLS） | 北向 MQTT + TLS 连接成功 |
| MB-03 | 进程间消息延迟 | < 100ms |
| MB-04 | 断线重连自动恢复 | 网络中断后自动恢复 |
| MB-05 | QoS 1 消息至少一次到达 | 单元测试验证 |
| MB-06 | QoS 2 消息恰好一次到达 | 单元测试验证 |
| MB-07 | 证书认证成功 | 集成测试验证 |
| MB-08 | 消息持久化正确 | 集成测试验证 |

### 7.5 安全验收标准

| ID | 安全点 | 验收条件 |
|----|--------|----------|
| S01 | SM2 签名 | 能使用 SM2 签名和验签 |
| S02 | SM4 加密 | 能使用 SM4 加密和解密 |
| S03 | 证书验证 | 双向证书认证功能正常 |
| S04 | MQTT TLS | MQTT over TLS 连接成功 |
| S05 | MMS TLS | MMS over TLS 连接成功 |
| S06 | 无硬编码密钥 | 检查 SM2/SM4 密钥残留 |
| S07 | 无新增 unsafe 块 | Code review 检查 |
| S08 | 错误实现 std::error::Error | 所有错误类型符合标准 |

### 7.6 质量验收标准

| ID | 质量点 | 验收条件 |
|----|--------|----------|
| Q01 | 编译通过 | `cargo build --release` 无警告 |
| Q02 | Clippy 检查 | `cargo clippy` 无 Error |
| Q03 | 单元测试 | `cargo test` 覆盖率 ≥ 80% |
| Q04 | 错误处理 | 所有错误实现 `std::error::Error` |
| Q05 | 文档 | 公共 API 有 rustdoc 注释 |
| Q06 | 格式化 | `cargo fmt` 格式化通过 |

---

## 8. 外设数据上云（U-74，含 U-71「MQTT 真做」）

> **本章性质**：**需求增量**，由用户于 **2026-09-23 裁定立项**（技术债 `U-74`；并含 `U-71` 的"MUST 真做"裁定）。出处：`docs/superpowers/reports/端到端数据流审查-外设采集到显示存储上云-2026-09-23.md`（下称"审查报告"）§三 #11/#12 与 §五 B 类。
> **本章只写需求与验收，不裁定实现**：凡涉及"怎么做"者一律标注给出处（如 `01 设计文档 §4`），不在此代为决定。
> **本章不覆盖上屏**：外设数值上屏是**另一立项**（技术债 `U-73`，落点 12 号 PRD），与本轮**不互相阻塞**；两条立项**共用同一「最新值入口」**（见 §8.5），不得各建一套。

### 8.1 背景与立项依据（Why）

**用户裁定（不得违背）**：外设（空调 HVAC / 消防 Fire / BMS / 储能表 / PCS）的数据**必须能上云**；MQTT **必须真做**（不接受"在台账上如实降级为未实现"作为终态）。

**现状（代码实证，逐条给出处）**：

| # | 现状 | 证据 |
|---|------|------|
| 1 | IEC 104 只上送**总表 6 点**（IOA 1–6），**无外设** | `mupc/crates/mupc-core-bin/src/startup.rs:316-405`（`broadcast_grid_iec104`）；IOA 表出处 `plans/2026-09-09-修复批次-审查P1P2-R1R2R3.md:276`，原文标注「**占位常量、现场追认**」 |
| 2 | IEC 104 **无总召**：主站没有"需要时取数"的手段 | `gateway/src/iec104/connection.rs:242-274` 只解析遥控/调节；`protocol.rs:398` 断言 `TypeId::from_u8(100) == None`（**C_IC_NA_1 当前不认识**） |
| 3 | IEC 104 **连接前缓存无快照** | `gateway/src/iec104/server.rs:298-303`（`broadcast_telemetry` 只向已连接的 `tx` 发送） |
| 4 | MQTT 北向/本地**两条链路全链空转，无生产者** | `mqtt-bridge/src/north_client.rs:143`、`local_client.rs:147` 的 `publish` **无任何调用方**；`mqtt-plugin/src/lib.rs:75` 的 `start()` 为**空实现** |
| 5 | MQTT **无配置承载** | `mupc-core-bin/src/core_config.rs:307-313` 的 `MqttBridgeConfig` **仅 2 个 bool**（`north_enabled`/`local_enabled`，缺省双 false）；两份 deploy YAML **无 `mqtt_bridge` 段** |
| 6 | MQTT 曾用 `::default()` **真连假域名 + 假证书** | `mqtt-bridge/src/config.rs:56-64`（`mqtt.example.com:8883` + dummy 证书） |
| 7 | 外设数据**无可读内存快照**，采完即进缓冲写库 | `mupc-southd/src/mapper.rs:461` 三角色（`meter_batt`/`hvac`/`fire`）仍为 `empty_package()`；仅 `telemetry_points` 经 `startup.rs:385` → `buffer_telemetry` 落库 |

**既有设计依据（可引用，不在本章重复）**：`01 设计文档 §4` 已给出**两层 MQTT 完整设计**（北向 `mqtt-plugin` → emqx:8883 TLS；本地 `mqtt-bridge` → mosquitto:1883）与 Topic/QoS/持久化表（§4.5/§4.6）。本章**不推翻该设计**，只补齐其**从未定义的外设上送点表与通道职责**。

**与相邻条目的关系**：`U-69` 解决"总表数据怎么出装置（落库）"；`U-74`（本章）解决"**总表以外的数据**怎么出装置"；`U-70`（遥测消费方改判）是需求文档动作，与本章并行、互不阻塞。

### 8.2 范围与通道分工

**范围**：本轮把**南向已接入的 5 类外设 + 既有总表**的数据接到**两条北向通道**上。南向采集侧的语义（点位、换算、周期、失败隔离）**不改**，沿用 02 号 PRD §9。

**两条通道的职责分工（本章硬性结论）**：

| 通道 | 对端 | 定位 | 上送范围 | 取数方式 |
|------|------|------|----------|----------|
| **IEC 104** | **调度主站** | 调度与安全关注量；**数据量受控、点位须与主站对点** | **非全量**：总表 6 点（既有，不动）+ 外设**调度子集**（逐项见 §8.3.2） | 周期上送 + 变化上送 + **总召**（§8.4） |
| **MQTT（北向）** | **物联平台 / VPP** | 全量上云；支持按主题订阅、按装置/站分片 | **全量**：总表 6 + 外设 **618** 点（逐项见 §8.3.3） | 周期上送 + 变化上送 + 断线补送（§8.6.5） |

**"是否都需上送外设"的逐项结论**：

| 设备（站 id） | 采集点数 | IEC 104 是否上送 | MQTT 是否上送 | 差异理由 |
|---------------|----------|------------------|---------------|----------|
| `grid_meter`（总表） | 6 | ✅ **是**（既有 IOA 1–6，**不动**） | ✅ 是 | 调度核心量；改动既有 IOA 会破坏现场追认 |
| `meter_batt`（储能表 ADL400） | 40 | ✅ **是（全量 40）** | ✅ 是 | 全部为电气量/电度（06 种电能 + 电压/电流/频率/功率/PF/不平衡度），均属调度关注面 |
| `bms`（BMS） | 345 | ⚠️ **部分**：遥测 57 点**全量** + 告警位**分类聚合**（**待产品确认**，建议 12 个聚合遥信） | ✅ 是（全量 345） | BMS 288 个告警位是**逐从控/逐极值**的细分位，主站点表无此粒度需求；但**"电池存在告警"必须上**（安全），故聚合而非丢弃 |
| `pcs`（PCS 3 区） | 72 | ⚠️ **条件性**：站启用后**全量 72** | ⚠️ **条件性**：同左 | **前置条件未满足** —— 见下方"PCS 前置条件" |
| `fire`（消防） | 127 | ⚠️ **部分**：系统态 **13** 点（含火警状态 / 故障 / 探测器登记数） | ✅ 是（全量 127，含探测器明细） | 火警总状态是安全量，必须上主站；探测器**明细 114 点**（逐只烟雾/温度/CO/VOC/H2）是运维数据，归物联平台 |
| `hvac`（空调） | 34 | ⚠️ **部分**：告警位 **31** 点 | ✅ 是（全量 34） | 柜内高温/低温/压缩机高低压等告警属安全告警，须上主站；**柜内温湿度 3 点**为纯展示量，主站侧无点位需求（**待产品确认**） |

> **PCS 前置条件（如实标注，不得绕过）**：`Role::Pcs` 当前配置在生效配置与生产模板中**整段为注释占位、未启用**（02 号 PRD §9.4.1 ① 明载"生效配置中对应行仍为注释占位"）。且 02 号 PRD 对 PCS 站有两项**未结清**阻塞：① **地址口径冲突**（3 区首点 1000 vs 示例帧 0000，须现场 FC04 实测裁定，§9.10 Q-4），原文"**在裁定前不得投产 PCS 站**"；② **接线前提**（PCS 只读站须走另一物理口，A1/B1 与 A2/B2 是否隔离未明确，§9.10 Q-15）。
> ⇒ **本轮 PCS 点在上送点表中标为「条件性纳入」**：**PCS 站未启用（未解除上述阻塞）时，其 72 点一律不上送，且不得以 0 / 占位值顶替**（口径同 §8.8）。

### 8.3 上送点表（核心交付物）

#### 8.3.1 上送范围总表

| 设备 | 采集点数（02 号 PRD） | IEC 104 上送点数（**建议基线**） | MQTT 上送点数 |
|------|----------------------|--------------------------------|---------------|
| `grid_meter` | 6 | 6 | 6 |
| `meter_batt` | 40 | 40 | 40 |
| `bms` | 345（遥测 57 + 告警位 288） | 57 + 12（聚合） = **69** | 345 |
| `pcs` | 72 | 72（条件性） | 72（条件性） |
| `fire` | 127（系统态 13 + 探测器 114） | **13** | 127 |
| `hvac` | 34（温湿度 3 + DI 31） | **31** | 34 |
| **合计** | **624** | **231**（含 PCS 则 303） | **624** |

> 采集点数出处：02 号 PRD §9.8.3「全部新增点 **618 点** —— battery 345 + pcs 72 + meter_batt 40 + fire 127（n=20）+ hvac 34」，加既有总表 6 点。

#### 8.3.2 IEC 104 上送点表（IOA 分配与命名约定）

**IOA 分段约定（需求级契约，可机械执行）**：

| 段 | IOA 区间 | 归属 | 已用/规划 |
|----|----------|------|-----------|
| 1 | **1–6** | 总表 `grid_meter` | **既有，维持不动**（现场追认口径） |
| 2 | **101–199** | 储能表 `meter_batt` | 101–140（40 点） |
| 3 | **201–299** | BMS **遥测** | 201–257（57 点） |
| 4 | **301–399** | BMS **遥信**（告警位聚合） | 301–312（建议 12 点） |
| 5 | **401–499** | PCS 遥测（**条件性**） | 401–472（72 点） |
| 6 | **501–599** | 消防 `fire` 系统态 | 501–513（13 点） |
| 7 | **601–699** | 空调 `hvac` 告警位 | 601–631（31 点） |
| 8 | **701–799** | 预留（装置自身 YX/YC） | — |
| 9 | **3001 起** | 预留扩展（多簇 BMS / 第二装置 / 若产品改选"告警位全量 288"则改用 3001–3288） | — |

**段内顺序规则（唯一）**：`IOA = 段基址 + 点序号`；点序号为该设备在 `02 号 PRD §9.5 点表`中的 **1 基序号**，规则为 —— **遥测按寄存器地址升序在前，位块（FC02）位地址升序在后**；跨相/跨分支的多值点按 A→B→C 展开。同一块内顺序与 `§9.4.2.2` 的点位序号一致。

**逐设备上送量清单**（量纲/单位/分辨率**沿用 02 号 PRD §9.7.5**，本章不重列，只定义"送什么、怎么送"）：

| 设备 | IEC 104 上送量 | 点数 | 上送方式 |
|------|----------------|------|----------|
| 总表 | 既有 6 点：总 P / 总 Q / U / I / cosφ / f | 6 | 周期（1 s，既有节流） |
| 储能表 | 6 电能（组合/正/反向 有功、组合/正/反向 无功，`0.01 kWh/kvarh`）、相电压 3、相电流 3、频率、线电压 3、分相正向有功电能 3、零序电流、电压不平衡度、电流不平衡度、PT/CT 2、有功功率 4、无功功率 4、视在功率 4、功率因数 4 | 40 | 总 P/Q/U/I/f **1 s**；电度/不平衡度/PT/CT **5 s** |
| BMS 遥测 | 簇状态、允许充/放最大功率、允许充/放最大电压/电流（6 点组）、簇组电压/电流、簇组模块温度、**SOC**、SOH、绝缘电阻、平均/最高/最低单体电压与温度（含对应点整字）、剩余可充/放电量、实时充放电功率、SOE、极差（温度/电压）、PACK 组压极值、端子温度 4、累计/单次充放电电量与容量 | 57 | **SOC / 簇组电压 / 簇组电流 / 实时功率 1 s**；其余 **5 s** |
| BMS 遥信（聚合） | 建议 12 个：簇端电压 轻/中/重、单体 过压/欠压 轻/中/重、单体 过温/欠温 轻/中/重、SOC 低、SOH 低、单体压差/温差、从控通讯失联（聚合）、采集故障、运行态（充/放/就绪/禁充放/故障） | 12 | **变化上送（COS）** |
| PCS（条件性） | 3 区 72 点：5 个故障告警位图、BMS 工作状态/可接受充放电流/系统总压总流/SOC（转述）、直流母线/中点电压、模块运行/故障/降额/并离网状态、故障告警代码、电网电压/频率、输出电流/视在/有功/无功/功率因数、PCS 温度、4 个 32 位电量、STS 区、负载区、低压区、工作模式、DCDC 温度 | 72 | 状态/总 P/Q **1 s**；其余 **5 s** |
| 消防 | 系统态 13 点（火警状态、故障/屏蔽/消音状态、**探测器登记数**、钢瓶气压、探测器 1 的 6 寄存器复合状态等） | 13 | **变化上送（COS）** |
| 空调 | FC02 告警位 31 点（柜内/柜外 温湿度 故障与高低告警、压缩机高低压、制冷/制热失效、三相电报警等） | 31 | **变化上送（COS）** |

> **BMS 288 位告警"聚合"与"全量"两案（待产品确认）**：**建议**采用聚合（12 点，见上表），理由是主站侧无逐从控（40 个"从控通讯失联"位）与三级（轻/中/重）粒度需求，且 288 点会占满一段。**备选**为全量上送（288 点，IOA 改用 3001–3288），代价是主站点表膨胀且绝大多数位长期恒 0。**裁剪规则**：聚合必须"**只并粒度、不丢语义**"——任一级（轻/中/重）置位时聚合点必须置位；**不得**因聚合而漏报。

> **IOA 分段的两案（待产品确认）**：**建议 A（上表分段式）**——与既有 IOA 1–6 **兼容**、改动最小、与 02 号 PRD 的设备分段同构。**备选 B**——按电力调度惯例重排（遥信 1–4096 / 遥测 16385–32767 / 电度 6401–6600 / 遥控 6001 起），代价是**既有 6 点须迁移**，与"现场追认"口径冲突，且须与主站**重新对点**。

> **逐点 IOA 映射表的产生方式（可执行性说明）**：本章只固定**分段 + 段内顺序规则 + 各设备点序号的唯一来源（02 号 PRD §9.5）**；**逐点 IOA ↔ 点名的完整对照表由设计按上表机械生成**，并在投运前**与主站逐点核对**（验收 RC-02）。**不得**由实现者手写映射常量（现存 6 点 IOA 即"手写常量"形态，是本次要消灭的形态）。

#### 8.3.3 MQTT 上送点表（主题 + 字段约定）

**主题命名（在 §4.5 既有主题下的细化，不新增顶层主题）**：

| Topic | 方向 | QoS | 载荷 | 说明 |
|-------|------|-----|------|------|
| `mupc/north/telemetry/{station_id}` | → 物联平台 | 1 | 遥测（周期） | `{station_id}` 取 02 号 PRD §9.3.3 的站 `id`（`grid_meter`/`bms`/`pcs`/`meter_batt`/`fire`/`hvac`） |
| `mupc/north/event/{station_id}` | → 物联平台 | 1 | 变化/告警事件 | 遥信变位、站 offline/online、SOC 越界、消防登记数不一致 |
| `mupc/north/status` | ↔ | 0 | 装置状态 | 沿用 §4.5 |
| `mupc/north/fault` | → | 2 | 故障事件（需事后分析） | 沿用 §4.5 |

- **兼容性要求**：`{station_id}` 分片**不得**破坏既有单主题订阅方 —— 订阅 `mupc/north/telemetry/#` 必须能收到全部站的数据。§4.5 原表须随设计同步为分片形态。
- **分片要求（强制）**：**不得**把多站数据合并进一条消息；**不得**把单站全量点（BMS 345）与其它站混发。
- **装置标识**：装置级标识（序列号 / 装置 ID）**放在载荷内**，**不进主题前缀**（避免破坏既有主题契约）。其**来源待产品确认**（现状无统一来源）。

**载荷格式（需求级字段契约）**：JSON / UTF-8 编码。字段语义：

| 字段 | 必填 | 含义与口径 |
|------|------|------------|
| `ts` | ✅ | **采集时刻**，UTC ISO-8601（毫秒）；**不得**用发送时刻顶替 |
| `dev` | ✅ | 装置标识（来源待产品确认） |
| `station` | ✅ | 站 `id`（与主题分片一致） |
| `role` | ✅ | 角色（`battery`/`pcs`/`meter_batt`/`fire`/`hvac`/`meter_grid`） |
| `seq` | ✅ | 单调递增轮次序号（供补送乱序/重复检测） |
| `points` | ✅ | 点集；每项含 `n`（点名）、`v`（工程值）、`u`（单位）、`q`（质量） |
| `q`（质量） | ✅ | 枚举：`ok` / `stale` / `invalid` / `unconfigured`；`q != ok` 时 `v` 须为**原值**（不可得时显式 `null`），**严禁用 `0` 顶替** |

- **点名唯一真源（强制）**：`n` 必须与 02 号 PRD §9.4.2.2 的**南向点名逐字一致**（`<块名>_<序号>`，显式 `name` 优先，如 `soc` / `fire_det_count`）。**严禁**在传输层改名、加前缀或用序号替代；点名漂移即断供下游对点。
- **单位口径**：`u` 与 `v` 必须为 02 号 PRD §9.7.5 的**工程值 + 工程单位**（已换算 `scale`/`offset`/`byte_swap`/`word_order`），**不得**上送原始寄存器值。

### 8.4 「需要时取数」：总召与连接初始快照

**裁定：总召纳入本轮**（否则主站没有"需要时取数"的手段，审查报告 §四 `U-74` 明载）。

| ID | 需求 | 说明 |
|----|------|------|
| GI-1 | **支持站召唤** `C_IC_NA_1`（TI=100，QOI=20），主站可主动索取全部当前值 | 现状 `TypeId::from_u8(100) == None`（`protocol.rs:398`）⇒ 当前**完全不认识**该报文 |
| GI-2 | 响应须为 `ACT_CON` → 数据 → `ACT_TERM` 完整三段 | 主站据 `ACT_TERM` 判定召唤结束 |
| GI-3 | 召唤内容 = 本节 §8.3.2 的**全部 IEC 104 上送点**中**当前有有效值**者 | 无有效值（站离线 / 点未配置）的点**不出现**在响应中，**不得**以 0 或旧值顶替 |
| GI-4 | **分组召唤**（QOI=21–36）**本轮不做**，登记为后续 | 建议依据：IEC 104 侧总点数仅 **231 点**，站召唤一轮即可完成；**待产品确认**（若主站要求分组，须在需求阶段重新纳入） |
| GI-5 | **连接初始快照（强制）**：`STARTDT` 握手完成后 **≤ 5 s** 内主动上送一次全量当前值，**不等主站召唤** | 现状 `server.rs:298-303` 只向"已连接"的连接广播 ⇒ **连接建立前产生的值全部丢失**、且主站无补齐手段 |
| GI-6 | 快照中**超过新鲜度门限**（`stale_timeout_s = 5 s`，02 号 PRD §9.7.1 同源）的点标为无效，口径同 §8.8 | 防"以陈旧值冒充实时值" |

**时标要求**：所有上送数据的时标为**采集时刻的 UTC**（对齐 §2.3 与本 PRD 既有口径）；总召/快照响应**不得**用"响应时刻"重新打时标（否则主站无法分辨数据新鲜度）。

### 8.5 外设实时值的数据可用性要求（与 03 / 12 号的口径统一）

**背景（Why）**：外设数据采集完成后**直接进入写库缓冲**，中途**没有任何可读的内存最新值入口**（`mapper.rs:461` 三角色仍为 `empty_package()`，仅 `telemetry_points` → `startup.rs:385` `buffer_telemetry`）。因此**屏幕与上云都取不到**外设实时值（审查报告 §一①、§二②）。

**需求（数据可用性，不作为实现形态裁定）**：

| ID | 需求 |
|----|------|
| LV-1 | **外设实时值必须可被消费**：每一已采集点须有一个**进程内可读的"最新值"入口**，任何合法消费方（12 号本地屏、01 号上云、后续新增消费方）**必须能读该入口** |
| LV-2 | **不得依赖轮询数据库**：消费方获取实时值**不得**通过轮询 `telemetry` 表实现（防止"实时"退化为"查历史"，并避免 SQLite 写读争用） |
| LV-3 | 该入口必须携带**新鲜度信息**（每点的采集时刻 / 是否过期），判据与 `stale_timeout_s = 5 s` 一致 |
| LV-4 | 该入口必须提供**变更通知**（新值到达即通知订阅方），通知**允许合并**（同一轮多点合并为一次），且端到端时延 **≤ 1 s** |
| LV-5 | 入口的**键必须是南向点名**（§9.4.2.2），**不得**二次改名；位块点（FC02，BMS 288 位 + 空调 31 位）**同样必须有入口**（现状位点仅有落库路径） |
| LV-6 | 入口必须能表达**"不可得"**：站离线 / 点未配置时，入口须给出显式的"不可得"状态，**不得**返回 0 / 上一有效值而不加标记（口径同 12 号 PRD §5"不造假值原则"） |

**口径一致性声明（三份 PRD 不得各说一套）**：

| 文档 | 既有口径 | 本章的对接方式 |
|------|----------|----------------|
| **12 号（本地显示）** | §4.4「跨进程数据通道可靠性边界（**通道形态留给设计**）」：允许丢帧、**不允许静默传输损坏数值**；断连 ≤ 3 s 感知、恢复 ≤ 1 s；§6 数据源真实可得性 | **同一口径**：本章同样**不裁定形态**（进程内共享 vs 跨进程通道由设计定），只要求"可读 + 不得轮询数据库 + 带新鲜度 + 带变更通知"。**通道可靠性边界以 12 号 §4.4 为准，本章不另立指标** |
| **03 号（存储）** | `get_latest_data()` 只覆盖 **intercore 侧**数据（03 号 PRD §2.1 `DP-DC-02`），**不含南向外设**；`telemetry` 表是**历史存储** | **职责分离**：本章的入口是**实时值**，03 号的 `telemetry` 是**历史查询**，**两者不是一回事、不得互相替代**；本章**不要求** 03 号承担实时值供给，也**不要求**上云走 `telemetry` 表 |
| **本 PRD（01 号）** | §4/§5 的 MQTT 与消息总线 | 上云**不得**依赖"读 `telemetry` 表"（`LV-2`）；具体由消息总线还是直读入口，**留给设计** |

> **不做的事（边界）**：本章**不**新增第 3 套实时值机制，**不**替 12 号立项决定字段（外设上屏属 `U-73`），**不**改动 03 号的存储语义。

### 8.6 MQTT 真做（北向 + 本地）

#### 8.6.1 配置承载（消除"默认值真连"缺陷）

| ID | 需求 |
|----|------|
| CFG-1 | 北向 MQTT 的 **broker 地址/端口、客户端 ID、用户名/密码（可选）、TLS 证书路径、主题前缀、默认 QoS、上送周期档、启用开关**必须**全部来自配置**，不得由代码 `::default()` 兜底 |
| CFG-2 | **未配置即不连接**：`mqtt_bridge` 段缺失或 `enabled=false` 时，**不得产生任何连接尝试**（回归防护：现状曾用 `NorthMqttConfig::default()` 真连 `mqtt.example.com:8883` + dummy 证书，`mqtt-bridge/src/config.rs:56-64`） |
| CFG-3 | 两份部署 YAML（`mupc/deploy/config/mupc_core_config.yaml`、`…production.yaml`）必须含 `mqtt_bridge` 段；**生产模板不得留占位域名/占位证书**（缺省 `enabled: false`，且证书路径不得指向 dummy） |
| CFG-4 | **不得存在"注册为 Running 但无行为"的模块**：`mqtt-plugin::start()` 的**空实现**（`mqtt-plugin/src/lib.rs:75`）必须消除或将该插件**显式下架**（并从 `auto_load` 移除） |
| CFG-5 | **北向承载须收敛为一套（待产品确认）**：现状 `mqtt-plugin`（北向客户端）与 `mqtt-bridge::NorthMqttClient` **两套并存且都空转**；**建议**保留一套（`01 设计文档 §4` 的分层形态以 `mqtt-bridge` 为桥、`mqtt-plugin` 为北向客户端），另一套显式下架 |

#### 8.6.2 主题与载荷

见 §8.3.3（主题命名、分片要求、JSON 字段契约、点名/单位口径）。**本节不重复。**

#### 8.6.3 上送周期与 QoS

| 数据档 | 内容 | 周期 | QoS |
|--------|------|------|-----|
| **A 档（关键/控制相关）** | 总表 6 点、BMS `soc`/簇组电压/电流/实时功率、储能表 总 P/Q/U/I/f、PCS 运行状态与总 P/Q | **1 s** | 1 |
| **B 档（缓变量）** | 其余遥测（温度、电度、单体极值、剩余可充放电量、PCS 明细、ADL400 电度与不平衡度等） | **5 s** | 1 |
| **C 档（遥信/状态）** | 全部遥信位（BMS 告警位、空调 DI、消防系统态位）+ 状态枚举（PCS 运行/并离网/降额、BMS 簇状态、火警状态）+ 站 offline/online | **变化上送（COS）**，变化后 **≤ 1 s** 内发出 | 1（故障类事件 2，沿用 §4.6） |
| 装置状态 | `mupc/north/status` | 周期（沿用 §4.5） | 0 |

> **COS 首次全量（强制）**：每次 broker 连接建立后，**C 档必须立即全量发送一次**（否则订阅方拿不到位点的初始状态）。位块首轮会突发 ≈319 点（BMS 288 + 空调 31），**允许**合并为一条消息。

#### 8.6.4 TLS 证书要求

| ID | 需求 |
|----|------|
| TLS-1 | 生产 broker **必须** MQTT over TLS **1.2+**，**双向证书认证**（CA + 客户端证书 + 客户端私钥），X.509 v3 / PEM（对齐 §4.3、§6.3） |
| TLS-2 | **无有效证书不得连生产 broker（fail-closed）**：证书缺失 / 过期 / 校验失败时，**拒绝建立连接**并产出显式告警事件；**严禁回退明文**连接 |
| TLS-3 | 证书**有效期监控**：到期前 **≥ 30 天**产出告警（门限**待产品确认**） |
| TLS-4 | **开发/仿真环境的明文 MQTT** 是否允许 —— **待产品确认**；**建议**：仅当**显式配置**开关且**非生产构建**时允许，并在启动日志与事件中响亮告警（不得静默） |

#### 8.6.5 离线 / 断线的缓存与补送策略

| ID | 需求 |
|----|------|
| BF-1 | broker 不可达 / 断线期间，外设数据进入**断线缓存**；连接恢复后**补送** |
| BF-2 | 补送数据**必须带原始采集时标**（`ts` 为采集时刻），**不得**用补送时刻顶替；补送**顺序须与采集时序单调一致**（不得出现"时间倒流"） |
| BF-3 | 补送**不得阻塞实时链路**：补送期间新数据仍须按 A/B/C 档正常上送（或至少 A 档不被阻塞） |
| BF-4 | **缓存上限必须显式配置**：建议 **时间窗 ≥ 30 min** 或 **条数 ≥ 10,000**（二者取先到），**待产品确认**；上限到达后**允许淘汰最旧**，但**必须留证**（计数 + 告警，含丢弃条数与时间范围），并可通过查询/事件获取 |
| BF-5 | **缓存是否掉电不丢 —— 待产品确认**。**建议**：**仅内存 + 显式上限**。理由：① 外设数据非计费/结算依据；② 掉电期间的历史仍有 03 号 `telemetry` 表兜底（03 号 PRD §4.1）；③ 若要求掉电不丢，须**先明确与 03 号的职责边界**，防双套持久化与口径分裂 |
| BF-6 | 上送失败必须**可观测**：累计失败/丢弃计数须可查询，并在连续失败时产出告警（**不得**静默丢弃） |

### 8.7 非功能性需求

| 指标 | 要求 | 说明/依据 |
|------|------|-----------|
| IEC 104 上送周期 | 总表 1 s（既有）；外设 A 档 ≤ 1 s、B 档 ≤ 5 s；C 档变化后 ≤ 1 s | 与 §8.6.3 同档；周期须**可配置** |
| MQTT 上送周期 | 同 §8.6.3 | |
| 端到端时延 | 采集完成 → IEC 104 发出 ≤ 周期 + 100 ms；采集完成 → MQTT 发出 ≤ 周期 + 1 s | |
| 总召响应时间 | 收到 `C_IC_NA_1` → `ACT_TERM` 完成 ≤ **5 s**（231 点全量） | |
| 初始快照时延 | `STARTDT` 完成 → 全量快照发出起始 ≤ **5 s** | GI-5 |
| 数据新鲜度 | 与 02 号 PRD §9.7.1 同源：`stale_timeout_s = 5 s` | 单一真源，不得另立门限 |
| **不丢语义** | MQTT：断线期间**不丢**（缓存补送，上限见 BF-4）；IEC 104：断线期间**周期遥测允许丢**（重连后由初始快照补齐），**遥信变位不得静默丢失**（重连后须补发或随快照/总召送达） | |
| **带宽量级（IEC 104 侧）** | 单点单 ASDU 帧 = **18 字节**（`encode_telemetry_asdu` 现形态：ASDU 12 + APCI 6，`protocol.rs:344-358`）。**231 点**、~200 点 @5 s + ~30 点 @1 s + COS 高峰 10 点/s ⇒ 稳态 **≈ 1.4 KB/s（≈ 12 kbps）**；总召/快照突发 **≈ 4.2 KB** 一次性 | 假设为单点单 ASDU；**设计可用顺序 ASDU（SQ=1）压缩**，届时实测值应显著低于上述上界；**精确值由设计给出，本节为上界量级** |
| **带宽量级（MQTT 侧）** | 624 点、按站分片、B 档 5 s + A 档 1 s + C 档 COS：稳态 **≤ 5 KB/s（≈ 40 kbps）**（含 TLS 帧开销的上界；若告警位按 COS 计，实际应 ≈ **1.5 KB/s**）；初始快照突发 **≈ 20 KB** | 载荷按 JSON 字符数估（点名 ≈18 B + 值/单位/质量 ≈25 B/点） |
| 资源增量 | 上送侧**不新增串口占用**；CPU/内存增量须由设计给出（南向采集侧增量见 02 号 PRD §9.8.3，< 100 KB / < 1%） | |
| 时标 | 一律 **UTC**；IEC 104 使用带时标 TypeID（§2.3 既有 TypeID 列表） | |
| 并发连接 | IEC 104 ≤ 5（§2.2 既有，不变） | |

> **⚠️ 带宽口径声明**：上表两个数字是**上界量级**（按最保守的"单点单 ASDU / 无压缩 JSON"假设），用途是**证明可行**，**不得**作为精确值引用；设计阶段须给出精确值并回填本表。

### 8.8 边界与异常（上送行为必须逐条明确）

| ID | 情形 | **上送行为（不得含糊）** |
|----|------|--------------------------|
| EX-1 | **外设站离线**（RS485 读失败 / 站 offline） | **发无效标记，不发旧值当实时值**：MQTT 侧发出 `q = "invalid"`（或 `stale`）并**保留原值与原始时标**；IEC 104 侧**不发数值**（该点从周期上送与总召响应中消失），并产出/上送"站离线"遥信。**超过 5 s 旧值不再重复上送**（避免以陈旧值刷屏） |
| EX-2 | **主站未连接** | 周期遥测**不入队列、允许丢**；连接建立后由**初始快照**（GI-5）补齐；**遥信变位不得静默丢**（重连后补发，或随快照/总召送达） |
| EX-3 | **broker 不可达 / 断线** | 进缓存补送（§8.6.5）；**不得**因不可达而静默丢弃且无计数 |
| EX-4 | **证书过期 / 校验失败 / 缺失** | **不建立连接** + 显式告警；**严禁回退明文**（TLS-2） |
| EX-5 | **点表与现场不符**（地址错 / 量纲错 / 点表版本错） | **传输层不做设备特判**：原始工程值照发；质量位**仅在**上层已判定越界时标 `invalid`（口径同 02 号 PRD §9.6.3：**仅 SOC 做域检查**，其余不做自动量程校验）。发现不符须修配置/重新取点表（02 号 PRD §9.7.2），**不得**在网关或桥接层加设备特判 |
| EX-6 | **值为"未配置 / 预留"** | **不发该点**（点表内本就不配块，如消防钢瓶气压无该功能、空调 30002/30005 预留寄存器）；**严禁发 0** 或用 0 表示"无功能"（02 号 PRD §9.7.6） |
| EX-7 | **PCS 站未启用**（§8.2 前置条件未满足） | 72 点**一律不上送**（IEC 104 与 MQTT 同），**不得**以 0 / 占位值顶替；站启用后按 §8.3 生效 |
| EX-8 | **位块首轮全量突发**（≈319 点） | 允许合并为一条消息 / 一轮顺序 ASDU；**不得**因突发造成后续周期包被丢弃 |
| EX-9 | **缓存满且无法补送** | 允许淘汰最旧（BF-4），**必须**留证（计数 + 告警） |
| EX-10 | **装置标识缺失**（`dev` 来源未定） | 须显式标注"未提供"，**不得**臆造（口径同 12 号 PRD EDGE-16） |

### 8.9 验收标准

#### 8.9.1 本机可验证（mock broker / 本机 IEC 104 客户端，不接真设备与真网络）

| ID | 验收条件 | 验证方法 |
|----|----------|----------|
| AC-U74-01 | **上送点表可执行**：按 §8.3.2 分段 + §9.5 点序号生成的 IOA 表，逐点均能被主站端收到；**收到点数 == 声明点数**（IEC 104 侧 231，PCS 未启用时不含 72） | 本机 IEC104 客户端对点 |
| AC-U74-02 | **MQTT 点数与点名**：mock broker 订阅 `mupc/north/telemetry/#`，收到的点集**恰为 624 点**（PCS 未启用时 552），且 `n` 与南向点名**逐字一致、零改名** | mock broker + 逐点比对 |
| AC-U74-03 | **分片与不合并**：每条消息只含**一个站**的数据；BMS 单包不与其站混发 | mock broker 断言 |
| AC-U74-04 | **变化上送（COS）**：连续两轮值不变时，C 档位点**不产生新消息**；B 档遥测按周期产生；连接建立后 C 档**立即全量一次** | mock broker 计数断言 |
| AC-U74-05 | **无效标记不假值**：mock 使某站采集失败 → 该站点位 `q` 变为 `invalid`/`stale`，`v` **保持原值或显式 null**，**不得变为 0** | mock 注入故障 |
| AC-U74-06 | **断线缓存与补送**：断开 mock broker ≥ 60 s → 重连后补送，`ts` **为原采集时刻**（非补送时刻）、`seq` 顺序单调、丢弃计数（若有）可查询 | mock broker 断连/恢复 |
| AC-U74-07 | **TLS fail-closed**：配置不存在的证书路径 / 故意置过期证书 → **不建立连接**，事件含明确原因；全流程**不出现明文连接**（抓包或日志断言） | 配置注入 + 抓包 |
| AC-U74-08 | **未配置即不连接**：移除 `mqtt_bridge` 段 → 进程启动后**零连接尝试**、零重连日志（回归防护 CFG-2） | 配置移除 + 日志断言 |
| AC-U74-09 | **无空转模块**：不存在"注册为 Running 但 `start()` 空实现"的 MQTT 相关模块（CFG-4） | 启动后模块状态与实际行为对照 |
| AC-U74-10 | **总召**：本机客户端发 `C_IC_NA_1`（QOI=20）→ 收到 `ACT_CON` → 全部有效点 → `ACT_TERM`；**无有效值的点不出现** | 本机 IEC104 客户端 |
| AC-U74-11 | **初始快照**：`STARTDT` 完成后 ≤ 5 s 收到一次全量快照，且**包含连接建立之前产生的值**（回归防护 GI-5：现状连接前缓存全丢） | 本机客户端 + 时序断言 |
| AC-U74-12 | **最新值入口**：mock 消费方通过"最新值入口"读取外设实时值，**全程零 SQL 查询**（以接口形态/依赖计数断言）；变更通知到达 **≤ 1 s**；位块点（BMS 288 位 / 空调 31 位）**同样可读到** | mock 消费方 + 查询计数断言 |
| AC-U74-13 | **不可得显式化**：站离线时该入口返回显式"不可得"，**不返回 0、不静默返回旧值**（LV-6） | mock 注入离线 |
| AC-U74-14 | **带宽上界**：按 §8.3 点表实测，稳态 ≤ §8.7 的上界（IEC 104 ≈12 kbps / MQTT ≤ 40 kbps），总召/快照突发 ≤ 上界 | 流量计数 |

#### 8.9.2 需真机 / 真网络验证

| ID | 验收条件 | 前置/说明 |
|----|----------|-----------|
| RC-U74-01 | 对**真实物联平台 broker**（真实双向证书）连接成功，订阅方可收到全部 624 点 | 需真实 broker 与证书 |
| RC-U74-02 | 与**真实调度主站逐点对点**：IOA ↔ 点名 ↔ 量纲**逐点核对一致**；总召响应被主站正确解析 | 需主站配合；**须在投运前完成** |
| RC-U74-03 | 外设量值与**设备本机显示/厂方调试工具**一致（量纲、分辨率、符号性） | 口径同 02 号 PRD §9.9.2 RC-1；**116/186 号点须按 Q-20 ② 专项核对** |
| RC-U74-04 | **断网 ≥ 30 min** 的补送行为对端可接受（补送量、时序、对端限流） | 上限口径待产品确认（BF-4） |
| RC-U74-05 | **证书轮换 / 到期**流程验证（含告警门限 TLS-3 与 fail-closed 行为） | |
| RC-U74-06 | **PCS 站**解除 §8.2 两项阻塞后，其 72 点逐点核对 | 未解除前**不得**投产（Q-4 / Q-15） |

### 8.10 前置依赖与待确认项

**前置依赖（不阻塞本章 PRD 定稿，但影响实现排期）**：

| # | 依赖 | 状态/口径 |
|---|------|-----------|
| D1 | 02 号南向采集（618 点）已实现 | ✅ S3b-2 已交付；本章**不改**采集侧语义 |
| D2 | 03 号 P0-1「遥测缓冲定时 + 退出 flush」 | ✅ 已修（审查报告 §"P0 修复批次核验"）。本章**不依赖**落库即上送（上云走实时入口），但"实时值 vs 历史"口径须分离（§8.5） |
| D3 | 12 号「帧契约」 | **无关、不阻塞**：U-73（外设上屏）是独立立项；本轮**不**要求 12 号增字段，但**共用**同一最新值入口（§8.5），不得各建一套 |
| D4 | 现场项 | `ttyS2–S6` 可打开性、DE/RE 方向、02 号 PRD §9.10 的 RC-1/RC-3/RC-5/RC-6/RC-8/RC-11 —— 点表与现场的一致性核对是投运前提（RC-U74-02/03） |
| D5 | `U-70`（遥测消费方改判） | 需求文档动作，与本章并行；本章**不**把 `telemetry` 表当作上云数据源，故不受其阻塞 |

**必须由产品确认的项（逐条给出两侧选项与建议）**：

| # | 待确认项 | 选项 | **建议** |
|---|----------|------|----------|
| Q1 | **IEC 104 上送范围** | ① 全量 624 点 ② 调度子集 231 点 | **② 子集**。理由：主站无 HVAC 温湿度、消防探测器明细（114 点）的粒度需求；全量徒增对点成本 |
| Q2 | **IOA 分段方案** | ① 分段式（§8.3.2 表，兼容既有 1–6） ② 电力调度惯例重排（遥信 1–4096 / 遥测 16385+） | **①**。理由：既有 6 点已按"现场追认"产出，方案 ② 会强制迁移并须主站重新对点 |
| Q3 | **BMS 告警位在 IEC 104 的口径** | ① 聚合 12 点 ② 全量 288 点 | **①**，但须满足"只并粒度、不丢语义"（任一级置位 ⇒ 聚合点置位） |
| Q4 | **HVAC 温湿度 3 点是否进 IEC 104** | ① 不进（仅 MQTT） ② 进 | **①**。理由：纯展示量、无调度用途；若进则 IOA 段 7 扩为 601–634 |
| Q5 | **分组召唤（QOI=21–36）** | ① 本轮不做（登记 P2） ② 本轮做 | **①**。理由：IEC 104 侧仅 231 点，站召唤一轮可完成 |
| Q6 | **北向 MQTT 承载收敛** | ① 收敛为一套（下架另一套） ② 两套并存 | **①**。理由：现状两套并存且都空转，双实现 = 双份缺陷面 |
| Q7 | **断线缓存持久化** | ① 仅内存 + 显式上限 ② 掉电不丢（须先与 03 号划界） | **①** |
| Q8 | **缓存上限口径** | ① 时间窗 ≥ 30 min ② 条数 ≥ 10,000 | **两者取先到**（§BF-4） |
| Q9 | **开发/仿真环境是否允许明文 MQTT** | ① 允许（需显式开关 + 非生产构建 + 响亮告警） ② 一律禁止 | **①**（否则仿真环境无法联调） |
| Q10 | **装置标识（`dev` / 载荷）来源** | ① 装置序列号 ② 其它 | **①**；现状**无统一来源**，须产品/设计定 |
| Q11 | **证书到期告警门限** | ① 30 天 ② 其它 | **①** |

---

## 附录 A：术语表

| 术语 | 说明 |
|------|------|
| MUPC | 微电网特种调控装置 |
| IEC 104 | IEC 60870-5-104，远动协议 |
| IEC 61850 | 变电站通信协议 |
| MMS | 制造报文规范（Manufacturing Message Specification） |
| GOOSE | 面向通用对象的变电站事件（Generic Object Oriented Substation Event） |
| ASN.1 | 抽象语法标记（Abstract Syntax Notation One） |
| QoS | 服务质量（Quality of Service） |
| MQTT | 物联网消息队列协议 |
| TLS | 传输层安全协议 |
| IED | 智能电子设备（Intelligent Electronic Device） |
| VPP | 虚拟电厂 |
| emqx | 企业级 MQTT Broker |
| mosquitto | 轻量开源 MQTT Broker |
| TTU | 台区智能融合终端 |
| SOC | 电池荷电状态 |
| SOH | 电池健康状态 |
| IOA | 信息对象地址（Information Object Address），IEC 104 中每个遥测/遥信/遥控点的寻址编号 |
| 总召 | 总召唤（General Interrogation），主站主动索取全站当前值的报文，TypeID `C_IC_NA_1`（TI=100） |
| QOI | 召唤限定词（Qualifier of Interrogation），`20` = 站召唤，`21`–`36` = 第 1–16 组召唤 |
| COS | 变化上送（Change of State），值/状态发生变化时才上送 |
| 质量位 | 载荷中标记数据可用性的字段（`ok` / `stale` / `invalid` / `unconfigured`） |
| 最新值入口 | 外设实时值的进程内可读入口（含新鲜度与变更通知），见 §8.5；与 03 号的 `telemetry` 历史表**语义不同** |

---
## 附录：版本演进

> 正文已整合全部历史补丁，本表仅作演进追溯。

| 版本 | 主要变更 |
|------|----------|
| v1.0 | 初版，合并四份来源文档中通信网关模块（gateway、iec61850-plugin、mqtt-plugin、mqtt-bridge、device-trait）相关需求 |
| v1.1 | 修复 5 项部分覆盖缺口：本地运维无线通道、纵向加密认证合规、看门狗监控实时控制模块、IEC 61850-7-420 DER 逻辑节点、OTA 远程升级引用 |
| **v1.2** | **新增 §8「外设数据上云（U-74，含 U-71）」**（2026-09-23，用户裁定立项）。内容：① **上送范围与点表** —— 定义 6 类设备（总表/BMS/PCS/储能表/消防/空调）的上送量、点数、单位与上送方式，合计 MQTT 全量 **624 点**、IEC 104 **231 点**（含 PCS 则 303）；② **IOA 分段与命名约定**（1–6 既有 + 101/201/301/401/501/601 分段，段内序号锚定 02 号 PRD §9.5）；③ **两条通道职责分工**（IEC 104 = 调度主站、调度/安全子集；MQTT = 物联平台/VPP、全量）；④ **总召（`C_IC_NA_1`）+ 连接初始快照**纳入本轮（分组召唤登记 P2）；⑤ **外设实时值数据可用性要求**（最新值 + 变更通知，不得依赖轮询数据库），与 12 号 §4.4 / 03 号 §2.1 口径统一；⑥ **MQTT 真做的需求面**（配置承载与"未配置即不连接"、主题分片与 JSON 字段契约、周期/QoS 三档、断线缓存补送、TLS fail-closed）；⑦ 非功能（时延/周期/不丢语义/带宽量级）；⑧ 边界异常 10 条（发旧值 / 发无效标记 / 不发送**逐条明确**）；⑨ 验收 14 条本机可验证 + 6 条需真机网络；⑩ 待产品确认 11 项。**新增术语 6 条。既有门禁标记与既有章节未改动。** 需求评审结论：**通过** —— `[REVIEWED: PASS: 2026-09-23]`（2026-09-23，**仅对本 v1.2 增量有效**）。 |

