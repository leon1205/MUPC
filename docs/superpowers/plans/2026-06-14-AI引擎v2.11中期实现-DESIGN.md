# AI引擎 v2.11 中期实现设计文档

> **状态**: REVIEWED
> **日期**: 2026-06-14
> **作者**: AI架构师

---

[DESIGN_APPROVED]

---

## 1. 概述

### 1.1 目的

本文档为 AI引擎 v2.11 中期实现提供详细设计规范，包含两个核心需求的架构设计、接口定义、数据流设计、错误处理和测试策略。

### 1.2 范围

- **需求1**：自适应权重优化器（Adaptive Weight Optimizer）
- **需求2**：冲击负荷概率预测（Impact Load Probability Prediction）

### 1.3 与现有架构的关系

```
v2.10: 安全覆盖 + 折扣累积奖励机制
         ↓
v2.11: 自适应权重优化器 + 冲击负荷概率预测
         ↓
v2.12: 多源气象数据融合（远期规划）
```

**依赖关系：**
- v2.11 依赖 v2.10 基础设施（折扣累积奖励、安全覆盖感知）
- 自适应权重优化器输出 SceneWeights，奖励计算器消费
- 概率预测输出注入 FusedSystemState，供 RL 模型和奖励函数使用

---

## 2. 需求1：自适应权重优化器

### 2.1 架构设计

#### 2.1.1 组件关系

```
┌─────────────────────────────────────────────────────────────────┐
│                    AdaptiveWeightOptimizer                      │
│  ┌─────────────────┐  ┌─────────────────┐  ┌────────────────┐ │
│  │   MetaLearner   │  │   Performance   │  │  WeightBounds  │ │
│  │                 │  │   Collector     │  │  Enforcer      │ │
│  └────────┬────────┘  └────────┬────────┘  └───────┬────────┘ │
│           │                     │                   │          │
│           └─────────────────────┼───────────────────┘          │
│                                 ↓                              │
│                    ┌────────────────────────┐                 │
│                    │  WeightAdjustmentEngine │                 │
│                    └───────────┬────────────┘                 │
└─────────────────────────────────┼─────────────────────────────┘
                                  ↓
                    ┌────────────────────────┐
                    │     SceneWeights       │
                    └───────────┬────────────┘
                                ↓
                    ┌────────────────────────┐
                    │   RewardCalculator     │
                    └────────────────────────┘
```

#### 2.1.2 新增文件

| 文件路径 | 职责 |
|---------|------|
| `ai-engine/src/adaptive_weight_optimizer.rs` | 自适应权重优化器核心实现 |
| `ai-engine/src/pareto_optimizer.rs` | NSGA-II 多目标优化器 |
| `ai-engine/src/performance_collector.rs` | 性能指标收集器 |

#### 2.1.3 配置结构

```rust
// ai-engine/src/config.rs

/// 自适应权重优化器配置（v2.11 新增）
#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct AdaptiveOptimizerConfig {
    /// 是否启用
    pub enabled: bool,
    /// 更新间隔（小时）
    pub update_interval_hours: u32,
    /// 元学习率
    pub meta_learning_rate: f64,
    /// 权重边界
    pub weight_bounds: WeightBounds,
    /// 约束条件
    pub constraints: WeightConstraints,
}

impl Default for AdaptiveOptimizerConfig {
    fn default() -> Self {
        Self {
            enabled: false,
            update_interval_hours: 168, // 周级更新
            meta_learning_rate: 0.001,
            weight_bounds: WeightBounds::default(),
            constraints: WeightConstraints::default(),
        }
    }
}

/// 权重边界
#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct WeightBounds {
    pub min: f64,
    pub max: f64,
}

impl Default for WeightBounds {
    fn default() -> Self {
        Self { min: 0.01, max: 10.0 }
    }
}

/// 权重约束
#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct WeightConstraints {
    /// 权重和归一化基准
    pub sum_normalized: f64,
    /// 单次最大调整幅度
    pub max_adjustment_per_update: f64,
}

impl Default for WeightConstraints {
    fn default() -> Self {
        Self {
            sum_normalized: 8.3,
            max_adjustment_per_update: 0.2,
        }
    }
}

/// NSGA-II Pareto 优化器配置（v2.11 新增）
#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct ParetoOptimizerConfig {
    pub enabled: bool,
    pub population_size: usize,
    pub generations: usize,
    pub crossover_rate: f64,
    pub mutation_rate: f64,
}

impl Default for ParetoOptimizerConfig {
    fn default() -> Self {
        Self {
            enabled: false,
            population_size: 100,
            generations: 50,
            crossover_rate: 0.9,
            mutation_rate: 0.1,
        }
    }
}
```

### 2.2 详细设计

#### 2.2.1 AdaptiveWeightOptimizer

```rust
// ai-engine/src/adaptive_weight_optimizer.rs

use crate::config::{AdaptiveOptimizerConfig, SceneWeights, WeightBounds, WeightConstraints};
use crate::error::AiEngineError;
use std::sync::Arc;
use tokio::sync::RwLock;

/// 性能特征（用于元学习器输入）
#[derive(Debug, Clone)]
pub struct PerformanceFeatures {
    /// 光伏消纳率 [0.0, 1.0]
    pub pv_utilization_rate: f64,
    /// 电池循环次数
    pub battery_cycle_count: u32,
    /// 变压器平均负载率 [0.0, 1.0]
    pub trafo_avg_load: f64,
    /// 需量超标次数
    pub demand_violation_count: u32,
    /// 累积奖励
    pub cumulative_reward: f64,
}

/// 权重调整量
#[derive(Debug, Clone)]
pub struct WeightAdjustment {
    /// 各场景权重调整量
    pub seasonal_load_delta: [f64; 8],
    pub commercial_arbitrage_delta: [f64; 2],
    pub demand_control_delta: [f64; 2],
    pub virtual_power_plant_delta: [f64; 3],
    pub ultra_green_delta: [f64; 2],
}

/// 历史性能数据
#[derive(Debug, Clone)]
pub struct HistoricalPerformance {
    pub features: PerformanceFeatures,
    pub timestamp: i64,
}

/// 自适应权重优化器
pub struct AdaptiveWeightOptimizer {
    config: AdaptiveOptimizerConfig,
    /// 当前权重快照
    current_weights: RwLock<SceneWeights>,
    /// 权重调整历史
    adjustment_history: RwLock<Vec<WeightAdjustment>>,
    /// 性能指标收集器引用
    performance_collector: Arc<dyn PerformanceCollector>,
}

impl AdaptiveWeightOptimizer {
    /// 创建优化器
    pub fn new(
        config: AdaptiveOptimizerConfig,
        initial_weights: SceneWeights,
        performance_collector: Arc<dyn PerformanceCollector>,
    ) -> Self {
        Self {
            config,
            current_weights: RwLock::new(initial_weights),
            adjustment_history: RwLock::new(Vec::new()),
            performance_collector,
        }
    }

    /// 基于元学习优化权重
    ///
    /// # 输入
    /// - historical_performance: 历史性能指标
    ///
    /// # 输出
    /// - 优化后的 SceneWeights
    pub async fn optimize_weights(
        &self,
        historical_performance: &HistoricalPerformance,
    ) -> Result<SceneWeights, AiEngineError> {
        // 1. 提取性能特征
        let features = self.extract_features(historical_performance);

        // 2. 元学习器预测最优权重调整方向（简化实现）
        let adjustment = self.meta_learn_predict(&features)?;

        // 3. 应用调整（带约束剪裁）
        let new_weights = self.apply_adjustment(&adjustment)?;

        // 4. 记录调整历史
        self.record_adjustment(historical_performance, &adjustment).await;

        Ok(new_weights)
    }

    /// 提取性能特征
    fn extract_features(&self, perf: &HistoricalPerformance) -> PerformanceFeatures {
        perf.features.clone()
    }

    /// 元学习器预测（简化版，实际应调用小型神经网络）
    fn meta_learn_predict(&self, features: &PerformanceFeatures) -> Result<WeightAdjustment, AiEngineError> {
        // 简化实现：基于规则的调整
        // 实际实现应调用训练好的元学习器模型
        let mut delta = WeightAdjustment {
            seasonal_load_delta: [0.0; 8],
            commercial_arbitrage_delta: [0.0; 2],
            demand_control_delta: [0.0; 2],
            virtual_power_plant_delta: [0.0; 3],
            ultra_green_delta: [0.0; 2],
        };

        // 根据性能特征调整权重
        // 如果光伏消纳率低，增加 w1（光伏消纳权重）
        if features.pv_utilization_rate < 0.7 {
            delta.seasonal_load_delta[0] = 0.1;
        }

        // 如果需量超标次数多，增加 w1（需量控制权重）
        if features.demand_violation_count > 3 {
            delta.demand_control_delta[0] = 0.15;
        }

        Ok(delta)
    }

    /// 应用权重调整（带物理约束剪裁）
    fn apply_adjustment(&self, adjustment: &WeightAdjustment) -> Result<SceneWeights, AiEngineError> {
        let current = self.current_weights.read().unwrap();
        let mut new_weights = current.clone();

        // 约束1：权重必须为正
        for (i, w) in adjustment.seasonal_load_delta.iter().enumerate() {
            new_weights.seasonal_load_management[i] =
                (current.seasonal_load_management[i] + w).max(self.config.weight_bounds.min);
        }

        // 约束2：权重比例合理性（归一化）
        let sum: f64 = new_weights.seasonal_load_management.iter().sum();
        if sum > 0.0 {
            let scale = self.config.constraints.sum_normalized / sum;
            for w in &mut new_weights.seasonal_load_management {
                *w *= scale;
            }
        }

        // 约束3：单次调整幅度限制
        for i in 0..8 {
            let diff = (new_weights.seasonal_load_management[i] - current.seasonal_load_management[i]).abs();
            if diff > self.config.constraints.max_adjustment_per_update * current.seasonal_load_management[i] {
                // 限制调整幅度
                let sign = if new_weights.seasonal_load_management[i] > current.seasonal_load_management[i] { 1.0 } else { -1.0 };
                new_weights.seasonal_load_management[i] = current.seasonal_load_management[i] +
                    sign * self.config.constraints.max_adjustment_per_update * current.seasonal_load_management[i];
            }
        }

        Ok(new_weights)
    }

    /// 记录调整历史
    async fn record_adjustment(&self, perf: &HistoricalPerformance, adjustment: &WeightAdjustment) {
        let mut history = self.adjustment_history.write().await;
        history.push(adjustment.clone());
        // 保留最近 100 条记录
        if history.len() > 100 {
            history.remove(0);
        }
    }

    /// 获取当前权重
    pub async fn get_current_weights(&self) -> SceneWeights {
        self.current_weights.read().unwrap().clone()
    }

    /// 更新当前权重
    pub async fn update_weights(&self, new_weights: SceneWeights) {
        *self.current_weights.write().await = new_weights;
    }

    /// AWO-06: 验证奖励偏移（优化后奖励函数与原始策略无显著偏离）
    ///
    /// # 输入
    /// - original_reward: 原始策略奖励值
    /// - optimized_reward: 优化后策略奖励值
    ///
    /// # 输出
    /// - true: 偏移 < 5%，验证通过
    /// - false: 偏移 >= 5%，验证失败
    pub async fn validate_reward_drift(
        &self,
        original_reward: f64,
        optimized_reward: f64,
    ) -> bool {
        if original_reward.abs() < 1e-6 {
            // 原始奖励接近零时，使用绝对误差
            return (optimized_reward - original_reward).abs() < 0.05;
        }
        let drift = ((optimized_reward - original_reward) / original_reward).abs();
        drift < 0.05 // 偏移 < 5%
    }
}

/// 性能指标收集器 trait
pub trait PerformanceCollector: Send + Sync {
    /// 收集历史性能数据
    fn collect_historical(&self) -> Result<HistoricalPerformance, AiEngineError>;
    /// 收集当前性能快照
    fn collect_current(&self) -> Result<PerformanceFeatures, AiEngineError>;
}
```

#### 2.2.2 ParetoWeightOptimizer (NSGA-II)

```rust
// ai-engine/src/pareto_optimizer.rs

use crate::config::ParetoOptimizerConfig;
use crate::error::AiEngineError;

/// 优化目标枚举
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum OptimizationObjective {
    MaximizePvUtilization,
    MinimizeBatteryDegradation,
    MinimizeTrafoOverload,
    MinimizeDemandViolation,
    MaximizeReward,
}

/// 权重候选解
#[derive(Debug, Clone)]
pub struct WeightCandidate {
    pub weights: Vec<f64>,
    pub objectives: Vec<f64>,
}

/// Pareto 解
#[derive(Debug, Clone)]
pub struct ParetoSolution {
    pub weights: Vec<f64>,
    pub objectives: Vec<f64>,
    pub crowding_distance: f64,
}

/// NSGA-II 多目标权重优化器
pub struct ParetoWeightOptimizer {
    config: ParetoOptimizerConfig,
    objectives: Vec<OptimizationObjective>,
    pareto_front: RwLock<Vec<ParetoSolution>>,
}

impl ParetoWeightOptimizer {
    pub fn new(config: ParetoOptimizerConfig) -> Self {
        Self {
            config,
            objectives: vec![
                OptimizationObjective::MaximizePvUtilization,
                OptimizationObjective::MinimizeBatteryDegradation,
                OptimizationObjective::MinimizeTrafoOverload,
                OptimizationObjective::MinimizeDemandViolation,
            ],
            pareto_front: RwLock::new(Vec::new()),
        }
    }

    /// 搜索 Pareto 前沿
    pub async fn find_pareto_front(&self, initial_population: &[WeightCandidate]) -> Result<Vec<ParetoSolution>, AiEngineError> {
        let mut population = initial_population.to_vec();

        for _gen in 0..self.config.generations {
            // 1. 快速非支配排序
            let fronts = self.fast_non_dominated_sort(&population);

            // 2. 计算拥挤度距离
            for front in &mut fronts.iter_mut() {
                self.calculate_crowding_distance(front);
            }

            // 3. 选择、交叉、变异生成下一代
            population = self.evolve(&fronts);
        }

        // 返回第一前沿（Pareto 最优解）
        let fronts = self.fast_non_dominated_sort(&population);
        if let Some(first_front) = fronts.first() {
            let mut solutions: Vec<ParetoSolution> = first_front
                .iter()
                .map(|c| ParetoSolution {
                    weights: c.weights.clone(),
                    objectives: c.objectives.clone(),
                    crowding_distance: 0.0,
                })
                .collect();
            self.calculate_crowding_distance(&mut solutions);
            Ok(solutions)
        } else {
            Ok(Vec::new())
        }
    }

    /// 快速非支配排序（NSGA-II 标准实现）
    ///
    /// # 算法
    /// 1. 对每个个体 p，遍历所有个体 q
    /// 2. 若 p 被任意 q 支配，则 p 不属于 Pareto 前沿
    /// 3. 若不存在支配 p 的个体，则 p 属于第一前沿
    /// 4. 标记属于第一前沿的个体，从集合中移除，重复上述过程得到后续前沿
    fn fast_non_dominated_sort(&self, population: &[WeightCandidate]) -> Vec<Vec<WeightCandidate>> {
        let n = population.len();
        if n == 0 {
            return vec![vec![]];
        }

        // 存储每个个体的支配解数量和被支配计数
        let mut domination_count: Vec<usize> = vec![0; n];
        let mut dominated_solutions: Vec<Vec<usize>> = vec![vec![]; n];

        // 第一前沿
        let mut fronts: Vec<Vec<WeightCandidate>> = vec![vec![]];

        for p in 0..n {
            for q in 0..n {
                if p == q {
                    continue;
                }
                // 检查 p 是否支配 q
                if self.dominates(&population[p], &population[q]) {
                    dominated_solutions[p].push(q);
                } else if self.dominates(&population[q], &population[p]) {
                    // q 支配 p
                    domination_count[p] += 1;
                }
            }
            // 若没有被任何个体支配，则属于第一前沿
            if domination_count[p] == 0 {
                fronts[0].push(population[p].clone());
            }
        }

        // 构造后续前沿
        let mut front_idx = 0;
        while !fronts[front_idx].is_empty() {
            let mut next_front: Vec<WeightCandidate> = vec![];
            for &p_idx in fronts[front_idx].iter() {
                let p_pos = population.iter().position(|c| std::ptr::eq(c, &population[p_pos])).unwrap_or(p_idx);
                // 降低被支配计数
                for &dominated_idx in &dominated_solutions[p_pos] {
                    domination_count[dominated_idx] = domination_count[dominated_idx].saturating_sub(1);
                    if domination_count[dominated_idx] == 0 {
                        next_front.push(population[dominated_idx].clone());
                    }
                }
            }
            front_idx += 1;
            if !next_front.is_empty() {
                fronts.push(next_front);
            }
        }

        fronts
    }

    /// 判断个体 a 是否支配个体 b（假设为最大化问题）
    fn dominates(&self, a: &WeightCandidate, b: &WeightCandidate) -> bool {
        let mut better_or_equal_in_all = true;
        let mut strictly_better_in_at_least_one = false;

        for (obj_a, obj_b) in a.objectives.iter().zip(b.objectives.iter()) {
            if obj_a > obj_b {
                strictly_better_in_at_least_one = true;
            } else if obj_a < obj_b {
                better_or_equal_in_all = false;
            }
        }

        better_or_equal_in_all && strictly_better_in_at_least_one
    }

    /// 计算拥挤度距离
    fn calculate_crowding_distance(&self, front: &mut Vec<ParetoSolution>) {
        let n = front.len();
        if n < 2 {
            return;
        }

        for i in 0..n {
            front[i].crowding_distance = 0.0;
        }

        for obj_idx in 0..front[0].objectives.len() {
            // 按目标值排序
            front.sort_by(|a, b| a.objectives[obj_idx].partial_cmp(&b.objectives[obj_idx]).unwrap());

            // 边界解距离设为无穷大
            front[0].crowding_distance = f64::INFINITY;
            front[n - 1].crowding_distance = f64::INFINITY;

            let obj_range = front[n - 1].objectives[obj_idx] - front[0].objectives[obj_idx];
            if obj_range > 0.0 {
                for i in 1..n - 1 {
                    front[i].crowding_distance +=
                        (front[i + 1].objectives[obj_idx] - front[i - 1].objectives[obj_idx]) / obj_range;
                }
            }
        }
    }

    /// 生成下一代
    fn evolve(&self, fronts: &[Vec<WeightCandidate>]) -> Vec<WeightCandidate> {
        // 简化实现：返回第一前沿
        fronts.first().map(|f| f.clone()).unwrap_or_default()
    }
}
```

#### 2.2.3 PerformanceCollector

```rust
// ai-engine/src/performance_collector.rs

use crate::error::AiEngineError;
use crate::adaptive_weight_optimizer::{HistoricalPerformance, PerformanceFeatures};
use std::sync::Arc;
use tokio::sync::RwLock;

/// 性能指标收集器实现
pub struct PerformanceCollectorImpl {
    /// 缓存的最近性能数据
    cached_features: RwLock<Option<PerformanceFeatures>>,
}

impl PerformanceCollectorImpl {
    pub fn new() -> Self {
        Self {
            cached_features: RwLock::new(None),
        }
    }

    /// 从 storage 收集历史性能数据
    fn collect_from_storage(&self) -> Result<HistoricalPerformance, AiEngineError> {
        // TODO: 实现从 storage 查询历史性能数据
        // 目前返回默认数据
        Ok(HistoricalPerformance {
            features: PerformanceFeatures {
                pv_utilization_rate: 0.8,
                battery_cycle_count: 0,
                trafo_avg_load: 0.5,
                demand_violation_count: 0,
                cumulative_reward: 0.0,
            },
            timestamp: chrono::Utc::now().timestamp(),
        })
    }
}

impl super::adaptive_weight_optimizer::PerformanceCollector for PerformanceCollectorImpl {
    fn collect_historical(&self) -> Result<HistoricalPerformance, AiEngineError> {
        self.collect_from_storage()
    }

    fn collect_current(&self) -> Result<PerformanceFeatures, AiEngineError> {
        let cached = self.cached_features.read().unwrap();
        cached.clone().ok_or_else(|| AiEngineError::InternalError("No cached performance data".into()))
    }
}
```

### 2.3 接口定义

#### 2.3.1 与 RewardCalculator 的集成

```rust
// 在 RewardCalculator 中新增方法

impl RewardCalculator {
    /// v2.11: 使用外部提供的 SceneWeights（由 AdaptiveWeightOptimizer 优化后）
    pub fn calculate_with_weights(
        &self,
        mode: RunningMode,
        action: &ActionOutput,
        state: &FusedSystemState,
        weights: &SceneWeights,
    ) -> f64 {
        // 使用提供的 weights 而非 self.weights
        match mode {
            RunningMode::SeasonalLoadManagement => {
                // ... 使用外部 weights
            }
            // ... 其他场景
        }
    }
}
```

#### 2.3.2 与 storage 的接口

| 接口 | 方向 | 说明 |
|------|------|------|
| `performance_history_query()` | storage → AI引擎 | 查询历史性能数据 |
| `weight_adjustment_log()` | AI引擎 → storage | 记录权重调整日志 |

### 2.4 数据流设计

```
离线训练管线
    ↓ (历史数据)
MetaLearner 训练
    ↓ (权重调整策略)
AdaptiveWeightOptimizer
    ↓ (在线优化)
SceneWeights 更新
    ↓ (奖励函数)
RL 模型决策
    ↓ (决策结果)
PerformanceCollector 收集性能指标
    ↓ (周期性反馈)
MetaLearner 持续学习
```

### 2.5 错误处理

| 错误场景 | 处理策略 |
|---------|---------|
| 元学习器推理失败 | 使用上一有效权重，回退告警 |
| 权重边界违规 | 强制裁剪到合法范围 |
| 性能数据缺失 | 跳过本轮优化，使用当前权重 |
| NSGA-II 收敛失败 | 返回空 Pareto 前沿，不更新权重 |

### 2.6 测试策略

| 测试项 | 测试方法 | 验收标准 |
|--------|---------|---------|
| AWO-01 | 单元测试：加载配置 | 配置正确解析，无 panic |
| AWO-04 | 单元测试：权重约束 | 权重为正，归一化和正确 |
| AWO-05 | 单元测试：调整幅度限制 | 单次变化不超过 20% |
| AWO-07 | 集成测试：权重更新后 RL 推理 | 推理延迟 < 1s |

---

## 3. 需求2：冲击负荷概率预测

### 3.1 架构设计

#### 3.1.1 组件关系

```
┌─────────────────────────────────────────────────────────────────┐
│                        LstmModel                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌────────────────┐  │
│  │  predict()      │  │ predict_quantiles() │  erfc()        │  │
│  │  (原有确定性)   │  │ (v2.11 分位数)    │  (正态 CDF)     │  │
│  └─────────────────┘  └─────────────────┘  └────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                ↓
                    ┌────────────────────────┐
                    │ ProbabilisticLoadOutput │
                    │  - quantiles[]         │
                    │  - base_load           │
                    │  - shock_probability   │
                    └───────────┬────────────┘
                                ↓
        ┌───────────────────────┼───────────────────────┐
        ↓                       ↓                       ↓
┌───────────────┐    ┌───────────────────┐    ┌───────────────┐
│RewardCalculator│    │  FusedSystemState  │    │StrategyEngine│
│calc_demand_   │    │ load_forecast_    │    │  冲击预警     │
│with_uncertainty│   │ quantiles        │    │              │
└───────────────┘    └───────────────────┘    └───────────────┘
```

#### 3.1.2 新增/修改文件

| 文件路径 | 改动类型 | 说明 |
|---------|---------|------|
| `ai-engine/src/lstm_model.rs` | 扩展 | 新增 `predict_quantiles()` 方法 |
| `ai-engine/src/load_covariates.rs` | 新增 | 负荷协变量结构体 |
| `ai-engine/src/reward_calculator.rs` | 扩展 | 新增 `calc_demand_with_uncertainty()` |
| `ai-engine/src/data_fusion.rs` | 扩展 | FusedSystemState 新增字段 |
| `ai-engine/src/config.rs` | 扩展 | LstmConfig 扩展 quantile_prediction |

### 3.2 详细设计

#### 3.2.1 LoadCovariates

```rust
// ai-engine/src/load_covariates.rs

/// 负荷预测协变量（v2.11 新增）
#[derive(Debug, Clone)]
pub struct LoadCovariates {
    /// 温度（摄氏度）
    pub temperature: f32,
    /// 日期类型（0=工作日，1=周末，2=节假日）
    pub date_type: u8,
    /// 是否灌溉季
    pub is_irrigation_season: bool,
    /// 小时（0-23）
    pub hour: u8,
}

impl Default for LoadCovariates {
    fn default() -> Self {
        Self {
            temperature: 25.0,
            date_type: 0,
            is_irrigation_season: false,
            hour: 12,
        }
    }
}
```

#### 3.2.1.1 WeatherService Trait (PLF-05 数据源定义)

```rust
// ai-engine/src/weather_service.rs

use crate::error::AiEngineError;

/// 气象数据服务 trait（PLF-05 新增）
///
/// 用于获取温度等气象协变量数据，供给 LSTM 分位数预测使用。
/// 实现者可对接气象局 API、本地传感器或 data_fusion.rs 的气象融合数据。
pub trait WeatherService: Send + Sync {
    /// 获取当前温度（摄氏度）
    fn get_current_temperature(&self) -> Result<f32, AiEngineError>;

    /// 获取指定时间范围的温度预测
    ///
    /// # 参数
    /// - hours_ahead: 向前预测的小时数
    ///
    /// # 返回
    /// - 温度预测数组（按小时排序）
    fn get_temperature_forecast(&self, hours_ahead: u32) -> Result<Vec<f32>, AiEngineError> {
        // 默认实现：返回空数组，子类可重写
        Ok(Vec::new())
    }
}

/// WeatherService 默认实现（使用静态温度值）
pub struct DefaultWeatherService {
    default_temperature: f32,
}

impl DefaultWeatherService {
    pub fn new(default_temperature: f32) -> Self {
        Self { default_temperature }
    }
}

impl WeatherService for DefaultWeatherService {
    fn get_current_temperature(&self) -> Result<f32, AiEngineError> {
        Ok(self.default_temperature)
    }
}

/// WeatherService 实现（从 data_fusion.rs 获取气象融合数据）
pub struct DataFusionWeatherAdapter {
    data_fusion: Arc<dyn DataFusionProvider>,
}

impl DataFusionWeatherAdapter {
    pub fn new(data_fusion: Arc<dyn DataFusionProvider>) -> Self {
        Self { data_fusion }
    }
}

impl WeatherService for DataFusionWeatherAdapter {
    fn get_current_temperature(&self) -> Result<f32, AiEngineError> {
        // 从 data_fusion 获取融合后的气象数据
        let state = self.data_fusion.get_fused_state()?;
        Ok(state.outdoor_temperature as f32)
    }
}

/// data_fusion.rs 需提供此 trait
pub trait DataFusionProvider: Send + Sync {
    fn get_fused_state(&self) -> Result<crate::data_fusion::FusedSystemState, AiEngineError>;
}
```

#### 3.2.2 ProbabilisticLoadOutput

```rust
// ai-engine/src/lstm_model.rs 新增结构体

/// 分位数预测（v2.11 新增）
#[derive(Debug, Clone)]
pub struct QuantilePrediction {
    /// 分位数（0.0 ~ 1.0）
    pub quantile: f32,
    /// 预测值 (kW)
    pub value: f32,
}

/// 概率负荷预测输出（v2.11 新增）
#[derive(Debug, Clone)]
pub struct ProbabilisticLoadOutput {
    /// 预测时间戳
    pub timestamp: i64,
    /// 各分位数预测值
    pub quantiles: Vec<QuantilePrediction>,
    /// 基础负荷（50% 分位数）
    pub base_load: f32,
    /// 冲击负荷发生概率
    pub shock_probability: f64,
    /// 预测置信度
    pub confidence: f64,
}
```

#### 3.2.3 LstmModel 扩展

```rust
// ai-engine/src/lstm_model.rs

impl LstmModel {
    /// v2.11: 分位数预测
    ///
    /// 输入：历史时间序列 + 协变量
    /// 输出：多分位数预测结果
    pub async fn predict_quantiles(
        &self,
        input: &LstmInput,
        covariates: &LoadCovariates,
    ) -> Result<ProbabilisticLoadOutput, AiEngineError> {
        // 1. 获取多个分位数的预测值
        let quantile_values = self.predict_multi_quantile(input, covariates).await?;

        // 2. 提取基础负荷（50% 分位数）
        let base_load = quantile_values
            .iter()
            .find(|q| (q.quantile - 0.5).abs() < 0.01)
            .map(|q| q.value)
            .unwrap_or(0.0);

        // 3. 计算冲击负荷概率
        let high_quantile = quantile_values
            .iter()
            .find(|q| (q.quantile - 0.9).abs() < 0.01)
            .map(|q| q.value)
            .unwrap_or(base_load);

        let shock_probability = self.calculate_shock_probability(base_load, high_quantile);

        // 4. 计算置信度
        let confidence = self.calculate_confidence(&quantile_values);

        Ok(ProbabilisticLoadOutput {
            timestamp: input.timestamp,
            quantiles: quantile_values,
            base_load,
            shock_probability,
            confidence,
        })
    }

    /// 预测多分位数（简化实现）
    async fn predict_multi_quantile(
        &self,
        input: &LstmInput,
        covariates: &LoadCovariates,
    ) -> Result<Vec<QuantilePrediction>, AiEngineError> {
        // 简化实现：调用原有 predict 生成基础预测，
        // 实际应使用支持多输出的 LSTM 模型
        let output = self.predict(input).await?;

        // 生成分位数预测（简化：P10=base*0.9, P50=base, P90=base*1.1）
        let base = output.predictions.first().copied().unwrap_or(0.0);
        Ok(vec![
            QuantilePrediction { quantile: 0.1, value: base * 0.9 },
            QuantilePrediction { quantile: 0.5, value: base },
            QuantilePrediction { quantile: 0.9, value: base * 1.1 },
        ])
    }

    /// v2.11: 计算冲击负荷发生概率
    ///
    /// P(shock) = 1 - Φ((median - mean) / std)
    /// 其中 std ≈ P90 - P50（高分位数与中位数的差值反映不确定性）
    fn calculate_shock_probability(&self, base_load: f32, high_quantile: f32) -> f64 {
        let spread = (high_quantile - base_load).max(1e-6);

        // 假设负荷服从正态分布，spread ≈ 1.28 * std（P90 对应 1.28σ）
        let std_approx = spread / 1.28;

        // 冲击阈值：超过 base_load + 2σ 视为冲击负荷
        let shock_threshold = base_load + 2.0 * std_approx;

        // 计算 P(load > shock_threshold)
        let z_score = (shock_threshold - base_load) / std_approx.max(1e-6);

        // 使用误差函数近似正态分布 CDF
        let shock_prob = 0.5 * Self::erfc(z_score / 1.41421356);

        shock_prob as f64
    }

    /// v2.11: 误差函数近似（用于正态分布 CDF 计算）
    fn erfc(x: f32) -> f32 {
        let abs_x = x.abs();
        if abs_x > 8.0 {
            return 0.0;
        }
        let exp_term = (-x * x).exp();
        let denom = std::f32::consts::PI * abs_x + (std::f32::consts::PI * x * x + 4.0).sqrt();
        exp_term / denom
    }

    /// v2.11: 计算置信度
    fn calculate_confidence(&self, quantiles: &[QuantilePrediction]) -> f64 {
        if quantiles.len() < 2 {
            return 0.5;
        }
        // 基于分位数间距计算置信度
        // 间距越小，置信度越高
        let p50 = quantiles.iter().find(|q| (q.quantile - 0.5).abs() < 0.01);
        let p90 = quantiles.iter().find(|q| (q.quantile - 0.9).abs() < 0.01);

        if let (Some(p50), Some(p90)) = (p50, p90) {
            let spread_ratio = (p90.value - p50.value) / p50.value.max(1e-6);
            (1.0 - spread_ratio.min(1.0)).max(0.0) as f64
        } else {
            0.5
        }
    }
}
```

#### 3.2.4 RewardCalculator 扩展

```rust
// ai-engine/src/reward_calculator.rs 新增方法

impl RewardCalculator {
    /// v2.11: SCENE-B2 需量控制（含不确定性）
    ///
    /// 考虑冲击负荷概率，预留额外安全裕度
    fn calc_demand_with_uncertainty(
        &self,
        action: &ActionOutput,
        state: &FusedSystemState,
        load_forecast: &ProbabilisticLoadOutput,
    ) -> f64 {
        let w = &self.weights.demand_control;

        // 基础需量控制奖励
        let demand_saved = (state.contract_demand - state.current_demand).max(0.0);
        let r_avoid = demand_saved * self.demand_penalty_rate;

        // 风险感知调整（v2.11 新增）
        // 考虑冲击负荷概率，预留额外安全裕度
        let high_quantile = load_forecast
            .quantiles
            .iter()
            .find(|q| (q.quantile - 0.9).abs() < 0.01)
            .map(|q| q.value)
            .unwrap_or(load_forecast.base_load);

        let risk_adjusted_demand = state.current_demand + 2.0 * (high_quantile - load_forecast.base_load) as f64;
        let risk_margin = if risk_adjusted_demand > state.contract_demand * 0.95 {
            // 预留 5% 安全裕度不足，产生风险惩罚
            -20.0 * ((risk_adjusted_demand - state.contract_demand * 0.95) / state.contract_demand)
        } else {
            0.0
        };

        let p_comfort = action.load_shedding * 0.5;

        w[0] * (r_avoid + risk_margin) - w[1] * p_comfort
    }
}
```

#### 3.2.5 FusedSystemState 扩展

```rust
// ai-engine/src/data_fusion.rs

impl FusedSystemState {
    // 在现有字段后添加（v2.11 新增）

    // ── D10: 概率负荷预测 (v2.11 新增) ──
    /// 分位数负荷预测（15 维，对应 15 分钟预测窗口）
    pub load_forecast_quantiles: Vec<f64>,
    /// 冲击负荷发生概率
    pub shock_load_probability: f64,
    /// 基础负荷（50% 分位数）
    pub base_load: f64,
}

impl Default for FusedSystemState {
    fn default() -> Self {
        Self {
            // ... 现有字段 ...

            // v2.11 新增字段
            load_forecast_quantiles: vec![],
            shock_load_probability: 0.0,
            base_load: 0.0,
        }
    }
}
```

### 3.3 接口定义

#### 3.3.1 LstmModel 接口

```rust
// 新增方法签名
pub async fn predict_quantiles(
    &self,
    input: &LstmInput,
    covariates: &LoadCovariates,
) -> Result<ProbabilisticLoadOutput, AiEngineError>;
```

#### 3.3.2 与 ModelManager 的集成

```rust
// model_manager.rs 新增方法

impl ModelManager {
    /// v2.11: 执行概率负荷预测
    pub async fn run_probabilistic_load_forecast(
        &self,
    ) -> Result<ProbabilisticLoadOutput, AiEngineError> {
        let lstm = self.lstm_model.read().await;
        let lstm = lstm.as_ref().ok_or(AiEngineError::ModelNotLoaded)?;

        let history = self.lstm_history.read().await;
        if history.len() < self.lstm_input_size {
            return Err(AiEngineError::InsufficientHistory);
        }

        // 构建负荷历史输入
        let load_history: Vec<f32> = history
            .iter()
            .rev()
            .take(self.lstm_input_size)
            .map(|&(_, load)| load as f32)
            .collect();
        let load_history: Vec<f32> = load_history.into_iter().rev().collect();

        let input = LstmInput {
            history: load_history,
            timestamp: chrono::Utc::now().timestamp(),
        };

        // 构建协变量
        let covariates = LoadCovariates {
            temperature: 25.0, // TODO: 从气象数据源获取
            date_type: 0,
            is_irrigation_season: false,
            hour: (chrono::Utc::now().timestamp() % 86400) as u8 / 3600,
        };

        lstm.predict_quantiles(&input, &covariates).await
    }
}
```

### 3.4 数据流设计

```
历史负荷数据
    ↓
LstmModel.predict_quantiles()
    ↓ (分位数预测)
ProbabilisticLoadOutput
    ↓ ┌─ RewardCalculator.calc_demand_with_uncertainty()
    │    └─ 需量控制奖励（含风险感知）
    └─ FusedSystemState
         └─ StrategyEngine
              └─ 冲击负荷预警触发
```

### 3.5 错误处理

| 错误场景 | 处理策略 |
|---------|---------|
| LSTM 模型不支持多输出 | 回退到确定性预测（50% 分位数）|
| 分位数预测值异常 | 校验范围 [0, max_capacity]，超范围裁剪 |
| 协变量缺失 | 使用默认值（温度=25°C，日期类型=工作日）|

### 3.6 测试策略

| 测试项 | 测试方法 | 验收标准 |
|--------|---------|---------|
| PLF-01 | 单元测试：分位数输出 | P10 < P50 < P90 |
| PLF-02 | 单元测试：冲击概率计算 | 已知分布验证概率值 |
| PLF-03 | 集成测试：预测延迟 | <= 1s |
| PLF-04 | 单元测试：风险奖励计算 | 风险惩罚正确应用 |
| PLF-07 | 集成测试：FusedSystemState 存储 | 字段正确填充 |

---

## 4. 配置扩展

### 4.1 YAML 配置

```yaml
# mupc/config/mupc_env_config.yaml 扩展

# AI引擎配置
ai_engine:
  # ... 现有配置 ...

  # v2.11 新增：自适应权重优化器
  adaptive_optimizer:
    enabled: true
    update_interval_hours: 168        # 周级更新
    meta_learning_rate: 0.001
    weight_bounds:
      min: 0.01
      max: 10.0
    constraints:
      sum_normalized: 8.3
      max_adjustment_per_update: 0.2

  pareto_optimizer:
    enabled: true
    population_size: 100
    generations: 50
    crossover_rate: 0.9
    mutation_rate: 0.1

  # v2.11 新增：LSTM 分位数预测
  lstm:
    # ... 现有配置 ...
    quantile_prediction:
      enabled: true
      quantiles: [0.1, 0.5, 0.9]
      shock_threshold_sigma: 2.0

load_forecast:
  update_interval_secs: 60
  history_window_secs: 3600
  output_horizon_secs: 900
  covariates:
    include_temperature: true
    include_date_type: true
    include_hour: true
```

---

## 5. 非功能性需求

### 5.1 性能指标

| 指标 | 要求 | 说明 |
|------|------|------|
| 权重优化推理延迟 | < 100ms | 元学习器单次推理 |
| 权重优化更新周期 | >= 1 小时 | 防止频繁更新 |
| 分位数预测延迟 | <= 1s | LSTM 分位数推理 |
| 冲击负荷概率计算延迟 | <= 10ms | 后处理计算 |

### 5.2 可靠性

| 指标 | 要求 |
|------|------|
| 权重优化失败处理 | 使用上一有效权重，回退告警 |
| 分位数预测失败处理 | 回退到确定性预测（50% 分位数）|
| 元学习器训练失败处理 | 保持当前权重，不更新 |

### 5.3 安全性

| 需求 | 说明 |
|------|------|
| 权重边界约束 | 所有权重必须 > 0.01 且 < 10.0 |
| 权重调整幅度限制 | 单次更新变化不超过 20% |
| 权重和归一化 | 季节性负荷权重和保持 8.3 基准 |
| 分位数合法性校验 | 分位数预测值必须在 [0, max_capacity] 范围内 |

### 5.4 PLF-06 精度验证机制

#### 5.4.1 离线评估框架

```rust
// ai-engine/src/精度评估框架

use crate::lstm_model::{LstmInput, ProbabilisticLoadOutput, QuantilePrediction};

/// 离线评估指标
#[derive(Debug, Clone)]
pub struct AccuracyMetrics {
    /// P90 分位数误差率
    pub p90_error_rate: f64,
    /// 平均绝对误差 (MAE)
    pub mae: f64,
    /// 均方根误差 (RMSE)
    pub rmse: f64,
}

/// 精度评估器（PLF-06 新增）
pub struct AccuracyEvaluator {
    /// 误差阈值（P90 分位数误差 < 15%）
    pub p90_error_threshold: f64,
}

impl AccuracyEvaluator {
    pub fn new() -> Self {
        Self {
            p90_error_threshold: 0.15,
        }
    }

    /// 评估分位数预测精度
    ///
    /// # 输入
    /// - predictions: 预测的分位数结果
    /// - actual: 实际观测值
    ///
    /// # 输出
    /// - 精度指标
    pub fn evaluate(
        &self,
        predictions: &[ProbabilisticLoadOutput],
        actual: &[f64],
    ) -> AccuracyMetrics {
        let n = predictions.len().min(actual.len());
        if n == 0 {
            return AccuracyMetrics {
                p90_error_rate: 1.0,
                mae: f64::INFINITY,
                rmse: f64::INFINITY,
            };
        }

        // 提取 P90 分位数预测值
        let p90_predictions: Vec<f64> = predictions
            .iter()
            .take(n)
            .map(|p| {
                p.quantiles
                    .iter()
                    .find(|q| (q.quantile - 0.9).abs() < 0.01)
                    .map(|q| q.value as f64)
                    .unwrap_or(p.base_load as f64)
            })
            .collect();

        // 计算误差
        let mut p90_errors: Vec<f64> = Vec::new();
        let mut abs_errors: Vec<f64> = Vec::new();

        for i in 0..n {
            let pred = p90_predictions[i];
            let act = actual[i];

            if act > 1e-6 {
                p90_errors.push(((pred - act) / act).abs());
            }
            abs_errors.push((pred - act).abs());
        }

        // P90 误差率
        let p90_error_rate = if !p90_errors.is_empty() {
            p90_errors.iter().sum::<f64>() / p90_errors.len() as f64
        } else {
            1.0
        };

        // MAE
        let mae = if !abs_errors.is_empty() {
            abs_errors.iter().sum::<f64>() / abs_errors.len() as f64
        } else {
            f64::INFINITY
        };

        // RMSE
        let rmse = if !abs_errors.is_empty() {
            let squared_sum: f64 = abs_errors.iter().map(|e| e * e).sum();
            (squared_sum / abs_errors.len() as f64).sqrt()
        } else {
            f64::INFINITY
        };

        AccuracyMetrics {
            p90_error_rate,
            mae,
            rmse,
        }
    }

    /// PLF-06 验证：检查 P90 分位数误差是否满足要求
    pub fn validate_p90_accuracy(&self, metrics: &AccuracyMetrics) -> bool {
        metrics.p90_error_rate < self.p90_error_threshold
    }
}

/// 精度日志记录器（PLF-06 新增）
pub struct AccuracyLogger {
    /// 日志记录
    log: Vec<AccuracyMetrics>,
}

impl AccuracyLogger {
    pub fn new() -> Self {
        Self { log: Vec::new() }
    }

    /// 记录评估结果
    pub fn record(&mut self, metrics: AccuracyMetrics) {
        self.log.push(metrics);
        // 保留最近 1000 条记录
        if self.log.len() > 1000 {
            self.log.remove(0);
        }
    }

    /// 获取最近 N 条记录的平均精度
    pub fn get_recent_average(&self, n: usize) -> Option<AccuracyMetrics> {
        let start = self.log.len().saturating_sub(n);
        let recent = &self.log[start..];

        if recent.is_empty() {
            return None;
        }

        let count = recent.len() as f64;
        Some(AccuracyMetrics {
            p90_error_rate: recent.iter().map(|m| m.p90_error_rate).sum::<f64>() / count,
            mae: recent.iter().map(|m| m.mae).sum::<f64>() / count,
            rmse: recent.iter().map(|m| m.rmse).sum::<f64>() / count,
        })
    }
}
```

#### 5.4.2 精度验证流程

```
离线评估管线
    ↓ (历史数据集)
AccuracyEvaluator.evaluate()
    ↓ (AccuracyMetrics)
AccuracyLogger.record()
    ↓
validate_p90_accuracy() → 精度告警
    ↓
ModelManager → 模型重训练触发
```

---

## 6. 交付物清单

| 交付物 | 文件路径 | 说明 |
|--------|----------|------|
| 自适应权重优化器 | `ai-engine/src/adaptive_weight_optimizer.rs` | MetaRL 权重优化器 |
| Pareto 优化器 | `ai-engine/src/pareto_optimizer.rs` | NSGA-II 多目标优化器 |
| 性能指标收集器 | `ai-engine/src/performance_collector.rs` | 性能数据收集 |
| 分位数预测扩展 | `ai-engine/src/lstm_model.rs` | LSTM 分位数预测 |
| 负荷协变量 | `ai-engine/src/load_covariates.rs` | 协变量结构体 |
| 配置扩展 | `ai-engine/src/config.rs` | 新增配置结构 |
| 奖励函数扩展 | `ai-engine/src/reward_calculator.rs` | 概率奖励计算 |
| 数据融合扩展 | `ai-engine/src/data_fusion.rs` | FusedSystemState 扩展 |
| YAML 配置更新 | `mupc/config/mupc_env_config.yaml` | 新增 v2.11 配置项 |

---

## 7. 与其他模块的接口

### 7.1 与 strategy-engine 的接口

| 接口 | 方向 | 说明 |
|------|------|------|
| 权重更新 | AI引擎 → strategy-engine | SceneWeights 更新后通知策略引擎重载 |
| 分位数预测 | AI引擎 → strategy-engine | 提供冲击负荷概率用于需量控制决策 |

### 7.2 与 storage 的接口

| 接口 | 方向 | 说明 |
|------|------|------|
| 性能历史数据 | storage → AI引擎 | 查询历史性能用于元学习器训练 |
| 权重调整日志 | AI引擎 → storage | 记录权重调整历史 |

---

## 8. 验收标准汇总

### 8.1 需求1（自适应权重优化器）

| ID | 标准 | 优先级 | 验证方法 |
|----|------|--------|----------|
| AWO-01 | 自适应权重优化器可正确加载配置 | P0 | 单元测试 |
| AWO-02 | 元学习器可基于历史性能数据输出权重调整 | P1 | 离线仿真验证 |
| AWO-03 | NSGA-II 可搜索 Pareto 前沿并输出多组权重候选 | P1 | 离线仿真验证 |
| AWO-04 | 权重调整受物理约束约束（正数、比例）| P0 | 单元测试 |
| AWO-05 | 单次更新周期内权重变化不超过 max_adjustment_per_update | P0 | 单元测试 |
| AWO-06 | 优化后的奖励函数与原始策略无显著偏离（偏移 < 5%）| P1 | 集成测试 |
| AWO-07 | 权重更新后 RL 模型可正常推理（推理延迟 < 1s）| P0 | 集成测试 |

### 8.2 需求2（冲击负荷概率预测）

| ID | 标准 | 优先级 | 验证方法 |
|----|------|--------|----------|
| PLF-01 | LSTM 可输出多分位数预测（10%、50%、90%）| P0 | 单元测试 |
| PLF-02 | 冲击负荷概率计算正确（P90 - P50 差值法）| P0 | 单元测试 |
| PLF-03 | 分位数预测延迟 <= 1s | P0 | 集成测试 |
| PLF-04 | 需量控制奖励函数考虑冲击负荷概率 | P1 | 单元测试 |
| PLF-05 | 协变量（温度、日期类型）正确传入 | P1 | 集成测试 |
| PLF-06 | 概率预测在测试集上 P90 分位数误差 < 15% | P1 | 离线评估 |
| PLF-07 | FusedSystemState 正确存储分位数预测结果 | P0 | 集成测试 |

---

## 附录 A：修订记录

| 版本 | 日期 | 修订项 |
|------|------|--------|
| v2.11 | 2026-06-14 | 初稿 |
| v2.11 | 2026-06-14 | 二次评审通过：5项问题已全部修复（AWO-06奖励偏移检测、PLF-05协变量数据源、NSGA-II标准实现、PRD附录C澄清问题回应、PLF-06精度验证机制），14项验收标准均有设计覆盖，未引入新问题 |

---

## 附录 B：术语表

| 术语 | 说明 |
|------|------|
| MetaRL | 元强化学习，一种学习如何学习的方法 |
| NSGA-II | 非支配排序遗传算法 II，多目标优化算法 |
| Pareto 前沿 | 多目标优化中，所有非劣解组成的集合 |
| 分位数预测 | 预测不同分位数的目标值，反映不确定性分布 |
| 冲击负荷 | 具有突发性、随机性的短时高功率负荷（如灌溉水泵）|

---

## 附录 C：PRD 附录C 澄清问题回应

> **状态**: 已回应
> **日期**: 2026-06-14

本文档回应 PRD 中附录 C 列出的待澄清问题（序号 1-4）。如需查看原始 PRD 附录 C 内容，请参考 `docs/superpowers/specs/modules/05-MUPC-AI引擎-PRD.md` 附录 C。

### 澄清问题回应

| 序号 | 问题描述 | 状态 | 回应说明 |
|------|---------|------|---------|
| 1 | 元学习器训练数据来源及更新频率 | **已解决** | 训练数据来源于 PerformanceCollector 收集的历史性能数据（包含光伏消纳率、电池循环次数、变压器负载率、需量超标次数、累积奖励），更新频率由 `update_interval_hours` 配置（默认 168 小时，周级更新） |
| 2 | NSGA-II 与现有强化学习流程的集成方式 | **已解决** | NSGA-II ParetoWeightOptimizer 作为 AdaptiveWeightOptimizer 的子组件，用于多目标优化权重候选解。集成方式：元学习器预测调整方向 → Pareto 优化器生成多组权重候选 → WeightBoundsEnforcer 约束剪裁 → 输出最优 SceneWeights |
| 3 | 冲击负荷概率计算的假设前提 | **已解决** | 假设：(1) 负荷服从正态分布；(2) P90-P50 差值反映不确定性，spread ≈ 1.28σ；(3) 冲击阈值为 base_load + 2σ；(4) 使用误差函数 erfc 近似正态分布 CDF |
| 4 | 离线评估数据集的来源及管理方式 | **已解决** | 离线评估数据集来源：(1) storage 中存储的历史负荷数据；(2) 分位数预测结果与实际观测值对比。管理方式：通过 AccuracyLogger 记录最近 1000 条评估结果，支持按时间窗口查询平均精度 |
