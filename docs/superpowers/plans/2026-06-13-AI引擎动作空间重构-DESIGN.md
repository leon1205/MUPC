# AI引擎动作空间重构 技术设计文档

[DESIGN_APPROVED]

| 版本 | 日期 | 作者 | 状态 |
|------|------|------|------|
| v1.0 | 2026-06-13 | 架构师 | 初版 |
| v1.1 | 2026-06-13 | 修订 TRAIN ID 避免与 PRD 重复 |

---

## 1. 技术选型

### 1.1 技术栈

| 组件 | 选择 | 说明 |
|------|------|------|
| 动作空间模式 | 双参数（P_ref + k_droop） | 替代单参数 p_batt_set |
| 下垂公式 | `P_output = P_ref + k_droop × ΔV` | 执行器端闭环计算 |
| TCP 帧版本 | v2.0 | 新协议格式，不兼容 v1.x |
| k_droop 来源 | intercore 从实时控制模块获取 | 通过 TCP 帧传递 |
| 通信中断处理 | 保持最后有效值 | 执行器维持最后 P_ref 和 k_droop |
| 模型输出维度 | 2 维 | [P_ref, k_droop] |

### 1.2 设计原则

- **KISS 原则**：选择最简单可靠的实现方案，不引入额外复杂性
- **向后兼容**：通过 `action_space.mode` 配置项切换 "dual"（新）和 "legacy"（旧）模式
- **本质安全**：通信中断时执行器保持最后有效值，不主动归零

---

## 2. 模块划分与职责

### 2.1 模块列表

| 模块 | 文件路径 | 职责 |
|------|----------|------|
| **ai-engine** | `crates/ai-engine/src/rl_model.rs` | ActionOutput 双参数结构体定义 |
| **ai-engine** | `crates/ai-engine/src/action_validator.rs` | 双参数校验逻辑 |
| **ai-engine** | `crates/ai-engine/src/action_space.rs` | ActionSpaceConfig 扩展 k_droop 范围 |
| **ai-engine** | `crates/ai-engine/src/model_manager.rs` | 决策输出适配双参数 |
| **ai-engine** | `crates/ai-engine/src/model_registry.rs` | 模型加载验证（维度检查） |
| **intercore** | `crates/intercore/src/protocol.rs` | TCP 帧 v2.0 格式定义 |
| **intercore** | `crates/intercore/src/tcp_server.rs` | ControlCmdPayload 扩展 |
| **strategy-engine** | `crates/strategy-engine/src/ai_integration.rs` | 双参数转发与降级逻辑 |

### 2.2 职责边界

| 模块 | 职责 | 边界 |
|------|------|------|
| ai-engine | 输出 P_ref 和 k_droop 双参数 | 不负责执行器端下垂计算 |
| action_validator | 校验 P_ref 和 k_droop 在各自约束范围内 | 不修改 k_droop |
| intercore | TCP 帧携带双参数并转发给实时控制模块 | 不修改 k_droop |
| strategy-engine | 接收双参数并转发给 intercore，处理降级 | 通信中断时提供兜底值 |

---

## 3. 核心数据结构调整

### 3.1 ActionOutput 结构体

**文件**: `crates/ai-engine/src/rl_model.rs`

```rust
/// 动作输出结构体（v2.0 双参数模式）
#[derive(Debug, Clone)]
pub struct ActionOutput {
    /// 有功功率基准点 (kW), 范围由 ActionSpaceConfig 确定
    /// 负值=充电，正值=放电
    pub p_ref: f64,
    /// 电压-有功下垂系数 (kW/V), 范围由实时控制模块提供
    /// 电压每升高 1V，输出功率增加 k_droop kW
    pub k_droop: f64,
    /// 可中断负荷切除量 (kW), [0.0, max_load_shedding]
    pub load_shedding: f64,
    /// 光伏限功率比例, [0.0, 1.0]
    pub pv_limit: f64,
    /// 决策置信度 (0.0 ~ 1.0)
    pub confidence: f64,
}

/// 动作输出结构体（v1.x 单参数模式，legacy）
/// 仅用于兼容旧模式，正常情况下不使用
#[derive(Debug, Clone)]
pub struct ActionOutputLegacy {
    pub p_batt_set: f64,      // 废弃
    pub load_shedding: f64,   // 保留
    pub pv_limit: f64,        // 保留
    pub confidence: f64,
}
```

### 3.2 ActionSpaceConfig 扩展

**文件**: `crates/ai-engine/src/action_space.rs`

```rust
/// 动作空间配置（可配置化）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ActionSpaceConfig {
    // ... 现有字段 ...

    // === v2.0 新增字段 ===
    /// 下垂系数下限 (kW/V)，由实时控制模块提供
    #[serde(default)]
    pub k_droop_min: f64,
    /// 下垂系数上限 (kW/V)，由实时控制模块提供
    #[serde(default)]
    pub k_droop_max: f64,
    /// 动作空间模式: "dual"（双参数）或 "legacy"（单参数）
    #[serde(default = "default_action_space_mode")]
    pub mode: String,
}

fn default_action_space_mode() -> String {
    "dual".to_string()
}
```

### 3.3 TCP 帧 v2.0 格式

**文件**: `crates/intercore/src/protocol.rs`

```rust
/// 核间通信帧 v2.0
///
/// 帧格式：0xAA 0x55 + 长度(2字节) + 类型(2字节) + 序号(2字节) + 数据(N字节) + CRC16(2字节)
/// 总长度：固定 64 字节（不足部分用 padding 填充）
///
/// v2.0 变更：
/// - ControlCmd 数据区携带 p_ref 和 k_droop 双参数
/// - FrameType 保持不变，通过数据区内容区分版本

/// 控制指令载荷 v2.0（JSON 格式）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ControlCmdPayloadV2 {
    #[serde(rename = "p_ref")]
    pub p_ref: Option<f64>,
    #[serde(rename = "k_droop")]
    pub k_droop: Option<f64>,
    #[serde(rename = "load_shedding")]
    pub load_shedding: Option<f64>,
    #[serde(rename = "pv_limit")]
    pub pv_limit: Option<f64>,
    #[serde(rename = "ai_ready")]
    pub ai_ready: Option<bool>,
    #[serde(rename = "strategy_mode")]
    pub strategy_mode: Option<String>,
    #[serde(rename = "timestamp_ms")]
    pub timestamp_ms: Option<u64>,
    /// 帧版本号，用于区分 v1.x 和 v2.0
    #[serde(rename = "frame_version")]
    pub frame_version: Option<u8>,
}

impl ControlCmdPayloadV2 {
    pub const FRAME_VERSION: u8 = 2;

    pub fn new() -> Self {
        Self {
            p_ref: None,
            k_droop: None,
            load_shedding: None,
            pv_limit: None,
            ai_ready: None,
            strategy_mode: None,
            timestamp_ms: None,
            frame_version: Some(Self::FRAME_VERSION),
        }
    }
}
```

### 3.4 帧版本协商

| 场景 | 检测方法 | 处理 |
|------|----------|------|
| 收到 v1.x 帧 | `frame_version` 字段缺失或为 1 | 拒绝处理，记录 ERROR |
| 收到 v2.0 帧 | `frame_version` = 2 | 正常解析双参数 |
| 版本不匹配 | 发送端与接收端版本不一致 | 记录 WARN，等待升级 |

---

## 4. 模块详细设计

### 4.1 ai-engine::rl_model（动作输出解析）

**文件**: `crates/ai-engine/src/rl_model.rs`

#### 4.1.1 parse_action_output 函数变更

```rust
/// 解析 RL 模型原始输出为 ActionOutput（双参数模式）
///
/// 输出格式: [p_ref, k_droop, load_shedding, pv_limit, confidence]
/// - p_ref: 有功基准点，范围 [-max_batt_discharge_power, max_batt_charge_power]
/// - k_droop: 下垂系数，范围 [k_droop_min, k_droop_max]
/// - load_shedding: 保留
/// - pv_limit: 保留
/// - confidence: 决策置信度
pub fn parse_action_output(raw: &[f32], config: &ActionSpaceConfig) -> Option<ActionOutput> {
    if raw.len() < 5 {
        return None;
    }

    // 获取 k_droop 范围（默认值为安全边界）
    let k_min = config.k_droop_min.unwrap_or(-100.0);
    let k_max = config.k_droop_max.unwrap_or(100.0);

    let mut action = ActionOutput {
        p_ref: (raw[0] as f64).clamp(
            -config.max_batt_discharge_power,
            config.max_batt_charge_power,
        ),
        k_droop: (raw[1] as f64).clamp(k_min, k_max),
        load_shedding: (raw[2] as f64).clamp(0.0, config.max_load_shedding),
        pv_limit: (raw[3] as f64).clamp(0.0, 1.0),
        confidence: raw.get(4).copied().unwrap_or(0.5) as f64,
    };

    action.confidence = action.confidence.clamp(0.0, 1.0);
    Some(action)
}

/// 解析 RL 模型原始输出为 ActionOutputLegacy（单参数模式，legacy）
///
/// 输出格式: [p_batt_set, load_shedding, pv_limit, confidence]
pub fn parse_action_output_legacy(raw: &[f32], config: &ActionSpaceConfig) -> Option<ActionOutputLegacy> {
    if raw.len() < 4 {
        return None;
    }

    Some(ActionOutputLegacy {
        p_batt_set: (raw[0] as f64).clamp(
            -config.max_batt_discharge_power,
            config.max_batt_charge_power,
        ),
        load_shedding: (raw[1] as f64).clamp(0.0, config.max_load_shedding),
        pv_limit: (raw[2] as f64).clamp(0.0, 1.0),
        confidence: raw.get(3).copied().unwrap_or(0.5) as f64,
    })
}
```

#### 4.1.2 ONNX 模型输出维度验证

```rust
impl RLModel {
    /// 验证模型输出维度（v2.0 要求 5 维）
    fn validate_output_dimensions(output: &[f32]) -> Result<(), AiEngineError> {
        if output.len() < 5 {
            return Err(AiEngineError::InferenceFailed(
                format!("模型输出维度不足: 期望 >= 5, 实际 {}", output.len())
            ));
        }
        Ok(())
    }
}
```

### 4.2 ai-engine::action_validator（约束校验）

**文件**: `crates/ai-engine/src/action_validator.rs`

#### 4.2.1 校验规则 ACT-DUAL-01 ~ ACT-DUAL-04

```rust
/// 动作约束校验器 v2.0（双参数模式）
pub struct ActionValidator {
    config: ActionConstraintConfig,
    last_action: RwLock<Option<ActionOutput>>,
    /// k_droop 范围（由实时控制模块通过 intercore 提供）
    droop_range: RwLock<(f64, f64)>,
    /// 动作空间模式
    mode: ActionSpaceMode,
}

enum ActionSpaceMode {
    Dual,    // 双参数模式
    Legacy,  // 单参数模式（兼容）
}

/// ACT-DUAL-01: p_ref ∈ [-max_batt_discharge_power, max_batt_charge_power]
/// ACT-DUAL-02: k_droop ∈ [k_droop_min, k_droop_max]
/// ACT-DUAL-03: Δp_ref 变化率 <= p_batt_ramp_limit_kw / 步
/// ACT-DUAL-04: 当 dispatch_p_set 有效时，p_ref 绝对值不得超过 dispatch_p_set 绝对值
pub fn validate(
    &self,
    action: &ActionOutput,
    dispatch_p_set: Option<f64>,
    is_anti_reverse: bool,
    action_space_config: &ActionSpaceConfig,
) -> (ActionOutput, Vec<ViolationRecord>) {
    let mut validated = action.clone();
    let mut violations = Vec::new();
    let last = self.last_action.read().unwrap();

    // ACT-DUAL-01: p_ref 值域约束
    let p_ref_min = -action_space_config.max_batt_discharge_power;
    let p_ref_max = action_space_config.max_batt_charge_power;
    if validated.p_ref < p_ref_min {
        violations.push(ViolationRecord {
            rule: "ACT-DUAL-01",
            field: "p_ref",
            original: action.p_ref,
            clamped: p_ref_min,
        });
        validated.p_ref = p_ref_min;
    } else if validated.p_ref > p_ref_max {
        violations.push(ViolationRecord {
            rule: "ACT-DUAL-01",
            field: "p_ref",
            original: action.p_ref,
            clamped: p_ref_max,
        });
        validated.p_ref = p_ref_max;
    }

    // ACT-DUAL-02: k_droop 值域约束
    let (k_min, k_max) = *self.droop_range.read().unwrap();
    if validated.k_droop < k_min {
        violations.push(ViolationRecord {
            rule: "ACT-DUAL-02",
            field: "k_droop",
            original: action.k_droop,
            clamped: k_min,
        });
        validated.k_droop = k_min;
    } else if validated.k_droop > k_max {
        violations.push(ViolationRecord {
            rule: "ACT-DUAL-02",
            field: "k_droop",
            original: action.k_droop,
            clamped: k_max,
        });
        validated.k_droop = k_max;
    }

    // ACT-DUAL-03: p_ref 变化率约束
    if let Some(ref prev) = *last {
        let delta = (action.p_ref - prev.p_ref).abs();
        if delta > self.config.p_batt_ramp_limit_kw {
            let sign = if action.p_ref > prev.p_ref { 1.0 } else { -1.0 };
            validated.p_ref = prev.p_ref + sign * self.config.p_batt_ramp_limit_kw;
            violations.push(ViolationRecord {
                rule: "ACT-DUAL-03",
                field: "p_ref",
                original: action.p_ref,
                clamped: validated.p_ref,
            });
        }
    }

    // ACT-DUAL-04: 调度指令权限约束
    if let Some(dp) = dispatch_p_set {
        if validated.p_ref.abs() > dp.abs() {
            let sign = validated.p_ref.signum();
            validated.p_ref = sign * dp.abs();
            violations.push(ViolationRecord {
                rule: "ACT-DUAL-04",
                field: "p_ref",
                original: action.p_ref,
                clamped: validated.p_ref,
            });
        }
    }

    // load_shedding 和 pv_limit 保留，校验方式不变
    validated.load_shedding = validated
        .load_shedding
        .clamp(0.0, action_space_config.max_load_shedding);
    validated.pv_limit = validated.pv_limit.clamp(0.0, 1.0);
    validated.confidence = validated.confidence.clamp(0.0, 1.0);

    *self.last_action.write().unwrap() = Some(validated.clone());
    (validated, violations)
}
```

#### 4.2.2 k_droop 范围更新接口

```rust
impl ActionValidator {
    /// 更新 k_droop 范围（由 intercore 从实时控制模块获取后调用）
    pub fn update_droop_range(&self, k_min: f64, k_max: f64) {
        let mut range = self.droop_range.write().unwrap();
        *range = (k_min, k_max);
        tracing::debug!("Updated k_droop range: [{}, {}]", k_min, k_max);
    }

    /// 获取当前 k_droop 范围
    pub fn get_droop_range(&self) -> (f64, f64) {
        *self.droop_range.read().unwrap()
    }
}
```

### 4.3 intercore::protocol（TCP 帧扩展）

**文件**: `crates/intercore/src/protocol.rs`

#### 4.3.1 帧格式定义

```
v2.0 帧格式（64 字节固定）：

 Byte 0-1:   Magic (0xAA 0x55)
 Byte 2-3:   Length (包含 header + data + crc16)
 Byte 4-5:   Frame Type (0x0010 = ControlCmd)
 Byte 6-7:   Sequence Number
 Byte 8-15:  数据区 (JSON, 可变长度)
             - p_ref: f64 (8 bytes)
             - k_droop: f64 (8 bytes)
             - load_shedding: f64 (8 bytes)
             - pv_limit: f64 (8 bytes)
             - ai_ready: bool (1 byte)
             - strategy_mode: string (var)
             - timestamp_ms: u64 (8 bytes)
             - frame_version: u8 (1 byte)
 Byte N-1:   CRC16 (2 bytes)
 Byte 62-63: Padding (0x00)
```

#### 4.3.2 ControlCmdPayloadV2 结构体

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ControlCmdPayloadV2 {
    #[serde(rename = "p_ref")]
    pub p_ref: Option<f64>,
    #[serde(rename = "k_droop")]
    pub k_droop: Option<f64>,
    #[serde(rename = "load_shedding")]
    pub load_shedding: Option<f64>,
    #[serde(rename = "pv_limit")]
    pub pv_limit: Option<f64>,
    #[serde(rename = "ai_ready")]
    pub ai_ready: Option<bool>,
    #[serde(rename = "strategy_mode")]
    pub strategy_mode: Option<String>,
    #[serde(rename = "timestamp_ms")]
    pub timestamp_ms: Option<u64>,
    #[serde(rename = "frame_version")]
    pub frame_version: Option<u8>,
}

impl ControlCmdPayloadV2 {
    pub const FRAME_VERSION: u8 = 2;

    pub fn from_action_output(action: &ActionOutput, ai_ready: bool, strategy_mode: &str) -> Self {
        Self {
            p_ref: Some(action.p_ref),
            k_droop: Some(action.k_droop),
            load_shedding: Some(action.load_shedding),
            pv_limit: Some(action.pv_limit),
            ai_ready: Some(ai_ready),
            strategy_mode: Some(strategy_mode.to_string()),
            timestamp_ms: Some(chrono::Utc::now().timestamp_millis() as u64),
            frame_version: Some(Self::FRAME_VERSION),
        }
    }

    /// 检测帧版本
    pub fn detect_version(data: &[u8]) -> Result<u8, MupcError> {
        match Self::from_json(data) {
            Ok(payload) => Ok(payload.frame_version.unwrap_or(1)),
            Err(_) => Ok(1), // 解析失败假设为 v1.x
        }
    }
}
```

### 4.4 intercore::tcp_server（连接处理）

**文件**: `crates/intercore/src/tcp_server.rs`

#### 4.4.1 双参数下发处理

```rust
impl IntercoreServer {
    /// 处理 ControlCmd（支持 v2.0 双参数）
    async fn handle_control_cmd(
        payload: &ControlCmdPayloadV2,
        addr: SocketAddr,
    ) -> Result<(), MupcError> {
        let version = payload.frame_version.unwrap_or(1);

        match version {
            2 => {
                tracing::debug!(
                    "ControlCmd v2.0: p_ref={:?}, k_droop={:?}, load_shedding={:?}, pv_limit={:?}",
                    payload.p_ref,
                    payload.k_droop,
                    payload.load_shedding,
                    payload.pv_limit,
                );
            }
            _ => {
                tracing::warn!(
                    "Unsupported frame version {} from {}, expected 2",
                    version,
                    addr
                );
                return Err(MupcError::new(
                    ErrorCode::FrameParseError,
                    format!("Unsupported frame version: {}", version),
                    "intercore",
                ));
            }
        }

        Ok(())
    }
}
```

#### 4.4.2 通信中断状态管理

```rust
/// 核间通信状态（用于通信中断检测）
#[derive(Debug, Clone)]
pub struct IntercoreConnectionState {
    /// 最后收到的有效 p_ref
    pub last_valid_p_ref: RwLock<Option<f64>>,
    /// 最后收到的有效 k_droop
    pub last_valid_k_droop: RwLock<Option<f64>>,
    /// 最后心跳时间戳
    pub last_heartbeat_ms: RwLock<u64>,
    /// 连接状态
    pub connected: RwLock<bool>,
}

impl IntercoreConnectionState {
    pub fn new() -> Self {
        Self {
            last_valid_p_ref: RwLock::new(None),
            last_valid_k_droop: RwLock::new(None),
            last_heartbeat_ms: RwLock::new(0),
            connected: RwLock::new(false),
        }
    }

    /// 更新收到的双参数
    pub fn update_valid_params(&self, p_ref: f64, k_droop: f64) {
        *self.last_valid_p_ref.write().unwrap() = Some(p_ref);
        *self.last_valid_k_droop.write().unwrap() = Some(k_droop);
    }

    /// 获取最后有效的双参数（通信中断时使用）
    pub fn get_last_valid_params(&self) -> (Option<f64>, Option<f64>) {
        let p_ref = *self.last_valid_p_ref.read().unwrap();
        let k_droop = *self.last_valid_k_droop.read().unwrap();
        (p_ref, k_droop)
    }

    /// 检查是否已收到有效参数
    pub fn has_valid_params(&self) -> bool {
        self.last_valid_p_ref.read().unwrap().is_some()
            && self.last_valid_k_droop.read().unwrap().is_some()
    }
}
```

### 4.5 strategy-engine::ai_integration（降级逻辑）

**文件**: `crates/strategy-engine/src/ai_integration.rs`

#### 4.5.1 降级决策逻辑

```rust
/// AI 集成器（处理双参数与降级）
pub struct AiIntegrator {
    // ... 现有字段 ...
    /// 通信中断检测器
    intercore_state: Arc<IntercoreConnectionState>,
    /// 降级状态
    fallback_active: RwLock<bool>,
}

impl AiIntegrator {
    /// 获取当前动作输出（支持降级）
    pub async fn get_action_output(&self) -> Result<ActionOutput, StrategyError> {
        let action = self.model_manager.full_decision_cycle().await?;

        // 检测通信中断
        if !self.is_intercore_connected().await {
            tracing::warn!("Intercore connection lost, activating fallback mode");
            *self.fallback_active.write().await = true;
            return self.get_fallback_action().await;
        }

        // 通信正常，更新最后有效参数
        self.intercore_state
            .update_valid_params(action.p_ref, action.k_droop);

        *self.fallback_active.write().await = false;
        Ok(action)
    }

    /// 获取降级动作（保持最后有效值）
    async fn get_fallback_action(&self) -> Result<ActionOutput, StrategyError> {
        let (p_ref, k_droop) = self.intercore_state.get_last_valid_params();

        match (p_ref, k_droop) {
            (Some(p), Some(k)) => {
                tracing::info!(
                    "Fallback: using last valid p_ref={}, k_droop={}",
                    p,
                    k
                );
                Ok(ActionOutput {
                    p_ref: p,
                    k_droop: k,
                    load_shedding: 0.0, // 降级时不修改负荷
                    pv_limit: 1.0,      // 降级时不限制光伏
                    confidence: 0.0,    // 降级模式置信度为 0
                })
            }
            _ => {
                // 无有效参数，使用安全默认值
                tracing::warn!("No valid params available, using safe defaults");
                Ok(ActionOutput {
                    p_ref: 0.0,
                    k_droop: 10.0, // 默认下垂系数
                    load_shedding: 0.0,
                    pv_limit: 1.0,
                    confidence: 0.0,
                })
            }
        }
    }

    /// 检查 intercore 连接状态
    async fn is_intercore_connected(&self) -> bool {
        *self.intercore_state.connected.read().unwrap()
    }
}
```

---

## 5. 数据库变更

### 5.1 变更说明

**本次重构不涉及数据库结构变更。**

理由：
- 双参数 P_ref 和 k_droop 通过 TCP 帧实时传输，不持久化到数据库
- k_droop 范围由实时控制模块通过 intercore 动态提供，不存储在数据库
- 决策记录（storage）中的 action 字段可扩展支持双参数 JSON

### 5.2 可选扩展（如需持久化）

如需在数据库中持久化双参数决策记录，可按以下方式扩展：

```sql
-- 决策记录表扩展（可选）
ALTER TABLE decision_records ADD COLUMN p_ref REAL;
ALTER TABLE decision_records ADD COLUMN k_droop REAL;
```

---

## 6. 训练管线对齐方案

### 6.1 ONNX 模型输出维度变更

| 版本 | 模型输出维度 | 输出含义 |
|------|-------------|----------|
| v1.x | 1 维 | p_batt_set（有功功率绝对值） |
| v2.0 | 5 维 | [P_ref, k_droop, load_shedding, pv_limit, confidence] |

### 6.2 训练环境改造

#### 6.2.1 下垂公式集成

```python
# 训练环境执行器模型（Python pseudocode）
class DroopControlEnv:
    def step(self, action):
        p_ref, k_droop, load_shedding, pv_limit, confidence = action
        delta_v = self.get_voltage_deviation()  # 获取当前电压偏差
        p_output = p_ref + k_droop * delta_v     # 下垂公式
        return self.execute(p_output)           # 执行并返回下一个状态
```

#### 6.2.2 奖励函数适配

| 场景 | 原表达式 | 新表达式 |
|------|----------|----------|
| SCENE-01 变化率惩罚 | λ × |ΔP_batt| / E_battery | λ × |ΔP_ref| / E_battery |
| SCENE-B1 电价差收益 | P_batt_set × Δt × (price_sell - price_buy) | P_ref × Δt × (price_sell - price_buy) |

### 6.3 模型版本管理

| 版本 | 文件名 | 说明 |
|------|--------|------|
| v1.x | `maddpg_v1.onnx` / `maddpg_v1.rknn` | 单参数输出（p_batt_set） |
| v2.0 | `maddpg_v2.onnx` / `maddpg_v2.rknn` | 双参数输出（P_ref + k_droop） |

#### 6.3.1 版本校验逻辑

```rust
/// 模型加载时校验输出维度
fn validate_model_version(runtime: &RknnRuntime) -> Result<(), AiEngineError> {
    let output = runtime.get_output_shape()?;

    if output.len() == 1 {
        return Err(AiEngineError::ModelLoadFailed(
            "v1.x 模型不兼容双参数模式，请升级至 v2.0".into(),
        ));
    }

    if output.len() < 5 {
        return Err(AiEngineError::ModelLoadFailed(
            format!("模型输出维度不足: 期望 >= 5, 实际 {}", output.len()),
        ));
    }

    Ok(())
}
```

---

## 7. 异常处理设计

### 7.1 异常场景与处理措施

| 异常场景 | 检测条件 | 处理措施 |
|----------|----------|----------|
| AI 推理失败 | 心跳超时或状态码异常 | 自动降级至本地策略引擎，`p_ref` 使用兜底值 |
| 通信中断 | intercore TCP 断开检测 | 执行器保持最后收到的 `P_ref` 和 `k_droop` |
| k_droop 超范围 | k_droop < k_min 或 k_droop > k_max | clamp 至 [k_min, k_max]，记录 WARN 日志 |
| p_ref 超范围 | p_ref < -P_safe 或 p_ref > P_safe | clamp 至 [-P_safe, +P_safe]，记录 WARN 日志 |
| 双参数缺失 | P_ref 和 k_droop 同时为 NaN | 触发 AI 降级，使用兜底策略 |
| 实时控制模块故障 | 执行器心跳丢失 | 触发系统告警，等待人工干预 |
| v1.x 模型加载 | 输出维度为 1 | 拒绝加载，记录 ERROR 日志 |

### 7.2 通信中断降级流程

```
通信中断检测（intercore TCP 断开）
    ↓
执行器收到通知（或检测到心跳超时）
    ↓
执行器保持最后收到的有效 P_ref 和 k_droop
    ↓
P_ref = 最后有效值（不主动归零）
    ↓
k_droop = 最后有效值（不主动归零）
    ↓
P_output = 最后 P_ref + 最后 k_droop × ΔV（保持控制能力）
    ↓
系统发出告警（AI通信中断，维持最后有效下垂控制）
    ↓
通信恢复后，自动切回 AI 双参数控制模式
```

### 7.3 兜底策略默认值

| 参数 | 兜底默认值 | 说明 |
|------|-----------|------|
| p_ref | 最后有效值（不主动归零） | 保持设备当前运行状态 |
| k_droop | 最后有效值（不主动归零） | 保持下垂控制能力 |
| load_shedding | 0.0 | 降级时不切除负荷 |
| pv_limit | 1.0 | 降级时不限制光伏 |

---

## 8. 配置变更

### 8.1 action_space.mode 配置

```yaml
# mupc/config/mupc_env_config.yaml
action_space:
  mode: "dual"    # "dual"=双参数模式, "legacy"=单参数模式
  k_droop_min: -100.0  # 下垂系数下限（默认，可被 intercore 覆盖）
  k_droop_max: 100.0   # 下垂系数上限（默认，可被 intercore 覆盖）
```

### 8.2 帧版本配置

```yaml
# intercore 配置
intercore:
  frame_version: 2  # v2.0 强制要求
```

---

## 9. 验收标准对照

### 9.1 核心功能验收

| ID | 标准 | 验证方法 | 优先级 |
|----|------|----------|--------|
| DUAL-01 | RL 模型输出包含 P_ref 和 k_droop 两个参数 | 单元测试 | P0 |
| DUAL-02 | P_ref 取值范围符合 ActionSpaceConfig 约束 | 单元测试（边界值验证）| P0 |
| DUAL-03 | k_droop 取值范围符合实时控制模块提供的 [k_min, k_max] | 集成测试 | P0 |
| DUAL-04 | 双参数通过 intercore 同时下发（TCP 帧 v2.0）| 集成测试（抓包验证）| P0 |
| DUAL-05 | 下垂公式 P = P_ref + k_droop × ΔV 在执行器端正确执行 | 集成测试（模拟 ΔV）| P0 |
| DUAL-06 | k_droop 管理权归 AI 引擎，实时控制模块不得修改 | 代码审查 | P0 |
| DUAL-07 | 通信中断时执行器保持最后有效的 P_ref 和 k_droop | 集成测试（断联测试）| P0 |
| DUAL-08 | P_ref 和 k_droop 任一超范围时自动 clamp 并记录 WARN 日志 | 集成测试 | P0 |

### 9.2 配置管理验收

| ID | 标准 | 验证方法 | 优先级 |
|----|------|----------|--------|
| CFG-01 | `action_space.mode` 可配置 "dual" / "legacy" | 配置测试 | P1 |
| CFG-02 | 双参数模式下 `load_shedding` 和 `pv_limit` 保留 | 单元测试 | P1 |
| CFG-03 | k_droop 范围可由 intercore 动态更新 | 配置测试 | P1 |

### 9.3 训练管线验收

| ID | 标准 | 验证方法 | 优先级 |
|----|------|----------|--------|
| TRAIN-EX-01 | ONNX 模型输出维度为 5 | 模型验证 | P0 |
| TRAIN-EX-02 | 训练环境正确实现下垂公式 | 单元测试 | P0 |
| TRAIN-EX-03 | v2.0 模型加载成功，v1.x 模型加载拒绝 | 集成测试 | P0 |

### 9.4 降级机制验收

| ID | 标准 | 验证方法 | 优先级 |
|----|------|----------|--------|
| FALLBACK-01 | AI 推理失败时自动降级至本地策略 | 集成测试（模拟推理失败）| P0 |
| FALLBACK-02 | 通信中断时执行器保持最后有效的 P_ref 和 k_droop | 集成测试（断联测试）| P0 |
| FALLBACK-03 | 通信恢复后自动切回 AI 双参数控制 | 集成测试 | P0 |

### 9.5 性能验收

| ID | 标准 | 验证方法 | 优先级 |
|----|------|----------|--------|
| PERF-01 | 双参数推理延迟 < 1s | 性能测试 | P0 |
| PERF-02 | intercore 双参数下发延迟 < 5ms | 性能测试 | P1 |

---

## 10. 实施计划

### 10.1 变更优先级

| 优先级 | 变更项 | 说明 |
|--------|--------|------|
| P0 | ActionOutput 双参数结构体 | 核心数据模型变更 |
| P0 | ActionValidator 双参数校验 | 校验规则 ACT-DUAL-01~04 |
| P0 | TCP 帧 v2.0 格式 | 双参数传输通道 |
| P1 | k_droop 范围动态更新 | intercore 到 ai-engine 的反向通知 |
| P1 | 通信中断降级逻辑 | 本质安全保障 |
| P2 | 配置项扩展 | action_space.mode 等 |
| P2 | 模型版本校验 | v1.x 模型拒绝加载 |

### 10.2 实施顺序

1. **Phase 1**: 数据模型变更（ActionOutput + ActionSpaceConfig）
2. **Phase 2**: 校验逻辑变更（ActionValidator）
3. **Phase 3**: TCP 帧扩展（protocol.rs + tcp_server.rs）
4. **Phase 4**: 降级逻辑（ai_integration.rs）
5. **Phase 5**: 配置项与模型版本校验

---

**文档状态：** v1.0 设计中

**修订记录：**

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-06-13 | 初始版本 |