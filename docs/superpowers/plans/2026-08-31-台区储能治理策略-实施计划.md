# 台区储能治理策略（第 4 策略）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在策略引擎中新增第 4 种兜底策略「台区储能治理」（TaiStorageStrategy），AI 失效时通过台区储能 PCS 分相 P/Q 控制，实现降返送、降三相不平衡度、提功率因数三目标，并用 6-27/7-04 历史数据离线回放验证。

**Architecture:** 扩展 DataPackage 分相字段 → 新增带状态控制器（4 状态机 + 共模P/差模P/分相Q 三通道积分）→ 输出分相设定经核间 V3 帧下发实时控制模块 → PCS。回放工具（tai_replay）复用同一 `control()` 纯函数读 xlsx 逐周期回放并统计 KPI。

**Tech Stack:** Rust 2021, Tokio, calamine（xlsx 读取）, chrono。改动 crate：`mupc-data-processing`、`mupc-intercore`、`mupc-strategy-engine`、`mupc-core-bin`。

**设计依据：** `docs/superpowers/plans/modules/04-MUPC-策略引擎-设计文档.md` §15（v2.16）；源设计 `docs/superpowers/plans/modules/2026-08-25-台区储能控制策略-design.md`。

**回放数据：** `E:/MUPC2/数据/2026_06_27_data_rule.xlsx`、`2026_07_04_data_rule.xlsx`（sheet「总表」）。

---

## 关键实现约定（跨任务）

**符号约定**：总表 `P_表 > 0`=受电，`<0`=返送；PCS `P_pcs > 0`=放电注入，`<0`=充电吸收。带符号电流 `I_i = sign(P_i) × |I_i|`（P_i 为分相有功，含符号）。`soc` 在控制层统一用 `0..1` 小数。

**xlsx「总表」sheet 列索引**（0 基）：0=时间, 1-3=U_A/B/C, 4-6=I_A/B/C(幅值), 7=P_总, 8-10=P_A/B/C, 11=Q_总, 12-14=Q_A/B/C, 15=S_总, 16-18=S_A/B/C, 19=PF_总, 20-22=PF_A/B/C, 27-29=相角_A/B/C, 30=SOC, 33-35=P 调控值, 36-38=Q 调控值, 39=三相不平衡度(基线)。

**测试命令**（Windows 本机，无 NPU，ai-engine 自动 stub）：
- `cargo test -p mupc-data-processing`
- `cargo test -p mupc-intercore`
- `cargo test -p mupc-strategy-engine`
- 全量：`cargo test --workspace --exclude mupc-iec61850-plugin --exclude rs485-plugin --exclude device-trait`

---

## Task 1: 扩展 DataPackage 分相字段

**Files:**
- Modify: `mupc/crates/data-processing/src/telemetry.rs:22-30`
- Test: `mupc/crates/data-processing/src/telemetry.rs`（追加 #[cfg(test)] 模块）

- [ ] **Step 1: 写失败测试**

在 `mupc/crates/data-processing/src/telemetry.rs` 末尾追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_phase_electrical_data_default() {
        let phase = PhaseElectricalData::default();
        assert!(phase.voltage.iter().all(Option::is_none));
        assert!(phase.active_power.iter().all(Option::is_none));
    }

    #[test]
    fn test_electrical_data_phase_none_by_default() {
        let e = ElectricalData::default();
        assert!(e.phase.is_none());
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cargo test -p mupc-data-processing phase_electrical`
Expected: FAIL — `PhaseElectricalData` 未定义（E0412）。

- [ ] **Step 3: 实现分相结构与字段**

在 `ElectricalData`（telemetry.rs）定义之前插入：

```rust
/// 分相电气数据（台区总表）
#[derive(Debug, Clone, Default)]
pub struct PhaseElectricalData {
    /// 分相电压 (V)
    pub voltage: [Option<f64>; 3],
    /// 分相电流 (A，带符号：正=受电方向，负=返送方向)
    pub current: [Option<f64>; 3],
    /// 分相有功 (kW，含符号：>0 受电 / <0 返送)
    pub active_power: [Option<f64>; 3],
    /// 分相无功 (kVAr，含符号)
    pub reactive_power: [Option<f64>; 3],
    /// 分相功率因数
    pub cos_phi: [Option<f64>; 3],
}
```

在 `ElectricalData` 末尾加 `phase` 字段：

```rust
pub struct ElectricalData {
    pub voltage: Option<f64>,        // 电压 (V)
    pub current: Option<f64>,        // 电流 (A)
    pub active_power: Option<f64>,   // 有功功率 (kW)
    pub reactive_power: Option<f64>, // 无功功率 (kVar)
    pub cos_phi: Option<f64>,        // 功率因数
    pub frequency: Option<f64>,      // 频率 (Hz)
    /// 分相数据（台区总表），None = 不可用
    pub phase: Option<PhaseElectricalData>,
}

impl Default for ElectricalData {
    fn default() -> Self {
        Self {
            voltage: None,
            current: None,
            active_power: None,
            reactive_power: None,
            cos_phi: None,
            frequency: None,
            phase: None,
        }
    }
}
```

- [ ] **Step 4: 修复既有构造处（加 `phase: None`）**

搜索所有 `ElectricalData {` 字面构造（`startup.rs`、各测试文件等），在字段列表加 `phase: None,`。已知构造处：
- `mupc/crates/mupc-core-bin/src/startup.rs:166`（dataframe_to_datapackage）
- `mupc/crates/strategy-engine/src/anti_reverse_test.rs:13`
- `mupc/crates/strategy-engine/src/peak_shaving_test.rs`、`demand_control_test.rs`（若存在）
- `mupc/crates/strategy-engine/src/south_command_sender_test.rs`（若存在）

用 Grep `ElectricalData {` 全项目逐一补字段。

- [ ] **Step 5: 运行测试验证通过**

Run: `cargo test -p mupc-data-processing`
Expected: PASS（2 个新测试 + 原有测试）

- [ ] **Step 6: Commit**

```bash
git add crates/data-processing/src/telemetry.rs crates/mupc-core-bin/src/startup.rs crates/strategy-engine/src
git commit -m "feat: DataPackage 扩展分相电气字段（PhaseElectricalData，供台区储能治理策略）"
```

---

## Task 2: 扩展 ControlCommand 分相设定字段

**Files:**
- Modify: `mupc/crates/strategy-engine/src/strategies.rs:18-38`
- Test: `mupc/crates/strategy-engine/src/strategies.rs`（追加测试）

- [ ] **Step 1: 写失败测试**

在 `strategies.rs` 末尾追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_control_command_phase_fields_default_none() {
        let cmd = ControlCommand {
            cmd_id: 0,
            cmd_type: CommandType::PowerRegulation,
            p_batt_set: None,
            q_batt_set: None,
            phase_compensation: None,
            start_stop: None,
            priority: 0,
            pv_limit: None,
            load_shedding: None,
            phase_p_set: None,
            phase_q_set: None,
        };
        assert!(cmd.phase_p_set.is_none());
        assert!(cmd.phase_q_set.is_none());
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cargo test -p mupc-strategy-engine control_command_phase`
Expected: FAIL — `phase_p_set` 字段不存在（E0560）。

- [ ] **Step 3: 实现字段**

在 `strategies.rs` 的 `ControlCommand` 中 `load_shedding` 之后加：

```rust
    /// 台区储能分相有功设定 (kW) [A/B/C]，正=放电/注入，仅由台区储能治理策略设置
    pub phase_p_set: Option<[f64; 3]>,
    /// 台区储能分相无功设定 (kVAr) [A/B/C]，仅由台区储能治理策略设置
    pub phase_q_set: Option<[f64; 3]>,
```

- [ ] **Step 4: 修复既有构造处（加 `phase_p_set: None, phase_q_set: None,`）**

搜索所有 `ControlCommand {` 字面构造（`peak_shaving.rs`、`demand_control.rs`、`anti_reverse.rs`、`ai_integration.rs` 测试、`south_command_sender_test.rs` 等），逐一补两个字段。

- [ ] **Step 5: 运行测试验证通过**

Run: `cargo test -p mupc-strategy-engine`
Expected: PASS（编译通过 + 新测试通过）

- [ ] **Step 6: Commit**

```bash
git add crates/strategy-engine/src
git commit -m "feat: ControlCommand 新增台区储能分相 P/Q 设定字段"
```

---

## Task 3: 核间协议 V3 帧（分相下发）

**Files:**
- Modify: `mupc/crates/intercore/src/tcp_server.rs`
- Test: `mupc/crates/intercore/src/tcp_server.rs`（追加 #[cfg(test)]）

- [ ] **Step 1: 写失败测试**

在 `tcp_server.rs` 末尾追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_v3_payload_roundtrip() {
        let p = ControlCmdPayloadV3 {
            frame_version: Some(3),
            p_ref: Some(10.0),
            k_droop: Some(5.0),
            phase_p_set: Some([1.0, 2.0, 3.0]),
            phase_q_set: Some([0.5, 0.5, 0.5]),
            ai_ready: Some(false),
            strategy_mode: Some("fallback".into()),
            timestamp_ms: Some(1_700_000_000_000),
        };
        let bytes = p.to_json().unwrap();
        let parsed = ControlCmdPayloadV3::from_json(&bytes).unwrap();
        assert_eq!(parsed.phase_p_set, Some([1.0, 2.0, 3.0]));
        assert_eq!(parsed.phase_q_set, Some([0.5, 0.5, 0.5]));
        assert_eq!(ControlCmdPayloadV3::detect_version(&bytes).unwrap(), 3);
    }

    #[test]
    fn test_v3_payload_missing_phase_ok() {
        let p = ControlCmdPayloadV3 {
            frame_version: Some(3),
            p_ref: Some(10.0),
            k_droop: Some(5.0),
            phase_p_set: None,
            phase_q_set: None,
            ai_ready: Some(true),
            strategy_mode: Some("intelligent".into()),
            timestamp_ms: None,
        };
        let bytes = p.to_json().unwrap();
        let parsed = ControlCmdPayloadV3::from_json(&bytes).unwrap();
        assert!(parsed.phase_p_set.is_none());
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cargo test -p mupc-intercore v3_payload`
Expected: FAIL — `ControlCmdPayloadV3` 未定义。

- [ ] **Step 3: 实现 ControlCmdPayloadV3**

在 `ControlCmdPayloadV2` 定义之后插入：

```rust
/// 控制指令 JSON Payload v3.0（分相模式）
///
/// v3.0 新增：分相 P/Q 设定（台区储能治理策略下发），兼容 v2 双参数。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ControlCmdPayloadV3 {
    #[serde(rename = "frame_version")]
    pub frame_version: Option<u8>,          // = 3
    #[serde(rename = "p_ref")]
    pub p_ref: Option<f64>,                 // 兼容 v2 双参数（分相模式可为 None）
    #[serde(rename = "k_droop")]
    pub k_droop: Option<f64>,
    #[serde(rename = "phase_p_set")]
    pub phase_p_set: Option<[f64; 3]>,      // 分相有功 (kW)
    #[serde(rename = "phase_q_set")]
    pub phase_q_set: Option<[f64; 3]>,      // 分相无功 (kVAr)
    #[serde(rename = "ai_ready")]
    pub ai_ready: Option<bool>,
    #[serde(rename = "strategy_mode")]
    pub strategy_mode: Option<String>,
    #[serde(rename = "timestamp_ms")]
    pub timestamp_ms: Option<u64>,
}

impl ControlCmdPayloadV3 {
    pub const FRAME_VERSION: u8 = 3;

    pub fn new() -> Self {
        Self {
            frame_version: Some(Self::FRAME_VERSION),
            p_ref: None,
            k_droop: None,
            phase_p_set: None,
            phase_q_set: None,
            ai_ready: None,
            strategy_mode: None,
            timestamp_ms: None,
        }
    }

    pub fn from_json(data: &[u8]) -> Result<Self, serde_json::Error> {
        serde_json::from_slice(data)
    }

    pub fn to_json(&self) -> Result<Vec<u8>, serde_json::Error> {
        serde_json::to_vec(self)
    }

    pub fn detect_version(data: &[u8]) -> Result<u8, serde_json::Error> {
        let v: serde_json::Value = serde_json::from_slice(data)?;
        Ok(v["frame_version"].as_u64().map(|x| x as u8).unwrap_or(1))
    }
}
```

注意：`ControlCmdPayloadV2::detect_version` 原实现依赖 `ControlCmdPayloadV2::from_json` 成功返回 v2、失败返回 1。为正确区分 v3，保持 v2 的 detect 逻辑不变，v3 由**新的 `ControlCmdPayloadV3::detect_version`** 提供。服务端处理改为「先读 frame_version：3→V3 解析；2→V2 解析；其余→V1 解析」。

- [ ] **Step 4: 服务端 v3 分支**

在 `tcp_server.rs` 的 `handle_connection` 中 `ControlCmd` 分支（原 `match ControlCmdPayloadV2::detect_version`）前插入统一版本分派：

```rust
IntercoreFrameType::ControlCmd => {
    info!("Received control command from {}", addr);
    if !frame.data.is_empty() {
        // 统一版本分派：3=V3 分相，2=V2 双参数，其余=V1
        let ver = ControlCmdPayloadV3::detect_version(&frame.data)
            .or_else(|_| Ok(1u8))
            .unwrap_or(1);
        match ver {
            3 => {
                match ControlCmdPayloadV3::from_json(&frame.data) {
                    Ok(payload) => {
                        info!(
                            "ControlCmd v3 parsed: phase_p={:?}, phase_q={:?}, strategy_mode={:?}",
                            payload.phase_p_set, payload.phase_q_set, payload.strategy_mode
                        );
                    }
                    Err(e) => warn!("Failed to parse ControlCmd V3 payload: {}", e),
                }
            }
            2 => {
                match ControlCmdPayloadV2::from_json(&frame.data) {
                    Ok(payload) => {
                        info!(
                            "ControlCmd v2 parsed: p_ref={:?}, k_droop={:?}, ai_ready={:?}, strategy_mode={:?}",
                            payload.p_ref, payload.k_droop, payload.ai_ready, payload.strategy_mode
                        );
                    }
                    Err(e) => warn!("Failed to parse ControlCmd V2 payload: {}", e),
                }
            }
            _ => {
                match ControlCmdPayload::from_json(&frame.data) {
                    Ok(payload) => {
                        info!(
                            "ControlCmd v1 parsed: p_batt_set={:?}, q_batt_set={:?}, ai_ready={:?}, strategy_mode={:?}",
                            payload.p_batt_set, payload.q_batt_set, payload.ai_ready, payload.strategy_mode
                        );
                    }
                    Err(e) => warn!("Failed to parse ControlCmd V1 payload: {}", e),
                }
            }
        }
    }
}
```

- [ ] **Step 5: IntercoreClient 新增 send_tai_command**

在 `IntercoreClient` 中 `send_dual_param` 之后追加：

```rust
/// 发送台区储能分相 P/Q 设定到实时控制模块（v3 分相模式）
pub async fn send_tai_command(
    &self,
    p: [f64; 3],
    q: [f64; 3],
    strategy_mode: &str,
) -> Result<(), MupcError> {
    let payload = ControlCmdPayloadV3 {
        frame_version: Some(ControlCmdPayloadV3::FRAME_VERSION),
        p_ref: None,
        k_droop: None,
        phase_p_set: Some(p),
        phase_q_set: Some(q),
        ai_ready: Some(false),
        strategy_mode: Some(strategy_mode.to_string()),
        timestamp_ms: Some(chrono::Utc::now().timestamp_millis() as u64),
    };
    let payload_bytes = payload.to_json().map_err(|e| {
        MupcError::new(
            ErrorCode::SerializeError,
            format!("Failed to serialize ControlCmdPayloadV3: {}", e),
            "intercore",
        )
    })?;
    let frame = IntercoreFrame::new(IntercoreFrameType::ControlCmd, 0, payload_bytes);
    let frame_bytes = frame.to_bytes()?;
    self.send_frame(&frame_bytes).await
}
```

重构 `send_dual_param` 中「获取/复用连接 + 写帧」逻辑为私有辅助 `send_frame`：

```rust
async fn send_frame(&self, frame_bytes: &[u8]) -> Result<(), MupcError> {
    let mut stream_guard = self.stream.lock().await;
    if stream_guard.is_none() {
        match TcpStream::connect(&self.remote_addr).await {
            Ok(s) => *stream_guard = Some(s),
            Err(e) => {
                return Err(MupcError::new(
                    ErrorCode::ConnectionFailed,
                    format!("Failed to connect to {}: {}", self.remote_addr, e),
                    "intercore",
                ))
            }
        }
    }
    let stream = stream_guard.as_mut().ok_or_else(|| {
        MupcError::new(ErrorCode::ConnectionFailed, "连接未建立", "intercore")
    })?;
    let write_result = timeout(
        Duration::from_millis(self.cmd_config.timeout_ms),
        stream.write_all(frame_bytes),
    )
    .await;
    match write_result {
        Ok(Ok(())) => {
            *self.connected.write().await = true;
            Ok(())
        }
        Ok(Err(e)) => {
            *stream_guard = None;
            Err(MupcError::new(
                ErrorCode::SendFailed,
                format!("Send error: {}", e),
                "intercore",
            ))
        }
        Err(_) => {
            *stream_guard = None;
            Err(MupcError::new(
                ErrorCode::IntercoreTimeout,
                format!("Send timed out after {}ms", self.cmd_config.timeout_ms),
                "intercore",
            ))
        }
    }
}
```

并将 `send_dual_param` 中的连接/写帧代码替换为 `self.send_frame(&frame_bytes).await`（保留 `last_p_ref`/`last_k_droop` 更新与日志）。

- [ ] **Step 6: 运行测试验证通过**

Run: `cargo test -p mupc-intercore`
Expected: PASS（2 个新测试 + 原有协议测试）

- [ ] **Step 7: Commit**

```bash
git add crates/intercore/src/tcp_server.rs
git commit -m "feat: 核间协议 V3 帧（ControlCmdPayloadV3）支持分相 P/Q 下发"
```

---

## Task 4: TaiStorage 数据结构与配置

**Files:**
- Modify: `mupc/crates/strategy-engine/src/config.rs`（追加 TaiStorageConfig）
- Create: `mupc/crates/strategy-engine/src/tai_storage.rs`（本 Task 只放数据结构 + 辅助函数）
- Modify: `mupc/crates/strategy-engine/src/lib.rs`（mod + re-export）

- [ ] **Step 1: 在 config.rs 追加 TaiStorageConfig**

```rust
/// 台区储能治理策略配置
#[derive(Debug, Clone)]
pub struct TaiStorageConfig {
    /// 控制周期 (s)
    pub control_period_s: u64,
    /// S1 返送吸收触发阈值 (kW)：P_表 < -p_abs_trig 进入 S1
    pub p_abs_trig: f64,
    /// S3 高峰放电触发阈值 (kW)：P_表 > p_dis_trig 进入 S3
    pub p_dis_trig: f64,
    /// S1 退出阈值 (kW)：P_表 >= s1_exit 退出 S1（目标 +4，留 2kW 裕度）
    pub s1_exit: f64,
    /// S1 目标进口 (kW)
    pub p_tgt_s1: f64,
    /// S3 目标进口 (kW)
    pub p_tgt_s3: f64,
    /// 电池功率上限 (kW)
    pub p_cap: f64,
    /// 斜坡限速 (kW/周期)
    pub slope: f64,
    /// 共模 P 积分增益
    pub kp: f64,
    /// 差模 P 积分增益
    pub k_diff: f64,
    /// 无功积分增益
    pub k_q: f64,
    /// 无功积分方向符号（±1，按表计/PCS 约定；发散则翻转）
    pub s_q_sign: f64,
    /// 差模上限 (kW/相)
    pub dp_max: f64,
    /// 无功上限 (kVAr/相)
    pub q_i_max: f64,
    /// 每相/中线电流额定 (A)
    pub i_rated: f64,
    /// 总视在额定 (kVA)
    pub s_rated: f64,
    /// 分时 SOC 上限（18:00 前）
    pub soc_cap_day: f64,
    /// SOC 滞回
    pub soc_hys: f64,
    /// 分时 SOC 上限释放时刻（当日秒）
    pub t_release_secs: f64,
    /// S4 清空起点（当日秒）
    pub t_clear_start_secs: f64,
    /// S4 清空截止（当日秒，达标目标）
    pub t_clear_end_secs: f64,
    /// 滑动滤波窗口（点数）
    pub window_size: u32,
    /// 电池容量 (kWh)
    pub battery_capacity_kwh: f64,
}

impl Default for TaiStorageConfig {
    fn default() -> Self {
        Self {
            control_period_s: 60,
            p_abs_trig: 10.0,
            p_dis_trig: 30.0,
            s1_exit: 4.0,
            p_tgt_s1: 2.0,
            p_tgt_s3: 5.0,
            p_cap: 60.0,
            slope: 5.0,
            kp: 0.4,
            k_diff: 0.4,
            k_q: 0.4,
            s_q_sign: 1.0,
            dp_max: 40.0,
            q_i_max: 30.0,
            i_rated: 190.0,
            s_rated: 125.0,
            soc_cap_day: 0.70,
            soc_hys: 0.03,
            t_release_secs: 18.0 * 3600.0,   // 18:00
            t_clear_start_secs: 21.0 * 3600.0, // 21:00
            t_clear_end_secs: 23.5 * 3600.0,   // 23:30
            window_size: 5,
            battery_capacity_kwh: 120.0,
        }
    }
}
```

- [ ] **Step 2: 创建 tai_storage.rs（数据结构 + 辅助函数 + 失败测试占位）**

`mupc/crates/strategy-engine/src/tai_storage.rs`：

```rust
//! 台区储能治理策略（第 4 策略）
//!
//! AI 失效兜底时，通过台区储能 PCS 分相 P/Q 控制实现：
//! 降返送、降三相不平衡度、提功率因数。
//! 设计见 04-MUPC-策略引擎-设计文档 §15。

use crate::config::TaiStorageConfig;
use std::collections::VecDeque;

/// 台区储能控制器状态（4 状态机）
#[derive(Debug, Clone, Copy, PartialEq)]
pub enum TaiState {
    S1PvAbsorb, // 光伏吸收
    S2Flat,     // 平段
    S3Peak,     // 高峰放电
    S4Clear,    // 日终清空
}

/// 台区总表单周期测量（控制律输入，含符号约定）
#[derive(Debug, Clone)]
pub struct MeterData {
    pub p: f64,        // 三相总有功 (kW，>0 受电 / <0 返送)
    pub q: f64,        // 三相总无功 (kVAr)
    pub pf: [f64; 3],  // 分相功率因数
    pub u: [f64; 3],   // 分相电压 (V)
    pub i: [f64; 3],   // 分相电流 (A，带符号)
    pub p_i: [f64; 3], // 分相有功 (kW，含符号)
    pub q_i: [f64; 3], // 分相无功 (kVAr，含符号)
}

impl Default for MeterData {
    fn default() -> Self {
        Self {
            p: 0.0,
            q: 0.0,
            pf: [1.0; 3],
            u: [220.0; 3],
            i: [0.0; 3],
            p_i: [0.0; 3],
            q_i: [0.0; 3],
        }
    }
}

/// 台区储能控制器跨周期状态
#[derive(Debug, Clone)]
pub struct TaiControllerState {
    pub st: TaiState,
    pub p_st: f64,
    pub q_pcs: [f64; 3],
    pub d_p: [f64; 3],
    pub q_active: [bool; 3],
    pub d_p_active: bool,
    pub q_last: [f64; 3],
    pub meter_buf: VecDeque<MeterData>,
    /// 上次控制周期时间戳（节流用）
    pub last_control_ts: u64,
}

impl Default for TaiControllerState {
    fn default() -> Self {
        Self {
            st: TaiState::S2Flat,
            p_st: 0.0,
            q_pcs: [0.0; 3],
            d_p: [0.0; 3],
            q_active: [false; 3],
            d_p_active: false,
            q_last: [0.0; 3],
            meter_buf: VecDeque::new(),
            last_control_ts: 0,
        }
    }
}

/// 每周期向 target 最多移动 step
pub(crate) fn move_toward(x: f64, target: f64, step: f64) -> f64 {
    if (x - target).abs() <= step {
        target
    } else {
        x + (target - x).signum() * step
    }
}
```

- [ ] **Step 3: lib.rs 注册模块**

在 `mupc/crates/strategy-engine/src/lib.rs` 加 `pub mod tai_storage;`，并在 `pub use` 段加：

```rust
pub use tai_storage::{MeterData, TaiControllerState, TaiState};
pub use config::TaiStorageConfig;
```

（`TaiStorageStrategy` 与 `control()` 在 Task 5/6 实现后加入 re-export。）

- [ ] **Step 4: 编译验证**

Run: `cargo check -p mupc-strategy-engine`
Expected: PASS（无错误；`TaiState`/`MeterData` 已导出）

- [ ] **Step 5: Commit**

```bash
git add crates/strategy-engine/src/config.rs crates/strategy-engine/src/tai_storage.rs crates/strategy-engine/src/lib.rs
git commit -m "feat: 台区储能策略数据结构（TaiStorageConfig/TaiState/MeterData/TaiControllerState）"
```

---

## Task 5: control() 纯函数（核心控制器）

**Files:**
- Modify: `mupc/crates/strategy-engine/src/tai_storage.rs`（追加 control/辅助函数）
- Test: `mupc/crates/strategy-engine/src/tai_storage_test.rs`（新建）

- [ ] **Step 1: 写失败测试**

新建 `mupc/crates/strategy-engine/src/tai_storage_test.rs`：

```rust
#[cfg(test)]
mod tai_storage_test {
    use crate::config::TaiStorageConfig;
    use crate::tai_storage::{control, move_toward, MeterData, TaiControllerState, TaiState};

    fn meter(p: f64, pi: [f64; 3], qi: [f64; 3], u: [f64; 3], pfi: [f64; 3]) -> MeterData {
        MeterData {
            p,
            q: qi.iter().sum(),
            pf: pfi,
            u,
            // 带符号电流近似（≈P/U，符号一致，幅值差异用于触发不平衡）
            i: [pi[0], pi[1], pi[2]],
            p_i: pi,
            q_i: qi,
        }
    }

    #[test]
    fn test_s2_flat_no_output_when_normal() {
        let cfg = TaiStorageConfig::default();
        let mut st = TaiControllerState::default();
        let m = meter(5.0, [1.0, 2.0, 2.0], [0.0; 3], [220.0; 3], [0.99; 3]);
        let (p, q) = control(&mut st, &cfg, &m, 0.5, 3600 * 10); // 10:00
        assert_eq!(st.st, TaiState::S2Flat);
        assert_eq!(p, [0.0; 3]);
        assert_eq!(q, [0.0; 3]);
    }

    #[test]
    fn test_s1_absorb_on_reverse() {
        let cfg = TaiStorageConfig::default();
        let mut st = TaiControllerState::default();
        // 白天 12:00，总表返送 -30kW（三相各有返送）
        let m = meter(-30.0, [-10.0, -10.0, -10.0], [0.0; 3], [220.0; 3], [0.99; 3]);
        let (p, _) = control(&mut st, &cfg, &m, 0.5, 3600 * 12);
        assert_eq!(st.st, TaiState::S1PvAbsorb);
        // 共模应充电（p_st < 0，分相 P 之和 = p_st）
        let sum: f64 = p.iter().sum();
        assert!(sum < 0.0, "S1 应充电吸收返送，p 之和={}", sum);
    }

    #[test]
    fn test_s3_discharge_on_high_load() {
        let cfg = TaiStorageConfig::default();
        let mut st = TaiControllerState::default();
        let m = meter(50.0, [20.0, 15.0, 15.0], [0.0; 3], [220.0; 3], [0.99; 3]);
        let (p, _) = control(&mut st, &cfg, &m, 0.5, 3600 * 16);
        assert_eq!(st.st, TaiState::S3Peak);
        let sum: f64 = p.iter().sum();
        assert!(sum > 0.0, "S3 应放电，p 之和={}", sum);
    }

    #[test]
    fn test_dp_zero_net_energy() {
        let cfg = TaiStorageConfig::default();
        let mut st = TaiControllerState::default();
        // 不平衡：B 相电流大（制造 unbal >25%）
        let m = meter(10.0, [2.0, 6.0, 2.0], [0.0; 3], [220.0; 3], [0.99; 3]);
        // 强制 d_p_active，多周期积分
        st.d_p_active = true;
        for _ in 0..5 {
            let (p, _) = control(&mut st, &cfg, &m, 0.5, 3600 * 10);
            // 差模零净能量：分相 P 之和 = 共模 P_st（S2 时 P_st 趋向 0）
            let sum: f64 = p.iter().sum();
            assert!((sum - st.p_st).abs() < 1e-6, "ΣΔP 应守恒: sum={} p_st={}", sum, st.p_st);
        }
        // d_p 三相之和应 ≈ 0
        let dsum: f64 = st.d_p.iter().sum();
        assert!(dsum.abs() < 1e-6, "ΣΔP 应=0: {}", dsum);
    }

    #[test]
    fn test_s4_force_discharge_to_soc_floor() {
        let cfg = TaiStorageConfig::default();
        let mut st = TaiControllerState::default();
        // 22:00 已过清空起点，SOC=0.5 → S4 强制放电
        let m = meter(5.0, [2.0, 1.0, 2.0], [0.0; 3], [220.0; 3], [0.99; 3]);
        let (p, _) = control(&mut st, &cfg, &m, 0.5, 3600 * 22);
        assert_eq!(st.st, TaiState::S4Clear);
        let sum: f64 = p.iter().sum();
        assert!(sum > 0.0, "S4 应强制放电: {}", sum);
    }

    #[test]
    fn test_failsafe_nan_regresses_to_zero() {
        let cfg = TaiStorageConfig::default();
        let mut st = TaiControllerState::default();
        st.p_st = -40.0; // 之前充电
        let m = meter(f64::NAN, [f64::NAN; 3], [0.0; 3], [220.0; 3], [0.99; 3]);
        let (p, _) = control(&mut st, &cfg, &m, 0.5, 3600 * 10);
        assert!(st.p_st.abs() < 40.0, "failsafe 应斜坡回归: {}", st.p_st);
        assert!(p.iter().all(|x| x.is_finite()));
    }

    #[test]
    fn test_move_toward_behavior() {
        assert_eq!(move_toward(10.0, 0.0, 5.0), 5.0);
        assert_eq!(move_toward(2.0, 0.0, 5.0), 0.0);
        assert_eq!(move_toward(-10.0, 0.0, 3.0), -7.0);
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cargo test -p mupc-strategy-engine tai_storage`
Expected: FAIL — `control` 未定义。

- [ ] **Step 3: 实现 control() 与辅助函数**

在 `tai_storage.rs` 追加：

```rust
/// 滑动滤波：meter 入窗，返回 n 点均值（缓冲未满直接取当前）
fn sliding_avg(state: &mut TaiControllerState, config: &TaiStorageConfig, meter: &MeterData) -> MeterData {
    state.meter_buf.push_back(meter.clone());
    while state.meter_buf.len() > config.window_size as usize {
        state.meter_buf.pop_front();
    }
    let n = state.meter_buf.len() as f64;
    let mut avg = meter.clone();
    for i in 0..3 {
        avg.u[i] = state.meter_buf.iter().map(|m| m.u[i]).sum::<f64>() / n;
        avg.i[i] = state.meter_buf.iter().map(|m| m.i[i]).sum::<f64>() / n;
        avg.p_i[i] = state.meter_buf.iter().map(|m| m.p_i[i]).sum::<f64>() / n;
        avg.q_i[i] = state.meter_buf.iter().map(|m| m.q_i[i]).sum::<f64>() / n;
        avg.pf[i] = state.meter_buf.iter().map(|m| m.pf[i]).sum::<f64>() / n;
    }
    avg.p = avg.p_i.iter().sum();
    avg.q = avg.q_i.iter().sum();
    avg
}

/// SOC 保护：充电 ≥90% 剪 0 / 88% 线性降额；放电 ≤10% 剪 0 / 12% 线性降额
fn soc_protect(p_st: f64, soc: f64) -> f64 {
    if p_st < 0.0 {
        // 充电
        if soc >= 0.90 {
            0.0
        } else if soc >= 0.88 {
            p_st * (1.0 - (soc - 0.88) / 0.02)
        } else {
            p_st
        }
    } else if p_st > 0.0 {
        // 放电
        if soc <= 0.10 {
            0.0
        } else if soc <= 0.12 {
            p_st * (soc - 0.10) / 0.02
        } else {
            p_st
        }
    } else {
        p_st
    }
}

/// 容量仲裁：每相电流 / 总视在 / 总有功约束 + ΔP 重归一
fn arbitrate(
    pcmd: &mut [f64; 3],
    q: &mut [f64; 3],
    state: &mut TaiControllerState,
    config: &TaiStorageConfig,
    u: &[f64; 3],
) {
    for _ in 0..8 {
        let mut s_total = 0.0;
        let mut violated = false;
        for i in 0..3 {
            let s = (pcmd[i].powi(2) + q[i].powi(2)).sqrt();
            s_total += s;
            let i_phase = s * 1000.0 / u[i].max(1.0);
            if i_phase > config.i_rated {
                violated = true;
                if q[i].abs() > 0.1 {
                    q[i] = 0.0; // 裁剪顺序 ①Q
                } else if state.d_p[i].abs() > 0.1 {
                    state.d_p[i] = move_toward(state.d_p[i], 0.0, config.slope); // ②差模P
                }
            }
        }
        if s_total > config.s_rated {
            violated = true;
            let scale = (config.s_rated / s_total).min(1.0);
            // 等比缩差模（共模最后剪，S4 不可剪由上层保证）
            for i in 0..3 {
                state.d_p[i] *= scale;
            }
        }
        if !violated {
            break;
        }
    }
    // 总有功 ≤ p_cap（共模限制；差模零净）
    state.p_st = state.p_st.clamp(-config.p_cap, config.p_cap);
    // 差模重归一 ΣΔP=0
    let d_sum = state.d_p.iter().sum::<f64>() / 3.0;
    for v in state.d_p.iter_mut() {
        *v -= d_sum;
    }
    for i in 0..3 {
        pcmd[i] = state.p_st / 3.0 + state.d_p[i];
    }
}

/// 核心控制器：单周期控制（纯函数，跨周期状态由 state 承载）
///
/// 返回 (分相有功 P [kW], 分相无功 Q [kVAr])，P>0 放电/注入，P<0 充电/吸收。
pub fn control(
    state: &mut TaiControllerState,
    config: &TaiStorageConfig,
    meter: &MeterData,
    soc: f64,   // 0..1
    t_now: u64, // unix 秒
) -> ([f64; 3], [f64; 3]) {
    // 1. 滤波
    let f = sliding_avg(state, config, meter);
    let p = f.p;
    let pi = f.p_i;
    let qi = f.q_i;
    let u = f.u;
    let pfi = f.pf;
    let ii = f.i;

    // 2. failsafe：数据异常 → 斜坡回归 0，保持最近有效 Q
    if !p.is_finite() || pi.iter().any(|x| !x.is_finite()) {
        state.p_st = move_toward(state.p_st, 0.0, config.slope);
        for i in 0..3 {
            state.d_p[i] = move_toward(state.d_p[i], 0.0, config.slope);
        }
        let pcmd = [state.p_st / 3.0; 3];
        return (pcmd, state.q_last);
    }

    // 3. 状态机（优先级 S4 > S1 > S3 > S2；滞回）
    let secs = (t_now % 86400) as f64;
    let soc_cap = if secs < config.t_release_secs {
        config.soc_cap_day
    } else {
        0.90
    };
    let hours_to_clear = ((config.t_clear_end_secs - secs) / 3600.0).max(0.1);
    let mut p_force = ((soc - 0.10) * config.battery_capacity_kwh / hours_to_clear).clamp(0.0, config.p_cap);
    if u.iter().any(|&x| x > 235.0) {
        p_force = p_force.min(p.max(0.0)); // 电压越限保护
    }

    state.st = if secs >= config.t_clear_start_secs && soc > 0.10 {
        TaiState::S4Clear
    } else {
        match state.st {
            TaiState::S1PvAbsorb => {
                if p < config.s1_exit && soc < soc_cap {
                    TaiState::S1PvAbsorb
                } else {
                    TaiState::S2Flat
                }
            }
            TaiState::S3Peak => {
                if state.p_st > 0.0 || p > config.p_tgt_s3 {
                    TaiState::S3Peak
                } else {
                    TaiState::S2Flat
                }
            }
            _ => {
                if p < -config.p_abs_trig && soc < soc_cap - config.soc_hys {
                    TaiState::S1PvAbsorb
                } else if p > config.p_dis_trig {
                    TaiState::S3Peak
                } else {
                    TaiState::S2Flat
                }
            }
        }
    };

    // 4. 分相 Q（积分式，把表计无功归零）
    let mut q = [0.0; 3];
    for i in 0..3 {
        if pfi[i].abs() > 0.98 {
            state.q_active[i] = false;
        } else if pfi[i].abs() < 0.95 {
            state.q_active[i] = true;
        }
        if state.q_active[i] {
            let inc = config.s_q_sign * config.k_q * qi[i];
            state.q_pcs[i] = (state.q_pcs[i] + inc).clamp(-config.q_i_max, config.q_i_max);
            q[i] = state.q_pcs[i];
        } else {
            state.q_pcs[i] = move_toward(state.q_pcs[i], 0.0, config.q_i_max);
            q[i] = state.q_pcs[i];
        }
    }

    // 5. 共模 P（增量式积分 + 斜坡限速）
    state.p_st = match state.st {
        TaiState::S1PvAbsorb => {
            let inc = (config.kp * (p - config.p_tgt_s1)).clamp(-config.slope, config.slope);
            (state.p_st + inc).clamp(-config.p_cap, 0.0)
        }
        TaiState::S2Flat => move_toward(state.p_st, 0.0, config.slope),
        TaiState::S3Peak => {
            let inc = (config.kp * (p - config.p_tgt_s3)).clamp(-config.slope, config.slope);
            (state.p_st + inc).clamp(0.0, config.p_cap)
        }
        TaiState::S4Clear => move_toward(state.p_st, p_force, config.slope),
    };
    state.p_st = soc_protect(state.p_st, soc);

    // 6. 差模 P（积分式，零净能量；I_i 带符号）
    let imean = ii.iter().sum::<f64>() / 3.0;
    let i_max = ii.iter().cloned().fold(0.0f64, f64::max);
    let i_min = ii.iter().cloned().fold(f64::MAX, f64::min);
    let unbal = if i_max < 1.0 {
        0.0
    } else {
        (1.0 - i_min / i_max) * 100.0
    };
    if unbal < 15.0 {
        state.d_p_active = false;
    } else if unbal > 25.0 {
        state.d_p_active = true;
    }
    for i in 0..3 {
        let inc = config.k_diff * u[i] * (ii[i] - imean);
        if state.d_p_active && inc.abs() > 0.5f64.max(0.05 * pi[i].abs()) {
            state.d_p[i] = (state.d_p[i] + inc).clamp(-config.dp_max, config.dp_max);
        } else {
            state.d_p[i] = move_toward(state.d_p[i], 0.0, config.dp_max);
        }
    }

    // 7. 指令合成
    let mut pcmd = [0.0; 3];
    for i in 0..3 {
        pcmd[i] = state.p_st / 3.0 + state.d_p[i];
    }

    // 8. 容量仲裁
    arbitrate(&mut pcmd, &mut q, state, config, &u);

    state.q_last = q;
    (pcmd, q)
}
```

- [ ] **Step 4: lib.rs 导出 control**

在 `lib.rs` 的 `pub use tai_storage::{...}` 中加 `control`：

```rust
pub use tai_storage::{control, MeterData, TaiControllerState, TaiState};
```

并注册测试模块：`#[cfg(test)] mod tai_storage_test;`

- [ ] **Step 5: 运行测试验证通过**

Run: `cargo test -p mupc-strategy-engine tai_storage`
Expected: PASS（7 个测试）

若 `test_dp_zero_net_energy` 因 unbal 判据不触发 d_p_active 而失败，检查 `meter()` 中电流幅值构造（应保证 i_max/i_min 比例使 unbal>25%）。

- [ ] **Step 6: Commit**

```bash
git add crates/strategy-engine/src/tai_storage.rs crates/strategy-engine/src/tai_storage_test.rs crates/strategy-engine/src/lib.rs
git commit -m "feat: 台区储能 control() 核心控制器（4 状态机 + 三通道积分 + 容量仲裁）"
```

---

## Task 6: TaiStorageStrategy（FallbackStrategy 实现 + 节流）

**Files:**
- Modify: `mupc/crates/strategy-engine/src/tai_storage.rs`
- Test: `mupc/crates/strategy-engine/src/tai_storage_test.rs`（追加）

- [ ] **Step 1: 写失败测试（追加到 tai_storage_test.rs）**

```rust
    use crate::strategies::{CommandType, ControlCommand, FallbackStrategy, StrategyType};
    use mupc_data_processing::telemetry::{
        BatteryData, DataPackage, DeviceStatus, ElectricalData, InverterStatus, PhaseElectricalData,
    };
    use crate::tai_storage::TaiStorageStrategy;

    fn create_phase_data(p: f64) -> PhaseElectricalData {
        PhaseElectricalData {
            voltage: [Some(228.0); 3],
            current: [Some(p.signum() * 10.0); 3],
            active_power: [Some(p / 3.0); 3],
            reactive_power: [Some(0.0); 3],
            cos_phi: [Some(0.99); 3],
        }
    }

    fn create_package(timestamp: u64, grid_power: f64, soc: f64) -> DataPackage {
        DataPackage {
            timestamp,
            electrical: ElectricalData {
                voltage: Some(220.0),
                current: Some(30.0),
                active_power: Some(grid_power),
                reactive_power: Some(0.0),
                cos_phi: Some(0.99),
                frequency: Some(50.0),
                phase: Some(create_phase_data(grid_power)),
            },
            device_status: DeviceStatus {
                inverter_status: InverterStatus::Running,
                pv_power: Some(30.0),
                load_power: Some(40.0),
                ev_charger_power: None,
            },
            battery: BatteryData {
                soc: Some(soc * 100.0),
                soh: Some(95.0),
                temperature: Some(25.0),
            },
        }
    }

    #[test]
    fn test_strategy_throttles_to_60s() {
        let strategy = TaiStorageStrategy::new(TaiStorageConfig::default());
        // 第一次调用（10:00:00）执行控制
        let cmd1 = poll(&strategy, 3600 * 10);
        // 1 秒后再次调用（<60s）→ 返回上次指令，不重新计算
        let cmd2 = poll(&strategy, 3600 * 10 + 1);
        assert_eq!(cmd1.phase_p_set, cmd2.phase_p_set);
    }

    fn poll(strategy: &TaiStorageStrategy, ts: u64) -> ControlCommand {
        let data = create_package(ts, 10.0, 0.5);
        tokio_test::block_on(strategy.evaluate(&data)).unwrap()
    }

    #[test]
    fn test_strategy_name_and_type() {
        let s = TaiStorageStrategy::new(TaiStorageConfig::default());
        assert_eq!(s.name(), "TaiStorageStrategy");
        assert_eq!(s.strategy_type(), StrategyType::Fallback);
    }

    #[test]
    fn test_strategy_outputs_phase_fields() {
        let strategy = TaiStorageStrategy::new(TaiStorageConfig::default());
        let cmd = poll(&strategy, 3600 * 10); // 10:00 返送场景 → S1 充电
        let _ = create_package(3600 * 10, -30.0, 0.5);
        assert!(cmd.phase_p_set.is_some());
        assert!(cmd.phase_q_set.is_some());
        assert_eq!(cmd.cmd_type, CommandType::ChargeDischarge);
    }
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cargo test -p mupc-strategy-engine strategy_throttles`
Expected: FAIL — `TaiStorageStrategy` 未定义。

- [ ] **Step 3: 实现 TaiStorageStrategy**

在 `tai_storage.rs` 追加：

```rust
use crate::config::TaiStorageConfig;
use crate::strategies::{CommandType, ControlCommand, FallbackStrategy, StrategyType};
use async_trait::async_trait;
use mupc_common::MupcError;
use mupc_data_processing::telemetry::DataPackage;
use std::sync::{Arc, Mutex};

/// 台区储能治理策略（第 4 策略）
///
/// 内部持跨周期控制状态（Arc<Mutex>），实现 FallbackStrategy。
/// 控制周期节流：距上次控制 ≥ control_period_s 才执行 control()。
pub struct TaiStorageStrategy {
    config: TaiStorageConfig,
    state: Arc<Mutex<TaiControllerState>>,
    last_cmd: Arc<Mutex<ControlCommand>>,
}

impl TaiStorageStrategy {
    pub fn new(config: TaiStorageConfig) -> Self {
        let last_cmd = ControlCommand {
            cmd_id: 4,
            cmd_type: CommandType::PowerRegulation,
            p_batt_set: None,
            q_batt_set: None,
            phase_compensation: None,
            start_stop: Some(true),
            priority: 3,
            pv_limit: None,
            load_shedding: None,
            phase_p_set: Some([0.0; 3]),
            phase_q_set: Some([0.0; 3]),
        };
        Self {
            config,
            state: Arc::new(Mutex::new(TaiControllerState::default())),
            last_cmd: Arc::new(Mutex::new(last_cmd)),
        }
    }

    /// 同步评估（用于测试与回放）：内部执行控制周期
    pub fn evaluate_sync(&self, data: &DataPackage) -> ControlCommand {
        let mut state = self.state.lock().unwrap();
        if data.timestamp.saturating_sub(state.last_control_ts) < self.config.control_period_s {
            return self.last_cmd.lock().unwrap().clone();
        }
        state.last_control_ts = data.timestamp;

        let meter = data_to_meter(data);
        let soc = data.battery.soc.unwrap_or(50.0) / 100.0;
        let (p, q) = control(&mut state, &self.config, &meter, soc, data.timestamp);

        let cmd = ControlCommand {
            cmd_id: 4,
            cmd_type: CommandType::ChargeDischarge,
            p_batt_set: Some(p.iter().sum()),
            q_batt_set: None,
            phase_compensation: None,
            start_stop: Some(true),
            priority: 3,
            pv_limit: None,
            load_shedding: None,
            phase_p_set: Some(p),
            phase_q_set: Some(q),
        };
        *self.last_cmd.lock().unwrap() = cmd.clone();
        cmd
    }
}

/// DataPackage → MeterData（分相字段缺失时按 failsafe：全零）
fn data_to_meter(data: &DataPackage) -> MeterData {
    let phase = match data.electrical.phase.as_ref() {
        Some(ph) => ph,
        None => return MeterData::default(),
    };
    let get = |a: &[Option<f64>; 3]| {
        [
            a[0].unwrap_or(0.0),
            a[1].unwrap_or(0.0),
            a[2].unwrap_or(0.0),
        ]
    };
    let p_i = get(&phase.active_power);
    let q_i = get(&phase.reactive_power);
    let u = get(&phase.voltage);
    let i_mag = get(&phase.current);
    let pf = get(&phase.cos_phi);
    // 带符号电流：方向由分相有功符号决定
    let i = [
        p_i[0].signum() * i_mag[0],
        p_i[1].signum() * i_mag[1],
        p_i[2].signum() * i_mag[2],
    ];
    MeterData {
        p: p_i.iter().sum(),
        q: q_i.iter().sum(),
        pf,
        u,
        i,
        p_i,
        q_i,
    }
}

#[async_trait]
impl FallbackStrategy for TaiStorageStrategy {
    async fn evaluate(&self, data: &DataPackage) -> Result<ControlCommand, MupcError> {
        Ok(self.evaluate_sync(data))
    }

    fn strategy_type(&self) -> StrategyType {
        StrategyType::Fallback
    }

    fn name(&self) -> &str {
        "TaiStorageStrategy"
    }
}
```

- [ ] **Step 4: lib.rs 导出 + dev-dependency**

`lib.rs` 的 `pub use tai_storage::{...}` 加 `TaiStorageStrategy`。若测试用 `tokio_test::block_on`，确认 `Cargo.toml` dev-dependencies 已有 `tokio-test = "0.4"`（已有）。

- [ ] **Step 5: 运行测试验证通过**

Run: `cargo test -p mupc-strategy-engine tai_storage`
Expected: PASS（+3 个测试）

- [ ] **Step 6: Commit**

```bash
git add crates/strategy-engine/src/tai_storage.rs crates/strategy-engine/src/tai_storage_test.rs crates/strategy-engine/src/lib.rs
git commit -m "feat: TaiStorageStrategy 实现 FallbackStrategy（控制周期节流 + 分相指令输出）"
```

---

## Task 7: AiIntegrator 集成 + 启动装配

**Files:**
- Modify: `mupc/crates/strategy-engine/src/ai_integration.rs`
- Modify: `mupc/crates/mupc-core-bin/src/startup.rs`

- [ ] **Step 1: AiIntegrator 加字段与注入**

`ai_integration.rs`：
- `use` 加 `use crate::config::TaiStorageConfig;`、`use crate::tai_storage::TaiStorageStrategy;`
- struct 加字段 `tai_storage: Option<Arc<TaiStorageStrategy>>`
- `new()` 初始化 `tai_storage: None`
- 加注入方法：

```rust
/// 注入台区储能治理策略（v2.16 第 4 策略）
pub fn set_tai_storage_strategy(&mut self, strategy: Arc<TaiStorageStrategy>) {
    self.tai_storage = Some(strategy);
}
```

- [ ] **Step 2: run_fallback_strategies 追加台区治理下发**

在 `run_fallback_strategies` 末尾（现有 pv_limit/load_shedding 分发之后）追加：

```rust
        // 台区储能治理策略：分相 P/Q 经核间下发实时控制模块
        if let Some(tai) = &self.tai_storage {
            let cmd = tai.evaluate(&data).await.map_err(|e| {
                AiEngineError::ModelError(format!("TaiStorageStrategy 执行失败: {}", e))
            })?;
            if let (Some(p), Some(q)) = (cmd.phase_p_set, cmd.phase_q_set) {
                if let Some(ref client) = self.intercore_client {
                    if let Err(e) = client.send_tai_command(p, q, "fallback").await {
                        tracing::warn!("台区储能分相指令下发失败: {:?}", e);
                    } else {
                        tracing::debug!(
                            "台区储能分相指令已下发: p={:?}, q={:?}",
                            p, q
                        );
                    }
                } else {
                    tracing::warn!("核间客户端未注入，台区储能分相指令未下发");
                }
            }
        }
```

- [ ] **Step 3: startup.rs 装配注入**

在 `startup.rs` 的「8. 策略引擎」步骤（`ai_integrator.set_south_dispatcher(...)` 之后、`set_model_manager` 之前）追加：

```rust
    // v2.16: 注入台区储能治理策略（AI 失效兜底，分相 P/Q 经核间下发）
    ai_integrator.set_tai_storage_strategy(Arc::new(
        mupc_strategy_engine::TaiStorageStrategy::new(
            mupc_strategy_engine::TaiStorageConfig::default(),
        ),
    ));
```

- [ ] **Step 4: 编译 + 测试**

Run: `cargo check -p mupc-strategy-engine && cargo check -p mupc-core-bin`
Expected: PASS（无错误）

Run: `cargo test -p mupc-strategy-engine`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add crates/strategy-engine/src/ai_integration.rs crates/mupc-core-bin/src/startup.rs
git commit -m "feat: AiIntegrator 集成台区储能治理策略（AI 失效兜底时经核间下发分相 P/Q）"
```

---

## Task 8: 离线回放工具 tai_replay

**Files:**
- Create: `mupc/crates/strategy-engine/src/bin/tai_replay.rs`
- Modify: `mupc/Cargo.toml`（workspace.dependencies 加 calamine）
- Modify: `mupc/crates/strategy-engine/Cargo.toml`（加 calamine + chrono）

- [ ] **Step 1: 加 calamine 依赖**

`mupc/Cargo.toml` 的 `[workspace.dependencies]` 加：
```toml
calamine = "0.24"
```

`mupc/crates/strategy-engine/Cargo.toml` 的 `[dependencies]` 加：
```toml
calamine.workspace = true
chrono.workspace = true
```

- [ ] **Step 2: 写回放工具**

创建 `mupc/crates/strategy-engine/src/bin/tai_replay.rs`：

```rust
//! 台区储能治理策略离线回放工具
//!
//! 读取 data_rule xlsx（sheet「总表」），按 60s 控制周期调用
//! TaiStorageStrategy::evaluate 逐周期回放，统计 KPI 并打印报告。
//!
//! 用法: cargo run -p mupc-strategy-engine --bin tai_replay -- <xlsx路径> [SOC初值0.0-1.0]

use calamine::{Reader, Xlsx, DataType};
use chrono::NaiveDateTime;
use mupc_data_processing::telemetry::{
    BatteryData, DataPackage, DeviceStatus, ElectricalData, InverterStatus, PhaseElectricalData,
};
use mupc_strategy_engine::{TaiStorageConfig, TaiStorageStrategy};
use std::env;

// xlsx「总表」列索引
const COL_TIME: usize = 0;
const COL_U: usize = 1; // U_A/B/C = 1,2,3
const COL_I: usize = 4; // I_A/B/C = 4,5,6
const COL_P_TOTAL: usize = 7;
const COL_PI: usize = 8; // P_A/B/C = 8,9,10
const COL_QI: usize = 12; // Q_A/B/C = 12,13,14
const COL_PFI: usize = 20; // PF_A/B/C = 20,21,22
const COL_UNBAL: usize = 39; // 三相不平衡度（基线）

fn f(row: &[DataType], i: usize) -> f64 {
    row.get(i)
        .and_then(|d| d.get_float())
        .unwrap_or(0.0)
}

fn parse_time(s: &str) -> Option<i64> {
    for fmt in ["%Y-%m-%d %H:%M:%S", "%Y-%m-%d %H:%M"] {
        if let Ok(nt) = NaiveDateTime::parse_from_str(s.trim(), fmt) {
            return Some(nt.and_utc().timestamp());
        }
    }
    None
}

fn main() {
    let args: Vec<String> = env::args().collect();
    let path = args.get(1).expect("用法: tai_replay <xlsx路径> [SOC初值]");
    let soc_init: f64 = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(0.50);
    let soc_init = soc_init.clamp(0.10, 0.90);

    let mut workbook = Xlsx::new(path).expect("无法打开 xlsx");
    let range = workbook
        .worksheet_range("总表")
        .expect("找不到「总表」sheet");

    let cfg = TaiStorageConfig::default();
    let strategy = TaiStorageStrategy::new(cfg.clone());

    // 回放统计
    let mut soc = soc_init;
    let mut last_ts: i64 = 0;
    let mut last_cmd: Option<[f64; 3]> = None;
    let mut n_samples = 0usize;
    let mut n_control = 0usize;
    let mut reverse_base_secs = 0.0; // 无储能返送时长
    let mut reverse_ctrl_secs = 0.0; // 有储能返送时长
    let mut reverse_base_peak = 0.0f64; // 无储能返送峰值
    let mut reverse_ctrl_peak = 0.0f64;
    let mut unbal_base_ok_secs = 0.0; // 基线不平衡<20% 时长
    let mut unbal_ctrl_ok_secs = 0.0; // 控制后不平衡<20% 时长
    let mut pf_good_secs = 0.0; // 控制后分相 PF>0.95 时长（3 相均）
    let mut soc_min = f64::MAX;
    let mut soc_max = f64::MIN;
    let mut reverse_ctrl_energy = 0.0; // 有储能返送能量 kwh
    let mut prev_p_ctrl: f64 = 0.0;
    let mut prev_ts: i64 = 0;

    for row in range.rows().skip(1) {
        let time_str = row
            .get(COL_TIME)
            .and_then(|d| d.get_string())
            .map(|s| s.to_string())
            .unwrap_or_default();
        let ts = match parse_time(&time_str) {
            Some(t) => t,
            None => continue,
        };
        let dt = if prev_ts == 0 { 60.0 } else { (ts - prev_ts) as f64 };

        let p_total = f(row, COL_P_TOTAL);
        let pi = [f(row, COL_PI), f(row, COL_PI + 1), f(row, COL_PI + 2)];
        let qi = [f(row, COL_QI), f(row, COL_QI + 1), f(row, COL_QI + 2)];
        let u = [f(row, COL_U), f(row, COL_U + 1), f(row, COL_U + 2)];
        let i_mag = [f(row, COL_I), f(row, COL_I + 1), f(row, COL_I + 2)];
        let pfi = [f(row, COL_PFI), f(row, COL_PFI + 1), f(row, COL_PFI + 2)];
        let unbal_base = f(row, COL_UNBAL);

        // 基线（无储能）KPI
        if p_total < 0.0 {
            reverse_base_secs += dt;
            reverse_base_peak = reverse_base_peak.max(-p_total);
        }
        if unbal_base < 20.0 {
            unbal_base_ok_secs += dt;
        }

        // 控制：按 60s 周期节流
        let cmd = if last_ts == 0 || ts.saturating_sub(last_ts) as u64 >= cfg.control_period_s {
            let phase = PhaseElectricalData {
                voltage: Some(u),
                current: Some([
                    pi[0].signum() * i_mag[0],
                    pi[1].signum() * i_mag[1],
                    pi[2].signum() * i_mag[2],
                ]),
                active_power: Some(pi),
                reactive_power: Some(qi),
                cos_phi: Some(pfi),
            };
            let pkg = DataPackage {
                timestamp: ts as u64,
                electrical: ElectricalData {
                    voltage: Some(u[0]),
                    current: Some(i_mag[0]),
                    active_power: Some(p_total),
                    reactive_power: Some(qi.iter().sum()),
                    cos_phi: Some(pfi[0]),
                    frequency: Some(50.0),
                    phase: Some(phase),
                },
                device_status: DeviceStatus {
                    inverter_status: InverterStatus::Running,
                    pv_power: None,
                    load_power: None,
                    ev_charger_power: None,
                },
                battery: BatteryData {
                    soc: Some(soc * 100.0),
                    soh: None,
                    temperature: None,
                },
            };
            // 同步评估（evaluate_sync 为 TaiStorageStrategy 的同步方法）
            let c = strategy.evaluate_sync(&pkg);
            last_ts = ts;
            n_control += 1;
            c.phase_p_set
        } else {
            last_cmd
        };
        last_cmd = cmd;

        // 控制后净功率 = 基线 - 储能注入
        let p_st = cmd.map(|c| c.iter().sum::<f64>()).unwrap_or(0.0);
        let p_ctrl = p_total - p_st; // 储能放电 p_st>0 → 净进口下降
        if p_ctrl < 0.0 {
            reverse_ctrl_secs += dt;
            reverse_ctrl_peak = reverse_ctrl_peak.max(-p_ctrl);
            reverse_ctrl_energy += -p_ctrl * dt / 3600.0;
        }

        // 控制后电流重算（近似：基线电流幅值方向翻转按净功率比例）
        let unbal_ctrl = if p_ctrl >= 0.0 {
            // 放电削峰场景：用基线不平衡近似（差模已均衡但回放无相角，取基线）
            unbal_base
        } else {
            unbal_base
        };
        if unbal_ctrl < 20.0 {
            unbal_ctrl_ok_secs += dt;
        }
        if pfi.iter().all(|&x| x.abs() > 0.95) {
            pf_good_secs += dt;
        }

        // SOC 积分（P_st 负=充电）
        soc += -p_st * dt / 3600.0 / cfg.battery_capacity_kwh;
        soc = soc.clamp(0.10, 0.90);
        soc_min = soc_min.min(soc);
        soc_max = soc_max.max(soc);
        prev_p_ctrl = p_ctrl;
        prev_ts = ts;
        n_samples += 1;
    }

    let total = n_samples as f64;
    let pct = |x: f64| x / total * 100.0;
    println!("=== 台区储能治理策略回放报告 ===");
    println!("数据源: {}", path);
    println!("样本数: {}  控制周期: {}", n_samples, n_control);
    println!("返送时长占比: 基线 {:.1}% → 控制后 {:.1}%", pct(reverse_base_secs), pct(reverse_ctrl_secs));
    println!("返送峰值(kW): 基线 {:.1} → 控制后 {:.1}", reverse_base_peak, reverse_ctrl_peak);
    println!("不平衡<20% 达标占比: 基线 {:.1}% → 控制后 {:.1}%", pct(unbal_base_ok_secs), pct(unbal_ctrl_ok_secs));
    println!("分相 PF>0.95 占比: {:.1}%", pct(pf_good_secs));
    println!("SOC 范围: {:.1}% ~ {:.1}%  日终: {:.1}%", soc_min * 100.0, soc_max * 100.0, soc * 100.0);
    println!("控制后返送能量: {:.1} kWh", reverse_ctrl_energy);
}
```

- [ ] **Step 3: 编译回放工具**

Run: `cargo build -p mupc-strategy-engine --bin tai_replay`
Expected: PASS（calamine 下载编译成功；若网络失败用 `--offline` 需先 `cargo fetch`）

- [ ] **Step 4: 运行回放（6-27 低负荷日）**

Run: `cargo run -p mupc-strategy-engine --bin tai_replay -- "E:/MUPC2/数据/2026_06_27_data_rule.xlsx" 0.50`
Expected: 打印 KPI 报告（返送降低、不平衡达标占比、SOC 范围）

- [ ] **Step 5: 运行回放（7-04 高负荷日）**

Run: `cargo run -p mupc-strategy-engine --bin tai_replay -- "E:/MUPC2/数据/2026_07_04_data_rule.xlsx" 0.50`
Expected: 打印 KPI 报告

- [ ] **Step 6: 记录回放结果到文档**

将两日回放 KPI 追加到 `docs/superpowers/plans/2026-08-31-台区储能治理策略-实施计划.md` 末尾「回放结果」小节（如实记录，不伪造数据）。

- [ ] **Step 7: Commit**

```bash
git add crates/strategy-engine/src/bin/tai_replay.rs Cargo.toml crates/strategy-engine/Cargo.toml
git commit -m "feat: 台区储能策略离线回放工具 tai_replay（读 data_rule xlsx 统计 KPI）"
```

---

## Task 9: 全量验证

- [ ] **Step 1: 全量编译 + 测试**

Run: `cargo test --workspace --exclude mupc-iec61850-plugin --exclude rs485-plugin --exclude device-trait`
Expected: PASS（无新增失败；预存失败项除外）

- [ ] **Step 2: clippy + fmt**

Run: `cargo clippy --workspace`
Expected: 无新增警告（tai_storage/replay 若有 clippy 建议则修复）

Run: `cargo fmt --all`

- [ ] **Step 3: 回放复跑确认**

Run: 两日回放命令，确认 KPI 稳定。

- [ ] **Step 4: Commit**

```bash
git add -u
git commit -m "chore: 台区储能治理策略全量验证（clippy/fmt/回放）"
```

（注：用 `git add -u` 只暂存已跟踪文件的修改，符合项目「精确暂存」规范——本步仅提交代码修订，不含新文件。）

---

## 回放结果（Task 8 实测）

运行命令（SOC 初值 0.50）：
```bash
cargo run -p mupc-strategy-engine --bin tai_replay -- "E:/MUPC2/数据/2026_06_27_data_rule.xlsx" 0.50
cargo run -p mupc-strategy-engine --bin tai_replay -- "E:/MUPC2/数据/2026_07_04_data_rule.xlsx" 0.50
```

**6-27（低负荷日）：**

```
=== 台区储能治理策略回放报告 ===
数据源: E:/MUPC2/数据/2026_06_27_data_rule.xlsx
样本数: 1422  控制周期: 1009
返送时长占比: 基线 6.2% → 控制后 9.9%
返送峰值(kW): 基线 25.3 → 控制后 35.6
不平衡<20% 达标占比: 基线 8.3% → 控制后 8.3%
分相 PF>0.95 占比: 6.8%
SOC 范围: 10.0% ~ 50.0%  日终: 10.0%
控制后返送能量: 37.4 kWh
```

**7-04（高负荷日）：**

```
=== 台区储能治理策略回放报告 ===
数据源: E:/MUPC2/数据/2026_07_04_data_rule.xlsx
样本数: 1427  控制周期: 1037
返送时长占比: 基线 16.5% → 控制后 19.9%
返送峰值(kW): 基线 57.0 → 控制后 64.8
不平衡<20% 达标占比: 基线 32.6% → 控制后 32.6%
分相 PF>0.95 占比: 9.3%
SOC 范围: 10.0% ~ 76.4%  日终: 10.0%
控制后返送能量: 84.5 kWh
```

**说明（如实记录，供 Task 9 审视）：**
- 「控制后返送」高于基线：S4 日终清空在夜间低负荷时段强制放电（21:00~23:30），向电网反送所致；非日间返送吸收失效。该 KPI 口径将"夜间清空放电反送"一并计入。
- 「不平衡<20% 达标占比」基线=控制后：回放对控制后不平衡采用基线值近似（无相角/电流重分布模型，差模均衡效果未建模），因此该 KPI 不体现差模改善。
- 「分相 PF>0.95 占比」为基线分相 PF 达标时长（回放未建模 Q 补偿对 PF 的提升）。

### 回放结果（Task 8 补充：S4 限幅 + 分时 SOC 上限参数扫描）

新增参数：`s4_limit_margin_kw`（S4 日终清空限幅裕度，控制 `p_force = min(p_force, P_表 + 裕度)`）。回放命令新增两个可选参数：`[xlsx] [SOC初值] [soc_cap_day] [s4_limit_margin_kw]`。

扫描网格（SOC 初值 0.50）：`soc_cap_day ∈ {0.60, 0.70, 0.80}` × `s4_limit_margin_kw ∈ {0.0, 15.0, 30.0}`。

**6-27（低负荷日，基线返送 6.2% / 峰值 25.3kW）：**

| soc_cap | s4_margin | 返送时长% | 返送峰值kW | 返送能量kWh | SOC 日终% | SOC 峰值% |
|---|---|---|---|---|---|---|
| 0.60 | 0.0 | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |
| 0.60 | 15.0 | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |
| 0.60 | 30.0 | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |
| 0.70 | 0.0 | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |
| 0.70 | 15.0 | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |
| 0.70 | 30.0 | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |
| 0.80 | 0.0 | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |
| 0.80 | 15.0 | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |
| 0.80 | 30.0 | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |

**7-04（高负荷日，基线返送 16.5% / 峰值 57.0kW）：**

| soc_cap | s4_margin | 返送时长% | 返送峰值kW | 返送能量kWh | SOC 日终% | SOC 峰值% |
|---|---|---|---|---|---|---|
| 0.60 | 0.0 | 20.4 | 64.8 | 85.8 | 10.0 | 70.5 |
| 0.60 | 15.0 | 20.4 | 64.8 | 85.8 | 10.0 | 70.5 |
| 0.60 | 30.0 | 20.4 | 64.8 | 85.8 | 10.0 | 70.5 |
| 0.70 | 0.0 | 19.9 | 64.8 | 84.5 | 10.0 | 76.4 |
| 0.70 | 15.0 | 19.9 | 64.8 | 84.5 | 10.0 | 76.4 |
| 0.70 | 30.0 | 19.9 | 64.8 | 84.5 | 10.0 | 76.4 |
| 0.80 | 0.0 | 19.7 | 64.8 | 86.1 | 10.0 | 86.7 |
| 0.80 | 15.0 | 19.7 | 64.8 | 86.1 | 10.0 | 86.7 |
| 0.80 | 30.0 | 19.7 | 64.8 | 86.1 | 10.0 | 86.7 |

**结论与建议：**
- **`s4_limit_margin_kw` 在两日回放中均无效（0/15/30 结果完全相同）**：两日 S4 窗口（21:00~23:30）开始前 SOC 均已被 S3 高峰放电降至 10% 地板（6-27 晨峰 06:30~07:30 即放空；7-04 傍晚 S3 放空），S4 从不实际带荷强放，限幅分支从不触发。该功能（含单元测试 `test_s4_limit_margin_reduces_force`）已验证可用，仅在 S4 窗口带入荷（SOC>10%）时才会生效。**默认值保持 0.0**。
- **`soc_cap_day` 仅对 7-04 有边际影响**：提高上限（0.60→0.70→0.80）可让 S1 在午后更多吸收光伏返送，返送时长 20.4%→19.9%→19.7%；但 0.80 使 SOC 峰值推高至 86.7%（逼近 90% 充电保护），返送能量反而 +1.6kWh（86.1 vs 84.5）。**默认值保持 0.70**（0.70 为吸收与裕度的平衡点）。
- **目标（控制后返送 ≤ 基线）两日均未达成**：6-27 恒为 9.9% vs 基线 6.2%，7-04 最优化为 19.7% vs 基线 16.5%。根因：超量返送主要来自 **S3 高峰放电过冲**（负荷快速回落时储能仍以 ≤60kW 满额放电，斜坡 5kW/周期跟不上），而非 S4 夜间清空——上述两个旋钮均不作用于 S3 放电速率/退出滞回。**建议后续 Task 增加 S3 放电速率限制 / 退出滞回 / 分时 S3 门控 的专项标定**。

### 回放结果（Task 8 补充：S3 专项调参）

新增参数：`s3_margin_limit`（S3 放电裕度限幅，`p_st = min(p_st, (P_表 - p_tgt_s3).max(0))`）。回放命令新增可选参数：`[xlsx] [SOC初值] [soc_cap_day] [s4_limit_margin_kw] [s3_margin 0|1]`。

对比网格（SOC 初值 0.50，soc_cap_day=0.70）：`s3_margin_limit ∈ {OFF, ON}`。

**6-27（低负荷日，基线返送 6.2% / 峰值 25.3kW）：**

| s3_margin | 返送时长% | 返送峰值kW | 返送能量kWh | SOC 日终% | SOC 峰值% |
|---|---|---|---|---|---|
| OFF（原默认） | 9.9 | 35.6 | 37.4 | 10.0 | 50.0 |
| ON | **4.0** | **25.6** | **5.4** | 10.2 | 50.0 |

**7-04（高负荷日，基线返送 16.5% / 峰值 57.0kW）：**

| s3_margin | 返送时长% | 返送峰值kW | 返送能量kWh | SOC 日终% | SOC 峰值% |
|---|---|---|---|---|---|
| OFF（原默认） | 19.9 | 64.8 | 84.5 | 10.0 | 76.4 |
| ON | **12.4** | **22.7** | **29.4** | 10.0 | 76.5 |

**结论与建议：**
- **`s3_margin_limit` 在两日均显著压降控制后返送，并把返送压至基线以下**：6-27 控制后返送 9.9%→**4.0%**（基线 6.2%）、峰值 35.6→**25.6**（基线 25.3）、能量 37.4→**5.4kWh**；7-04 19.9%→**12.4%**（基线 16.5%）、峰值 64.8→**22.7**（基线 57.0）、能量 84.5→**29.4kWh**。根因即 Task 8 首轮定位的 **S3 高峰放电过冲**——限幅后负荷回落时放电即时跟随至负荷裕度，不再以 ≤60kW 满额向电网反送。
- **SOC 日终不受影响**：6-27 日终 10.2%（≈10% 地板，限幅仅在傍晚 S3 末端略微保留电量）、7-04 日终 10.0%——两日均完成日终清空。SOC 峰值不变（50.0 / 76.5）。
- **机理说明**：S3 稳态目标恰为 p_st = P_表 - p_tgt_s3，故该钳位不改变稳态目标跟踪，仅在积分过冲（负荷快速回落、p_st 高于当前负荷裕度）时拦截放电，等效于针对斜坡速率不足（5kW/周期 vs 负荷瞬时跌落）的防过冲 windup 钳位。⚠️ 该结论基于 **gross 开环**模型（控制器看不到自身输出的净效应）；在 **net 闭环反馈**下「该钳位不改变稳态目标跟踪」不成立——clamp 会压住控制器看到的高峰，诱发削峰减半的稳态偏移（见下文「net 反馈模型重标定」节）。
- **默认值改为 `true`**：两日控制后返送均低于基线、SOC 日终仍达地板，目标（控制后返送 ≤ 基线）达成。`TaiStorageConfig::s3_margin_limit` 默认置 `true`（2026-08-31）；新增单元测试 `test_s3_margin_limit_prevents_overshoot`（含 OFF 对照组）覆盖限幅路径。

### 回放结果（Task 8 补充：net 反馈模型重标定）

**模型修正（2026-08-31 规格审视）：** 原回放为 **gross（开环）模型**——把 xlsx 基线（总表 gross）直接作为表计测量送入控制器，储能输出只在 KPI 计算（`p_ctrl = p_total - p_st`）时扣除，控制器永远看不到自己输出的净效应。而运行时台区总表测量的是**净功率**（储能输出在测量环内，闭环）。故将回放改为 **net 反馈模型**，与运行时闭环一致：

- 新增 `last_p_out/last_q_out`（最近一次指令的分相 P/Q，初始 `[0;3]`）。
- 每个控制周期边界：`p_i_net[i] = p_i_base[i] - last_p_out[i]`、`q_i_net[i] = q_i_base[i] - last_q_out[i]`，`p_net_total = Σp_i_net`；DataPackage 以净值为表计测量（`active_power = p_net_total`，分相有功/无功 = 净分相值），电压/电流幅值/PF 取基线（简化），带符号电流方向以净分相有功符号承载：`i_sign[i] = p_i_net[i].signum() * i_mag[i]`。
- `evaluate_sync` 后更新 `last_p_out/last_q_out = cmd.phase_p_set/phase_q_set`。
- KPI 仍为净：`p_ctrl = p_total_base - p_st`；`p_st = Σ p_out_prev`（**生效间隔输出** = 更新前的 `last_p_out`，即上一控制点发出的命令，覆盖 `[t_{k-1}, t_k]`；控制周期行上 `evaluate_sync` 产生的新命令要到下一间隔才生效，故 KPI/SOC 记账用 `p_out_prev` 而非更新后的 `last_p_out`）；SOC 积分不变。

**对比网格（SOC 初值 0.50，soc_cap_day=0.70，s4_margin=0）：** `s3_margin_limit ∈ {OFF, ON}`。新增削峰诊断列（基线 > p_dis_trig=30kW 时段净进口均值、储能放电/充电能量、SOC 10% 地板占比、受电峰值）。

**6-27（低负荷日，基线 返送 6.2% / 返送峰值 25.3kW / 受电峰值 44.9kW）：**

| s3_margin | 返送时长% | 返送峰值kW | 返送能量kWh | SOC日终% | SOC峰值% | 削峰净进口均值kW | 放电kWh | 充电kWh | SOC地板% | 受电峰值kW |
|---|---|---|---|---|---|---|---|---|---|---|
| OFF | 5.6 | 21.4 | 7.0 | 10.0 | 50.0 | 14.6 | 55.7 | 7.8 | 18.0 | 41.5 |
| ON（默认） | **5.1** | 21.4 | 6.9 | 10.0 | 50.0 | 21.9 | 55.7 | 7.8 | **0.0** | 41.5 |

**7-04（高负荷日，基线 返送 16.5% / 返送峰值 57.0kW / 受电峰值 77.6kW）：**

| s3_margin | 返送时长% | 返送峰值kW | 返送能量kWh | SOC日终% | SOC峰值% | 削峰净进口均值kW | 放电kWh | 充电kWh | SOC地板% | 受电峰值kW |
|---|---|---|---|---|---|---|---|---|---|---|
| OFF | 13.2 | 52.0 | 34.8 | 10.0 | 50.0 | 46.5 | 89.2 | 41.2 | 30.6 | 77.6 |
| ON（默认） | **12.3** | 52.0 | 30.1 | 10.0 | 52.0 | 44.8 | 89.2 | 41.2 | 20.1 | 77.6 |

**结论与决定（规格审视预测的"限幅在 net 下破坏削峰"未发生）：**

- **gross-clamp 在 net 反馈下表现良好，保留原形式**：两日 ON 控制后返送均低于基线（6-27 5.1%<6.2%，7-04 12.3%<16.5%）、低于 OFF（5.1<5.6，12.3<13.2）；SOC 日终均达 10% 地板（日终清空不受影响）；未出现限幅/欠输送限循环（恒稳高峰下的稳态偏移为已知理论局限，见下）。
- **限幅反而避免电池过早放空**：6-27 ON 的 SOC 地板占比 0.0%（OFF 18.0%），放电能量两档相同（55.7 kWh）——OFF 提前放空后只能干坐，ON 把能量保留到后段峰期仍能放电；7-04 地板占比 20.1%（OFF 30.6%）。
- **受电峰值两档完全相同**（6-27 41.5 / 7-04 77.6）：限幅不造成需量峰值惩罚。7-04 受电峰值未削（77.6→77.6）是电池能量（SOC 地板）约束所致，非限幅问题。
- **6-27 ON 削峰净进口均值（21.9）高于 OFF（14.6）的解读**：这是 OFF 在负荷回落阶段**过冲到反向送电**（p_ctrl 为负值拉低均值，对应 OFF 返送 5.6%>ON 5.1%）造成的统计偏差，恰是限幅要拦截的过冲；非限幅导致的削峰欠输送。限幅下最大受电峰值与 OFF 相同、总放电能量相同，仅把"峰值期的过冲放电"转为"削峰+防返送"的合理分配。
- **理论稳态限循环（恒定高峰下 p_st 在 (base-5)/2 附近震荡）在实测数据中不显现**：两日高峰多为短时/被 SOC 地板约束，电池能量而非限幅成为高峰时段实际限制因素。更精确地说：在默认 `window_size=5` 平滑下，S3 clamp 在持续恒定高峰且 SOC 充足的场景下表现为**稳态偏移**——`p_st` 收敛到 `(P_表净 − p_tgt_s3)/2`（削峰减半的直流偏置），而非限循环震荡。真实数据中电池 SOC 地板先成为约束，该场景未显现；属已知理论局限。
- **决定：保留 gross-clamp 原形式（`p_st = min(p_st, (P_表净 - p_tgt_s3).max(0))`），`s3_margin_limit` 默认保持 `true`**，不改 `tai_storage.rs`/`config.rs`/`tai_storage_test.rs`。
- **注**：回放现已与运行时 net 闭环一致（上一节 gross 模型扫描数据作废，以此节为准）。

### 回放结果（Task 8 补充：S1 激进调参）

**目标：** 在既有（S3 专项 + net 反馈模型重标定）基础上，进一步压降返送——即把旋钮转向 **S1（光伏返送吸收）状态**。新增 4 个回放参数覆盖（位置在 `[s3_margin]` 之后）：`[p_abs_trig] [p_tgt_s1] [kp] [slope]`，默认分别 10.0 / 2.0 / 0.4 / 5.0。命令形如：

```bash
cargo run -p mupc-strategy-engine --bin tai_replay -- "E:/MUPC2/数据/2026_07_04_data_rule.xlsx" 0.50 <soc_cap> 0 1 <p_abs_trig> <p_tgt_s1> <kp> <slope>
```

**扫描网格（7-04，SOC 初值 0.50，s4_margin=0，s3_margin=1）：** A→F 为任务网格（每项叠加前项），G/H/I 为判别性补充（验证 `p_tgt_s1=0` vs `2`、`slope=6/8`、`soc_cap 0.70/0.80` 的边际效果）。基准=既有默认（10/2/0.4/5/0.70）。

| 组合 | p_abs_trig | p_tgt_s1 | kp | slope | soc_cap | 返送时长% | 返送峰值kW | 返送能量kWh | SOC日终% | SOC峰值% | 受电峰值kW |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 基准 | 10 | 2 | 0.4 | 5 | 0.70 | 12.3 | 52.0 | 30.1 | 10.0 | 52.0 | 77.6 |
| A | 5 | 2 | 0.4 | 5 | 0.70 | 11.1 | 52.0 | 25.5 | 10.0 | 56.8 | 77.6 |
| B | 5 | 0 | 0.4 | 5 | 0.70 | 11.5 | 52.0 | 25.0 | 10.0 | 55.3 | 77.6 |
| C | 5 | 0 | 0.6 | 5 | 0.70 | 11.0 | 52.0 | 25.8 | 10.0 | 56.8 | 77.6 |
| D | 5 | 0 | 0.6 | 8 | 0.70 | 10.8 | 50.3 | 23.8 | 10.0 | 57.8 | 77.6 |
| E | 5 | 0 | 0.6 | 8 | 0.80 | 10.8 | 50.3 | 23.8 | 10.0 | 57.8 | 77.6 |
| F | 2 | 0 | 0.6 | 8 | 0.80 | 10.3 | 49.1 | 23.4 | 10.0 | 58.8 | 77.6 |
| G | 5 | 2 | 0.6 | 8 | 0.70 | 10.5 | 50.3 | 23.2 | 10.0 | 60.4 | 77.6 |
| H | 2 | 2 | 0.6 | 8 | 0.80 | 9.6 | 47.9 | 23.9 | 10.0 | 62.0 | 77.6 |
| I（选中） | 2 | 2 | 0.6 | 6 | 0.70 | **9.4** | **48.6** | **22.9** | 10.0 | 61.7 | 77.6 |

（注：H 与 `trig2/tgt2/kp0.6/slope8/cap0.70` 完全同值，`soc_cap 0.70→0.80` 无边际差异——两日 SOC 峰值均 <70% 未触顶，故 soc_cap 保持默认 0.70。）

**选定组合（写入默认值）：** `p_abs_trig=2.0`、`p_tgt_s1=2.0`（不变，保留 +2 设计裕度）、`kp=0.6`、`slope=6.0`、`soc_cap_day=0.70`（不变）。

**两日结果（net 模型，SOC 初值 0.50）：**

| 日 | 指标 | 既有默认（调参前） | 新默认（选中组合） |
|---|---|---|---|
| 7-04 | 返送时长% | 12.3 | **9.4** |
| 7-04 | 返送峰值kW | 52.0 | **48.6** |
| 7-04 | 返送能量kWh | 30.1 | **22.9** |
| 7-04 | SOC 日终% | 10.0 | 10.0（完成清空） |
| 7-04 | SOC 峰值% | 52.0 | 61.7（<70 上限，留裕度） |
| 7-04 | 受电峰值kW | 77.6 | 77.6（削峰不牺牲） |
| 6-27 | 返送时长% | 5.1 | **4.0** |
| 6-27 | 返送峰值kW | 21.4 | **11.0** |
| 6-27 | 返送能量kWh | 6.9 | **3.8** |
| 6-27 | SOC 日终% | 10.0 | 10.2（≈地板） |
| 6-27 | 受电峰值kW | 41.5 | 41.5（削峰不牺牲） |

**关键发现：**
- **`p_tgt_s1=0`（吸到零进口）在两日回放中反而更差**：G（tgt=2）10.5% < D（tgt=0）10.8%，H（tgt=2）9.6% < F（tgt=0）10.3%。设计保留的 **+2kW 目标进口裕度不仅规避临界点振荡风险，且实测更优**——吸到 0 会把控制器推至临界点（±小扰动反复进出），故维持 `p_tgt_s1=2.0`。
- **`slope=8` 不成立**：（a）使 `test_arbitrate_recomputes_and_breaks` 失败——S2 斜坡 + step6 差模增量更粗（8kW/周期）使仲裁前 A 相 178.8A（<190）不触发裁剪，但 ΣΔP=0 重归一后升至 190.9A，超 `i_rated+1e-6` 严格断言；（b）6-27 返送峰值 16.6kW 反而劣于 slope=6 的 11.0kW。故取 **`slope=6.0`**（两日均更优且测试全绿）。
- **`p_abs_trig=10→2` 是最大收益旋钮**：A（11.1%）→ I（9.4%），配合 kp=0.6 进一步压峰值。
- **`soc_cap_day` 两日均无收益**（SOC 峰值 50%/61.7% 未触顶），保持 0.70。

**默认值变更（`mupc/crates/strategy-engine/src/config.rs`，2026-08-31）：**
- `p_abs_trig`: 10.0 → **2.0**（更强返送吸收）
- `kp`: 0.4 → **0.6**（更快共模响应）
- `slope`: 5.0 → **6.0**（更快斜坡；8.0 因 arbitrate 重归一过限 + 6-27 峰值劣化弃用）
- `p_tgt_s1`/`s1_exit`/`soc_cap_day`/`p_dis_trig`/`p_cap` 保持默认不变。

**权衡与风险（如实记录）：**
- `p_abs_trig=2.0` → S1 触发更频繁，**更多电池充放循环**（7-04 充电 53.8kWh vs 既有 41.2kWh），加速电池老化。
- `p_tgt_s1=0` 未采用——临界点振荡风险（设计保留 +2 裕度；回放也证明 +2 更优）。
- `kp=0.6` → 共模响应更快，**超调/临界点振铃风险略增**（回放数据未见振荡，但需实机观察）。
- `slope=6.0` → 斜坡更快，**响应过冲风险略增**；且步长更粗使 arbitrate 裁剪分辨率降低（6.0 恰好使重归一结果 ≤190A 达标，余量仅 0.5%）。
- 两日 SOC 日终均清空至 10% 地板（6-27 10.2% 为 0.24kWh 残量，可忽略），S4 硬约束不受影响；受电峰值两日均不劣化（削峰目标不受牺牲）。

---

## 自审记录

- **Spec 覆盖**：§15.7 数据接入 → Task 1；§15.8 核间 V3 → Task 3；§15.9 带状态控制器 → Task 4/5/6；§15.10 配置 → Task 4；§15.11 集成 → Task 7；§15.12 回放 → Task 8；§15.13 测试 → Task 5/6/8 内嵌；§15.14 依赖确认 → 已列在 Task 3（核间 V3 对接为外部依赖，实现后需与实时控制模块确认）。
- **占位符扫描**：无 TBD/TODO；所有代码步骤含完整实现。
- **类型一致性**：`control(state, config, meter, soc, t_now) -> ([f64;3],[f64;3])` 在 Task 5 定义、Task 6/8 调用一致；`TaiStorageConfig` 字段在 Task 4 定义、Task 5/8 使用一致；`phase_p_set/phase_q_set` 在 Task 2 定义、Task 6/7 使用一致。
