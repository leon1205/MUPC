# 修复批次（审查 P1/P2 未吸收项）· R1+R2+R3 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落实 2026-09-09 项目审查 P1/P2 中未被"AI 停用 + 国密框架化"吸收的独立修复项：R1（本地默认下发的可信与安全 5 项）+ R2（北向可信/部署诚实 2 项）+ R3 核心（鉴权降级诚实化 1 项）。DE/RE 真机极性项后置（需真机，另排）。

**Architecture:** 全部为聚焦单点的代码修复，不引入新架构。关键前置事实（2026-09-09 双 agent 核实）：本地 TaiStorage 分相下发已双重 clamp（evaluate 按档 + transport `clamp_phase` ±25，**确认安全**）；真正缺口是 IEC104 外部 p_set 经 1001 恒功率直通无校验；decisions 表与 insert 方法齐全但无生产调用方；AiIntegrator 无 storage 依赖（用回调注入最小侵入）；watchdog.rs 全仓无调用为死代码；mqtt-bridge 恒连假域名无 enabled 开关；RBAC 纯占位。

**Tech Stack:** Rust、tokio、strategy-engine/ai_integration、intercore（modbus transport）、mupc-core-bin/startup、southd、web-api/auth、mqtt-bridge、core/service_coord。

**⚠️ 前置门（GATED）**：Task 3 修改 `mupc/crates/intercore/src/transport/modbus.rs`——该文件为用户工作区既有 dirty（历史红线禁止触碰）。**执行 Task 3 前须用户处理 dirty（stash/commit）或明确授权**；未授权则跳过 Task 3 并在总结记录（其余 Task 不受阻）。

**执行基线（2026-09-09，commit 2846625）**：cargo 一律 `cd /e/MUPC2/mupc`；Windows 低内存 `-j 2`；commit 单行、精确 `git add <path>`、禁 push/amend/`-A/-u`、勿跑 `cargo fmt --all`。跨 crate 多文件 task 按仓库 Git 规则（>5 文件/200 行须评审——本批走 subagent 双审等效）。

---

## 文件结构

| Task | 文件 | 动作 |
|---|---|---|
| T1 | `mupc-crates/mupc-core-bin/src/startup.rs`（StrategyCommandHandler）+ `mupc-crates/ai-engine/src/pcs.rs`（如整机限幅函数收口） | 修改 |
| T2 | `strategy-engine/src/ai_integration.rs` + `mupc-core-bin/src/startup.rs` | 修改 |
| T3 | `intercore/src/transport/modbus.rs`（3 处毒锁） | 修改（GATED） |
| T4 | `intercore/src/watchdog.rs` + `intercore/src/lib.rs`（删除导出） | 删除/修改 |
| T5 | `strategy-engine/src/ai_integration.rs` | 修改 |
| T6 | `mupc-southd`（SouthSink/mapper 侧）+ `mupc-core-bin/src/startup.rs` + `core_config.rs` | 修改 |
| T7 | `mqtt-bridge/src/config.rs` + `mupc-core-bin/src/startup.rs` + `mupc-core-bin/src/core_config.rs` | 修改 |
| T8 | `web-api/src/auth.rs` + `web-api/src/routes/ai/auth.rs` + `web-api/src/routes/interlock.rs` + `mupc-core-bin/src/interlock.rs` | 修改 |

---

### Task 1: IEC104 外部指令校验 + clamp（R1-A1）

**Files:**
- Modify: `mupc/crates/mupc-core-bin/src/startup.rs`（`StrategyCommandHandler::handle_command`，约 :74-127）
- Modify（可选收口）: `mupc/crates/ai-engine/src/pcs.rs`

**背景**：外部主站 IEC104 控制（PowerRegulation）链路：gateway parse → `StrategyCommandHandler` → `DualParamCommand::new(p_set, k, ...)` → `send_dual_param` → modbus `modbus.rs:511` 写 1001 恒功率**无任何 clamp/校验**——p_set 无界直通 PCS，越 i16 靠 `as` 饱和截断（静默失真）。本地策略默认下发后，此为主站真实控制入口，须加防线。联锁闸门已有（:82-93 保留）。

- [ ] **Step 1: handler 持有容量限幅**

读 `StrategyCommandHandler` 定义与构造处（startup.rs 约 :600-615 附近 `StrategyCommandHandler { intercore, interlock }`）。给 handler 加字段 `p_max_kw: f64`（额定有功上限），构造时从已加载的 `tai_cfg`（约 :481-491 容量档）提取（如 `tai_cfg.p_cap` 或档位额定，读 TaiStorageConfig 结构确认字段名；无合适字段则用模块常量如 50.0 并注释来源，标注产品确认）。

- [ ] **Step 2: handle_command 加校验 clamp**

在 `PowerRegulation`/`ChargeDischarge` 分支构造 `DualParamCommand::new(p_set, ...)`（约 :95-101）**之前**插入：
```rust
            // 外部指令防护（审查 R1-A1，2026-09-09）：p_set 有限性 + 额定限幅——外部主站
            // 指令不再无界直通 PCS 1001（原越界仅靠 i16 as 饱和截断静默失真）。
            let p_set = match p_set {
                v if !v.is_finite() => { tracing::warn!("IEC104 p_set 非有限，拒绝下发: {v}"); return Ok(()); }
                v => v.clamp(-self.p_max_kw, self.p_max_kw),
            };
            if (p_set - raw_p_set).abs() > f64::EPSILON { tracing::warn!(...clamp 提示); }
```
（`raw_p_set` 为解析原值；若 handler 现无 p_set 局部变量名，读代码按实际变量名适配。q_set/k_value 现被丢弃的语义不改——仅防护主控制量。）

- [ ] **Step 3: 验证 + 提交**

```bash
cd /e/MUPC2/mupc && cargo check -p mupc-core-bin -j 2 2>&1 | tail -3
```
Expected：0 errors。可加单测（若 StrategyCommandHandler 有测试模块则仿照加 clamp 用例；无则靠 review）。
```bash
cd /e/MUPC2 && git add mupc/crates/mupc-core-bin/src/startup.rs
git commit -m "fix(core-bin): IEC104 外部指令 p_set 有限性校验 + 额定 clamp——不再无界直通 PCS 1001（审查 R1-A1）"
```

---

### Task 2: decisions 决策落库生产接入（R1-A3）

**Files:**
- Modify: `mupc/crates/strategy-engine/src/ai_integration.rs`（AiIntegrator 加可选决策回调 + send 成功后触发）
- Modify: `mupc/crates/mupc-core-bin/src/startup.rs`（注入 storage decisions 落库回调）

**背景**：`AiDecisionRecord`（storage models.rs:29-36）/表（services.rs:354）/insert（repository.rs:272-286）齐全但全仓无生产 insert——本地策略决策不可审计/回放。AiIntegrator 无 storage 依赖——用回调注入最小侵入，不改 crate 依赖方向。

- [ ] **Step 1: AiIntegrator 加决策回调字段 + setter**

`ai_integration.rs`：加字段与 setter。类型用同步闭包（落库异步由闭包内自 spawn，避免 async trait 与依赖引入）：
```rust
    /// 决策落库回调（审查 R1-A3，2026-09-09）：本地策略每拍实际下发成功后触发，
    /// 由启动侧注入 storage decisions 落库闭包。None = 不落库（默认）。
    decision_sink: RwLock<Option<Arc<dyn Fn(Option<f64>, Option<f64>) + Send + Sync>>>,
```
```rust
    /// 注入决策落库回调（phase_p_set, phase_q_set）
    pub async fn set_decision_sink(&self, sink: Arc<dyn Fn(Option<f64>, Option<f64>) + Send + Sync>) {
        *self.decision_sink.write().await = Some(sink);
    }
```
在 `new()` 初始化 `decision_sink: RwLock::new(None)`。

- [ ] **Step 2: 实际下发成功处触发**

`run_fallback_strategies`（约 :269-287）`send_tai_command(p, q, "fallback")` 的 **Ok 分支**（:276 else 内/Ok 后）触发回调：
```rust
                            if let Err(e) = client.send_tai_command(p, q, "fallback").await {
                                tracing::warn!("台区储能分相指令下发失败: {:?}", e);
                            } else {
                                tracing::debug!("台区储能分相指令已下发: p={:?}, q={:?}", p, q);
                                if let Some(sink) = self.decision_sink.read().await.as_ref() {
                                    sink(p, q);
                                }
                            }
```
（freshness 停发拍不 evaluate 不产生下发 → 不落库，语义正确。）

- [ ] **Step 3: startup 注入落库闭包**

`startup.rs` 决策循环装配段（约 :540-565，`let ai_integrator = Arc::new(...)` 之后）注入闭包。先读 `storage.decisions` 可用 insert API（DecisionRepository trait repository.rs:51-58 / insert 方法签名，storage 集成测试有范例）。注入：
```rust
    // 审查 R1-A3：本地策略决策落库（decisions 表，供 web /decisions 审计回放）
    let ai_decision_storage = storage.clone();
    ai_integrator
        .set_decision_sink(Arc::new(move |phase_p, phase_q| {
            let st = ai_decision_storage.clone();
            tokio::spawn(async move {
                // 用 storage decisions insert（构造 AiDecisionRecord：scene_type="local_tai"、
                // action_json={phase_p_set,phase_q_set}、confidence=1.0、model_version="local"）
                if let Err(e) = st.decisions.insert(/* 按实际 API */).await {
                    tracing::debug!("决策落库失败: {}", e);
                }
            });
        }))
        .await;
```
（`storage.decisions` 具体字段/方法以读代码为准；insert 签名若含 timestamp 用 now。）

- [ ] **Step 4: 验证 + 提交**

```bash
cd /e/MUPC2/mupc && cargo check --workspace -j 2 2>&1 | tail -3
```
Expected：0 errors。检查 strategy-engine 不新增对 storage 的依赖（回调注入保持了 crate 边界）。
```bash
cd /e/MUPC2 && git add mupc/crates/strategy-engine/src/ai_integration.rs mupc/crates/mupc-core-bin/src/startup.rs
git commit -m "feat(strategy/core-bin): 本地策略决策落库——AiIntegrator 回调注入 + send 成功后 insert decisions（审查 R1-A3）"
```

---

### Task 3: 核间实时路径毒锁（R1-B3）—— ⚠️ GATED

**Files:**
- Modify: `mupc/crates/intercore/src/transport/modbus.rs`（:203 mark_offline 写 / :404 run_heartbeat_loop 写 / :582 last_run_state 读 3 处 `.unwrap()`）

**背景**：std RwLock `.unwrap()` 在周期路径上若毒化即 panic 崩进程。AI 侧（action_validator/reward_calculator）已随 AI 停用不跑（低优先，随 AI 重启清）；本 task 只做核间实时路径 3 处（范本 ai_integration.rs:209/229 的 `unwrap_or_else(|e| e.into_inner())`）。

- [ ] **Step 1（前置，GATED）**：与用户确认可写 `intercore/transport/modbus.rs`（该文件为用户 dirty，历史红线禁止触碰）。用户已 stash/commit 其改动或明确授权后执行；否则**跳过本 Task**（其余不受阻），总结记录。

- [ ] **Step 2: 3 处毒锁改 `unwrap_or_else`**

读 :203/:404/:582，逐个 `.unwrap()` → `.unwrap_or_else(|e| e.into_inner())`（std sync RwLock 的 PoisonError::into_inner）。

- [ ] **Step 3: 验证 + 提交**

```bash
cd /e/MUPC2/mupc && cargo check -p mupc-intercore -j 2 2>&1 | tail -3
```
Expected：0 errors。
```bash
cd /e/MUPC2 && git add mupc/crates/intercore/src/transport/modbus.rs
git commit -m "fix(intercore): modbus transport 周期路径 3 处毒锁改 unwrap_or_else(into_inner)（审查 R1-B3）"
```

---

### Task 4: watchdog 死代码收敛（R1-B4）

**Files:**
- Modify/Delete: `mupc/crates/intercore/src/watchdog.rs`、`mupc/crates/intercore/src/lib.rs`（删除导出）

**背景**：`watchdog.rs` `trigger_reset`（:98）为空壳，全仓无 `Watchdog::new()`/`trigger_reset` 调用（仅 lib.rs:9/22 导出）→ 死代码。核间健康由 `modbus.rs` run_heartbeat_loop（PCS 3 区轮询 + M1 停机 + interlock stop）承担，职责已有归属。删除前核 `tcp_server.rs`（sim-bridge 联调）是否依赖 watchdog。

- [ ] **Step 1: 使用面核查**

```bash
cd /e/MUPC2 && grep -rn "Watchdog\|watchdog::\|trigger_reset" mupc/crates/intercore/src mupc/crates/ --include=*.rs
```
确认除 lib.rs 导出与 watchdog.rs 自身外无生产/测试调用（tcp_server.rs 也 grep——若 tcp_server 有 watchdog 依赖则**不删**，改为在 watchdog.rs:98 标注 TODO 并停；若无依赖继续 Step2）。

- [ ] **Step 2: 删除**

删除 `intercore/src/watchdog.rs` 文件；`lib.rs` 中 `pub mod watchdog;` 行与相关导出删除。若 `lib.rs:22` 附近 `pub use watchdog::...` 一并删。

- [ ] **Step 3: 验证 + 提交**

```bash
cd /e/MUPC2/mupc && cargo check --workspace -j 2 2>&1 | tail -3
```
Expected：0 errors。
```bash
cd /e/MUPC2 && git rm mupc/crates/intercore/src/watchdog.rs && git add mupc/crates/intercore/src/lib.rs
git commit -m "chore(intercore): 删 watchdog.rs 死代码空壳——核间健康由 modbus 心跳+interlock 承担（审查 R1-B4）"
```

---

### Task 5: TaiStorage 节流期空耗（R1-B6）

**Files:**
- Modify: `mupc/crates/strategy-engine/src/ai_integration.rs`

**背景**：`tai_storage.rs` evaluate 60s 节流命中时返回缓存 cmd，但 `ai_integration.rs:269-287` 每 dispatch 拍仍 `send_tai_command` 重写 6 个 4 区寄存器——空耗 RS485 带宽并放大抖动窗口。改：AiIntegrator 缓存上次实际下发的相位，值不变则跳过 send。

- [ ] **Step 1: 加 last_sent 缓存**

`ai_integration.rs` AiIntegrator 加字段（std Mutex，纯同步比对）：
```rust
    /// 节流期空耗防护（审查 R1-B6，2026-09-09）：上次实际下发的分相 P/Q，值不变跳过重发
    last_sent_tai: std::sync::Mutex<Option<(Option<f64>, Option<f64>)>>,
```
`new()` 初始化 `last_sent_tai: std::sync::Mutex::new(None)`。

- [ ] **Step 2: 比对跳过**

`run_fallback_strategies` 下发分支：`(Some(p), Some(q))` 时、`send_tai_command` 前比对：
```rust
                    (Some(p), Some(q)) => {
                        let mut last = self.last_sent_tai.lock().unwrap_or_else(|e| e.into_inner());
                        if *last == Some((p, q)) {
                            tracing::debug!("分相指令与上拍相同（节流期），跳过重发");
                        } else {
                            *last = Some((p, q));
                            drop(last);
                            if let Some(ref client) = self.intercore_client {
                                // ...原有 send_tai_command Ok/Err 处理，Ok 分支触发决策落库回调（T2）...
                            }
                        }
                    }
```
（保持 `(p,q)` 与上次完全相同时跳过。注意与 Task 2 落库回调交互：跳过拍不落库——需产品口径（跳过=未产生新决策）合理；若期望节流期也记录每拍则跳过拍也触发回调，**实现者读 Task2 完成后的代码协同**。默认：跳过拍不回调。）

- [ ] **Step 3: 验证 + 提交**

```bash
cd /e/MUPC2/mupc && cargo test -p mupc-strategy-engine -j 2 2>&1 | tail -3 && cargo check --workspace -j 2 2>&1 | tail -2
```
Expected：全绿 + 0 errors。
```bash
cd /e/MUPC2 && git add mupc/crates/strategy-engine/src/ai_integration.rs
git commit -m "perf(strategy-engine): TaiStorage 节流期相位不变跳过重发——消除每拍 RS485 空耗（审查 R1-B6）"
```

---

### Task 6: IEC104 上送 meter_grid 真值 + 端口 config（R2-A2）

**Files:**
- Modify: `mupc/crates/mupc-southd`（`SouthSink` 所在/或 `scheduler` 侧注入口）+ `mupc/crates/mupc-core-bin/src/startup.rs`（SouthSink 构造 + IEC104 server 注入）
- Modify: `mupc/crates/mupc-core-bin/src/core_config.rs`（gateway/iec104 端口段）

**背景**：IEC104 上送唯一来源是 pv/load 模拟假值（固定 380V/100A）；southd meter_grid 真值只经 `SouthSink::on_grid_package`→`set_latest_data` 进 AiIntegrator，**从未进 IEC104**。端口硬编码 2404。改：meter_grid 真值接入 IEC104 上送 + 端口读 config。IOA 点表先固定分配并标注现场追认。

- [ ] **Step 1: CoreConfig 加 gateway 段**

读 `core_config.rs` 顶层结构（约 :10-34）。新增 serde 结构（含默认）：
```rust
    pub gateway: GatewayConfig,
```
```rust
#[derive(Deserialize, Clone)]
pub struct GatewayConfig { pub listen_addr: String, pub listen_port: u16 }
impl Default for GatewayConfig { ... "0.0.0.0", 2404 ... }
```
（serde default 保证旧配置兼容。）

- [ ] **Step 2: startup 端口读 config + SouthSink 注入 iec104**

- IEC104 装配（约 :568-572 `Iec104Config { listen_addr:"0.0.0.0", listen_port:2404, ... }`）改为 `config.gateway.listen_addr/listen_port`。
- 读 SouthSink 定义（startup.rs 约 :240-260）与构造处。给 SouthSink 加可选 `iec104: Option<Arc<mupc_gateway::iec104::server::Iec104Server>>`（构造时注入，None 兼容测试）。
- `on_grid_package`（约 :252 收到 grid DataPackage）：在其处理内（或返回给 startup 的 grid 接收闭包内——实现者读实际接线定最适注入点），按固定 IOA 表把 `pkg.electrical` 的 `active_power/reactive_power/voltage/current/cos_phi/frequency`（取主量，分相可选暂不做）各 `encode_telemetry_asdu(ioa_seq++, v, 1)` + `broadcast_telemetry`。
- 频率控制：meter_grid 站采集周期下每拍上送频率——读 southd 站轮询周期，若 >100ms 需节流（如 1s 一拍）避免 flood；实现者以实际周期定（注释记录）。
- IOA 分配（先固定，文件注释标「现场点表追认」）：1=总P 2=总Q 3=U 4=I 5=cosφ 6=f（kW/kVAr/V/A/p.u./Hz，encode 单位按现有调用 v as f32 直接数值）。

- [ ] **Step 3: 验证 + 提交**

```bash
cd /e/MUPC2/mupc && cargo check --workspace -j 2 2>&1 | tail -3
```
Expected：0 errors（southd 若引入对 mupc-gateway 的依赖——确认 southd 依赖方向允许：gateway 是北向，southd 南向，引入反向依赖不当。**若 southd Cargo.toml 不宜依赖 gateway，改由 startup 层接线**：grid 接收回调留在 startup（其已持 iec104_server），在 startup 的 grid on_package 处理处直接 broadcast——SouthSink 不依赖 gateway，最稳。实现者按依赖方向定：优先 startup 层接线，勿让 southd 反向依赖 gateway）。
```bash
cd /e/MUPC2 && git add mupc/crates/mupc-core-bin/src/startup.rs mupc/crates/mupc-core-bin/src/core_config.rs
git commit -m "feat(core-bin): IEC104 上送 meter_grid 真值（固定 IOA 待现场追认）+ gateway 端口读 config（审查 R2-A2）"
```

---

### Task 7: MQTT enabled 开关 + stub 不谎报 Running（R2-B5）

**Files:**
- Modify: `mupc/crates/mqtt-bridge/src/config.rs`（North/Local 增 enabled，缺省 false）
- Modify: `mupc/crates/mupc-core-bin/src/core_config.rs`（增 mqtt 段，缺省 disabled）
- Modify: `mupc/crates/mupc-core-bin/src/startup.rs`（mqtt spawn 前查 enabled；security/wireless stub 标 Stopped）

**背景**：mqtt-bridge 恒用 Default（North `mqtt.example.com:8883` + dummy 证书）并被无条件 spawn（startup 约 :859-883）——生产会真连假域名。security（:348 附近）/wireless（:889 附近）stub 注册 `Running` 谎报。ServiceCoordinator 无 NotStarted 变体（core/service_coord.rs:9-15），最小诚实化用已有 `Stopped`。

- [ ] **Step 1: mqtt 配置 enabled**

读 `mqtt-bridge/src/config.rs`（约 :23-60）North/Local 配置结构。各加 `#[serde(default)] pub enabled: bool`（Default 实现补 `enabled: false`）。
读 `core_config.rs` 顶层，加 `pub mqtt_bridge: MqttBridgeConfig`（含 `north_enabled`/`local_enabled` 或整段 serde default false）——实现者按 config 现有风格定字段层级。

- [ ] **Step 2: startup 条件 spawn**

读 startup.rs mqtt-bridge 装配段（约 :855-890）：构造/ spawn 前加 `if config.mqtt_bridge.<enabled> { ... } else { tracing::warn!("mqtt-bridge 未启用（config.mqtt_bridge.enabled=false），跳过"); }`。未启用不 spawn、不注册 Running（或注册 Stopped）。

- [ ] **Step 3: security/wireless stub 诚实化**

startup.rs security 装配（约 :371-377）与 wireless（约 :914-917）的 `coord.register_service("security", ServiceStatus::Running)` / wireless 同——改为 `ServiceStatus::Stopped` + 注释「占位/Phase 2+，未实现不谎报 Running（审查 R2-B5）」。读 service_coord 枚举确认 Stopped 变体名（core/service_coord.rs:9-15）。

- [ ] **Step 4: 验证 + 提交**

```bash
cd /e/MUPC2/mupc && cargo check --workspace -j 2 2>&1 | tail -3
```
Expected：0 errors。
```bash
cd /e/MUPC2 && git add mupc/crates/mqtt-bridge/src/config.rs mupc/crates/mupc-core-bin/src/core_config.rs mupc/crates/mupc-core-bin/src/startup.rs
git commit -m "fix(mqtt/core-bin): MQTT enabled 开关（缺省禁）+ security/wireless stub 标 Stopped——不再真连假域名/谎报 Running（审查 R2-B5）"
```

---

### Task 8: 鉴权降级诚实化（R3-B1 核心）

**Files:**
- Modify: `mupc/crates/web-api/src/auth.rs`（login 角色与实际一致 + session 语义）
- Modify: `mupc/crates/web-api/src/routes/ai/auth.rs`（RequireRole 标注最严语义）
- Modify: `mupc/crates/web-api/src/routes/interlock.rs` + `mupc/crates/mupc-core-bin/src/interlock.rs`（release/ack 审计带操作者）

**背景**：RBAC 真实化（users/session 落库）另项（技术债 U-01）。本 task 做降级诚实化：登录响应 role 与实际（仅 admin 可登录）一致；RequireRole 显式标注"角色未实现=恒最严 Admin"；联锁释放/急停审计记录操作者（从 session）。

- [ ] **Step 1: login role 语义一致**

读 `web-api/src/auth.rs`（约 :74-119 login：仅 `username=="admin"` 可登；:207-211 响应硬编码 `role:"operator"`）。改为响应 `role:"admin"`（与实际一致）+ 注释「角色未分层（技术债 U-01）：当前仅 admin 账号可登，硬编码 admin 反映实际」。若 session 结构可存 username（读 :74 附近），把 username 存入 session。

- [ ] **Step 2: RequireRole 标注最严**

`routes/ai/auth.rs` RequireRole 提取器（约 :82-93）：保留行为（有效 session 即 Admin），加 doc 注释「角色模型未实现（技术债 U-01）：恒返回最严 Admin，勿据此做降权判断」。若 RequireRole 可携带 username（读实现），暴露 getter 供 handler 审计用。

- [ ] **Step 3: interlock release/ack 审计带操作者**

读 web-api `routes/interlock.rs`（release/ack_m1，约 :35/52 用 `_op: RequireRole`）与 core-bin `interlock.rs`（do_request_release 约 :925-985，含 record_event）。改：RequireRole 提取 username（若 Step1/2 已存）→ 传入 `record_event`/审计的 operator 字段；username 拿不到则用 `"session:<id截断>"` 或 `"operator(admin-会话)"`（读现有 record_event 签名定字段）。核心目标：审计留痕含操作者身份，不再匿名。

- [ ] **Step 4: 验证 + 提交**

```bash
cd /e/MUPC2/mupc && cargo check --workspace -j 2 2>&1 | tail -3 && cargo test -p mupc-web-api -j 2 2>&1 | tail -3
```
Expected：0 errors；web-api 既有测试不破（auth/interlock handler 若签名变化影响测试则同步适配）。
```bash
cd /e/MUPC2 && git add mupc/crates/web-api/src/auth.rs mupc/crates/web-api/src/routes/ai/auth.rs mupc/crates/web-api/src/routes/interlock.rs mupc/crates/mupc-core-bin/src/interlock.rs
git commit -m "fix(web/core-bin): 鉴权降级诚实化——login role 与实际一致 + RequireRole 标注最严 + 联锁释放审计带操作者（审查 R3-B1，真实 RBAC 另项 U-01）"
```

---

## Self-Review 对照（审查 P1/P2 清单 → 本计划）

| 审查项 | 计划落点 |
|---|---|
| P1-1 AiValidator 空壳（AI 路径） | 已随 AI 停用归档（validator 不在 dispatch）；本批补外部指令防护 T1 |
| P1-1 剩余：IEC104 p_set 无校验直通 | T1（有限性 + clamp） |
| P1-3 clamp 不一致（本地路径） | 已确认分相双重 clamp 安全；1001 恒功率缺口并入 T1 |
| P1-4 IEC104 上送假值/端口 | T6（meter_grid 真值 + config 端口） |
| P1-5 decisions 无生产者 | T2（回调落库） |
| P1-6 联锁/Web 鉴权 | T8（降级诚实化；真实 RBAC 另项） |
| P1-2 应急绕过（AI 停用后不可达） | 已吸收，无需修（归档） |
| P2 DE/RE 半双工 | 后置（真机极性待测，另排），非本批 |
| P2 毒锁（核间实时路径） | T3（GATED：用户 dirty 授权后执行） |
| P2 watchdog 空壳 | T4（死代码删除，核 tcp_server 无依赖） |
| P2 stub 标 Running / MQTT 假域名 | T7（MQTT enabled + stub Stopped） |
| P2 TaiStorage 节流空耗 | T5（相位比对跳过） |
| P2 termios RAII / unsafe SAFETY | 未列本批（低危，随后续/DE-RE 批）；AiStorage/scheduler panic 观测另排 |

**Placeholder/一致性**：无 TBD。改动点基于 2026-09-09 双 agent 核实地图（file:line 已探）。Task 间依赖：T2 与 T5 同文件（ai_integration.rs）顺序执行避免冲突；T6 依赖方向约束（southd 不反向依赖 gateway，接线在 startup 层）。**Task 3 GATED**：执行前须用户处理 `intercore/transport/modbus.rs` dirty 或授权。IOA 点表/额定限幅值为占位常量，现场/产品追认（注释标注）。DE/RE、termios RAII、真实 RBAC、scheduler supervisor 明确另排。
