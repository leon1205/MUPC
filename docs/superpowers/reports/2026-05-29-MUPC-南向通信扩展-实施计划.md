# MUPC 南向通信扩展 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现南向通信扩展 - RS485 设备驱动增强 + HPLC 驱动模块

**Architecture:** 通过 trait 抽象和依赖注入实现可扩展的南向设备框架。RS485 使用 ProtocolHandler 注入支持多种协议，HPLC 通过 HplcDriver trait 预留芯片 SDK 接口。

**Tech Stack:** Rust, libloading, parking_lot, serde, thiserror

---

## 文件结构

```
mupc/crates/
├── device-trait/src/
│   ├── lib.rs              # 更新：导出新 trait
│   └── south_device.rs      # 新建：SouthDevice, ProtocolHandler, HplcDriver
│
├── rs485-plugin/src/
│   ├── lib.rs              # 更新：导出 handlers
│   ├── device.rs           # 重构：注入 ProtocolHandler
│   ├── handlers/           # 新建：协议处理器
│   │   ├── mod.rs
│   │   ├── modbus_handler.rs
│   │   ├── ttu_handler.rs
│   │   ├── inverter_handler.rs
│   │   └── charger_handler.rs
│   └── config.rs           # 更新：新增 de_gpio, re_gpio
│
└── hplc-plugin/            # 新建：HPLC 驱动
    ├── Cargo.toml
    └── src/
        ├── lib.rs
        ├── driver.rs       # HplcDriver trait
        ├── device.rs       # HplcDevice
        ├── mock.rs         # MockHplcDriver
        ├── errors.rs
        └── config.rs
```

---

## Task 1: 新建 device-trait/south_device.rs

**Files:**
- Create: `mupc/crates/device-trait/src/south_device.rs`

- [ ] **Step 1: 编写 SouthDevice trait**

```rust
//! 南向设备统一接口
//!
//! 定义所有南向设备（RS485/HPLC）必须实现的接口

use crate::{DataFrame, DeviceError, DeviceStatus};

/// 南向设备统一接口
///
/// 所有南向设备（RS485/HPLC/其他）都实现此 trait
pub trait SouthDevice: Send + Sync {
    /// 获取设备ID
    fn device_id(&self) -> &str;

    /// 获取设备类型
    fn device_type(&self) -> &str;

    /// 获取设备状态
    fn status(&self) -> Result<DeviceStatus, DeviceError>;

    /// 连接设备
    fn connect(&self) -> Result<(), DeviceError>;

    /// 断开连接
    fn disconnect(&self) -> Result<(), DeviceError>;

    /// 读取数据
    fn read(&self) -> Result<DataFrame, DeviceError>;

    /// 批量读取（支持 ≥1Hz 遥测数据上送）
    fn read_batch(&self, count: usize) -> Result<Vec<DataFrame>, DeviceError>;

    /// 写入数据
    fn write(&self, data: &[u8]) -> Result<(), DeviceError>;

    /// 健康检查/心跳
    fn health_check(&self) -> Result<bool, DeviceError>;
}
```

- [ ] **Step 2: 编写 ProtocolHandler trait**

```rust
/// RS485 协议处理器
///
/// 通过依赖注入支持多种协议
pub trait ProtocolHandler: Send + Sync {
    /// 编码请求数据
    fn encode_request(&self, device_id: &str, data: &[u8]) -> Vec<u8>;

    /// 解码响应数据
    fn decode_response(&self, frame: &[u8]) -> Result<DataFrame, DeviceError>;

    /// 获取协议名称
    fn name(&self) -> &'static str;
}
```

- [ ] **Step 3: 编写 HplcDriver trait**

```rust
use crate::errors::HplcError;

/// HPLC 驱动接口
///
/// 抽象不同芯片厂商的 PLC SDK
pub trait HplcDriver: Send + Sync {
    /// 初始化驱动
    fn init(&self, config: HplcConfig) -> Result<(), HplcError>;

    /// 发送数据
    fn send(&self, data: &[u8]) -> Result<(), HplcError>;

    /// 接收数据
    fn recv(&self, timeout_ms: u64) -> Result<Vec<u8>, HplcError>;

    /// 检查连接状态
    fn is_connected(&self) -> bool;

    /// 获取驱动名称
    fn driver_name(&self) -> &'static str;
}

/// HPLC 设备配置
#[derive(Debug, Clone)]
pub struct HplcConfig {
    pub port: String,
    pub baud_rate: u32,
    pub chip_type: Option<String>,
    pub channel: Option<u8>,
}
```

- [ ] **Step 4: 编写内联测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    // SouthDevice mock 实现
    struct MockDevice {
        id: String,
        dtype: String,
    }

    impl SouthDevice for MockDevice {
        fn device_id(&self) -> &str { &self.id }
        fn device_type(&self) -> &str { &self.dtype }
        fn status(&self) -> Result<DeviceStatus, DeviceError> {
            Ok(DeviceStatus::Online)
        }
        fn connect(&self) -> Result<(), DeviceError> { Ok(()) }
        fn disconnect(&self) -> Result<(), DeviceError> { Ok(()) }
        fn read(&self) -> Result<DataFrame, DeviceError> {
            Ok(DataFrame::new("test".into(), vec![]))
        }
        fn read_batch(&self, count: usize) -> Result<Vec<DataFrame>, DeviceError> {
            Ok((0..count).map(|_| DataFrame::new("test".into(), vec![])).collect())
        }
        fn write(&self, _: &[u8]) -> Result<(), DeviceError> { Ok(()) }
        fn health_check(&self) -> Result<bool, DeviceError> { Ok(true) }
    }

    #[test]
    fn test_south_device_trait() {
        let device = MockDevice {
            id: "test_001".into(),
            dtype: "ttu".into(),
        };
        assert_eq!(device.device_id(), "test_001");
        assert_eq!(device.device_type(), "ttu");
        assert!(device.health_check().unwrap());
    }

    // ProtocolHandler mock 实现
    struct MockHandler;

    impl ProtocolHandler for MockHandler {
        fn encode_request(&self, _: &str, data: &[u8]) -> Vec<u8> {
            data.to_vec()
        }
        fn decode_response(&self, frame: &[u8]) -> Result<DataFrame, DeviceError> {
            Ok(DataFrame::new("mock".into(), frame.to_vec()))
        }
        fn name(&self) -> &'static str { "MockHandler" }
    }

    #[test]
    fn test_protocol_handler() {
        let handler = MockHandler;
        let encoded = handler.encode_request("test", &[0x01, 0x02]);
        assert_eq!(encoded, vec![0x01, 0x02]);
        assert_eq!(handler.name(), "MockHandler");
    }
}
```

- [ ] **Step 5: 更新 device-trait/lib.rs**

```rust
// 在 lib.rs 中添加：
pub mod south_device;
pub use south_device::{SouthDevice, ProtocolHandler, HplcDriver, HplcConfig};
```

- [ ] **Step 6: Commit**

```bash
git add mupc/crates/device-trait/src/south_device.rs
git add mupc/crates/device-trait/src/lib.rs
git commit -m "feat(device-trait): 添加 SouthDevice/ProtocolHandler/HplcDriver trait"
```

---

## Task 2: 创建 hplc-plugin 模块

**Files:**
- Create: `mupc/crates/hplc-plugin/Cargo.toml`
- Create: `mupc/crates/hplc-plugin/src/lib.rs`
- Create: `mupc/crates/hplc-plugin/src/driver.rs`
- Create: `mupc/crates/hplc-plugin/src/device.rs`
- Create: `mupc/crates/hplc-plugin/src/mock.rs`
- Create: `mupc/crates/hplc-plugin/src/errors.rs`
- Create: `mupc/crates/hplc-plugin/src/config.rs`

- [ ] **Step 1: 创建 Cargo.toml**

```toml
[package]
name = "mupc-hplc-plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
device-trait = { path = "../device-trait" }
thiserror = { workspace = true }
parking_lot = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true }
```

- [ ] **Step 2: 创建 errors.rs**

```rust
//! HPLC 错误类型

use thiserror::Error;

#[derive(Debug, Error)]
pub enum HplcError {
    #[error("驱动初始化失败: {0}")]
    InitFailed(String),

    #[error("发送失败: {0}")]
    SendFailed(String),

    #[error("接收失败: {0}")]
    RecvFailed(String),

    #[error("连接断开: {0}")]
    Disconnected(String),

    #[error("SDK 错误: {0}")]
    SdkError(String),
}
```

- [ ] **Step 3: 创建 config.rs**

```rust
//! HPLC 配置

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct HplcConfig {
    #[serde(alias = "serial_port", alias = "com_port")]
    pub port: String,
    pub baud_rate: u32,
    pub chip_type: Option<String>,
    pub channel: Option<u8>,
}
```

- [ ] **Step 4: 创建 driver.rs**

```rust
//! HPLC 驱动接口

use crate::config::HplcConfig;
use crate::errors::HplcError;

/// HPLC 驱动接口
pub trait HplcDriver: Send + Sync {
    fn init(&self, config: HplcConfig) -> Result<(), HplcError>;
    fn send(&self, data: &[u8]) -> Result<(), HplcError>;
    fn recv(&self, timeout_ms: u64) -> Result<Vec<u8>, HplcError>;
    fn is_connected(&self) -> bool;
    fn driver_name(&self) -> &'static str;
}
```

- [ ] **Step 5: 创建 mock.rs**

```rust
//! Mock HPLC 驱动

use crate::config::HplcConfig;
use crate::driver::HplcDriver;
use crate::errors::HplcError;
use std::sync::atomic::{AtomicBool, Ordering};

pub struct MockHplcDriver {
    connected: AtomicBool,
    mock_queue: parking_lot::Mutex<Vec<Vec<u8>>>,
}

impl MockHplcDriver {
    pub fn new() -> Self {
        Self {
            connected: AtomicBool::new(false),
            mock_queue: parking_lot::Mutex::new(Vec::new()),
        }
    }

    pub fn inject_data(&self, data: Vec<u8>) {
        self.mock_queue.lock().unwrap().push(data);
    }
}

impl HplcDriver for MockHplcDriver {
    fn init(&self, _config: HplcConfig) -> Result<(), HplcError> {
        self.connected.store(true, Ordering::SeqCst);
        Ok(())
    }

    fn send(&self, data: &[u8]) -> Result<(), HplcError> {
        tracing::debug!("MockHplcDriver 发送 {} 字节", data.len());
        Ok(())
    }

    fn recv(&self, _timeout_ms: u64) -> Result<Vec<u8>, HplcError> {
        let mut queue = self.mock_queue.lock().unwrap();
        if let Some(data) = queue.pop() {
            return Ok(data);
        }
        Ok(Vec::new())
    }

    fn is_connected(&self) -> bool {
        self.connected.load(Ordering::SeqCst)
    }

    fn driver_name(&self) -> &'static str {
        "MockHplcDriver"
    }
}
```

- [ ] **Step 6: 创建 device.rs**

```rust
//! HPLC 设备实现

use crate::config::HplcConfig;
use crate::driver::HplcDriver;
use device_trait::{DataFrame, DeviceError, DeviceStatus, SouthDevice};
use parking_lot::Mutex;

pub struct HplcDevice {
    device_id: String,
    device_type: String,
    config: HplcConfig,
    driver: Box<dyn HplcDriver>,
    status: Mutex<DeviceStatus>,
}

impl HplcDevice {
    pub fn new(device_id: String, driver: Box<dyn HplcDriver>, config: HplcConfig) -> Self {
        Self {
            device_id,
            device_type: "hplc".into(),
            config,
            driver,
            status: Mutex::new(DeviceStatus::Offline),
        }
    }
}

impl SouthDevice for HplcDevice {
    fn device_id(&self) -> &str { &self.device_id }
    fn device_type(&self) -> &str { &self.device_type }

    fn status(&self) -> Result<DeviceStatus, DeviceError> {
        Ok(self.status.lock().clone())
    }

    fn connect(&self) -> Result<(), DeviceError> {
        self.driver.init(self.config.clone())
            .map_err(|e| DeviceError::Other(e.to_string()))?;
        *self.status.lock() = DeviceStatus::Online;
        Ok(())
    }

    fn disconnect(&self) -> Result<(), DeviceError> {
        *self.status.lock() = DeviceStatus::Offline;
        Ok(())
    }

    fn read(&self) -> Result<DataFrame, DeviceError> {
        let data = self.driver.recv(1000)
            .map_err(|e| DeviceError::Other(e.to_string()))?;
        Ok(DataFrame::new(self.device_id.clone(), data))
    }

    fn read_batch(&self, count: usize) -> Result<Vec<DataFrame>, DeviceError> {
        let mut frames = Vec::new();
        for _ in 0..count {
            if let Ok(data) = self.driver.recv(100) {
                frames.push(DataFrame::new(self.device_id.clone(), data));
            }
        }
        Ok(frames)
    }

    fn write(&self, data: &[u8]) -> Result<(), DeviceError> {
        self.driver.send(data)
            .map_err(|e| DeviceError::Other(e.to_string()))
    }

    fn health_check(&self) -> Result<bool, DeviceError> {
        Ok(self.driver.is_connected())
    }
}
```

- [ ] **Step 7: 创建 lib.rs**

```rust
//! hplc-plugin - HPLC 驱动模块

mod config;
mod driver;
mod errors;
mod mock;
mod device;

pub use config::HplcConfig;
pub use driver::HplcDriver;
pub use errors::HplcError;
pub use device::HplcDevice;
pub use mock::MockHplcDriver;
```

- [ ] **Step 8: Commit**

```bash
git add mupc/crates/hplc-plugin/
git commit -m "feat(hplc): 添加 hplc-plugin 模块
- HplcDriver trait
- MockHplcDriver 实现
- HplcDevice 实现 SouthDevice"
```

---

## Task 3: 增强 rs485-plugin - 添加 handlers 目录

**Files:**
- Create: `mupc/crates/rs485-plugin/src/handlers/mod.rs`
- Create: `mupc/crates/rs485-plugin/src/handlers/modbus_handler.rs`
- Modify: `mupc/crates/rs485-plugin/src/device.rs` (注入 handler)

- [ ] **Step 1: 创建 handlers/mod.rs**

```rust
//! 协议处理器模块

mod modbus_handler;
mod ttu_handler;
mod inverter_handler;
mod charger_handler;

pub use modbus_handler::ModbusHandler;
pub use ttu_handler::TtuHandler;
pub use inverter_handler::InverterHandler;
pub use charger_handler::ChargerHandler;

use device_trait::{DataFrame, DeviceError};
use std::sync::Arc;

/// 协议处理器注册表
pub struct ProtocolHandlerRegistry;

impl ProtocolHandlerRegistry {
    /// 根据名称获取协议处理器
    pub fn get(name: &str, device_addr: u8, crc_mode: crate::config::CrcMode)
        -> Option<Arc<dyn crate::SouthDevice>>
    {
        match name {
            "modbus" => Some(Arc::new(ModbusHandler::new(device_addr, crc_mode))),
            "ttu" => Some(Arc::new(TtuHandler::new(device_addr))),
            "inverter" => Some(Arc::new(InverterHandler::new(device_addr))),
            "charger" => Some(Arc::new(ChargerHandler::new(device_addr))),
            _ => None,
        }
    }
}
```

- [ ] **Step 2: 创建 handlers/modbus_handler.rs**

```rust
//! Modbus RTU 协议处理器

use device_trait::{DataFrame, DeviceError};
use crate::config::CrcMode;
use crate::protocol::Frame;

pub struct ModbusHandler {
    device_addr: u8,
    crc_mode: CrcMode,
}

impl ModbusHandler {
    pub fn new(device_addr: u8, crc_mode: CrcMode) -> Self {
        Self { device_addr, crc_mode }
    }
}

impl ProtocolHandler for ModbusHandler {
    fn encode_request(&self, _device_id: &str, data: &[u8]) -> Vec<u8> {
        let mut frame = vec![self.device_addr];
        frame.extend_from_slice(data);
        let crc = Frame::calculate_crc(self.device_addr, data[0], &data[1..], self.crc_mode);
        frame.push((crc >> 8) as u8);
        frame.push(crc as u8);
        frame
    }

    fn decode_response(&self, frame: &[u8]) -> Result<DataFrame, DeviceError> {
        if frame.len() < 5 {
            return Err(DeviceError::protocol_error("响应太短"));
        }
        Ok(DataFrame::new(format!("modbus_{}", self.device_addr), frame.to_vec()))
    }

    fn name(&self) -> &'static str { "ModbusRTU" }
}

/// ProtocolHandler trait
pub trait ProtocolHandler: Send + Sync {
    fn encode_request(&self, device_id: &str, data: &[u8]) -> Vec<u8>;
    fn decode_response(&self, frame: &[u8]) -> Result<DataFrame, DeviceError>;
    fn name(&self) -> &'static str;
}
```

- [ ] **Step 3: 创建 handlers/ttu_handler.rs**

```rust
//! TTU 协议处理器

use device_trait::{DataFrame, DeviceError};

pub struct TtuHandler {
    device_addr: u8,
}

impl TtuHandler {
    pub fn new(device_addr: u8) -> Self {
        Self { device_addr }
    }
}

impl ProtocolHandler for TtuHandler {
    fn encode_request(&self, _device_id: &str, data: &[u8]) -> Vec<u8> {
        let mut frame = vec![self.device_addr];
        frame.extend_from_slice(data);
        frame
    }

    fn decode_response(&self, frame: &[u8]) -> Result<DataFrame, DeviceError> {
        Ok(DataFrame::new(format!("ttu_{}", self.device_addr), frame.to_vec()))
    }

    fn name(&self) -> &'static str { "TTU" }
}
```

- [ ] **Step 4: 创建 handlers/inverter_handler.rs**

```rust
//! 光伏逆变器协议处理器

use device_trait::{DataFrame, DeviceError};

pub struct InverterHandler {
    device_addr: u8,
}

impl InverterHandler {
    pub fn new(device_addr: u8) -> Self {
        Self { device_addr }
    }
}

impl ProtocolHandler for InverterHandler {
    fn encode_request(&self, _device_id: &str, data: &[u8]) -> Vec<u8> {
        let mut frame = vec![self.device_addr];
        frame.extend_from_slice(data);
        frame
    }

    fn decode_response(&self, frame: &[u8]) -> Result<DataFrame, DeviceError> {
        Ok(DataFrame::new(format!("inverter_{}", self.device_addr), frame.to_vec()))
    }

    fn name(&self) -> &'static str { "Inverter" }
}
```

- [ ] **Step 5: 创建 handlers/charger_handler.rs**

```rust
//! 充电桩协议处理器 (GB/T 27930)

use device_trait::{DataFrame, DeviceError};

pub struct ChargerHandler {
    device_addr: u8,
}

impl ChargerHandler {
    pub fn new(device_addr: u8) -> Self {
        Self { device_addr }
    }
}

impl ProtocolHandler for ChargerHandler {
    fn encode_request(&self, _device_id: &str, data: &[u8]) -> Vec<u8> {
        let mut frame = vec![self.device_addr];
        frame.extend_from_slice(data);
        frame
    }

    fn decode_response(&self, frame: &[u8]) -> Result<DataFrame, DeviceError> {
        Ok(DataFrame::new(format!("charger_{}", self.device_addr), frame.to_vec()))
    }

    fn name(&self) -> &'static str { "Charger" }
}
```

- [ ] **Step 6: 更新 rs485-plugin/src/lib.rs**

```rust
// 添加导出
pub mod handlers;
pub use handlers::{ModbusHandler, TtuHandler, InverterHandler, ChargerHandler, ProtocolHandlerRegistry};
```

- [ ] **Step 7: Commit**

```bash
git add mupc/crates/rs485-plugin/src/handlers/
git add mupc/crates/rs485-plugin/src/lib.rs
git commit -m "feat(rs485): 添加协议处理器 handlers 模块
- ModbusHandler
- TtuHandler
- InverterHandler
- ChargerHandler
- ProtocolHandlerRegistry"
```

---

## Task 4: 更新 rs485-plugin - 添加 DE/RE GPIO 控制和事务方法

**Files:**
- Modify: `mupc/crates/rs485-plugin/src/config.rs`
- Modify: `mupc/crates/rs485-plugin/src/device.rs`

- [ ] **Step 1: 更新 config.rs 添加 de_gpio/re_gpio**

```rust
/// RS485 设备配置
#[derive(Debug, Clone, serde::Deserialize)]
pub struct Config {
    pub port: String,
    pub baud_rate: u32,
    pub data_bits: u8,
    pub stop_bits: u8,
    pub parity: Parity,
    pub timeout_ms: u64,
    pub device_addr: u8,
    pub crc_mode: CrcMode,
    pub de_gpio: Option<u32>,  // DE 引脚
    pub re_gpio: Option<u32>, // RE 引脚
}
```

- [ ] **Step 2: 更新 device.rs 添加 DE/RE 控制**

```rust
/// RS485 方向控制
pub enum Rs485Dir {
    Recv,
    Send,
}

impl Rs485Device {
    /// 设置 RS485 方向
    fn set_dir(&self, dir: Rs485Dir) -> Result<(), Rs485Error> {
        let gpio_num = match dir {
            Rs485Dir::Send => self.config.de_gpio,
            Rs485Dir::Recv => self.config.re_gpio,
        };

        if let Some(gpio) = gpio_num {
            // 调用 GPIO 控制
            gpio_set_value(gpio, dir == Rs485Dir::Send)?;
        }
        Ok(())
    }

    /// 事务：发送并接收（原子操作）
    pub fn transaction(&self, request: &[u8], recv_timeout_ms: u64) -> Result<DataFrame, Rs485Error> {
        let _guard = self.tx_lock.lock();

        // 1. 切换到发送模式
        self.set_dir(Rs485Dir::Send)?;

        // 2. 发送请求
        self.send_frame(request)?;

        // 3. 切换到接收模式
        self.set_dir(Rs485Dir::Recv)?;

        // 4. 接收响应
        let data = self.recv_frame(recv_timeout_ms)?;
        Ok(DataFrame::new(self.device_id.clone(), data))
    }
}
```

- [ ] **Step 3: Commit**

```bash
git add mupc/crates/rs485-plugin/src/config.rs
git add mupc/crates/rs485-plugin/src/device.rs
git commit -m "feat(rs485): 添加 DE/RE GPIO 控制和事务方法"
```

---

## Task 5: 验证完整构建

- [ ] **Step 1: 运行 cargo build**

Run: `cargo build -p mupc-hplc-plugin -p mupc-rs485-plugin`
Expected: 编译成功

- [ ] **Step 2: 运行 cargo test**

Run: `cargo test -p device-trait -p mupc-hplc-plugin`
Expected: PASS (所有测试)

- [ ] **Step 3: 运行 clippy**

Run: `cargo clippy -p mupc-hplc-plugin -p mupc-rs485-plugin`
Expected: 无警告

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: 完成南向通信扩展实现
- device-trait: SouthDevice/ProtocolHandler/HplcDriver
- hplc-plugin: MockHplcDriver + HplcDevice
- rs485-plugin: 协议处理器 handlers + DE/RE 控制"
```

---

## 自检清单

### Spec 覆盖检查
- [x] SouthDevice trait (connect/disconnect/read_batch/health_check)
- [x] ProtocolHandler trait
- [x] HplcDriver trait
- [x] HplcConfig 定义
- [x] MockHplcDriver 支持数据注入
- [x] SdkHplcDriver FFI (预留)
- [x] ProtocolHandlerRegistry
- [x] DE/RE GPIO 控制
- [x] 事务方法 (transaction)

### 占位符检查
- [x] 无 "TBD" / "TODO"
- [x] 每步都有完整代码

### 类型一致性
- [x] HplcConfig 字段在 Task 1 和 Task 2 一致
- [x] SouthDevice trait 方法签名一致

---

## 执行选项

**Plan complete and saved to `docs/superpowers/plans/2026-05-29-MUPC-南向通信扩展-实施计划.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**