# S3b-2「站级南向设备语义点表集成」代码评审报告

`[CODE_REVIEWED: PASS: 2026-09-22]`

> **本报告是项目级最终代码评审门禁**（不是单 Task 的中间评审）。评审对象是特性 **S3b-2** 的
> **T1–T7 全部实现**（23 个提交，`518a4e2` … `db54c47`），分支 `feature/s3b-2-south-point-table`
> （自 `master` 分叉，**未 push**）。各 Task 的中间评审结论（T3 返工一次后 PASS、T5/T6 各有按裁定补改）
> **不代替本次评审**：本报告对**整个实现**重新取证，不采信自述。

---

## 0. 方法与边界（先说清楚，避免误读）

- **评审基准（合同）**：
  - 需求：`docs/superpowers/specs/modules/02-MUPC-南向通信-PRD.md` **§9**（`[REVIEWED: PASS: 2026-09-21]`，含 v1.7 补登 / v1.8 订正）
  - 设计：`docs/superpowers/plans/modules/02-MUPC-南向通信-设计文档.md` **§11**（`[DESIGN_APPROVED: 2026-09-21]`，含 v1.5–v1.8 四次勘误）
- **方法（不采信自述）**：每一条主张执行「**grep 到落点 / 跑用例 / 读源码 / git 独立验证**」三选一以上；
  跨 Task 一致性与回归锚**一律用 `git diff master...HEAD` 独立核对**，不只看测试绿。
- **本机无法验证的**（真机 RC-1…RC-12）**一律不写"通过"**，集中列于 §9。
- **探针纪律**：判别性验证所用的临时改动**已全部还原**，评审结束时 `git status` 除会话开始即存在的
  未跟踪文件外**无任何残留**（§6 有复核命令）。

---

## 1. 裁决

**通过。** 本报告即本特性的**代码评审合同**，标记落于本行上方。
严重问题 **0**；警告 **2**（均为契约口径/登记类，非代码缺陷）；优化建议 **3**。
进入测试阶段（本机 AC 部分已可测；真机 RC 部分见 §9）。

---

## 2. AC-1…AC-7 逐条落点核对（穷举，无缺项）

| AC | 落点（实测存在） | 结论 |
|----|------------------|------|
| **AC-1** | `mupc-southd/tests/s3b2_config.rs`（**24 用例**）+ `tests/fixtures/south_stations_s3b2.yaml`（= PRD §9.4.1 六站） | **有落点**。① `ac1_legacy_field_subset_parses_and_legacy_conditions_do_not_fire`；② `ac1_full_reference_config_parses_and_passes`、`ac1_grid_meter_only_passes`；③ 规则 1/2/3/4/5/6(×3)/7/8/9/10/11/12/13/14/15(×2)/16/18/19 **逐条各有用例**，规则 17 由既有跨段用例承载（见 §3） |
| **AC-2** | L1：`data-processing/src/meter_regs.rs` 单测（57 绿）；L2：`mupc-southd/tests/s3b2_decode_e2e.rs`（**7 用例**） | **有落点**。`ac2_bms_uint16_scale_and_negative_offset_decode_end_to_end`、`ac2_bms_current_uint16_beats_int16_at_high_raw`（**+4953.5 vs −1600.1 的判别点**）、`ac2_pcs_byte_swap_and_32bit_lo_hi_decode_end_to_end`（**6563.6 ≠ 误用 `hi_lo` 的 655360.1**）、`ac2_meter_batt_...`、`ac2_fire_enum_and_packed_word_are_raw_values`（16720 整字）、`ac2_hvac_int16_uint16_and_fc02_bits`、`ac2_reference_config_every_collected_station_produces_points` |
| **AC-3** | `rs485-plugin/src/device.rs` 单测（`test_unpack_bits_prd_original_example` 0x3B/0xBB、`_31_bits_tail_byte_does_not_pollute`、`_288_bits_bms_scale`、`_cross_byte_boundary_explicit`、`_edges_and_non_multiples`）+ 点位名映射（`scheduler.rs::alarm_bit_block_emits_rising_edge_only` 钉 `bms_alarm_225` ↔ 位 424；`point_table_vs_reference_config.rs::fire_labels_and_contract_point_resolvable`；`point_table.rs::fire_signal_specs_match_prd_bits`；`label_disambiguates_register_and_bit_space`） | **有落点**，288 位/31 位/跨字节/非 8 倍数尾部**齐备** |
| **AC-4** | `scheduler.rs`：`pcs_priority_is_between_grid_and_hvac`、`pcs_station_isolated_then_recovers_online_once`、`offline_reason_contains_slave_addr_count`、`due_calc_respects_intervals_and_priority`、`due_calc_delay_station_backs_off`、`backoff_extra_caps_at_max_shift` | **有落点**，`role_priority(Pcs)=1`（与 `MeterBatt` 同档、**不与 grid/battery 同档**）实测于 `scheduler.rs:98-104` |
| **AC-5** | `scheduler.rs`：`battery_station_soc_pushed_via_dedicated_channel`、`soc_out_of_range_event_follows_state_flip`、`soc_out_of_range_first_observation_emits`、`non_battery_role_never_triggers_soc_channel`、`pcs_station_never_triggers_grid_or_soc_channels`、`battery_station_without_soc_block_does_not_push`；消防事件侧另有 13 个用例（见 §2.1） | **有落点**。闸门侧见 **§3 注 1** |
| **AC-6** | `point_table_vs_reference_config.rs::ac6_point_production_matches_design`（①–⑦ 全覆盖）+ `points.rs` 单测 6 例 | **有落点**（含 `pcs_3zone` 76 寄存器 → **72 点**、`bms_meta` 8 点、`bms_io_8..15`、单寄存器点必产 1 点） |
| **AC-7** | 行级 fmt 口径（设计 §11.11.2.1）：本报告**独立复算**并实测（§7） | **有落点**，判据 ①②③ 全部实测（含 T7 对 `s3b2_config.rs:377` 的收口） |

### 2.1 AC-5 的「事件侧扩展」（设计 §11.11.2 v1.3 承接、PRD AC 表未单列，属需求承接）

消防/站级事件侧 13 个用例**逐条对表**设计 AC-5 的事件条目，无缺项：
`fire_wordbit_signals_emit_enter_and_exit_events`（0x4400 → 恰 2 条）、
`fire_wordenum_emits_transition_events_between_active_values`（0→1→2 的 `level1` 退出 + `level2` 进入）、
`fire_reserved_bit_yields_no_event_but_keeps_telemetry`（bit2 预留零事件、telemetry 仍落 4）、
`fire_cylinder_pressure_never_emits_event`、`fire_detector_state_emits_alarm_and_fault_only`、
`fire_detector_addr_order_violation_emits_event`（首轮即产 1 / 连续轮 0 / 恢复 `@recovered`）、
`fire_addr_order_flag_recovery_emits_recovered_event`、`fire_addr_order_flag_reemits_after_station_recovery`、
`fire_addr_order_chain_head_checks_detector_one`（**探测器 1 地址最大仍须产**）、
`fire_detector_count_mismatch_follows_state_flip`、`fire_detector_count_mismatch_first_observation_emits`、
`fire_cylinder_pressure_configured_latches_on_nonzero`、`alarm_bit_block_emits_rising_edge_only`
（**位块无退出事件** —— 与消防双向形成刻意的对照）、`state_and_reserved_bits_yield_no_event_alarm_bit_does`。

---

## 3. 设计 §11.5.1「十七条 + 规则 18/19」落地核对（**逐条，非抽样**）

| # | 规则 | 落点（实测 `file:行`） | 用例 | 结论 |
|---|------|------------------------|------|------|
| 1 | 新 role 合法性 | serde 枚举 `Role`（`config.rs:45`） | `ac1_rule1_unknown_role_rejected_by_serde` | ✓ |
| 2 | 单站约束（pcs >1） | `config.rs:291-297` | `ac1_rule2_multiple_pcs_rejected` | ✓ |
| 3 | 必填点表（pcs 空 regs） | `config.rs:298-303` | `ac1_rule3_pcs_empty_regs_rejected` | ✓ |
| 4 | `soc` 点契约 | `config.rs:486-491` | `ac1_rule4_battery_soc_point_contract` + `validate_rejects_battery_without_soc_point` | ✓ |
| 5 | 格式与标度 | `validate_scale`（`config.rs:510`） | `ac1_rule5_zero_scale_rejected` | ✓ |
| 6 | 符号性一致性 | `check_symbolicity` / `check_symbolicity_row`（`config.rs:613/627`） | `ac1_rule6_unknown_registry_row_passes_through`（**无行不拒**）、`ac1_rule6_registry_offset_drift_rejected`、`ac1_rule6_scope_boundaries` | ✓ |
| 7 | 点位越界 | `points::expand`（`points.rs:229`） | `ac1_rule7_point_out_of_window_rejected` | ✓ |
| 8 | 点位重叠 | `check_overlap`（`points.rs:163`） | `ac1_rule8_overlapping_points_rejected` | ✓ |
| 9 | 32 位点对齐 | `points.rs:219/229` | `ac1_rule9_32bit_point_alignment_rejected` | ✓ |
| 10 | 点名唯一 | `config.rs:492-502` | `ac1_rule10_duplicate_metric_rejected` | ✓ |
| 11 | 空洞上限 + 首尾锚定 | `validate_anchoring`（`config.rs:547`） | `ac1_rule11_hole_cap_and_anchoring` | ✓ |
| 12 | 位块上限 | `config.rs:438-446` | `ac1_rule12_discrete_bit_cap` | ✓ |
| 13 | 地址有效性 | `config.rs:449-454` | `ac1_rule13_addr_zero_by_role` | ✓ |
| 14 | 区间与重叠（按 func 空间） | `validate_block_spans`（`config.rs:590`） | `ac1_rule14_overlap_by_func_space` | ✓ |
| 15 | 块落地极大性 | `validate_maximality`（`config.rs:673`）+ `merged_max_hole`（`:728`） | `ac1_rule15_maximality_forms`（**8 形态：Ok 7 + Err 1**）、`ac1_rule15_maximality_independent_of_written_order`（按**地址序**枚举，非书写序） | ✓ |
| 16 | 同口一致性（baud + parity） | `config.rs:308-329` | `ac1_rule16_same_port_parity_consistency`、`validate_rejects_same_port_mixed_baud{,_3_station}` | ✓ |
| 17 | 跨段互斥（port ≠ intercore 口） | `core_config::validate_south_stations`（**既有实现，零改动**） | `test_south_stations_shared_serial_with_pcs_short_rejected` / `_fullpath_rejected` / `_same_port_alias_spelling_rejected`（**实跑 8 个 `south_stations` 用例全绿**） | ✓ |
| **18** | pcs 站周期下界（≥500ms） | `config.rs:243-248`（`PCS_MIN_INTERVAL_MS`） | `ac1_rule18_pcs_interval_lower_bound`（499→Err / 500→Ok） | ✓ |
| **19** | 无 `points` 块的宽度护栏 | `config.rs:459-467` | `ac1_rule19_width_guard_without_points` | ✓ |

**另有设计补充规则**（§11.5.2(4)：点级 `name` 与 `count > 1` 并存 → 拒）落 `points.rs:152 check_name_count`，
用例 `points.rs::point_name_with_multi_count_rejected`。**无缺项。**

**注 1（AC-5 闸门侧的落点说明）**：设计 AC-5 的括号注写"（core-bin 侧单测断言 `last_data_ts` 不被刷新）"，
实现**未**在 core-bin 侧建该断言，而是把等价的硬保证钉在**唯一写入路径的反面**：
`last_data_ts` 的唯一刷新点是 `SouthSink::on_grid_package` → `AiIntegrator::set_latest_data`
（`mupc-core-bin/src/startup.rs:378`），而 `on_station_telemetry`（`:385`）与 `on_battery_soc`（`:445`）**均不触碰它**；
scheduler 侧 `pcs_station_never_triggers_grid_or_soc_channels` / `non_battery_role_never_triggers_soc_channel`
断言 `on_grid_package` **未被调用** ⇒ 闸门**结构性不可被非 grid 站推进**。
**结论：需求语义达成，落点位置与设计括号注略有出入（列为警告 W2）。**

---

## 4. 跨 Task 一致性（单 Task 评审看不见的部分）

| 检查项 | 结论 | 依据 |
|--------|------|------|
| 同一概念命名/语义一致 | **一致** | `RegDecode`（唯一定义 `meter_regs.rs:60`）、`TelemetrySample`/`SampleKind`（`mapper.rs:470/481`）、`EdgeTracker`（`scheduler.rs:215`，**位点 / 字级信号 / `StationFlag` 三类共用**）、`BlockData::{Regs,Bits}`（`mapper.rs:27`）、`AddrSpace`（`point_table.rs:80`）、`SocOutcome`（`mapper.rs:227`）—— T1→T7 无同义异名 |
| 重复定义 | **无** | `WordOrder` **全项目仅一处定义**（`data-processing/src/meter_regs.rs:50`），`mupc-southd/src/config.rs:15` 为 `pub use` 复用；`grep` 全仓 `enum WordOrder` 仅 1 命中 |
| 半成品 / 死代码 | **未发现** | 设计范围内的能力**全部接线**：`unpack_bits` → `parse_bits_response` → `read_discrete_inputs_from`（`device.rs`）→ `StationBus::read_discrete`（`port_runtime.rs:43`）→ `scheduler.rs:585` 分发；`points::expand` 校验期与运行期**同一函数**（无"校验通过但运行期不同"）；`cylinder_pressure_configured` 取数接缝（`scheduler.rs`）已实现（消费方属后续任务，设计 §11.12.1 项 9 已登记） |
| 裁定 1（16 位量一律 `uint16` / PCS·空调厂方明写） | **忠于裁定** | 参考配置 + `POINT_REGS` 逐点核对；`ac2_bms_current_uint16_beats_int16_at_high_raw` 把两种解释的差异钉在可观测边界 |
| 裁定 2（查表键 `(role, space, addr)`） | **忠于裁定** | `lookup_in(role, space, addr)`（`point_table.rs:875`）；`lookup` 保留为 `Reg` 空间包装（规则 6 调用点不变）；HVAC 位 0 与寄存器 0 **不再互相遮蔽**（`label_disambiguates_register_and_bit_space`） |
| 裁定 3（事件一律状态翻转制、**不得"命中即产"**） | **忠于裁定** | `EdgeTracker::edges` 只在 `prev != v` 时产出；`station_flag_events` 只管改名/载荷，**不重复产出**；全仓 `grep events.push` 在 `mupc-southd` **零命中**；**判别性已用探针实测（§6）** |
| 裁定 3 附则（**不得并入 §10.4 联锁 OR**） | **未并入** | 事件只经 `SouthSink::on_station_telemetry` 落 `events` + SSE；现存联锁（`core-bin`）只由 DI/GPIO 驱动、尚未消费 southd 的 fire 事件（设计 §11.12.2 末注已核实） |
| 裁定 4（登记数容量式 `1 + Σ/6`） | **忠于裁定** | `mapper.rs:377-385`：`groups + u32::from(fire_chain_head(reads).is_some())`；链首不可得 → 退化为 `Σ/6`，**与地址序校验共用 `fire_chain_head` ⇒ 降级口径结构性一致**；用例 `fire_detector_count_reference_config_includes_detector_one`（`1 + 114/6 = 20`，读回 20 ⇒ **不产告警**） |
| 裁定 5（fire 站已启用 / PCS 保持注释） | **忠于裁定** | `deploy/config/*.yaml` 启用 `bms`/`meter_batt`/`fire`/`hvac` 四站；`pcs` 整段注释（`mupc_core_config.yaml:231-279`） |
| 表↔配置双向不漂移 | **有钉子** | `point_table_vs_reference_config.rs` 16 用例（每展开点命中登记行且 `kind/scale/offset` 一致、每登记行恰被认领一次、展开点数符合 PRD、`label` 反查同一行） |
| 参考配置 ↔ 生效配置 | **逐字同源** | 本报告独立复算：把 `tests/fixtures/south_stations_s3b2.yaml` 与 `deploy/config/mupc_core_config.yaml` 的五站块**去注释去空白后 diff** ⇒ `meter_batt`/`fire`/`hvac`/`grid_meter` 差异 **0 行**，`bms` 仅切片边界行不同（脚本切到下一站首行），**实质逐字一致** |

---

## 5. 回归锚（**git 独立验证**，不只看测试绿）

| 锚 | `git diff master...HEAD -- <path>` 实测 | 判定 |
|----|------------------------------------------|------|
| **C1 `tests/grid_convergence.rs`** | 全部改动仅为：① 导入补 `BlockData`；② `block()` helper 补 4 个新字段 + 读结果包 `BlockData::Regs`；③ 4 处 `Vec<(RegBlockConf, Result<Vec<u16>,String>)>` → `...Result<BlockData,String>` 的**类型标注**。**断言体（期望值）一个字节未改**（4 个用例的 `assert*` 行无 `-`/`+`） | **未改 ✓** |
| **C4 `core_config.rs`** | 12 行改动 = 6 处 fixture 的 `battery_1` 行补 `regs: [{ name: bms_io, addr: 118, ... points: [{ at: 1, name: soc }] }]`；**6 个用例的断言（含 `err.contains("重复") && err.contains("仲裁")` / `"别名"` / 两处 `is_ok()`）零改动** | **只改 fixture ✓** |
| **C5 `scheduler.rs` 调度/退避/隔离/SOC gating 断言** | 全量扫 `git diff` 的**删除行**：断言类删除**恰 9 处**，**全部是 metric 更名**（`temp`→`temp_1` ×5、`m == "temp"`/`"alarm_in"`/`"alarm_in" && 0.5`/`"status_in" && 1.5`），**与设计 §11.5.3.3 逐条列明的 9 处一致**；`call_count` / `offline_count` / `event_count` / `soc_of` / `grid_count` / `input_call_count` 类断言**零改动** | **未改 ✓**（更名属规则变更传导） |
| C2 `config.rs` meter_grid-only 系列 | 该系列用例全部存在且绿；顺序约束（既有 `meter_grid` 整组 `validate_meter_grid_regs` **原地不动、先于** `validate_station_regs`）实测于 `config.rs:262-266`，C2 的"块名重复"文案未被规则 10 取代 | **未改 ✓** |
| C3 解析类（legacy 写法） | `default_reg_format_used_when_omitted` / `reg_block_func_parses_holding_input` / `parses_valid_5_station_yaml` 等绿；fixture 仍保留 `name: soc` 旧写法 | **未改 ✓** |
| C6 `battery_station_without_soc_block_does_not_push` | 用例在且绿；配置期侧新增 `validate_rejects_battery_without_soc_point`（设计 C6 要求的两层各有其测） | **未改 ✓** |
| C7 `mapper.rs` meter_grid/other_role 锚 | 存在且绿 | **未改 ✓** |

**结论：四个回归锚（C1/C4/C5 + C2/C3）全部未被改；唯一的断言改动集与 §11.5.3.3 逐条吻合。**

---

## 6. 实跑测试（本项目红线：**未跑** `cargo test --workspace`、**未跑** `cargo fmt --all`）

命令与端口严格按评审交办执行（`-p` 逐包、`-j 2`）：

```
cargo test -p mupc-southd      -j 2   test result: ok. 137 passed; 0 failed   (lib)
                                      test result: ok.   4 passed; 0 failed   (grid_convergence)
                                      test result: ok.  16 passed; 0 failed   (point_table_vs_reference_config)
                                      test result: ok.  24 passed; 0 failed   (s3b2_config)
                                      test result: ok.   7 passed; 0 failed   (s3b2_decode_e2e)
cargo test -p mupc-core-bin    -j 2   test result: ok. 310 passed; 0 failed
cargo test -p mupc-data-processing -j 2  test result: ok. 57/3/1/3/4 passed; 0 failed
cargo test -p rs485-plugin     -j 2   test result: FAILED. 41 passed; 1 failed
                                      test config::tests::test_config_with_gpio ... FAILED
cargo check --workspace               Finished（0 error；警告均为**既有**：rs485-plugin 5 条 unused
                                      + ota-update 2 条 unused import，实测 `device.rs` 的警告行
                                      在 `master` 同位置已存在，非本特性引入）
cargo clippy -p mupc-southd -p rs485-plugin -p mupc-data-processing -p mupc-core-bin
                                      Finished（**0 error**；仅上述 7 条既有警告，
                                      `mupc-southd`/`mupc-data-processing`/`mupc-core-bin` **零警告**）
```

**`rs485-plugin` 的唯一失败 = `config::tests::test_config_with_gpio`**，与根 `CLAUDE.md`
「已知测试失败」表**逐字吻合**（既有项，非本轮引入）。

### 6.1 探针判别性验证（**2 个探针，均已还原**）

| 探针 | 临时改动 | 结果 | 判别力 |
|------|----------|------|--------|
| **P1（位分类）** | `scheduler.rs::edges_to_events` 的位点过滤由 `*value == 1.0 && rs.alarm_bits.contains(metric)` 退化为 `*value == 1.0`（即**去掉 Alarm 分类**） | `state_and_reserved_bits_yield_no_event_alarm_bit_does` **FAILED**（`scheduler.rs:2369`，"State/Reserved 位不产事件"断言被打破） | **有判别力** ✓ |
| **P2（状态翻转制 = 裁定 3）** | `EdgeTracker::edges` 的 `Some(prev) if prev != v` 退化为 `Some(_prev)`（即**"命中即产"**） | **15 个用例同时 FAILED**：`edge_tracker_first_round_primes_then_reports_both_directions`、`edge_tracker_unknown_signal_is_not_an_edge`、`bit_points_land_in_telemetry_only_on_change`、`fire_wordbit_signals_emit_enter_and_exit_events`、`fire_wordenum_...`、`fire_addr_order_*`（3 个）、`fire_detector_*`（3 个）、`fire_reserved_bit_...`、`fire_cylinder_pressure_*`（2 个）、`soc_out_of_range_event_follows_state_flip`、`tracker_reset_after_recovery_suppresses_event_burst` | **判别力极强** ✓ —— 正是 T5 实测"5 轮 = 5 条 ⇒ 8.6 万条/日"的那个形态 |

**还原核验**：两处探针已按原样写回；`git status --porcelain`（滤去会话开始即存在的未跟踪文件）**输出为空**；
`cargo test -p mupc-southd --lib` 复原为 **137 passed / 0 failed**。

---

## 7. AC-7（fmt）行级判据的**独立复算**

按设计 §11.11.2.1 判据 ①（取 `rustfmt --check` 输出中**带 `-` 的行**的行号，与 `git diff` 的**新增行号**求交；
两者**同一坐标系**= 工作区文件行号）逐文件复算：

| 判据 | 结果 |
|------|------|
| **① 主判据（本特性新增行 fmt-clean）** | 对全部 **16 个被触及的 `.rs` 文件**复算。**逐行 `git blame` 归因**后：**T3（`f83a838` + `8db56df`）引入了 66 处不符行**（`points.rs` 16、`s3b2_config.rs` 33、`config.rs` 7、`scheduler.rs` 6、`mapper.rs` 3、`port_runtime.rs` 1）；**T4/T5/T6/T7 自身新增的行零处不符** —— 设计 §11.11.2.1 条目 1 记录的唯一 T4 违反（`s3b2_config.rs` 报 `:374`、实际不符行 `:377`）**已由 T7 收口**：实测重建该文件的带 `-` 行集合 = `{66,70,90,91,98,99,102,120,222,234,247,254,274,279,280,281,282,338,342,497,527,532,537,554,558,570,588,630,637,639,729,734,738}`，**`:377` 不在其中**（`git show 463aa3a` 确认其为"按 `rustfmt` 建议拆链、断言语义不变"） |
| **② 纯新增文件零报出** | `mupc-southd/src/point_table.rs` = **0**、`tests/point_table_vs_reference_config.rs` = **0**、`tests/s3b2_decode_e2e.rs` = **0** ⇒ 与设计 §11.11.2.1 条目 1 的实测一致，T7 复跑仍 0 |
| **③ 既存基线一律不动** | `master` 侧即不符的行（`config.rs` 14、`scheduler.rs` 19、`port_runtime.rs` 11、`mapper.rs` 1 等）**未被顺手格式化**；`cargo fmt --all` **未被执行**（工作区无全仓格式化痕迹） |

---

## 8. 安全性 / 红线

| 检查项 | 结论 |
|--------|------|
| **PCS 写操作（本轮范围外，N-4）** | **未偷偷写 PCS**。`StationBus` trait 仅 3 个 `read_*` 方法（`port_runtime.rs:37/39/43`），**无任何写方法**；`mupc-southd` 全仓对 rs485 的 `write_single_register`（既有 API）**零调用**；PCS 站在生效配置中**整段注释**（未启用） |
| **硬编码密钥 / 敏感信息** | diff 全量扫描 `password/passwd/secret/api_key/token/private_key/BEGIN * PRIVATE` ⇒ **零命中** |
| **新增 `unsafe`** | diff 中 `unsafe` 仅出现在**文档表格**里（"无新增 unsafe"的声明），**代码零处新增** |
| **错误类型实现 `std::error::Error`** | 本轮未新增错误类型；既有 `Rs485Error`/`BusError` 语义不变（`StationBus::read_discrete` 复用 `BusError::Read{slave,addr,count,reason}`） |
| **§10.4 联锁误并（裁定 3 附则）** | 状态型事件（SOC 越界 / 地址序 / 登记数）**未并入**联锁 OR；`station_flag_events` 只在事件命名空间改名与换载荷，不触碰任何判据/联锁/telemetry 路径（`scheduler.rs:403-431` 的文档已把该红线写成约束） |
| **PCS 站不推进 5s 闸门（N-1/N-2）** | `pcs_station_never_triggers_grid_or_soc_channels` 实测 `on_grid_package` 断言"不得走" |

---

## 9. 遗留项登记核对

| 遗留项 | 登记位置（实测） | 表述准确性 |
|--------|------------------|------------|
| **Q-1**（BMS 主从方向，文档自相矛盾） | PRD §9.10 **Q-1**（【设计阶段阻塞】+ 影响面 + 解除条件 a/b/c）+ 设计 §11.9；生效配置注释亦写明"本配置按 MUPC 为主机、BMS 主控为从机、slave = 簇号落位" | **准确**（现场启用 BMS 站前须按 RC-4 实证或厂方书面确认） |
| **RC-11**（消防/空调若并接在 BMS 485-2 ⇒ 该站不得启用） | 设计 §11.11.3（RC-11 行）+ 生效配置注释（`mupc_core_config.yaml:129-130`） | **准确**，且与 RC-8 并列为 Q-19 的现场裁定项 |
| **Δ-9**（PRD §9.8.4「PCS 32 位电量比对未通过 → 事件」无运行期落点） | 设计 §11.12.2 **Δ-9**（标注"须需求侧追认或订正 PRD"，并给出 a/b 两条择一处置）；§11.4.7 事件表 ⑦ 与 §11.7.4 迁移清单末行给出一眼可见的处置 | **准确**，本轮实现口径 = **不产该事件**（以 RC-2「未通过则不配点」替代） |
| **fmt 基线**（1204 处 / 106 文件） | 设计 §11.11.2.1（含"为什么不能 `cargo fmt --all`"与"代价刻意接受"）+ §11.13 T7 行 ③ | **准确**；本报告 §7 独立复算后结论一致 |
| **PRD §9.5.4 同款算式少算 1** | 设计 §11.4.6 **就地登记**（"PRD 原文同样是 `Σ(fire_det 前缀块 count)/6`（未含链首）⇒ 同样少算 1……**建议需求侧随 §9.5.4 同步订正**，不改 PRD，仅登记"）+ §11.12.2 v1.8 说明 | **准确且未擅改 PRD**；实现按订正口径 `1 + Σ/6` 落地 |
| 设计新增契约须追认（`read_slice` / `fire_det_count` / 状态型事件频次 / 钢瓶气压取数接缝 等） | 设计 §11.12.1 项 1–9（含项 8/8b 的事件契约与"建议需求侧补登频次口径"，**明确不追认亦按本设计执行**） | **齐全** |
| RC-9…RC-12（设计续号补的现场项） | 设计 §11.11.3（含"来源与编号说明"：PRD §9.9.2 只到 RC-8，本设计续号并上报 Δ-6） | **齐全** |

**评审过程中发现、但尚未登记的一项**：见 §10 **W1**（契约内部口径冲突）与 **W2**（AC-5 落点位置）。

---

## 10. 问题清单

### 严重问题（0）

无。AC-1…AC-7 **全部有落点**；十七条 + 18/19 **全部有落点**；四个回归锚**未被改**；
关键用例**经探针证实有判别力**；安全性红线**无违反**；**未发现与裁定相悖之处**。

### 警告（2）

**W1 —— 契约口径内部冲突（文档问题，非代码缺陷）：设计 §11.11.2.1 判据 ② 与本特性的实际文件清单相左。**
- 位置：设计 §11.11.2.1 判据 ②（"本 Task **新建**的 `.rs` 文件必须 **0 处**报出（无既存基线可援引）"）
  与同节条目 1 的"覆盖三个新增文件"清单。
- 事实（本报告实测）：T3（`f83a838`）**新建**的 `mupc-southd/src/points.rs` 有 **16 处** `rustfmt` 不符行；
  条目 1 的三文件清单未含它，条目 3 又以"由 T3 引入 ⇒ 判据 ③ 基线，不计入本特性"处理。
- 为什么是问题：同一节的判据 ②（**文件级**、对新建文件要求 0）与条目 3（把 T3 新增行当作**基线**）
  **对同一文件给出相反口径**；后续 Task 若照判据 ② 的字面自查会得出"T3 违规"。
- 改法（择一，**不要求改代码**）：① 把判据 ② 的适用对象限定为"**本轮收口 Task（T7）** 新建的文件"
  （措辞上与本特性的实际做法对齐）；或 ② 明确"**T1–T3 期间新建/修改的 `.rs` 文件，其不符行统一按判据 ③ 基线处理**"。
  **强烈建议同时**把 `points.rs` 的 16 处（及 T3 其余 50 处）列入一个**独立的格式债任务**（设计已声明
  "历史债清理须单独立项、独立 PR"，与之同构）。

**W2 —— AC-5 的括号注无直接落点（等价保证已由别处钉住）。**
- 位置：设计 §11.11.2 AC-5 行："（core-bin 侧单测断言 `last_data_ts` 不被刷新）"。
- 事实：core-bin 侧**无**该断言；等价保证由 scheduler 侧
  `pcs_station_never_triggers_grid_or_soc_channels` / `non_battery_role_never_triggers_soc_channel`
  （断言 `on_grid_package` 未被调用）+ "`on_grid_package` 是 `last_data_ts` 唯一写入路径"
  （`startup.rs:378`）构成。
- 改法：① 补一条 core-bin 侧断言（读 `AiIntegrator` 的闸门时间戳前后比对），或
  ② 把设计该括号注改为"**等价落点**：scheduler 侧断言 `on_grid_package` 不被非 grid 站调用 +
  core-bin 的 `on_grid_package` 是 `last_data_ts` 唯一写入路径"。二者择一即可，**不阻塞**。

### 优化建议（3）

- **O1**：`points.rs` 的 16 处 + T3 其余 50 处 `rustfmt` 不符行，建议随 W1 的**格式债任务**一并清理
  （涉及 `s3b2_config.rs` 33 处、`config.rs` 7 处、`scheduler.rs` 6 处、`mapper.rs` 3 处、`port_runtime.rs` 1 处）。
- **O2**：`cargo check --workspace` 仍有 **7 条既有 `unused` 警告**（`rs485-plugin/src/device.rs` 5 条 +
  `ota-update` 2 条），经核实**本特性之前即在**（同一代码行在 `master` 同处存在），
  建议按"既有债"单独立项清理，勿与本特性混提。
- **O3**：生效配置注释已把 RC-4/RC-8/RC-11/Q-1/Q-15 写进站块旁（好做法）；建议在**同一处**再补一句
  "`fire` 站启用前须已结清 §11.4.6 登记数容量式（v1.8 勘误 1）"，使 T7 的准入门槛在现场配置里**自证**。

---

## 11. 需真机验证（本机**不判通过**，仅登记）

RC-1（逐站逐点比对，**必须点名** BMS 116/186、ADL400 0x0092、BMS 2991–2994）、RC-2（PCS 字节/字序 +
32 位电量比对）、RC-3（PCS 地址基准）、RC-4（BMS 主从方向，Q-1 的实证判据）、RC-5（消防全量探测器 +
登记数）、RC-6（空调校验位；`parity` 透传**代码侧已就位**，只待现场回写配置）、RC-7（连续运行 ≥1h）、
RC-8（`ttyS7` 与 `ttyS0` 物理独立）、RC-9（钢瓶气压功能有无）、RC-10（探测器地址序）、
RC-11（消防/空调总线归属）、RC-12（消防/PCS 设备软件版本核对，**刻意无代码落点**）。

> **RC-6 的代码侧前置已实测就位**：`Rs485PortBus::open` 经 `bus_config()`
> （`port_runtime.rs`）把站级 `parity` 透传到 `rs485_plugin::Parity`，现场裁定为 `even` 后**只改配置即可生效**。

---

## 12. 结论

S3b-2 的 **T1–T7 全部实现通过项目级代码评审门禁**，达到**可交付 / 可进入测试阶段**的水准：

- 合同的**每一条可测断言都有落点**（AC-1…AC-7、十七条 + 规则 18/19、§11.13 各 Task DoD）；
- **四个回归锚未被改**，唯一的断言改动集与设计 §11.5.3.3 逐条吻合（规则变更传导，非回归）；
- **关键裁定（状态翻转制 / 位分类 / 全 uint16 / `(role,space,addr)` 查表 / `1 + Σ/6` 容量式）忠于设计**，
  且经**探针实测**证明其用例具备判别力（P2 一处退化即引 15 用例红）；
- 安全性红线（无 PCS 写、无密钥、无新增 `unsafe`、事件不误并联锁）**无违反**；
- 未结清项（Q-1 / RC-11 / Δ-9 / fmt 基线 / PRD §9.5.4 算式）**全部在 PRD Q 表或设计 Δ 表登记且表述准确**。

警告 W1/W2 为文档口径类，**不影响放行**，建议由项目经理指派**文档订正 + 格式债独立任务**跟进。
