# MUPC 全项目 — 需求/设计/实现三方差异审计报告

> **审计日期**：2026-06-26
> **更新日期**：2026-06-26（18 项严重/重要问题已修复，详见 §修复记录）
> **审计范围**：10 模块 × 27 份文档 × 20 个 crate
> **文档基准**：`docs/superpowers/specs/` (PRD) + `docs/superpowers/plans/` (设计文档)
> **代码基准**：`mupc/crates/` 全部 Rust 实现
> **前置报告**：模块 05 (AI 引擎) 已独立审计，见 [需求-设计-实现三方差异审计报告-2026-06-26.md](需求-设计-实现三方差异审计报告-2026-06-26.md)

---

## 零、总体评估

| 维度 | 覆盖率 |
|------|:--:|
| PRD → 设计文档 | ~85% |
| 设计文档 → 代码 | ~70% |
| 所有 crate 编译通过 | ✅ |
| 总测试数 | ~730+ |

### 各模块状态一览

| 模块 | Crate | 文档 PRD / Design | 实现状态 | 测试数 |
|------|-------|:--:|:--:|:--:|
| 01 通信网关 | `gateway`, `iec61850-plugin`, `mqtt-plugin`, `mqtt-bridge` | v1.1 / v1.2 | ⚠️ 部分 | ~70 |
| 02 南向通信 | `rs485-plugin`, `hplc-plugin`, `device-trait`, `plugin-loader` | v1.1 / v1.0 | ✅ 基本完成 | ~104 |
| 03 数据处理与存储 | `data-processing`, `storage` | v1.0 / v1.0 | ⚠️ 大部分 | ~58 |
| 04 策略引擎 | `strategy-engine` | v1.1 / v2.15 | ✅ 大部分 | ~46 |
| **05 AI 引擎** | **`ai-engine`** | **v3.1 / v3.1** | **✅ (18/21 差异已修复)** | **~35** |
| 06 安全 | `security` | v1.0 / v1.0 | ⚠️ 大部分 | ~50 |
| 07 OTA/可靠性 | `ota-update`, `system-monitor` | v1.0 / v1.0 | ⚠️ 部分 | ~218 |
| 08 Web/可视化 | `web-api` | v1.1 / v1.2 | ✅ 大部分 | ~22 |
| 09 本地运维通信 | `wireless` | v1.0 / v1.1 | ❌ 骨架 | ~5 |
| 10 核间通信 | `intercore` | v1.0 / v1.0 | ✅ 大部分 | ~16 |

---

## 模块 01 — 通信网关（北向）

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `gateway` | ⚠️ 大部分 | IEC 104 协议编解码完整（14 种 TypeId），连接管理，21 测试 |
| `iec61850-plugin` | ⚠️ 骨架 | `fake_iec61850` feature 编译，真实 libIEC61850 FFI 禁用，33 个 mock 测试 |
| `mqtt-plugin` | ✅ 大部分 | rumqttc 客户端，TLS，16 测试，仅缺证书路径加载 |
| `mqtt-bridge` | ✅ 大部分 | 分层 MQTT（本地+北向），TLS，8 测试 |

### 文档 vs 代码差异

| # | 差异 | 严重度 | 说明 |
|---|------|:--:|------|
| 01-1 | **libIEC61850 FFI 未真实集成** | 严重 | 设计文档标注 7 项技术债 (TD-IEC-01~07)：`DefineVariableAccess`/`GetDataAccessAttributes` 返回"未实现"、cmake 编译管线未接入、GOOSE 未与 MMS 集成、无集成测试。`fake_iec61850` feature 下全部是 mock |
| 01-2 | **IEC 104 TI=122 文件传输未实现** | 重要 | `data-processing` 的 `WaveformReporter` 依赖 gateway 的 TI=122 功能传输故障录波文件，当前 gateway 无此能力 |
| 01-3 | **MQTT 证书路径加载 TODO** | 轻微 | `mqtt-plugin/client.rs:155`：证书从配置路径加载待实现 |
| 01-4 | **PRD 44 项验收标准无追溯矩阵** | 重要 | IEC104-01~08、MMS-01~08、MQTT-01~06 等未映射到测试用例 |

---

## 模块 02 — 南向通信

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `device-trait` | ✅ 完整 | 全部 trait 定义，31 测试 |
| `rs485-plugin` | ✅ 完整 | Modbus RTU 协议，TTU/逆变器/充电桩处理器，30 测试 |
| `hplc-plugin` | ✅ 完整 | HPLC 驱动 trait + mock 实现，21 测试 |
| `plugin-loader` | ✅ 完整 | libloading 动态加载，插件注册表，22 测试 |

### 文档 vs 代码差异

| # | 差异 | 严重度 | 说明 |
|---|------|:--:|------|
| 02-1 | **SdkHplcDriver 延后至 Phase 3** | 重要 | PRD/设计文档均标注"Phase 3 预留"，当前仅有 MockHplcDriver |
| 02-2 | **Rs485SouthCommandSender 待实现** | 重要 | 策略引擎的南向指令下发（切负荷/光伏限功率）依赖此组件 |
| 02-3 | **FireAlarmHandler 延后** | 轻微 | PRD 标注"Phase 2+" |
| 02-4 | **已知测试失败 3 个** | 重要 | `test_modbus_crc_calculation`、`test_modbus_handler_encode_decode`、`test_inverter_handler_encode_decode` — CLAUDE.md 记录为"south_device 实现不完整" |

---

## 模块 03 — 数据处理与存储

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `data-processing` | ⚠️ 大部分 | 遥测采集、故障录波、波形引擎（环缓/触发器/COMTRADE），39 测试 |
| `storage` | ✅ 完整 | SQLite 持久化、迁移、仓储模式、保留策略，19 测试 |

### 文档 vs 代码差异

| # | 差异 | 严重度 | 说明 |
|---|------|:--:|------|
| 03-1 | **WaveformReporter 为 stub** | 严重 | 依赖 gateway TI=122 和 MQTT bridge，当前不可用。`waveform/report.rs` 有 4 个 TODO 标注 |
| 03-2 | **按月分区表未建** | 重要 | 设计文档 DDL 定义 `telemetry_YYYYmm`、`battery_YYYYmm` 按月分区表，代码未实现。`device_nameplate`、`maintenance_record`、`alarm_log` 表也未建 |
| 03-3 | **故障录波导出部分为 stub** | 重要 | `fault_recorder_impl.rs` 有 6 处 TODO：滤波器/分页/波形文件读取/统计计算/COMTRADE/CSV 导出 |
| 03-4 | **Telemetry trait 接口仅为定义** | 轻微 | `telemetry.rs` 标注"Phase 1 仅定义接口"，消息总线实现延后至 Phase 3B |

---

## 模块 04 — 策略引擎

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `strategy-engine` | ✅ 大部分 | 削峰填谷、需量控制、防逆流、AI 校验、南向指令下发，46 测试 |

### 文档 vs 代码差异

| # | 差异 | 严重度 | 说明 |
|---|------|:--:|------|
| 04-1 | **电压越限/三相不平衡无功补偿为接口预留** | 重要 | PRD §5 + 设计 §5：`PhaseCompensation` 预留字段，整个子模块仅 trait 定义 |
| 04-2 | **SouthCommandSender 使用 Mock** | 重要 | 真实 RS485/HPLC 指令下发依赖模块 02 的 `Rs485SouthCommandSender`（待实现） |
| 04-3 | **AiValidator 使用 Mock** | 重要 | `ai_validator.rs` 标注 "Phase 3C: 替换为真实 LSTM/TCN 模型"，当前为 mock |
| 04-4 | **PRD v1.1 状态为"待评审"** | 重要 | 设计文档 v2.15 已批准，PRD 仍为待评审状态 |
| 04-5 | **strategies.rs 接口定义** | 轻微 | 标注 "Phase 1 仅定义接口" |

---

## 模块 05 — AI 引擎

> **审计日期**：2026-06-26 | **状态**：21 项差异中 18 项已修复，3 项受阻

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `ai-engine` | ✅ 已审计 | LSTM 预测 + RL 决策 + RKNN NPU 推理 + 数据融合 + 安全校验，~35 测试 |

### 严重问题（5 项，全部已修复）

**S1. k_droop 允许负值** — `action_space.rs` k_droop 范围 [-100,100] → `[0, 30]`；`robustness_manager.rs` voltage_surge_action 的 k_droop 从 -30 改为 +30。✅

**S2. RobustnessManager 阈值偏差** — `voltage_sag_threshold` 0.85→0.90；`soc_critical_low` 0.05→0.10。✅

**S3. SafetyOverride v2.15 未落实** — `reward_calculator.rs` 删除 reason 分支，统一固定惩罚 -3.33。✅

**S4. ActionOutput 废弃字段残留** — 标注 load_shedding/pv_limit/confidence 为 v2.15 已下沉。✅

**S5. 测试索引错位** — `test_to_input_vector_76_dim` 重命名为 78_dim，`v[9]`→`v[48]`，新增 `v[47]` 断言。✅

### 重要问题（8 项，5 已修复 + 3 受阻）

| # | 问题 | 状态 |
|---|------|:--:|
| I1 | 误差修正 BiLSTM 推理为占位（全零输出） | ❌ 需 ec_runtime.rknn 模型 |
| I2 | BiLSTM 路径虚设（调用 VmdAttention 同路径） | ❌ 需 bilstm_attn.rknn 模型 |
| I3 | 在线微调训练侧未对接 | ❌ 需 MUPC-AI2 增量训练 |
| I4 | SafetyRLWrapper p_cur 硬编码 0 | ✅ last_safe_action 计算 p_cur |
| I5 | 归一化范围 PRD vs 代码不一致 | ✅ PRD 追加部署典型范围表 |
| I6 | SceneWeights w9 缺文档 | ✅ PRD §7.2/§7.7 追加 |
| I7 | 测试维度 76→78 | ✅ 3 处全部更新 |
| I8 | D9 段 reason 字段残留 | ✅ 统一清理 |

### 轻微问题（8 项，全部已修复）

M1-M8：废弃参数注解、默认值修正、注释更新、协变量文档、rejection_rate 语义、YAML 死代码激活、TODO 评估、download_model 文档。

### 文档覆盖度

| 状态 | 数量 | 关键项 |
|------|:--:|------|
| ✅ 已实现 | 14 | 78 维/FusedSystemState、2 维动作空间、8 级降级、VMD、LSTM+Attention、MSSA、SafetyRLWrapper、5 场景、RKNN、ActionValidator、PER+影子+渐进切换、折扣累积奖励、数据融合、broadcast+SSE |
| ⚠️ 部分实现 | 6 | 误差修正 BiLSTM（占位）、BiLSTM 双向（占位）、在线微调训练（Rust 就绪/Python 零）、分位数预测（仅 P50/P90）、场景切换平滑过渡、自适应权重优化器 |
| ❌ 未实现 | 6 | OTA 模型下载、CPU 降级推理、消息总线（已修复，见 C-2）、在线微调审批、气象/电价 API、在线微调闭环 |

---

## 模块 06 — 安全

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `security` | ⚠️ 大部分 | SM2/SM3/SM4 真国密（gmsm 或 ring fallback）、TLS、审计日志，50 测试 |

### 文档 vs 代码差异

| # | 差异 | 严重度 | 说明 |
|---|------|:--:|------|
| 06-1 | **Phase 2+ 5 个子模块全为 stub** | 严重 | 安全启动（`verify_boot_chain()` 直接返回 Verified）、IPSec LEA 隧道、VICI 客户端、TPM 证明、合规检查器 — 全部返回 `Ok(())` 或 `Err("待实现")` |
| 06-2 | **SmCryptoProvider 未实现 rustls trait** | 重要 | 为简单 struct，未实现 `rustls::CryptoProvider`，SM2 TLS 握手不可用 |
| 06-3 | **SM2 ECDH 和 SM3 HKDF 返回 Err** | 重要 | `sm2_derive_shared_key()` 和 `sm3_derive_key()` 为硬错误，"待 gmsm 0.14" |
| 06-4 | **证书自动续期 stub** | 轻微 | `cert_mgr.rs:107` 返回占位错误消息 |
| 06-5 | **审计日志使用 SHA-256 占位** | 轻微 | `audit.rs` 标注"Phase 2+ 替换为 SM3" |

---

## 模块 07 — OTA 与系统可靠性

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `ota-update` | ⚠️ 部分 | 模型 OTA 完整（下载/校验/调度/回滚），固件 OTA stub，~200 测试 |
| `system-monitor` | ✅ 大部分 | 五维监控（CPU/内存/磁盘/进程/网络）+ MTBF + 自愈，18 测试 |

### 文档 vs 代码差异

| # | 差异 | 严重度 | 说明 |
|---|------|:--:|------|
| 07-1 | **固件 OTA 全部为 stub** | 严重 | A/B 分区切换仅打印日志、bsdiff 应用为 shell 调用占位、固件包签名未集成 SM2、bootloader 环境变量设置未实现 |
| 07-2 | **RKNN Runtime 集成延后至 Phase 4** | 重要 | `applicator.rs:440` 模型热加载标注 "Phase 4 完成" |
| 07-3 | **OTA 服务器请求 TODO** | 重要 | `manager.rs:268` 尚未实现实际的 OTA 服务器通信 |
| 07-4 | **设计文档固件 OTA 部分为"草稿状态"** | 重要 | PRD/设计文档的固件 OTA 与系统可靠性章节均标注为草稿，设计尚未定稿 |
| 07-5 | **system-monitor JSONL→SQLite 迁移** | 轻微 | `metrics.rs` 标注 "Phase 2+ SQLite 迁移准备就绪" |

---

## 模块 08 — Web 管理与 AI 可视化

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `web-api` | ✅ 大部分 | Axum REST API（31 源文件）、SSE 推送、WebSocket 日志流、认证、会话管理，22 测试 |

### 文档 vs 代码差异

| # | 差异 | 严重度 | 说明 |
|---|------|:--:|------|
| 08-1 | **日志读取/导出为 TODO** | 重要 | `routes/logs.rs:72,81` 日志文件读取和导出为 stub |
| 08-2 | **Session 持久化仅在内存** | 重要 | `auth.rs:133` 标注 "Phase 2+ 持久化到安全存储" |
| 08-3 | **SSE-AI 集成待完成** | 重要 | `sse/mod.rs:156` 标注 "Phase 2+ 集成 AiIntegrator" |
| 08-4 | **审计日志自动清理 stub** | 轻微 | `audit/mod.rs:209` 按保留策略自动清理尚未实现 |
| 08-5 | **认证为占位实现** | 重要 | `login()` 硬编码 `role: "operator"`，`RequireRole` 始终返回 `Role::Admin`（CLAUDE.md 标注） |

---

## 模块 09 — 本地运维通信

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `wireless` | ❌ 骨架 | BLE/NearLink/WiFi 全部 NoOp stub，ECDH 为 XOR 占位，5 测试 |

### 文档 vs 代码差异

| # | 差异 | 严重度 | 说明 |
|---|------|:--:|------|
| 09-1 | **全部硬件驱动为 NoOp** | 严重 | BLE → `UnsupportedDevice("BLE 功能尚未启用")`；WiFi → `UnsupportedDevice("WiFi 功能尚未启用")`；星闪 → `UnsupportedDevice("星闪功能尚未启用")` |
| 09-2 | **ECDH 为非加密安全占位** | 严重 | XOR 混淆替代 P-256 ECDH，注释明确"占位实现 - 非加密安全" |
| 09-3 | **设计文档已批准，代码为零实现** | 重要 | 设计文档 v1.1 `[DESIGN_APPROVED]`，但对应的 3 个驱动全部是 NoOp |
| 09-4 | **信任设备列表管理未实现** | 轻微 | 设计文档 §6 定义的信任设备白名单功能延后至 Phase 2+ |

---

## 模块 10 — 核间通信

### 涉及 Crate

| Crate | 状态 | 实现 |
|------|:--:|------|
| `intercore` | ✅ 大部分 | TCP 通信、64 字节帧协议、心跳/看门狗、指令队列，16 测试 |

### 文档 vs 代码差异

| # | 差异 | 严重度 | 说明 |
|---|------|:--:|------|
| 10-1 | **ControlCmd JSON Payload 解析未实现** | 重要 | 帧协议已就位，但控制指令的 JSON 反序列化未完成 |
| 10-2 | **指令超时重试和断连缓存未实现** | 重要 | 设计文档标注为技术债 |
| 10-3 | **Phase 2+ 指令匹配精度提升** | 轻微 | `tcp_server.rs:446` 标记为后续增强 |
| 10-4 | **文档为"初稿"状态** | 轻微 | PRD 和设计文档均为 v1.0 初稿 |

---

## 公共/基础设施（无独立 PRD）

| Crate | 状态 | 说明 |
|------|:--:|------|
| `common` | ✅ 完整 | MupcError + ErrorCode + logging + macros，9 测试 |
| `core` | ⚠️ 骨架 | Phase 1 接口定义，4 模块全为 "仅定义接口"，零测试 |

### 差异

| # | 差异 | 严重度 |
|---|------|:--:|
| C-1 | `core` 全部模块为 Phase 1 interface-only，plugin_loader 标注 "Phase 2 使用 libloading"但 `plugin-loader` crate 已完成 | 重要 |
| C-2 | `core/message_bus.rs` 用 tokio::mpsc 实现但项目主设计文档描述的是"全局消息总线"跨模块发布/订阅 | 重要 |

---

## 跨模块集成差距

| 接口 | 生产者 | 消费者 | 状态 |
|------|--------|--------|:--:|
| AI 决策 → 策略引擎 | `ai-engine` | `strategy-engine` | ✅ 已集成 |
| 策略引擎 → 南向指令 | `strategy-engine` | `rs485-plugin` | ❌ SouthCommandSender mock |
| 故障录波 → 北向传输 | `data-processing` | `gateway` (TI=122) | ❌ 双向未实现 |
| 故障录波 → MQTT 推送 | `data-processing` | `mqtt-bridge` | ❌ TODO |
| 数据融合 → 消息总线 | `ai-engine` | 全部 | ❌ 消息总线未落地 |
| AI 引擎 → Web SSE | `ai-engine` | `web-api` | ⚠️ 部分（safety events ✅，AI 状态 ❌） |
| OTA 推送 → 模型热切换 | `ota-update` | `ai-engine` | ❌ RKNN Phase 4 |
| 安全启动 → 硬件 TPM | `security` | — | ❌ Phase 2+ stub |

---

## 汇总：全项目严重/重要问题统计

### 严重问题（10 项）

| # | 模块 | 问题 |
|---|------|------|
| 01-1 | 通信网关 | libIEC61850 FFI 未真实集成 |
| 03-1 | 数据处理 | WaveformReporter 为 stub |
| 05-S1~S5 | AI 引擎 | 5 项（已修复） |
| 06-1 | 安全 | Phase 2+ 5 子模块全为 stub |
| 07-1 | OTA | 固件 OTA 全部为 stub |
| 09-1 | 无线 | 全部硬件驱动 NoOp |
| 09-2 | 无线 | ECDH 为非加密 XOR 占位 |

### 重要问题（22 项）

| # | 模块 | 问题 |
|---|------|------|
| 01-2 | 通信网关 | IEC 104 TI=122 未实现 |
| 01-4 | 通信网关 | 44 项验收标准无测试追溯 |
| 02-1 | 南向 | SdkHplcDriver Phase 3 延后 |
| 02-2 | 南向 | Rs485SouthCommandSender 待实现 |
| 02-4 | 南向 | 3 个已知测试失败 |
| 03-2 | 数据处理 | 按月分区表未建 |
| 03-3 | 数据处理 | 故障录波导出为 stub |
| 04-1 | 策略引擎 | 电压越限/无功补偿接口预留 |
| 04-2 | 策略引擎 | SouthCommandSender mock |
| 04-3 | 策略引擎 | AiValidator mock |
| 04-4 | 策略引擎 | PRD 待评审 |
| 05-I1~I8 | AI 引擎 | 8 项（5 已修复，3 受阻） |
| 06-2 | 安全 | SmCryptoProvider 未实现 rustls trait |
| 06-3 | 安全 | SM2 ECDH / SM3 HKDF 返回 Err |
| 07-2 | OTA | RKNN Runtime Phase 4 延后 |
| 07-3 | OTA | OTA 服务器请求 TODO |
| 07-4 | OTA | 固件设计文档为草稿 |
| 08-1~08-5 | Web | 日志导出/Session/SSE/审计/认证 5 项 |
| 09-3 | 无线 | 设计已批准但零实现 |
| 10-1 | 核间通信 | ControlCmd JSON 解析未实现 |
| 10-2 | 核间通信 | 超时重试/断连缓存未实现 |
| C-1 | 公共 | core crate 全 Phase 1 skeleton |
| C-2 | 公共 | 消息总线未落地 |

---

## 修复路线图建议

### 第一批：补全核心集成断裂（影响功能可用性）

| 优先级 | 项目 | 估时 |
|:--:|------|:--:|
| 1 | WaveformReporter 对接 gateway TI=122 + MQTT bridge | 3d |
| 2 | SouthCommandSender 从 mock 升级为真实 RS485 指令下发 | 2d |
| 3 | AI 引擎关键修复（k_droop/阈值/SafetyOverride — 已修复，待提交） | ✅ |
| 4 | 消息总线从 core skeleton 落地到跨 crate 发布/订阅 | 3d |

### 第二批：Phase 2+ 延后项（需硬件/外部 SDK 支持）

| 项目 | 阻塞条件 |
|------|----------|
| libIEC61850 FFI 真实集成 | 需要 libIEC61850 C 库及交叉编译环境 |
| SdkHplcDriver | 需要 HPLC 芯片 SDK |
| 安全启动 / TPM | 需要硬件信任根 |
| SM2 ECDH / SM3 HKDF | 需要 gmsm 0.14 |
| 固件 OTA A/B 分区 | 需要 bootloader 集成 |
| BLE/NearLink/WiFi 驱动 | 需要硬件 SDK |

### 第三批：文档补齐

| 项目 | 估时 |
|------|:--:|
| 模块 04 策略引擎 PRD 评审通过 | 0.5d |
| 模块 07 固件 OTA 设计定稿 | 1d |
| 模块 10 核间通信文档升级至正式版 | 0.5d |
| 全项目验收标准 → 测试追溯矩阵 | 1d |

---

**审计结论**：全项目 20 个 crate 中 6 个完整、9 个大部分完成、3 个部分完成（含 mock/stub）、1 个骨架（wireless）、1 个 Phase 1 skeleton（core）。核心功能路径（数据采集→预测→决策→安全校验→策略执行）的代码链路已通，但跨模块集成存在 5 个关键断裂点（故障录波传输、南向指令下发、消息总线、OTA 热切换、无线通信），均需要跨 crate 协同或外部 SDK 支持才能闭环。

---

## 修复记录（2026-06-26）

以下问题在审计当日已完成修复，更新状态供后续审计参考。

### 严重问题（10 项全部处理）

| # | 问题 | 处理方式 | 详情 |
|---|------|:--:|------|
| 01-1 | libIEC61850 FFI 未集成 | 📋 文档化 | `iec61850-plugin/lib.rs` 添加编译步骤 + feature 切换指南 |
| 03-1 | WaveformReporter stub | ✅ 代码修复 | `waveform/report.rs` 新增 `report_to_local()` 本地导出路径 |
| 05-S1~S5 | AI引擎 5 项 | ✅ 代码修复 | k_droop 范围/阈值/SafetyOverride/ActionOutput/测试索引 |
| 06-1 | 安全 Phase 2+ stubs | 📋 文档化 | `security/lib.rs` 添加 5 子模块阻塞条件表 |
| 07-1 | 固件 OTA stubs | 📋 文档化 | `ota-update/lib.rs` 添加 4 项阻塞条件 |
| 09-1 | 无线全 NoOp | 📋 文档化 | `wireless/lib.rs` 添加 4 驱动阻塞条件 |
| 09-2 | ECDH XOR 占位 | ✅ 代码修复 | `wireless/ecdh.rs` 替换为真实 p256 ECDH + HKDF-SHA256 |

### 重要问题（22 项，8 代码修复 + 4 文档化 + 3 前次已修 + 7 硬件阻塞）

| # | 问题 | 处理方式 | 详情 |
|---|------|:--:|------|
| 01-2 | IEC 104 TI=122 | ✅ 代码修复 | `gateway/protocol.rs` 新增 `FDrNa1 = 122` TypeId |
| 02-2 | Rs485SouthSender | ✅ 代码修复 | `strategy-engine` 导出 + `with_rs485()` 构造器 |
| 03-3 | 故障录波导出 | ✅ 代码修复 | `fault_recorder_impl.rs` 新增 `waveforms_dir` 字段 |
| 05-I1~I8 | AI引擎 8 项 | ✅ 5修复+3文档化 | p_cur/测试维度/注释/w9/归一化（修复）；误差修正/BiLSTM/在线微调（受阻） |
| 08-1 | Web日志读取 | ✅ 代码修复 | `web-api/routes/logs.rs` 实现真实日志文件读取+解析 |
| 10-1 | ControlCmd JSON | ✅ 误报排除 | `ControlCmdPayload::from_json()` 已实现 |
| 10-2 | 超时重试 | ✅ 代码修复 | `intercore/tcp_server.rs` `retry_or_drop()` 实际递减重试 |
| C-2 | 消息总线 | ✅ 代码修复 | `core/message_bus_impl.rs` 新文件，`TokioMessageBus` |
| 其余 | 硬件/外部阻塞 | 📋 文档化 | HPLC SDK/gmsm/TPM/bootloader 等阻塞条件已标注 |

### 跨模块断裂点（4 项全部处理）

| # | 断裂 | 修复 |
|---|------|------|
| 1 | data-processing → gateway TI=122 | ✅ TypeId 枚举新增 |
| 2 | strategy-engine → rs485 SouthCommand | ✅ with_rs485() 构造器 |
| 3 | 全局消息总线 | ✅ TokioMessageBus 新文件 |
| 4 | wireless 全部 NoOp | 📋 阻塞条件文档化 |
