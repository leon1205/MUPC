# 12-MUPC 本地显示终端（LVGL 触摸屏）代码评审报告

`[CODE_REVIEWED: PASS]` —— **v2.0（LVGL 触摸屏）重认证**

> ⚠️ **失效披露（必读，不静默改写历史）**
>
> 本文件的前一版（**2026-09-10**，同带 `[CODE_REVIEWED: PASS]` 标记，含其 §6 整改复核）评审的是 **v1.0（自绘渲染器）**，
> 其结论对 **v2.0 不成立**：
>
> | 前一版内容 | v2.0 现状 |
> |---|---|
> | 评审对象 commit `26db910` → `249b10d` → `33bad2c` → `a62d321` → `aa2f2da`（+ 整改 `e5b7e44`） | 全部为 v1.0 提交，已被 v2.0 取代；当前 HEAD = `4e2d66c` |
> | 依据文件 `layout.rs` / `run.rs` / `renderer.rs` / `font.rs` / `tests/full_chain.rs` | **均已删除**（`lib.rs` 的「v2.0 已废弃的 v1.0 模块」段登记） |
> | 警告 W1–W5（`modbus.rs` 的展示读副作用、`channel.rs` body 上限、帧版本校验、卡头状态点、佐证行口径）/ 优化 O1–O8 | 针对的是已不存在的实现；**不代表 v2.0 的结论** |
> | 引用用例（`full_chain_*`、`layout::*`、`run_loop_*`） | **全库不存在** |
>
> 前一版正文见 git 历史：`git log -p -- docs/TODO/12-本地显示终端-代码评审报告.md`。
> **本版为 v2.0 就地重认证**；下文每条结论都给出**可复现的落点（用例名 / 文件:行 / 命令）**。

---

## 0. 本次重认证的方法与边界（**先说清楚，避免误读**）

- **评审对象**：`4e2d66c`（工作单元 H/I/J/K 全部交付后的 HEAD）+ **工作单元 L 的未提交改动**
  （L 只改**测试与文档**，见 §4）。
- **评审基准**：PRD `docs/superpowers/specs/modules/12-MUPC-本地显示终端-PRD.md`（`[REVIEWED: PASS]`）、
  设计 `docs/superpowers/plans/modules/12-MUPC-本地显示终端-设计文档.md`（`[DESIGN_APPROVED]`）、
  UI 设计 `.../12-MUPC-本地显示终端-UI设计文档.md`（`[DESIGN_APPROVED]`）。
- **方法（**不采信自述**）**：对每一条主张，执行「grep 到落点 / 跑用例 / 读源码」三选一以上；
  凡是**本机无法执行**的（真机、交叉编译、视觉标定）一律**不写"通过"**，集中列于 §3。
- **本报告的作者身份（如实）**：作者是**工作单元 L 的实施工程师**，不是独立的第三方评审员。
  ⇒ 本报告的性质是**"v2.0 现状重认证 + 证据清单"**：把此前分散在各单元（A…K）交付中的评审结论，
  重新锚定到**当前代码里真实存在的东西**上。**任何一条主张都可以按下文的命令当场复核**；
  **未复核项在 §3 逐条列出**（含本轮**没有**做的复核）。

---

## 1. 逐项核查（对照设计与 PRD 的硬约束）

| # | 审查点 | 结论 | 落点 / 复核命令 |
|---|---|---|---|
| 1 | **双进程拓扑 + 双通道**：读 9810（仅 GET）/ 控制 9811（GET 查询 + POST 写），均硬绑回环 | ✅ | `display-proto/src/config.rs::display_config_rejects_non_loopback_bind`、`loopback_check_parses_host_and_port_not_prefix_match`；`console_host.rs` 的 `startup` 段在启动期拒绝非回环/两址相同（`console_host.rs:356`）；`mupc-core-bin` 304 全绿 |
| 2 | **8 条契约端点全部落地**（§3.4） | ✅ | `console_host.rs::router()`（:289）**由契约表 `ConsoleEndpoint` 逐条驱动**，未实现臂只留 `not_implemented` 兜底；`display-proto::control::tests::endpoint_paths_and_methods_match_design`、`routing_table_is_pinned_endpoint_by_endpoint`（local-display 侧）、`console_host` 52 条用例（含 `log_endpoints_are_get_only_and_reject_writes_with_405` / `audit_endpoints_are_get_only_and_reject_writes_with_405`） |
| 3 | **`web-api` crate 整体移除**（D13 / §7.2） | ✅ | `ls crates/` **无** `web-api`；`grep -rn "web-api" crates/*/Cargo.toml` **零命中**；残留只有注释（`common/src/error.rs:41` 线格式编号、`display-proto/src/interlock.rs` 的迁移口径说明、`alert_feed.rs` 的替代物说明）；`cargo check --workspace` 0 error |
| 4 | **零键盘红线**（F12 / TT-02）三层保证 | ✅ | ① 类型层 `ConfigKind`（`display-proto/src/control.rs::config_kind_*`）；② **构建产物层**：`lvgl-sys/lv_conf.h:86-88` = `LV_USE_TEXTAREA 0 / LV_USE_KEYBOARD 0 / LV_USE_SPINBOX 0`；③ 源码扫描层：`ui/**` 六条 `*_static_constraints` + **`no_text_input_widget_symbol_anywhere_in_src`（本轮新增，覆盖 `src/**` **除扫描器自身（`ui/tests.rs`）外的 38 个** `.rs`；实测全目录 39 个 = 38 + 扫描器）** |
| 5 | **禁直连总线**（§4.4.6）：渲染端不得依赖 `mupc-intercore`/`mupc-southd`/`mupc-gateway` | ✅ | `crates/local-display/Cargo.toml` 依赖面 = `display-proto / lvgl-sys / serde / serde_json / thiserror / uuid`（+ Linux 侧 `libc / evdev`）；**本轮补上自动化断言** `local_display_manifest_has_no_direct_dependency_on_core_crates`（允许清单钉死为这 8 个） |
| 6 | **unsafe 边界收敛**（§1.1.1.2 纪律 1）：只有 `src/lvgl/**` 可触碰绑定 | ✅ | **权威口径 = 剥注释后的自动化断言** `binding_crate_is_not_used_outside_the_thin_layer`（扫 26 个非薄层文件；**本轮补上**），它证明**业务代码零引用**。`grep` 只作旁证：`grep -rn "lvgl_sys" crates/local-display/src`（cwd=`mupc/`）**命中 26 行 / 18 个文件**，其中**非薄层占 6 个文件 11 行** —— `app.rs:112`、`canvas.rs:41`、`screen.rs:21`、`touch.rs:31`、`ui/components.rs:299`、`ui/tests.rs:848/892/911/912/916/917` —— **全部落在注释 / 文档注释内**；剩余 15 行在 `src/lvgl/**` 薄层（`use lvgl_sys as sys;`，那正是它的职责）。⚠️ **订正记录（L 收尾）**：本行原写"命中**只在** `src/lvgl/**`" —— **该句不可复现**（实测就是上列 6 个非薄层文件），属本文档自身反复批评的"引用不可复现"类缺陷，已改为如上可当场执行的表述 |
| 7 | **bindgen 绑定面双向断言**（§12.1，禁 `lv_*` 通配） | ✅ | `lvgl-sys/tests/allowlist_consistency.rs::allowlist_is_bidirectionally_consistent`：① 导出 ⊆ `allowlist.txt`（**fn / var / type 均为硬断言** —— 设计原文说 `type` 待薄层落地后升级，**现已升级**）；② 清单内 fn/var 在 `src/**` 有真实引用（无死符号）。当前 allowlist：**147 fn / 60 type / 11 var = 218 条**（构建期 `cargo:warning` 实测打印） |
| 8 | **数据真实性**（不造假值 / 不补 0） | ✅ | `display_host::tests::device_source_links_and_hmi_channel_never_faked`、`alarm_source_failure_is_unavailable_not_empty`、`interlock_failure_is_unavailable_not_unlatched`、`frame_dual_lost_*`；`display-proto::frame::tests::missing_required_segments_are_rejected_not_zero_filled`；`state::tests::num_view_degraded_flags_map_to_dash` |
| 9 | **审计 fail-closed**（EDGE-18 / D7） | ✅ | `display-proto::control::tests::audit_failure_is_fail_closed`、`config_service::audit_failure_is_fail_closed_and_changes_nothing`、`interlock_ops::audit_intent_failure_is_fail_closed_and_executes_nothing`、`console_audit::unavailable_audit_dir_fails_loudly` |
| 10 | **幂等（30 s 窗口）+ 结构化错误码**（D7 / EDGE-14） | ✅ | `idempotency::*`（5）+ `config_service::duplicate_request_replays_the_first_receipt_without_re_executing`、`concurrent_same_request_id_gets_busy_not_a_second_execution`、`replay_window_rejects_outside_and_accepts_inside`；契约侧 `display-proto::control::tests::envelope_replay_window_boundary`、`idempotency_key_is_op_plus_request_id` |
| 11 | **yaml 保留式编辑**（D16 / EDGE-23，**本模块最大净新增项**） | ✅ | `yaml_edit::*`（13：`preserve_edit_changes_exactly_one_scalar_line`、`batch_edits_replace_exactly_n_lines`、`inline_comment_and_trailing_spaces_survive`、`crlf_line_endings_are_preserved`、`quote_style_is_inherited_from_the_old_token`、`missing_section_or_key_is_loud_not_silent`、`non_scalar_values_are_refused`）+ `config_service::legacy_web_api_section_survives_a_real_save_verbatim`、`unlocatable_key_falls_back_to_full_rewrite_visibly`、`write_mode_audit_entry_records_the_real_before_and_after` |
| 12 | **上屏时延的落地约束**（§4.2.1） | ✅ | 配置硬校验：`display-proto/src/config.rs:155/164/173/182`（`alarm/interlock_poll_ms ≤ 1000`、`device_poll_ms ≤ 4000`、`min_publish_interval_ms ∈ [100, publish_ms]`，**越界即 Err**）+ `display_config_timing_bounds_rejected_not_silently_accepted`、`validate_new_invariants_each_have_reject_case`；防退化（"变更即组帧"）：`pacer_publishes_change_after_merge_window_not_waiting_main_tick`、`run_publishes_on_slow_change_before_main_tick`、`run_publishes_tail_change_of_burst_not_swallowed_by_merge_window` |
| 13 | **码表覆盖率**（§1.1.2 / §11.1） | ✅ | `ui::tests::ui_texts_covered_by_font_cmap`（基线 = **生成字体的实际 cmap**，回落 `fonts/lv_font_cmap.txt`）+ `runtime_formatters_emit_only_cmap_glyphs`（**C1 的根因封堵**：`format!` 的运行时输出）+ `contract_strings_emit_only_cmap_glyphs` + 三条逐页运行期网：`ui::tests::p3_runtime_texts_emit_only_cmap_glyphs`、`ui::tests::p5_runtime_texts_emit_only_cmap_glyphs`、`ui::pages::p4_interlock::tests::runtime_texts_are_cmap_safe`；**本轮把扫描面补到 `src/state.rs`**（设计原文要求，见 §4.2）。⚠️ **订正记录（L 收尾）**：本行原写 `p3/p4/p5_runtime_texts_*` —— **该简写误导**（p4 的同款网**不叫这个名字**：`grep -rn "p4_runtime_texts" mupc/crates` **零命中**），已改为上列**真实全名**（取自 `cargo test -p local-display --lib -- --list`） |
| 14 | **`lv_refr_now` 只准测试用**（§5.2 不变量 2） | ✅ | `FORBIDDEN_UI_SYMBOLS`（`ui/tests.rs`）+ 各 `*_static_constraints`；生产侧唯一消费者是 `disp.refr_now_for_test()`（测试名即自证） |
| 15 | **二次确认与默认焦点**（D8 / TT-09 / TT-10） | ✅ | `confirm_level_has_no_default`（`level` 无默认值）、`ConfirmDialog` 段（`default_focus_is_cancel()`、500 ms 防重、L2 长按进度）、`p2_config::tests::save_level_follows_requires_reconnect` / `reset_level_is_at_least_l2`、`p4_interlock::tests::every_reject_variant_has_specific_inplace_text` |
| 16 | **配置元数据一致性**（§11.3，防"UI 能改一个不存在的键"） | ✅ | `console_host::tests::yaml_path_matches_key_for_every_field`、`config_service::each_invalid_field_kind_is_reported_field_by_field`、`display-proto::control::tests::config_field_rejects_read_only_edit`、`config_kind_u16_rejects_out_of_range_and_bad_step` |
| 17 | **单实例/单线程 LVGL 会话纪律**（LVGL 非线程安全） | ✅ | `src/ui/tests.rs` 模块头裁定：触碰 LVGL 的用例**不另起 `#[test]`**，统一由 `lvgl::tests::lvgl_core_bridge_chain`（唯一 LVGL `#[test]`）串行调起；本轮新增的两条交互用例同样并入 `shell_chain`（未破坏该纪律） |
| 18 | **测试充分性（本轮复核）** | ✅ | 实测 766 条全绿（§2）；缺 1 项（墨量密度）已如实登记，见 §3 |

---

## 2. 实测证据（复现命令）

```bash
export LIBCLANG_PATH='C:\Program Files\LLVM\bin'   # Windows 开发机
cd <repo>/mupc

cargo test -p display-proto -j 2        #  72 lib + 6 集成 = 78  全绿
cargo test -p local-display -j 2        # 369 lib + 6 + 6 + 3 doc = 384 全绿
cargo test -p mupc-core-bin -j 2        # 304 全绿
cargo check --workspace --all-targets   # 0 error
```

> **数字口径**：`local-display` 的 369 = 基线 365 + 本轮新增 **4** 条静态约束 / 扫描器用例
> （G1/G2/G3 + `scanner_recognizes_every_raw_string_on_the_scan_face`）；
> `mupc-core-bin` 的 304 与单元 K 交付后基线**一致**（L 未改该 crate）。
> 逐条构成与获取命令见同目录《12-本地显示终端-测试报告.md》§2。

---

## 3. 本次**未复核** / 不可复核的项（**如实列出**）

| 项 | 状态 | 为什么 |
|---|---|---|
| **aarch64 交叉编译**（设计 §14 R-20/R-23 的**头号未决项**） | ❌ **未复核** | 本机无 `aarch64-linux-gnu-gcc`、无 Docker（WSL 无发行版）。**必须在 Linux 构建机**跑 `cargo build --target aarch64-unknown-linux-gnu -p lvgl-sys` 与 `-p mupc-local-display --release`。这是 v2.0 相对 v1.0 的**新增风险面**（LVGL C 源码 + bindgen + 10 档位图字库都要过交叉） |
| **x86_64-Linux CI 构建** | ❌ **未复核** | 同上（R-20 ②） |
| **真机 `/dev/fb0` / evdev / HDMI / 时延 / 资源 / 长稳** | ❌ **未复核** | 无硬件（§11.2 右列） |
| **CJK 位图字体的视觉清晰度**（R-01 残余 / UI V-4） | ❌ **未复核** | 需真机/人眼标定；本机只能做"字形存在性"（码表网）与"墨量密度"（**该断言尚未写**，见 §4.3） |
| **本报告未逐行重读 A…K 各单元的实现代码** | ⚠️ **限制** | 本次按"证据重锚定"方法做（每条主张给落点/命令），**不等于**对 5 万余行 HMI 代码的重读。**建议**：若需要独立性更高的复核，另派独立评审员做一次增量代码走查 |
| **`docs/TODO/12-v2.0-LVGL-spike-报告.md` 的结论** | ⚠️ 部分复核 | 本轮独立复核了两条：`lv_conf.h` 三者置 0（同 #4）、allowlist 规模（同 #7）；其余（字体体积、构建耗时）未重测 |

---

## 4. 本轮（工作单元 L）引入的改动及其复核

> L 的改动**全部落在测试与文档**，`cargo check --workspace --all-targets` 0 error，无产品实现改动。

### 4.1 新增的自动化网（5 条，均带破坏性验证）

| 网 | 复核 | 破坏性验证（实测） |
|---|---|---|
| `binding_crate_is_not_used_outside_the_thin_layer`（静态约束 ⑤） | ✅ 通过 | 往 `src/console.rs` 插 `use lvgl_sys as lvgl_sys;` ⇒ 红；还原（`cp` + `cmp`）⇒ 绿 |
| `no_text_input_widget_symbol_anywhere_in_src`（静态约束 ① 的 `src/**` 面） | ✅ 通过 | 往 `src/state.rs` 插 `_lv_spinbox_marker` ⇒ 红；还原 ⇒ 绿 |
| `local_display_manifest_has_no_direct_dependency_on_core_crates`（静态约束 ②） | ✅ 通过 | 往 `Cargo.toml` 加 `mupc-intercore` ⇒ 红；还原 ⇒ 绿 |
| `ui_texts_covered_by_font_cmap` 的扫描面扩展（+`src/state.rs`，码表覆盖率） | ✅ 通过 | 往 `src/state.rs` 加含 cmap 外字形的字面量 ⇒ 红，报 `U+9519 错 @ src/state.rs:108`；还原 ⇒ 绿 |
| `scanner_recognizes_every_raw_string_on_the_scan_face`（**L 收尾补**，"原始串前缀失认"哨） | ✅ 通过 | 往 `literal_prefix` 注入 `if cs[i] == 'r' { return None; }` ⇒ 全量 **368 passed / 1 failed**，唯一红点即本条（报内容泄漏 `r# INNER_SENTINEL #;`）；还原（`cp` + `cmp`）⇒ **369 passed / 0 failed**。**对照组**：同一注入、本条未加入时 = **368 passed / 0 failed**（该红却没红） |

### 4.2 共享扫描器的失真修复 + **"原始串前缀失认"哨的补上**（**影响所有静态网**）

`ui/tests.rs::strip_comments_and_literals` 的旧判据 ③b 断言"非原始串不得含裸换行"——
**该前提为假**（Rust 允许字符串含裸换行；独立 crate 实测确认），首例误报是 `src/config.rs::help_text`。
**已删除该判据**（保留 EOF 未闭合的 ③a），并改写回归用例。

> ⚠️ **订正记录（L 收尾，2026-09-18）—— 原文在此处把话说过头，已如实降级**
>
> 本段原写"**代价已登记**：……替代控制 = 各静态网的**扫描面自证** + `literal_prefix` 的前缀清单"。
> **实务测证明该"替代控制"接不住**：往 `literal_prefix` 注入"`r` 前缀失认"
> （`if cs[i] == 'r' { return None; }` —— 正是该失真专防的形态）⇒ 全量 **368 passed / 0 failed**，
> 三条所谓替代控制**一条都没接住**：
> ① 扫描面自证（must-contain token）查的是**别的 token**（`pub struct App` / `ScrollContainer`），与字面量前缀无关；
> ② EOF 未闭合（③a）只在"恰好扫到文件尾仍未闭合"时才炸，而真实源码里引号**奇偶恰好配平** ⇒ 永不触发；
> ③ `literal_prefix` 的前缀清单是**文档**，不是**判据**。
>
> **处置（本轮已做，非"留白"）**：**补一条真控制** ——
> `ui/tests.rs::scanner_recognizes_every_raw_string_on_the_scan_face`，判据 = 两段：
> **① 端到端**（原始串**内含 `"`** 与**跨行**两种真实形态喂进扫描器 ⇒ 内容必须整枚剥空、后续源码可见、行数守恒）；
> **② 对拍**（扫**全部 38 个扫描面文件**，逐文件"被识别为原始串的条数"必须等于清单：
> `src/control_route.rs` **13** / `src/channel.rs` **1** / `src/console.rs` **1**，其余 **0**，合计 **15**）。
> 该清单由 `grep -cE '(^|[^A-Za-z0-9_])r#*"'` 抄录，**未列出的文件隐含期望 0** ⇒ 前缀失认时条数归零、**必红**（已实测）。
> 为此给扫描器加了审计出口 `strip_comments_and_literals_audited`（**同一份实现**的薄包装，不是第二份扫描器）。
> **未立 U-43**：能想出可靠控制 ⇒ 按任务书选项 **a** 处置（理由：控制可做且已做破坏性验证，
> 立号反而会把"已闭环"记成"欠账"）。
>
> **残余（如实登记）**：本条哨**只覆盖原始串前缀**（`r` / `r#…` / `br#…`）。`b"…"` / `c"…"` 前缀失认
> 仍无自动哨（扫描面内这两类前缀**零使用** ⇒ 补了也是恒真断言）；若将来引入，须同步扩充本哨。

### 4.3 未补的缺口（**明确留白，不粉饰**）

1. **§11.1「中文文案区域墨量密度断言」未写** —— 该判据在默认 feature（无字库）下不成立
   （CJK 恒缺字，"文本区有墨"这一前提就没有），只能进 `noto-font` 条件门禁；
   是否把 `gen_fonts.sh` + `noto-font` 编入 CI 属**流程裁定**。
2. **§11.1「滚动条带内起手 = 内容区起手，变化量完全一致」口径订正** —— 实测该量在本 harness 下**双峰**
   （`[60,110,113]` 三连，跨 x 复现），与 x 无关 ⇒ 无法用"相等"判定；已改判为
   "仍然拖动内容 + 无跳变"，**结构性主张**由 `lvgl/tests_a3.rs` 的 `ScrollContainer` 段 + §5.6-A 的源码引用承担。
   详见测试报告 §4.3.2。
3. **真字库在位时暴露的既有测试缺陷**（`ui/tests.rs:3692` 的"DOTS 只影响绘制"断言在 LVGL v9.5.0 上为假）
   + **由此牵出的产品风险**：`PHASE_P_SLOT_W`（2 em）在真字库下**装不下带符号的数值**
   （28 px 档：`−12.3` 需 70.1 px > 槽宽 56 px）⇒ **真机上充电相 P 值会被截断成 `−1...`**。
   **L 未擅自改版式**（槽宽属 UI §6.1 定案），**上报 PM/UI 裁决**。详见测试报告 §4.4。

---

## 5. 结论

- **功能正确性、安全红线、数据真实性、边界与降级、契约与幂等、yaml 保真、测试充分性**：
  在**本机可验证范围内**全部通过（766 条用例全绿 + §11.4 两条冒烟实跑通过）。
- **未发现阻断性问题**；本轮新引入的改动**仅测试与文档**，且每条新增网都做了破坏性验证（改坏即红、还原即绿）。
- **放行本次开发（工作单元 L）**，同时**明确上报 3 项留白/风险**：
  ① aarch64 交叉编译**仍未实测**（R-20/R-23，本模块最高风险）；
  ② 真机 P1 相卡**带符号数值截断**（§4.3 第 3 条，需 UI/PM 裁定）；
  ③ 「墨量密度」判据与「滚动条等值」判据的口径订正（§4.3 第 1/2 条）。
- **前一版（v1.0）的 `[CODE_REVIEWED: PASS]` 结论随 v1.0 一并失效**；本版是 v2.0 的重认证，
  两版之间的差异已在顶部「失效披露」中逐条写明，历史正文保留于 git 历史。
