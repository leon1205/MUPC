# Rust build artifacts
target/
**/target/
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Worktrees
.worktrees/

# Temporary files
*.tmp
*.bak
*-bk.mdanthropic.claude-code-*.vsix

# Playwright MCP artifacts
.playwright-mcp/

# Python artifacts
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# MSSA optimizer cache (auto-generated)
tools/mssa_optimizer/mssa_cache.json

# External dependencies (installed via scripts/setup-deps.sh)
external/
rknn-toolkit2-*/
vendor/rknn/librknnrt.so
vendor/rknn/librknnrt.so.sha256

# Build system artifacts
build/
build-*/

# 12-MUPC 本地显示终端：字库源与 lv_font_conv 生成物
#   不入库（OFL 字库可公开下载、生成物是构建产物，入库会平白膨胀仓库）；
#   构建前先跑 mupc/crates/local-display/fonts/gen_fonts.sh。
#   入库项：gen_fonts.sh / extract_charset.py / font_subset_charset.txt / NotoSansSC-LICENSE.txt
#           / lv_font_cmap.txt（**派生清单而非字体产物**：每行一个 U+XXXX，
#             由 gen_fonts.sh 随字体生成并更新；它让码表检查在**没有 .c 产物**的
#             干净 clone / CI 上仍有权威基线 —— 见 ui/tests.rs::load_font_cmap，I3）
mupc/crates/local-display/fonts/*.otf
mupc/crates/local-display/fonts/*.ttf
mupc/crates/local-display/fonts/*.ttc
mupc/crates/local-display/fonts/lv_font_noto_sc_*.c
# 显式反选：确保上面的通配（或将来放宽的通配）**不会**把入库清单扫进去。
!mupc/crates/local-display/fonts/lv_font_cmap.txt
